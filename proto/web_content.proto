syntax = "proto3";

package cline;
option java_package = "bot.cline.proto";
option java_multiple_files = true;

import "common.proto";

service WebContentService {
  rpc checkIsImageUrl(StringRequest) returns (ImageUrlCheckResult);
}

message ImageUrlCheckResult {
  bool is_image = 1;
  string url = 2;
}
