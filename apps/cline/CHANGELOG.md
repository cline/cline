# Cline Desktop Changelog

## 0.1.0-test.2

- Bundle the persistent Cline Gate server with the desktop app for local use.
- Use the Cline Dad profile and its Gateway-native support tools by default.
- Add a one-command Docker installer and multi-architecture container release flow.
- Refresh the Cline Bots application icon across macOS, Windows, Linux, iOS, and Android assets.

## 0.0.1

- Initial fork of `apps/examples/desktop-app`, adding a sandboxed backend option (see [SANDBOX.md](./SANDBOX.md)). Everything up to this point is inherited, working code from that app; this file starts fresh rather than carrying over its unrelated release history.
