# Phase 12 results

## Baseline

- Phase 11 commit: `1d8b40425567b5d2fef75364648b4e21e672166a`
- Working folder: 1,231,700,324 bytes
- Files: 96,448

## Final identity

- Extension: `fffalexgo.bedrock-coder@0.1.0`
- Display name: `Bedrock Coder`
- Description: `A local-first VS Code coding agent powered exclusively by Amazon Bedrock.`
- Commands, settings, and context keys: `bedrockCoder.*`
- SDK packages: `@bedrock-coder/{shared,llms,agents,core}`
- Protobuf source/package: `proto/bedrock_coder`, `package bedrock_coder`
- Home/workspace configuration: `.bedrock-coder/`
- Environment prefix: `BEDROCK_CODER_`
- Repository: <https://github.com/FFFalexgo/AWS_Bedrock_Coder>

The manifest identity check also validates command declarations, keybindings,
view containers, views, menus, settings, SDK package names/versions, protobuf
packages, and forbidden executable product identities.

## Clean-extension boundary

- Removed pre-fork task-history readers, resume translation, deletion fallback,
  and state merging.
- Removed VS Code state, approval-settings, MCP-settings, and file-storage
  migration paths that existed for the upstream product.
- Consolidated rules, hooks, workflows, skills, plugins, MCP settings, and
  session data under `.bedrock-coder/`.
- Removed the parallel `.bedrock-coder-rules` and Documents-based config paths.
- Bedrock Coder history, checkpoints, teams, and settings remain backed by the
  retained SDK session stores.

## Branding

The user-supplied 1254x1254 PNG is used byte-for-byte for the Marketplace icon
and in-webview mark:

- `apps/vscode/assets/brand/bedrock-coder-mark-1024.png`
- `apps/vscode/assets/icons/icon.png`
- `apps/vscode/webview-ui/src/assets/bedrock-coder-mark.png`

SHA-256 for the attachment and every listed copy:

```text
4E468960EFC1243B9C23AEEF5E871B43337133E842FD4D568570843C85946C57
```

`assets/brand/bedrock-coder-mark.svg` references the exact artwork.
`assets/icons/icon.svg` is the simplified `currentColor` B/C silhouette used by
the VS Code Activity Bar in light, dark, and high-contrast themes. Old icon
fonts, robot/sleepy art, seasonal logos, social icons, and upstream branding
were removed.

## Product and dependency cleanup

- Removed Storybook configuration, stories, scripts, and dependencies.
- Removed Marketplace publishing scripts/workflows and the obsolete internal
  SDK npm release/check machinery.
- Removed stale hosted-community, account, subscription, provider, and release
  documentation.
- Removed Office/PDF extraction and its `exceljs`, `mammoth`, `pdf-parse`, and
  `@types/pdf-parse` dependencies; retained plain-text/source extraction.
- Removed unused `dotenv`.
- Moved `@playwright/test` and `ts-morph` to development dependencies.
- Removed duplicate root `@tailwindcss/vite` and `tailwindcss`; the webview owns
  these build dependencies.
- Retained Mermaid because `MermaidBlock` renders chat diagrams.
- Retained Puppeteer/chrome-launcher for the browser tool, protobuf/Buf tooling
  for normal contract generation, and Mocha/Chai/Should/Sinon/C8/Vitest/
  Playwright/VS Code runners as development-only test infrastructure.
- Production Knip now enters both `src/extension.ts` and
  `webview-ui/src/main.tsx`; the production files/exports/types check passes.

## Legal

- Added `NOTICE` and `MODIFICATIONS.md`.
- Preserved Apache-2.0 and the applicable Cline Bot Inc. notice.
- The README identifies Bedrock Coder as an independent derivative and states
  that it is not affiliated with Cline Bot Inc. or Amazon Web Services.

## Verification

Completed successfully:

- Clean removal of 1,598 workspace `node_modules` directories.
- `bun install` with Bun 1.3.13; lockfile regenerated by Bun.
- `bun run build:sdk`.
- `apps/vscode: bun run check-types`.
- `apps/vscode: bun esbuild.mjs --production`.
- `apps/vscode: bun run package`.
- `apps/vscode: bun run check:identity`.
- `apps/vscode: bun run analyze:unused:prod`.
- Local VSIX packaging and archive inspection.
- Install into isolated VS Code user-data/extensions directories.
- Hidden isolated-profile launch: extension host activated
  `fffalexgo.bedrock-coder`; the Bedrock Coder output channel initialized and
  no activation errors were logged.

The exact identity searches now return only:

- the legally retained `Cline Bot Inc.` notice in `apps/vscode/LICENSE`;
- false substring matches in `srcLine` and `isAnthropicLineageValue`.

Credential-dependent model discovery, a live Bedrock chat/edit/terminal turn,
and checkpoint resume were not executed because the isolated profile was not
given AWS credentials. Theme-by-theme visual inspection was also not automated;
the Activity Bar asset uses `currentColor` and the exact supplied PNG was hash
verified through the production webview bundle.

## Package and working-folder measurements

- Final working folder: 1,136,021,101 bytes across 91,400 files.
- Reduction from baseline: 95,679,223 bytes (7.77%) and 5,048 files.
- Final VSIX: 10,282,372 bytes (9.81 MB), 20 archive entries.
- Final VSIX SHA-256:
  `360E498A83D9872E03EB155CF7DE570529D3C57C5A29D34298A8C572744C6A37`.
- VSIX inspection found zero tests, fixtures, Storybook files, source maps,
  `node_modules`, Playwright/browser downloads, caches, or obsolete branding.
