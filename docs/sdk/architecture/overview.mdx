---
title: "Architecture Overview"
sidebarTitle: "Overview"
description: "How SDK packages fit together, what each one exports, and where boundaries are enforced."
---

The SDK is organized as a layered package stack. Each package has a clear responsibility and can only depend on lower layers.

If you only need a stateless agent loop, use `@clinebot/agents`. If you need sessions, persistence, built-in tools, scheduling, and hub support, use `@clinebot/core` (or just install `@clinebot/sdk`).

## Package Stack

```
┌──────────────────────────────────────────────────────────────┐
│  Your Application / CLI / VS Code / JetBrains                │
└────────────────────────┬─────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────┐
│  @clinebot/core                                              │
│  Sessions, persistence, config discovery, built-in tools,    │
│  hub daemon, scheduling, telemetry                           │
└────┬──────────────┬──────────────────────────────────────────┘
     │              │
┌────▼─────┐  ┌─────▼──────┐
│@clinebot │  │ @clinebot  │
│/agents   │  │ /llms      │
│          │  │            │
│Stateless │  │Provider    │
│agent loop│  │gateway,    │
│          │  │model       │
│          │  │catalogs    │
└────┬─────┘  └─────┬──────┘
     │              │
     └──────┬───────┘
            │
┌───────────▼──────────────────────────────────────────────────┐
│  @clinebot/shared                                            │
│  Contracts, types, schemas, hook engine, tool interfaces      │
└──────────────────────────────────────────────────────────────┘
```

## Package Responsibilities

### @clinebot/shared

Foundation package for contracts and shared utilities.

- `Tool` interface and `createTool` helper
- `AgentEvent` union type
- `HookEngine` for lifecycle callbacks
- `ContributionRegistry` for extension registration
- `Message` and `ContentBlock` types
- Logging interfaces (`BasicLogger`)
- Path helpers and schemas

No higher-layer dependencies. Safe to import anywhere.

### @clinebot/llms

Provider gateway and model catalog layer.

- `DefaultGateway` for provider registration and model discovery
- Provider handlers for Anthropic, OpenAI, Google, Bedrock, Mistral, and more
- Model catalogs with pricing, capabilities, and context window metadata
- Streaming support with consistent event format across providers
- Provider settings management

Depends on: `@clinebot/shared`

### @clinebot/agents

Stateless execution loop.

- `Agent` class with `run()`, `continue()`, `abort()`, `shutdown()`
- Turn processing (text, reasoning, tool calls)
- Tool orchestration with parallel execution support
- Loop detection and mistake tracking
- Plugin and hook runtime
- `AgentStream` for async event iteration

Depends on: `@clinebot/shared`, `@clinebot/llms`

### @clinebot/core

Full runtime/orchestration layer.

- `ClineCore` entry point
- Session lifecycle and persistence (SQLite)
- Built-in tools (bash, editor, read_files, apply_patch, search, fetch_web)
- Config file discovery and watching (rules, skills, hooks, plugins)
- MCP server integration
- Tool approval flows
- Telemetry (OpenTelemetry)
- Hub integration for multi-process architectures
- Team management for multi-agent coordination

Depends on: `@clinebot/shared`, `@clinebot/llms`, `@clinebot/agents`

## Package Reference

This section replaces the old separate "Packages" page.

### Install each package

```bash
npm install @clinebot/shared
npm install @clinebot/llms
npm install @clinebot/agents
npm install @clinebot/core
```

### @clinebot/shared key exports

| Export | Description |
|--------|-------------|
| `createTool` | Helper to create a typed `Tool` |
| `Tool` | Tool interface |
| `ToolContext` | Context passed to tool execute functions |
| `ToolPolicy` | Per-tool approval/enablement config |
| `AgentEvent` | Union type of agent events |
| `Message` | Conversation message type |
| `ContentBlock` | Text/tool/thinking content blocks |
| `HookEngine` | Lifecycle hook engine |
| `ContributionRegistry` | Extension registration system |
| `BasicLogger` | Logging interface |

### @clinebot/llms key exports

| Export | Description |
|--------|-------------|
| `DefaultGateway` | Main gateway for provider/model management |
| `GatewayRegistry` | Registry for provider/model lookup |
| `ModelInfo` | Model metadata (pricing/capabilities/context window) |
| `ProviderInfo` | Provider metadata and capabilities |
| Provider handlers | Anthropic, OpenAI, Google, Bedrock, Mistral, etc. |

### @clinebot/agents key exports

| Export | Description |
|--------|-------------|
| `Agent` | Main agent class |
| `AgentConfig` | Constructor configuration type |
| `AgentResult` | Return type from `run()` and `continue()` |
| `AgentPlugin` | Plugin interface |
| `AgentStream` | Async iterable event stream |
| `streamRun` | Helper for streaming runs |
| `AgentUsage` | Token usage tracking type |

### @clinebot/core key exports

| Export | Description |
|--------|-------------|
| `ClineCore` | Main runtime entry point |
| `ClineCoreOptions` | Constructor options |
| `StartSessionInput` | Session creation input |
| `StartSessionResult` | Session creation result |
| `CoreSessionConfig` | Session configuration type |
| `SessionRecord` | Persisted session metadata |


## Design Principles

### Strict dependency direction

Dependencies flow downward only. `core` depends on `agents`, never the reverse. This keeps lower layers testable and embeddable without pulling in the full stack.

### Stateless agent loop

The `Agent` class in `@clinebot/agents` does not do I/O, persistence, or transport. It's a pure state machine: take messages in, produce tool calls and text out. This makes it easy to test, embed, and reason about.

### Hub as coordinator, not executor

The [hub](/sdk/architecture/hub-spoke) coordinates sessions, routes events, manages schedules, and brokers capabilities between clients. It does not run the agent loop. Spoke workers handle execution with a process boundary so the hub stays responsive. Hub functionality lives inside `@clinebot/core`.

### Config as files

Rules, skills, hooks, and plugins are discovered from the file system. New instruction sources should materialize as files and use the watcher discovery system, not parallel in-memory paths. This makes configuration version-controllable and inspectable.

