[
 {
  "number": 9983,
  "title": "Degraded performance after upgrading to vscode 1.113.0",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The obfuscated frame the reporter cites (`n.responseStream.e.<computed>`) maps to the gRPC streaming subscription plumbing, which still exists on main at `apps/vscode/src/core/controller/grpc-handler.ts` (`responseStream` handler) and `apps/vscode/src/core/controller/grpc-request-registry.ts`. I found no idle-CPU fix commit in `git log --since=2026-03-26 -- apps/vscode/src/core/controller`, but the reporter self-reported the load average returning to normal on 3.75->3.78.0.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed against the legacy 3.75 marketplace build and self-resolved on 3.78.0. No profile/trace attached that names a symbol still on main; would need a fresh CPU profile against apps/vscode before it is actionable.",
  "cluster": ""
 },
 {
  "number": 9990,
  "title": "Cline Kanban CLI: TypeError: schema is not a function in @ai-sdk/provider-utils",
  "area": "unknown",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Every stack frame is inside `node_modules/kanban/...`, and Kanban is a separate repository per `README.md` (`| **Kanban** | ... | cline/kanban |`). In this repo `apps/cli/src/commands/kanban.ts` only shells out to install/launch the external `kanban` npm package; no `asSchema`/`prepareToolsAndToolChoice` tool-schema construction exists under `sdk/packages/llms/src/providers`.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Maintainer ClineXDiego already routed it to the Kanban team. Belongs in cline/kanban.",
  "cluster": ""
 },
 {
  "number": 9994,
  "title": "Global hooks created in wrong directory on WSL",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Still split on main: the hook management RPCs (`apps/vscode/src/core/controller/file/createHook.ts`, `toggleHook.ts`, `deleteHook.ts`) call `resolveHooksDirectory()` which hardcodes `path.join(os.homedir(), \"Documents\", \"Cline\", \"Hooks\")` (`apps/vscode/src/core/hooks/utils.ts:54`, same literal in `refreshHooks.ts:14`), while hook execution reads `getAllHooksDirs()` -> `getGlobalHooksDir()` -> `ensureHooksDirectoryExists()` (`apps/vscode/src/core/storage/disk.ts:94`), which resolves through `getDocumentsPath()` (`apps/vscode/src/core/storage/documents-path.ts`) and returns `xdg-user-dir DOCUMENTS` \u2014 i.e. `$HOME` on WSL where XDG_DOCUMENTS_DIR is unset.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Silent failure: hooks appear in the UI but never fire. Fix is mechanical (route resolveHooksDirectory/refreshHooks through ensureHooksDirectoryExists) but needs a product call on whether ~/Documents/Cline or XDG Documents is canonical, and a migration for hooks already written to the wrong path. Owned by apps/vscode.",
  "cluster": ""
 },
 {
  "number": 9999,
  "title": "Cline Kanban Claude Code integration not working",
  "area": "unknown",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Reported against Kanban v0.1.45, which lives in `cline/kanban` (see `README.md` products table); `apps/cli/src/commands/kanban.ts` is only a launcher. The Claude Code provider on main is a thin wrapper over the third-party `ai-sdk-provider-claude-code` package (`sdk/packages/llms/src/providers/vendors/community.ts:85 createClaudeCodeProviderModule`) and contains no 120000ms request timeout \u2014 `grep -rn \"120000\"` finds nothing in `sdk/packages/llms/src`.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "The 120s timeout comes from Kanban's own agent loop, not from @cline/llms. Should be moved to cline/kanban.",
  "cluster": ""
 },
 {
  "number": 10006,
  "title": "npm i -g cline fails on macOS arm64 \u2014 sharp build error via ink-picture",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`apps/cli/package.json` (package `@cline/cli`, bin `cline`) has no `ink`, `ink-picture` or `sharp` dependency and no optionalDependencies; the TUI is OpenTUI-based (`@opentui/core`, `@opentui/react`). The Ink CLI that pulled in `ink-picture` was the old repo-root `cli/` tree, deleted in `00d423937 chore: remove legacy cli/ source and direct references` (2026-05-13) after the OpenTUI rewrite `2dfc796b3 feat: rewrite CLI TUI from Ink to OpenTUI (#227)`.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Safely auto-closeable: the native-dependency chain that broke global install no longer exists in the published CLI.",
  "cluster": ""
 },
 {
  "number": 10011,
  "title": "Unable to use Cline via ACP in JetBrains IDEs",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The ACP agent was rewritten and now lives at `apps/cli/src/acp/acpAgent.ts`; `newSession()` (line 112) is a completely different implementation from the 3.67/3.75 path the reporter hit. It does perform an unguarded `await Llms.getModelsForProvider(providerId)` inside `newSession`, so any network failure (the Linear title mentions a corporate proxy) would surface exactly as the reported opaque `-32603 Internal error` with empty `data`.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Unreproducible as filed (no proxy config, no cline-side stderr beyond the JSON-RPC envelope). Worth a small hardening pass regardless: wrap the model fetch in acpAgent.newSession so proxy/network failures return a typed ACP error instead of a bare internal error.",
  "cluster": ""
 },
 {
  "number": 10015,
  "title": "Cline wants to execute this command:  Skipped, thinking",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The hang the commenters diagnosed (a `for await` over `execution.read()` that never terminates in `VscodeTerminalProcess`) was rewritten on main by `12703bf40 Improve VS Code terminal reliability: OSC 633 parser, exit codes, timeout handling (#11972)` (2026-07-15): `apps/vscode/src/hosts/vscode/terminal/VscodeTerminalProcess.ts` now pulls the iterator manually and races each read against MARKERLESS_FIRST_DATA_TIMEOUT/MARKERLESS_IDLE_TIMEOUT/MARKERLESS_MAX_QUIET_TIME and terminal closure, plus a 3s EXIT_CODE_EVENT_TIMEOUT_MS. However, once the OSC 633 `C` marker is seen the code deliberately passes `idleTimeoutMs = undefined` (line ~207), so a post-C stream that never ends is still unbounded except by terminal closure.",
  "verdict": "duplicate",
  "duplicate_of": 12079,
  "severity": "high",
  "repro": "1. Build and launch the new extension: `cd apps/vscode && bun run build:webview && bun esbuild.mjs`, then `DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <some-folder>`.\n2. In Cline Settings > Terminal, leave Terminal Execution Mode on the default (VS Code shell integration), NOT Background Exec.\n3. Use a shell where VS Code shell integration attaches (bash/zsh on Linux or macOS; the reporters were on Ubuntu/Fedora/Debian and macOS).\n4. Ask the agent in Act mode to run a short command repeatedly, e.g. `ls -lh /home/ubuntu` then `echo hi` then a piped `curl ... | python3 -c ...`, approving each.\n5. Expected: each command shows Executing then Completed, and the turn advances.\n6. Actual (bug): the command row renders `Skipped`, the sidebar sits on `Thinking...` indefinitely, and `[TerminalManager] Terminal N completed, setting busy to false` never appears in the Cline output channel.\n7. Cross-check by flipping Terminal Execution Mode to Background Exec (StandaloneTerminalProcess/child_process.spawn path) \u2014 if the hang disappears there, it is the shell-integration read loop.",
  "human_attention": "no",
  "notes": "Highest-volume complaint in this batch (10+ distinct reporters, plus dupe #10031 and cross-refs #9857/#10208). #11972 should have bounded it; needs a verification pass on main rather than a blind close. Exorion1er's comment is a genuinely good root-cause writeup worth keeping.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 10016,
  "title": "GUI configuration shows blank/incorrect models from LM Studio API on Windows 11",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The `providers.json` the workaround edits is the SDK-owned store (`sdk/packages/core/src/services/storage/provider-settings-manager.ts`), so the root cause is in scope even though the UI is Kanban's. On main `sdk/packages/llms/src/providers/builtins.ts:885` registers lmstudio with `modelsSourceUrl: \"http://localhost:1234/v1/models\"`, and `sdk/packages/core/src/services/llms/provider-defaults.ts:905-927` now runs keyless live model sources \"even if the caller didn't pass a config\", falling back to the spec base URL. That path plus manual model entry came from `ffc01770e fix(cli): improve local provider onboarding and model selection [ENG-1927] (#338)` (2026-05-04), whose description names exactly these gaps for Ollama/LM Studio.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Worth re-verifying in Kanban specifically, since Kanban pins its own @cline/core version.",
  "cluster": ""
 },
 {
  "number": 10022,
  "title": "Sixth does not detect custom MCP server from sixth-mcp-settings.json even though the stdio server starts successfully",
  "area": "unknown",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The report is entirely about the `sixth.sixth-ai` VS Code extension and its `sixth-mcp-settings.json`. A case-insensitive search for `sixth` across the whole repo (excluding node_modules/dist) returns zero hits; Cline's MCP settings file is `cline_mcp_settings.json` (`apps/vscode/src/core/storage/disk.ts:28`).",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Third-party Cline fork; filed on the wrong tracker. Auto-closeable.",
  "cluster": ""
 },
 {
  "number": 10030,
  "title": "Extension host terminated unexpectedly 3 times within the last 5 minutes",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "No stack, no extension-host log, no VS Code version. Nothing to ground against: the 3.76 extension host code has been replaced by `apps/vscode/src/extension.ts` + `apps/vscode/src/common.ts` on main, and there is no reported crash signature (OOM, native module, unhandled rejection) to search for.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter never supplied the extension host log; stale since March. Ask for `Developer: Open Extension Logs Folder` output or close.",
  "cluster": "extension-host-crash"
 },
 {
  "number": 10031,
  "title": "Stuck on Thinking...",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Same symptom, same trigger (`ls`/`echo`), same log signature as #10015 \u2014 `[TerminalManager Test] Shell integration activated ... Proceeding with command execution` with no matching completion line from `apps/vscode/src/hosts/vscode/terminal/VscodeTerminalManager.ts`. Author AhmadSuweiti also commented \"I can reproduce this issue also\" on #10015 the same day.",
  "verdict": "duplicate",
  "duplicate_of": 10015,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Fold into #10015. Keep #10031's terminal logs (ian-hailey, kong62) as evidence there \u2014 they are cleaner than #10015's.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 10035,
  "title": "Save/Reject buttons become disabled on second file during multi-file edit approval",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Fixed on main by `0175548dd fix(vscode): re-enable approval buttons for consecutive asks` (2026-06-03), whose message describes exactly this: \"The footer Approve/Reject buttons stayed disabled when a second consecutive approval ask arrived. The button configs are shared singletons (e.g. BUTTON_CONFIGS.tool_approve), so two identical asks return the same object reference.\" The fix is present in `apps/vscode/webview-ui/src/components/chat/chat-view/components/layout/ActionButtons.tsx` (ref-based latch keyed on `askIdentity = anchorTs:primaryText:secondaryText`, line 53) with a regression test in `ActionButtons.test.tsx`. The shared singletons are still in `chat-view/shared/buttonConfig.ts:53 tool_approve`.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Fix is in the shared webview, so JetBrains gets it too once the plugin picks up the new bundle. Needs a human only to post a status/version comment \u2014 this thread has five escalating users (\"How is this not P0\", two churn reports) and the P3 label is now wrong.",
  "cluster": "diff-approve-buttons-stuck"
 },
 {
  "number": 10042,
  "title": "Kanban view shows \"No working changes\" for tasks that have diffs",
  "area": "unknown",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "Reporter self-resolved in the first comment: the diff was empty because auto-commit was on. The remaining ask (\"a tab that shows all changes against the main branch\") is a Kanban UI feature, and Kanban lives in `cline/kanban` per `README.md`; nothing under `apps/` or `sdk/packages/` renders the Kanban board.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Not a defect. Maintainer already acknowledged the feedback; move the enhancement to cline/kanban.",
  "cluster": ""
 },
 {
  "number": 10052,
  "title": "cline kanban: not restoring sessions after process crash",
  "area": "unknown",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Kanban card/session state is owned by the `cline/kanban` package (`README.md` products table); `apps/cli/src/commands/kanban.ts` only installs and spawns it. The SDK session store on main (`sdk/packages/core/src/services/session-data.ts`, `sdk/packages/shared/src/storage/paths.ts resolveSessionDataDir`) has no Kanban card/trash concept.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Maintainer said session recovery improved in later Kanban builds; chik-lab's follow-up (work lost when a reviewing card is trashed) is a genuine data-loss report but belongs in cline/kanban. The ctrl+w and 'rust-native kanban' asks are separate enhancements.",
  "cluster": "kanban-crash-data-loss"
 },
 {
  "number": 10054,
  "title": "Cline preventing sleep on macos",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Nothing on main obviously holds a macOS power assertion: the only long-lived timers in the extension are `apps/vscode/src/sdk/SdkController.ts:715` (remote config poll), `apps/vscode/src/services/temp/ClineTempManager.ts:195` (temp cleanup) and `apps/vscode/src/shared/services/worker/worker.ts:154` (storage sync queue), and the only listening sockets are the ephemeral OAuth callback servers (`apps/vscode/src/hosts/external/AuthHandler.ts:142`, `apps/vscode/src/integrations/openai-codex/oauth.ts:779`). Node timers do not create sleep assertions, so I could not attribute this to a specific symbol.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Extension bisect is decent evidence it is Cline, but the maintainer's three follow-up questions were never answered and it went stale. `pmset -g assertions` output while idle would make it actionable. Possibly the same root cause as #9983 (both 3.76 + VS Code 1.113.0).",
  "cluster": ""
 },
 {
  "number": 10094,
  "title": "Cline is forcefully using 128k context window for all models of copilot",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Still present on main: `apps/vscode/src/sdk/vscode-lm/vscode-lm-handler.ts:18` defines `FALLBACK_CONTEXT_WINDOW = 128_000`, and `getModel()` returns it unconditionally (lines 226-241) whenever `this.client` is unset \u2014 the client is only populated during `createMessage()`, so before the first request the UI always reports 128k. Even after the client exists, line 210 only uses `client.maxInputTokens` when it is a positive number.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Agent-fixable: resolve the LanguageModelChat eagerly in getModel() (or cache maxInputTokens from getVsCodeLmModels) instead of returning the fallback. Beyond the display, the under-reported contextWindow also drives compaction budgets, so it truncates earlier than necessary.",
  "cluster": "openai-compat-context-window-ignored"
 },
 {
  "number": 10115,
  "title": "OpenAl Compatible Use exception",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The 'Proceed Anyways' prompt is the mistake-limit ask (`apps/vscode/webview-ui/src/components/chat/chat-view/shared/buttonConfig.ts:43 mistake_limit_reached`). HEAD commit `e4091c3686 Stop the task at the mistake limit like the CLI and remove the max-mistakes setting (#12561)` describes exactly the 'Proceed Anyways does nothing' failure: \"the extension used to block on an ask (Proceed Anyways / Start New Task) while the agent loop kept running against the provider\"; it is now replaced by the non-interactive stop resolver in `apps/vscode/src/sdk/sdk-interaction-coordinator.ts:61-86`. I found no model-capability gate for openai-compatible on main \u2014 the only such text is an informational note in `apps/vscode/webview-ui/src/components/settings/providers/OpenAICompatible.tsx:603`.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Screenshots are the only description of the actual error, so this is partly inferred. If the reporter's real blocker was something other than the stuck mistake-limit ask, it needs re-filing with logs. The maintainer's 'model recognition' theory is not backed by code on main.",
  "cluster": ""
 },
 {
  "number": 10128,
  "title": "/tmp/cline directory permission conflict when multiple users run Cline CLI",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/vscode/src/services/temp/ClineTempManager.ts:39-47` still does `fs.mkdirSync(path.join(os.tmpdir(), \"cline\"))` with no per-user namespacing and no restrictive mode, so on Linux the first user owns `/tmp/cline` at 0755 and later users get EACCES when `createTempFilePath()` output is written (e.g. from `apps/vscode/src/sdk/vscode-run-commands-tool.ts:117`). `apps/cli/src/utils/helpers.ts:535` has the same shape for `join(tmpdir(), \"cline-sandbox\")`. The CLI's normal state dir is safe \u2014 `sdk/packages/shared/src/storage/paths.ts:40` explicitly notes it lives under the cline data dir \"not os.tmpdir()\" so \"the path is private to the user on multi-user hosts\".",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "The open community PR #10150 patches `src/services/temp/ClineTempManager.ts` \u2014 a path that no longer exists on main. It needs to be re-targeted to `apps/vscode/src/services/temp/ClineTempManager.ts` (and ideally `apps/cli/src/utils/helpers.ts resolveSandboxDataDir` too), which is why a human should look.",
  "cluster": ""
 },
 {
  "number": 10129,
  "title": "When typing anywhere in the IDE, the cursor unexpectedly returns to the cline text box.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/vscode/webview-ui/src/components/chat/ChatView.tsx` still steals focus unconditionally: a `useMount(() => textAreaRef.current?.focus())` (line ~338) plus an effect keyed on `[isHidden, sendingDisabled, enableButtons]` that calls `textAreaRef.current?.focus()` on a 50ms timer (lines ~341-350) with no check that the webview currently owns focus. `sendingDisabled` flips at every request boundary, so the composer grabs focus mid-task while the user is typing in the editor. The showWebview subscription (line ~287) is the only path that honours a `preserveEditorFocus` flag.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Third reporter says it happens consistently at VS Code startup, which matches the useMount focus. Fix needs a judgment call on which of the three focus paths to gate on document.hasFocus()/webview visibility. Owned by apps/vscode/webview-ui.",
  "cluster": ""
 },
 {
  "number": 10130,
  "title": "A common occurrence among amateurs",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Abusive tone aside, the structural claim checks out on main: `apps/vscode/src/extension.ts activate()` runs `await cleanupLegacyVSCodeStorage(context)` (line 76), `await exportVSCodeStorageToSharedFiles(...)` (line 82) and `await initialize(storageContext)` (line 86) with no try/catch, and the first `vscode.commands.registerCommand` is not reached until line 128. If any of those awaits rejects, the sidebar view still renders but every command (`commands.PlusButton`, `commands.SettingsButton`, ...) is unregistered \u2014 exactly the 'command not found' ghost-ship symptom.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Two separate decisions for a human: (a) harden activate() by registering commands before the storage-migration awaits or wrapping them in try/catch, and (b) the Code of Conduct response to the report itself. There is no repro and no version info attached, so the hardening is the only actionable part.",
  "cluster": "extension-not-activating"
 },
 {
  "number": 10133,
  "title": "Task command failed at http://127.0.0.1:3484: fetch failed",
  "area": "unknown",
  "kind": "question-support",
  "relevant_to_main": "no",
  "evidence": "'Operation not permitted' on a localhost connect is an OS/CLI sandbox denial, not a Cline error string \u2014 it appears nowhere in `sdk/packages` or `apps/`. Cline's own `--data-dir`/`CLINE_SANDBOX` mode only relocates state (`apps/cli/src/utils/helpers.ts:529 resolveSandboxDataDir`, `configureSandboxEnvironment`) and never restricts network. The reporter was on GPT-5.4, which routes through the Codex CLI (`sdk/packages/llms/src/providers/vendors/community.ts createOpenAICodexProviderModule`), whose own seatbelt sandbox blocks outbound connections by default \u2014 matching the maintainer's unanswered question \"Is this being run with codex cli?\".",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Almost certainly the Codex CLI network sandbox, i.e. user configuration. Also a Kanban usage question. Close with the sandbox explanation if the reporter stays silent.",
  "cluster": ""
 },
 {
  "number": 10135,
  "title": "Claude Code API Provider: Command failed with exit code 1 for all sonnet models",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Both error strings from the thread \u2014 \"Command failed with exit code 1\" and \"Unrecognized Claude Code chunk type: result\" \u2014 are absent from the whole tree (`grep -rn` across `sdk/` and `apps/` excluding node_modules/dist). The hand-rolled `claude -p --output-format stream-json` subprocess handler that produced them was replaced on main by a thin wrapper over the third-party `ai-sdk-provider-claude-code` package: `sdk/packages/llms/src/providers/vendors/community.ts:85 createClaudeCodeProviderModule`, dispatched from `sdk/packages/llms/src/providers/ai-sdk.ts:1122`.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "The defect itself lives in retired 3.x code, but it is a total loss of Sonnet on the currently shipped build for both JetBrains and VS Code (confirmed on 3.77/3.78 by davogler and kenth619). A human should decide whether to patch the legacy branch or just tell users the SDK rewrite supersedes it \u2014 and should re-verify Sonnet + Claude Code end-to-end on apps/vscode before closing.",
  "cluster": "claude-code-exit-code-1"
 },
 {
  "number": 10139,
  "title": "429 with long retry-after retried instead of surfaced \u2014 WAIT vs STOP not distinguished",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The file the report blames, `src/core/api/retry.ts`, does not exist on main (the only retry helper left is the unrelated `apps/vscode/src/utils/retry.ts`, and `grep -rn 'retry-after'` across `sdk/packages` + `apps/*/src` matches only `apps/vscode/src/services/banner/BannerService.ts:371`). LLM retries now go through the AI SDK's `streamText` (`sdk/packages/llms/src/providers/ai-sdk.ts:1212`), and `ai@6.0.224`'s `getRetryDelayInMs` already discards header-derived delays that are not `< 60s`, falling back to exponential backoff \u2014 so the described 3-hour stall cannot happen.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Human needed because of open community PR #11722 (\"Fix long retry-after handling\", adds a 5-minute retry-after guard to sdk/packages/llms/src/providers/ai-sdk.ts). Its premise is largely obsoleted by the AI SDK's own 60s cap, so someone should decide whether it is still worth taking. The residual, much smaller issue is that quota-exhaustion 429s still consume 2 retries rather than surfacing immediately. Also note Mike668866's comment is affiliate spam for a third-party 'nexus-inference.com' gateway and should be removed.",
  "cluster": ""
 },
 {
  "number": 10147,
  "title": "Cline disconnects from Ollama models via SSH tunnel",
  "area": "sdk-llms",
  "kind": "question-support",
  "relevant_to_main": "unsure",
  "evidence": "Ollama was rewritten on main into a native vendor with an explicit, configurable request timeout that only bounds time-to-first-byte and never cuts off streaming (`sdk/packages/llms/src/providers/vendors/ollama.ts:60-110`, landed in `7f9d2e96d fix(ollama): restore native API routing so context window and timeout settings work (#12286)`), so the 3.77 handler the reporter used is gone. The reporter's own comments attribute the drops to duplicated autossh IPv4/IPv6 forwards, and they still saw failures after switching to WireGuard.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Environment/networking, not a Cline defect as filed. The one Cline-side nugget worth keeping is the reconnect loop the reporter describes; that would need its own issue with the loop's log output.",
  "cluster": ""
 },
 {
  "number": 10148,
  "title": "Impossible token count in context window",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The accumulation the thread diagnosed (Cline summing the per-chunk `usage` that vLLM/ik_llama.cpp emit under `--enable-force-include-usage`) is gone on main: `emitAiSdkEvents` in `sdk/packages/llms/src/providers/ai-sdk.ts` yields exactly one `{type:\"usage\"}` event after the stream ends, sourced from the AI SDK's aggregated `await stream.usage` with a fallback to the single finish-part usage (lines ~1048-1074), and there is no `+=` on token counts anywhere in `sdk/packages` outside `basic-compaction.ts` (which sums across messages, not chunks). The upstream `@ai-sdk/openai-compatible` provider also overwrites rather than accumulates (`usage = value.usage`).",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Worth a one-line confirmation on the thread since the secondary symptom (over-truncation of history from inflated counts) was the more damaging one. Reporters were on 3.77.",
  "cluster": "inflated-token-count"
 },
 {
  "number": 10151,
  "title": "Kanban model selector has different/much smaller selection than VScode extension model selector",
  "area": "cli",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "`apps/cli/src/commands/kanban.ts` only resolves, installs (`KANBAN_INSTALL_COMMANDS` -> `kanban@latest`) and spawns the external `kanban` npm binary via `spawnKanbanProcess`; no Kanban UI or model-selector source exists anywhere in this repo, and the extension's catalog lives separately in `sdk/packages/llms/src/catalog/catalog.generated.ts`. Kanban is also being superseded per `apps/cli/src/kanban-migration/notice.ts` (\"Try ClinePass\").",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Model-catalog parity has to be fixed in the separate `kanban` package; maintainer already acknowledged it as backlog.",
  "cluster": ""
 },
 {
  "number": 10158,
  "title": "Plan Mode: Interface hangs after plan_mode_respond, prevents bug reporting and all conversation flow",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "There is no `plan_mode_respond` tool on main: the default tool set is `ALL_DEFAULT_TOOL_NAMES` in `sdk/packages/core/src/extensions/tools/constants.ts` (read_files/search_codebase/run_commands/fetch_web_content/apply_patch/editor/skills/ask/submit_and_exit), and plan mode is prose-driven via `PLAN_MODE_INSTRUCTIONS` in `sdk/packages/shared/src/prompt/cline.ts` plus a `switch_to_act_mode` tool added in `apps/vscode/src/sdk/sdk-session-config-builder.ts`. The remaining `plan_mode_respond` strings in `apps/vscode/src/shared/tools.ts` and `apps/vscode/src/core/prompts/responses.ts` are legacy message-rendering shims.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter's System Information says IntelliJ IDEA + litellm, so it was the JetBrains-embedded 3.76 core. The whole ask/response loop it blames was replaced.",
  "cluster": ""
 },
 {
  "number": 10163,
  "title": "Cline fails to spawn kanban",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Exactly the requested fix is on main: `apps/cli/src/main.ts` (lines ~690 and ~745) passes `preferredInstaller: getPreferredKanbanInstaller()` into `launchKanban`, and `getPreferredKanbanInstaller` in `apps/cli/src/commands/update.ts` maps `getInstallationInfo().packageManager` (which detects `/pnpm/global`, `/.bun/bin`, npx, yarn from the resolved script path) onto the installer that `resolveKanbanInstallCommand` in `apps/cli/src/commands/kanban.ts` prefers.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Introduced in bff7bf1c4 \"feat(cli): add kanban command and notice dialog (#97)\" (2026-05-11). `ensureKanbanInstalled` also now re-checks PATH after install and prints an actionable message instead of a bare ENOENT.",
  "cluster": ""
 },
 {
  "number": 10166,
  "title": "Multi-platform localization failure - All languages except English fail to apply in both VS Code and JetBrains plugins",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "There is no webview i18n implementation to fail: no `package.nls*`, locale or i18n bundle files exist anywhere under `apps/vscode`. The only language feature is `preferredLanguage`, a prompt-only setting (`apps/vscode/webview-ui/src/components/settings/PreferredLanguageSetting.tsx` -> `apps/vscode/src/shared/storage/state-keys.ts`), and it is wired on main in `apps/vscode/src/sdk/cline-session-factory.ts` (~line 796) which appends `# Preferred Language\\n\\nSpeak in <lang>.` to the system prompt.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Localized UI is a new feature, not a regression. Minor real nit found while checking: `cline-session-factory.ts` injects the language *key* from `getLanguageKey` in `apps/vscode/src/shared/Languages.ts` (\"ja\", \"zh-CN\") rather than the display name, which weakens the instruction.",
  "cluster": "ui-localization"
 },
 {
  "number": 10167,
  "title": "Kanban Agent fails with OCA OAuth re-authentication error despite active session",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The reported error text (`OAuth session for \"oca\" requires re-authentication. Run \"clite auth oca\"`) does not exist anywhere on main. OAuth resolution was centralized into `sdk/packages/core/src/runtime/orchestration/runtime-oauth-token-manager.ts` (single-flight `resolveProviderApiKey`, `OAuthReauthRequiredError`) with a retry wrapper `runWithAuthRetry`/`syncOAuthCredentials` in `sdk/packages/core/src/runtime/host/local-runtime-host.ts` (~line 2105).",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Relevant commits: 0c027d273 \"Centralize OAuth management to the SDK (#11260)\" (2026-06-09) and a3989acc3 \"fix(sdk): don't log users out when token refresh fails due to network/server errors (#12255)\" (2026-07-13), which is the exact class of false re-auth prompt reported. Worth asking the reporter to retest before closing.",
  "cluster": ""
 },
 {
  "number": 10169,
  "title": "\"\\x64\" in a path keeps getting removed",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Reproduced the corruption mechanism directly against main's code: `sdk/packages/llms/src/providers/ai-sdk.ts:1218` wires `experimental_repairToolCall: repairMalformedToolCall`, which routes malformed tool arguments through `parseJsonStream` (`sdk/packages/shared/src/parse/json.ts`, strategy `JSON.parse(jsonrepair(text))`). Running that function on `{\"path\":\"C:\\Users\\me\\proj\\x64\\Debug\\app.exe\"}` returns `C:Usersmeprojx64Debugapp.exe` -- every single backslash is silently dropped and the repaired call is executed with no warning.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Surface: `apps/vscode` (or the CLI); any provider going through the AI-SDK gateway.\n2. Open a Windows workspace containing a path with backslashes, e.g. `C:\\repo\\x64\\Debug\\app.exe`.\n3. Ask Cline to read/edit that file so the model emits a tool call whose JSON string is under-escaped, e.g. `{\"path\":\"C:\\repo\\x64\\Debug\\app.exe\"}` (single backslashes). A local/weaker model reproduces this readily; you can also force it with a scripted model in `sdk/packages/llms/src/providers/ai-sdk.tool-calls.test.ts`.\n4. Expected: the tool call is rejected with the \"invalid JSON arguments\" tool error so the model retries with correct escaping.\n5. Actual: `repairMalformedToolCall` -> `parseJsonStream` -> `jsonrepair` silently strips the backslashes and the tool runs against `C:repox64Debugapp.exe`, so path segments (including `\\x64`) disappear from the command/edit that follows.",
  "human_attention": "yes",
  "notes": "Fix likely belongs in `sdk/packages/shared/src/parse/json.ts`: do not accept a jsonrepair result whose string values lost backslashes, or repair escapes before delegating. Silent lossy repair of Windows paths is a data-correctness issue, not just cosmetic.",
  "cluster": ""
 },
 {
  "number": 10172,
  "title": "README.md and VSCode description are very out-of-date (mostly from 2024)",
  "area": "docs",
  "kind": "meta-task",
  "relevant_to_main": "yes",
  "evidence": "The GitHub README half is done (`README.md` was rewritten in 09885cb88 \"docs: rewrite README for monorepo structure (#10461)\" and polished in 6c9f72049 / 678b0ae95). The Marketplace half is still open: `apps/vscode/README.marketplace.md` -- the source `.vscodeignore` names as the marketplace README swapped in by `scripts/marketplace-readme.mjs` -- still opens with the 2024 copy (\"Meet Cline, an AI assistant that can use your CLI aNd Editor\" / \"Thanks to Claude Sonnet's agentic coding capabilities\"), and `apps/vscode/README.md` is a 0-byte file.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Marketing copy for the 4.0 extension listing needs a human author; also worth deciding whether the empty `apps/vscode/README.md` should be a stub or symlink.",
  "cluster": ""
 },
 {
  "number": 10179,
  "title": "Sub-agents do not work properly with z-ai/glm-5 (and glm-5.1)",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The mechanism the reporter correctly diagnosed (a GLM prompt variant calling `.overrideComponent(\"TOOL_USE_SECTION\", ...)`) no longer exists: `rg` finds zero hits for `overrideComponent` or `TOOL_USE_SECTION` anywhere on main, and there is no per-model prompt-variant directory (`sdk/packages/agents/src` is just `agent-runtime.ts`; prompts live in `sdk/packages/shared/src/prompt/`). Subagents are now real function-calling tools named `use_subagent_<name>` (`apps/vscode/src/core/task/tools/subagent/SubagentToolName.ts`), so they are advertised through the tool schema rather than a hand-written prose section.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Excellent report against the 3.x bundle; the reporter's PR targets dead code paths. Same reasoning retires the Hermes/XS variant part of it.",
  "cluster": ""
 },
 {
  "number": 10187,
  "title": "Impossible to enable auto-approve for specific MCP tools",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The gating the reporter calls inverted is still on main: `apps/vscode/webview-ui/src/components/mcp/configuration/tabs/installed/server-row/McpToolRow.tsx:53` renders the per-tool Auto-approve checkbox only when `autoApprovalSettings.actions.useMcp` is true (same at `ServerRow.tsx:331`), so with the master MCP toggle off there is no way to set per-tool auto-approve. Runtime side in `apps/vscode/src/sdk/sdk-tool-policies.ts:64` also short-circuits to `false` when `!settings.actions.useMcp`.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Partially improved on main: `isToolAutoApproved` now requires `tool.autoApprove` as well, so with the master toggle ON the per-tool checkboxes are no longer no-ops (the second half of the report is fixed). What remains is a UX/labelling decision: \"Use MCP servers\" now behaves as \"allow per-tool auto-approve\" but hides the per-tool controls when off. Reporter confirms still broken as of 3.88.1 and Nightly.",
  "cluster": ""
 },
 {
  "number": 10197,
  "title": "CLI TUI interactive mode does not show reasoning messages with `-v` despite documentation",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The blamed file is gone: `apps/cli/src/components/ChatMessage.tsx` does not exist (there is no `apps/cli/src/components` directory at all). The OpenTUI rewrite renders reasoning unconditionally -- `apps/cli/src/tui/components/chat-entry.tsx:581` maps the `reasoning` entry kind to `<ReasoningBlock>` (with a streaming \"Thinking...\" spinner), fed by `apps/cli/src/tui/hooks/use-agent-events.ts`. `apps/cli/DEVELOPMENT.md` no longer contains the contradicting `-v` claim.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "PR #10456 referenced in comments targets the retired CLI component tree; safe to close pointing at the new TUI.",
  "cluster": ""
 },
 {
  "number": 10198,
  "title": "Clipboard operations are failing.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Every webview copy path on main still calls the browser API with no fallback: `apps/vscode/webview-ui/src/components/common/CopyButton.tsx:44` (`navigator.clipboard.writeText(text).catch(err => console.error(...))`), same in `task-header/buttons/CopyTaskButton.tsx:18` and `chat/ChatView.tsx:217`. A host-side clipboard bridge already exists and is unused by these components: `HostProvider.env.clipboardWriteText` in `apps/vscode/src/utils/env.ts`.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. Surface: `apps/vscode` extension dev host (`DISPLAY=:1 code --no-sandbox --extensionDevelopmentPath=/workspace/apps/vscode <folder>`), Cline sidebar open.\n2. Run a task so a chat row with a copy button is visible; confirm copying works right after startup.\n3. Move focus out of the webview iframe (click into the editor, another VS Code window, or another app), then click the Cline copy button without first clicking inside the webview.\n4. Expected: text lands on the system clipboard.\n5. Actual: `navigator.clipboard.writeText` rejects (typically `NotAllowedError: Document is not focused` / missing transient activation), the promise's only handler is `console.error`, so nothing is copied and the UI gives no feedback.",
  "human_attention": "yes",
  "notes": "Cheap fix: on rejection, fall back to the host clipboard RPC in `apps/vscode/src/utils/env.ts` (or `document.execCommand('copy')`) and surface a failure toast instead of a silent console error.",
  "cluster": "webview-clipboard"
 },
 {
  "number": 10199,
  "title": "Add Nexlayer MCP as recommended cloud deployment server",
  "area": "unknown",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "The MCP marketplace catalog is not stored in this repo -- `apps/vscode/src/services/mcp/McpHub.ts` and `apps/vscode/src/config.ts` fetch it from the remote `mcpBaseUrl` service endpoint (`sdk/packages/shared/src/runtime/cline-environment.ts`), and there is no server registry file under `apps/vscode` or `sdk/packages/core/src/extensions/mcp/`.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Vendor listing request; belongs in the MCP marketplace submission process, not cline/cline.",
  "cluster": ""
 },
 {
  "number": 10201,
  "title": "\"Cline's view tends to crash easily when the conversation gets too long.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The chat list on main is virtualized (`Virtuoso` at `apps/vscode/webview-ui/src/components/chat/chat-view/components/layout/MessagesArea.tsx:294`, `react-virtuoso ^4.12.3` in `apps/vscode/webview-ui/package.json`), so row count alone should not kill the renderer -- but nothing bounds retained message text or per-row rendered content, and there is no crash/liveness indicator: the webview shows task state purely from `ClineMessage[]`, so a renderer crash leaves exactly the blank grey panel described. Recent related work (b72d7f1a6 \"keep webview alive when moved between primary and secondary sidebars\") addresses a different cause.",
  "verdict": "duplicate",
  "duplicate_of": 10626,
  "severity": "high",
  "repro": "1. Surface: `apps/vscode` extension dev host with the Cline sidebar open.\n2. Run a long task (many `api_req_started` rows, large diffs/terminal outputs) until the conversation is tens of thousands of lines, or replay an exported long task from history.\n3. Open the webview devtools (`Developer: Open Webview Developer Tools`) and watch heap usage while scrolling.\n4. Expected: panel stays responsive; if the renderer does die, the user is told the view crashed and whether the task is still running.\n5. Actual (reported): panel goes solid grey with no indication whether the task is still executing; only a window reload recovers it.",
  "human_attention": "no",
  "notes": "Two separable asks: (a) the OOM/crash itself, (b) a crash-recovery affordance so users know the underlying task is still alive. (b) is worth doing regardless of whether (a) reproduces.",
  "cluster": "grey-blank-webview"
 },
 {
  "number": 10202,
  "title": "Infinite loop calling write_to_file in PLAN MODE after task interruption",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The loop requires a tool that exists but is refused; on main plan mode simply does not register editing tools -- `ToolPresets.plan` in `sdk/packages/core/src/extensions/tools/presets.ts` sets `enableEditor: false`, and `write_to_file` is not in `ALL_DEFAULT_TOOL_NAMES` (`sdk/packages/core/src/extensions/tools/constants.ts`) at all. Repeated-failure runaway is also now bounded by HEAD commit e4091c368 \"Stop the task at the mistake limit like the CLI and remove the max-mistakes setting (#12561)\".",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Legacy 3.x emitted \"Tool 'write_to_file' is not available in PLAN MODE\" from the XML tool dispatcher; that string exists nowhere on main.",
  "cluster": ""
 },
 {
  "number": 10203,
  "title": "MCP tool parameters with integer/boolean types are sent as strings, causing Zod literal validation failures",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "On main MCP tools are registered as first-class agent tools carrying the server's own JSON Schema and are invoked with the model's native JSON object: `createMcpTools` in `sdk/packages/core/src/extensions/mcp/tools.ts` passes `inputSchema: descriptor.inputSchema` straight through and forwards `input as Record<string, unknown>` to `provider.callTool` with no stringification. Tool arguments are parsed with plain `JSON.parse` (`parseToolArguments`, `sdk/packages/agents/src/agent-runtime.ts:1756`), so `8` stays a number and `true` stays a boolean.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "The 3.x `use_mcp_tool` XML path that stringified every parameter is gone. Worth a retest note to the reporter rather than a code change.",
  "cluster": ""
 },
 {
  "number": 10206,
  "title": "Add AgentWeb \u2014 free business data MCP server (11M+ businesses)",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "Pure product advertisement with no defect and no requested change to this repo; as with #10199 the MCP catalog is served from the remote `mcpBaseUrl` endpoint (`sdk/packages/shared/src/runtime/cline-environment.ts`, consumed by `apps/vscode/src/services/mcp/McpHub.ts`), not from any file under `apps/` or `sdk/`.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Auto-closeable.",
  "cluster": ""
 },
 {
  "number": 10208,
  "title": "Stuck thinking for ever.",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Main has no stall/idle watchdog for model requests: `sdk/packages/llms/src/providers/ai-sdk.ts:1217` passes only `abortSignal: request.signal` to the stream, and the sole timeout implementation in the package is Ollama-specific (`sdk/packages/llms/src/providers/vendors/ollama.ts` `wrapFetchWithTimeout`, response-start only). `rg` finds no `timeoutMs` defaults in `sdk/packages/llms/src/providers/builtins.ts` for `lmstudio`, `openai-compatible` or `openrouter`, so a silently dropped/never-answered connection leaves the UI spinning indefinitely with no retry prompt.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "critical",
  "repro": "1. Surface: `apps/vscode` extension dev host (or `bun run cli -i`).\n2. Configure the LM Studio / OpenAI Compatible provider pointed at a local endpoint you control (e.g. `http://127.0.0.1:1234/v1`).\n3. Send any prompt, then make the endpoint hang: pause the server process (`kill -STOP <pid>`) or drop the TCP connection with a firewall rule so no bytes and no FIN arrive.\n4. Expected: after a bounded idle period the request fails with a timeout and the user is offered a retry.\n5. Actual: the UI stays on \"Thinking...\" forever; only cancelling the task or reloading the window recovers. Repeat with Ollama to confirm the contrast -- `ollama.ts` does time out on response start, which is why Ollama users report this less.",
  "human_attention": "yes",
  "notes": "High-volume umbrella issue (commenters link #10015 and #9546). Two concrete asks that are both implementable in `@cline/llms`: (a) a provider-agnostic response-start/idle timeout generalizing the Ollama wrapper, (b) surfacing a \"still waiting, retry?\" prompt. A commenter also reports local llama-server inference continuing after cancel -- worth verifying `request.signal` really propagates to the underlying fetch for openai-compatible.",
  "cluster": "stream-never-starts"
 },
 {
  "number": 10212,
  "title": "Incorrect token count",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Usage parsing was rewritten for main: `normalizedUsage` in `sdk/packages/llms/src/providers/ai-sdk.ts` (~lines 744-800) reads `inputTokens`/`prompt_tokens`/`promptTokenCount` with nested cache-detail fallbacks, and `apps/vscode/src/sdk/message-translator.ts` `normalizeUsageEvent` then splits them into disjoint buckets. I could not find a code path that would yield 1M+ from a one-word prompt, and the report gives no raw usage payload from chutes.ai / `zai-org/GLM5-TEE`.",
  "verdict": "duplicate",
  "duplicate_of": 11660,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Needs the provider's raw `usage` object to tell whether this is bad upstream reporting or a field-mapping miss in `normalizedUsage`. Distinct from #10244, which has an identifiable cause.",
  "cluster": "inflated-token-count"
 },
 {
  "number": 10216,
  "title": "Cline Kanban crashes with \u201dError: Unable to update lock within the stale threshold\u201c",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The stack trace is entirely inside the external package (`node_modules/kanban/node_modules/proper-lockfile/lib/lockfile.js`). `proper-lockfile` is not a dependency of anything in this repo -- the only `bun.lock` entries for it come transitively from the `@microsoft/tui-test` dev dependency -- and `apps/cli/src/commands/kanban.ts` merely spawns the `kanban` binary.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Belongs to the separate `kanban` package. #10245 is the same crash.",
  "cluster": "kanban-crash-data-loss"
 },
 {
  "number": 10218,
  "title": "Unable to connect to Anthropic models provided through SAP AI Core",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The error string \"Error while iterating over SSE stream.\" does not exist anywhere on main -- it came from the retired hand-rolled SAP handler. On main SAP AI Core is implemented through the AI SDK community provider `@jerome-benoit/sap-ai-provider` in `sdk/packages/llms/src/providers/vendors/community.ts` (`createSAPAIProvider`, `buildSapServiceKey`, `resolveSapApi`), routed via `ai-sdk-community` in `sdk/packages/llms/src/providers/builtins.ts:519`.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "SDK-side SAP support landed in 1ffe3f6d4 \"feat: SapAiCoreHandler\" (2026-03-05) and the AI-SDK migration 27c93efee. Ask the reporter to retest Anthropic models on the 4.x extension; if it still fails the bug is in the community provider path, not the old SSE reader.",
  "cluster": "sap-ai-core"
 },
 {
  "number": 10233,
  "title": "deekspeek-chat create same plan twice",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The duplication is the legacy pattern of streaming assistant text and then repeating it inside a `plan_mode_respond` tool payload. On main there is no `plan_mode_respond` tool (`ALL_DEFAULT_TOOL_NAMES` in `sdk/packages/core/src/extensions/tools/constants.ts`); plan mode just produces assistant prose per `PLAN_MODE_INSTRUCTIONS` in `sdk/packages/shared/src/prompt/cline.ts`, so there is no second render surface for the plan.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Also partly model behavior (deepseek-chat restating its plan).",
  "cluster": ""
 },
 {
  "number": 10238,
  "title": "Qwen3.5-35B-A3B-Q8_0.gguf -  Invalid API Response: The provider returned an empty or unparsable response.",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The quoted message (\"Invalid API Response: The provider returned an empty or unparsable response...\") appears nowhere on main; the equivalent handling now returns a per-tool-call error (\"Tool call <name> emitted invalid JSON arguments\") from `parseToolArguments` in `sdk/packages/agents/src/agent-runtime.ts` after `repairMalformedToolCall` (`sdk/packages/llms/src/providers/ai-sdk.ts:423`) tries to recover. The report itself contains no logs, provider id, or endpoint.",
  "verdict": "duplicate",
  "duplicate_of": 10413,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Self-describes as provider-side; a local quantized MoE gguf failing to emit well-formed tool calls is a model-capability problem, and there are no reproduction details to act on.",
  "cluster": "invalid-api-response-empty"
 },
 {
  "number": 10239,
  "title": "line SDK start failed: 404 with OpenAI Compatible provider (NVIDIA NIM)",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "URL construction on main looks correct: `createOpenAICompatibleProviderModule` in `sdk/packages/llms/src/providers/vendors/openai-compatible.ts:127` passes `baseURL: config.baseUrl` unmodified to `createOpenAICompatible`, which appends `/chat/completions`, so `https://integrate.api.nvidia.com/v1` resolves to the documented NIM endpoint and no extra `/v1` is injected. The reported model (`z-ai/glm5`) is not part of NVIDIA's NIM catalog, which is a much likelier source of a bare \"404 page not found\".",
  "verdict": "duplicate",
  "duplicate_of": 10495,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Need the exact failing request URL/model id. The \"No agent configured\" half is Kanban-side (external package), see #10151/#10216.",
  "cluster": "custom-base-url-404"
 },
 {
  "number": 10244,
  "title": "UI shows double the actual token usage compared to llama.cpp backend (off by factor of ~2)",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The exact double-count is fixed on main. `getLastApiReqTotalTokens` in `apps/vscode/src/shared/getApiMetrics.ts` sums `tokensIn + tokensOut + cacheWrites + cacheReads`, which inflated the bar whenever a provider reported `prompt_tokens` inclusive of cached tokens; `normalizeUsageEvent` in `apps/vscode/src/sdk/message-translator.ts` (lines 86-97) now subtracts them first -- `uncachedInputTokens = max(0, inputTokens - cacheReads - cacheWrites)` -- with an explicit comment that the buckets must be disjoint.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Landed as part of the SDK-extension migration squash (51e9a8f64). Secondary caveat if it recurs: when a provider reports no usage at all the fallback estimator in `sdk/packages/shared/src/llms/tokens.ts` uses a deliberately conservative `CHARS_PER_TOKEN = 3`, which over-counts against a real tokenizer.",
  "cluster": "inflated-token-count"
 },
 {
  "number": 10245,
  "title": "Cline Kanban crashes with error: Unable to update lock within stale threshold",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Same `proper-lockfile` `ECOMPROMISED` crash inside the external `kanban` npm package as #10216; `proper-lockfile` appears in `bun.lock` only as a transitive dev dependency of `@microsoft/tui-test` and is used by no source file in `apps/` or `sdk/`.",
  "verdict": "duplicate",
  "duplicate_of": 10216,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Mislabelled \"VS Code\"; it is the CLI-spawned Kanban app. Both belong to the separate `kanban` package.",
  "cluster": "kanban-crash-data-loss"
 },
 {
  "number": 10246,
  "title": "CLI: `cline` fails on startup with npm 404 for `@clinebot/agents` (missing package)",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "On main the kanban app is only launched from the explicit `cline kanban` subcommand or `--kanban` flag (`apps/cli/src/main.ts` lines 686-748), never on a bare `cline` startup, and `ensureKanbanInstalled` in `apps/cli/src/commands/kanban.ts` is the only auto-install path. The failing dependency belongs to the external `kanban` npm package (repo `cline/kanban`, see README.md line 135), and the `@clinebot` npm scope was renamed to `@cline` in commit c84da27d0 (2026-05-06); no file in /workspace references `@clinebot` as a dependency.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "CLI is 3.0.46 on main (apps/cli/package.json) vs 2.13.0 reported. The 404 package was a dep of the separately-published `kanban` package, so even the residual risk is owned by cline/kanban.",
  "cluster": ""
 },
 {
  "number": 10248,
  "title": "UI State Bug: Model metadata fails to update on selection, causing incorrect \"Free\" label and credit loss",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "In `apps/vscode/webview-ui/src/components/settings/ClineModelPicker.tsx` the featured/recommended cards and the dropdown both call the same `handleModelChange`, and `selectedModelInfo` is recomputed from `currentClineModelId` merged with the live catalog (`liveModelInfo ? {...persistedModelInfo, ...liveModelInfo} : persistedModelInfo`), with the zero-price override applied only when the *currently selected* id is in `freeClineModelIdSet`. There is no card-specific path that can leave stale pricing behind.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Reported against 3.78.0 legacy settings UI; the model picker on main is a rewrite that derives price display from the committed selection + live catalog.",
  "cluster": ""
 },
 {
  "number": 10250,
  "title": "Within the Cline Visual Code Extension, copying from any text field using CTRL+C is not possible",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "I could not find any code on main that would intercept copy: the only clipboard-relevant key handler in `apps/vscode/webview-ui/src/components/chat/ChatTextArea.tsx` preventDefaults Ctrl/Cmd+A only, and the two global `window.addEventListener(\"keydown\")` handlers (`apps/vscode/webview-ui/src/utils/hooks.ts`, `.../chat-view/components/layout/ActionButtons.tsx`) only handle Escape/shortcut combos. `user-select: none` appears only on decorative layers (ChatTextArea highlight layer, ModelInfoView, McpResponseDisplay), not on chat transcript text.",
  "verdict": "duplicate",
  "duplicate_of": 10198,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Smells like a host/environment issue (Windows 11 + WSL2 remote webview focus) rather than extension code. Needs a retest on the 4.x webview before spending maintainer time.",
  "cluster": "webview-clipboard"
 },
 {
  "number": 10253,
  "title": "Error when trying to query Qwen",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The `qwen-code` provider still exists on main (`sdk/packages/llms/src/providers/builtins.ts` line 791, id `qwen-code`, default model `qwen3-coder-plus`, base URL `https://dashscope.aliyuncs.com/compatible-mode/v1`), so the client config is intact; the 400 `invalid_parameter_error` is returned by DashScope itself. The issue thread cites QwenLM/qwen-code#3203 announcing that the Qwen OAuth free tier was discontinued on 2026-04-15.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Provider-side entitlement change, not a Cline defect. Could be closed with a pointer to the Qwen announcement.",
  "cluster": "qwen-400-bad-request"
 },
 {
  "number": 10255,
  "title": "Cline Kanban crashes trash all in progress and review cards",
  "area": "unknown",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Kanban is a separate product/repo: README.md line 135 lists it as `cline/kanban` (`npm i -g kanban`), and this repo only references it as an external binary that the CLI shells out to (`apps/cli/src/commands/kanban.ts` spawns the `kanban`/`kanban.cmd` executable). No Kanban board/card persistence code exists under /workspace.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Data-loss report, but it belongs in cline/kanban; safe to close with a transfer/redirect.",
  "cluster": "kanban-crash-data-loss"
 },
 {
  "number": 10257,
  "title": "balance negative",
  "area": "unknown",
  "kind": "question-support",
  "relevant_to_main": "unsure",
  "evidence": "The message the user saw is server-issued: the clients only classify it (`apps/vscode/src/services/error/ClineError.ts` handles `code === \"insufficient_credits\"` with `details.current_balance`, and `apps/vscode/src/sdk/message-translator.ts` reshapes the same payload). Balance accounting lives behind `https://api.cline.bot` (`sdk/packages/shared/src/runtime/cline-environment.ts`), not in this repo.",
  "verdict": "duplicate",
  "duplicate_of": 10952,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "No repro steps, no logs; an account/billing support question for the Cline backend. Version string \"3.7.8\" is also not a real release.",
  "cluster": "insufficient-balance-free-models"
 },
 {
  "number": 10260,
  "title": "\u3010Bug\u3011Cline SDK start failed: 404 \"Not Found\". You can send another message to continue the conversation.",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Neither the string \"SDK start failed\" nor \"send another message to continue the conversation\" exists anywhere in /workspace (ripgrep over the whole tree excluding node_modules), so the message comes from the retired 2.14.0 CLI build. On main a provider 404 surfaces through `extractErrorMessage` in `sdk/packages/llms/src/providers/format.ts` with the provider's own detail text.",
  "verdict": "duplicate",
  "duplicate_of": 10495,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Report has empty repro steps, no provider/model, no system info. Most likely a model-not-found 404 from whatever provider was configured.",
  "cluster": "custom-base-url-404"
 },
 {
  "number": 10265,
  "title": "Recent Regression \u2014 400 Bad Request with qwen3-coder-plus",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Same provider/model/error code as #10253 (`providerId: qwen-code`, `modelId: qwen3-coder-plus`, `invalid_parameter_error` 400) and the same commenter posted the QwenLM/qwen-code#3203 free-tier-discontinued notice on both. The provider definition on main is unchanged (`sdk/packages/llms/src/providers/builtins.ts` line 791).",
  "verdict": "duplicate",
  "duplicate_of": 10253,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed two days after #10253; both are the DashScope OAuth entitlement change.",
  "cluster": "qwen-400-bad-request"
 },
 {
  "number": 10268,
  "title": "User must hit cancel after every response before being able to reply",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "On main the chat input enable/disable state is centralized in `apps/vscode/webview-ui/src/components/chat/chat-view/shared/buttonConfig.ts`, where `plan_mode_respond` is explicitly `{ sendingDisabled: false, enableButtons: false }` and only genuinely-busy states (`partial`, `api_req_active`, `foreground_command_running`, `api_req_failed`) set `sendingDisabled: true`. `ActionButtons.tsx` pushes that value through `setSendingDisabled(buttonConfig.sendingDisabled)`, so a completed plan-mode response leaves the textarea live.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Reported on 3.77.0 via IntelliJ; the SDK webview's single-source-of-truth button config removes the stuck-disabled state.",
  "cluster": ""
 },
 {
  "number": 10276,
  "title": "incorrect colors in CLine in PHPstorm when in dark mode with light editor theme",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Theme values are supplied by the JetBrains host, not by this repo: the standalone core that JetBrains embeds only stubs the VS Code theme APIs (`apps/vscode/standalone/runtime-files/vscode/vscode-stubs.js` lines 452, 779-780 stub `ColorThemeKind`, `window.activeColorTheme`, `onDidChangeActiveColorTheme`). No IDE-theme-to-CSS-variable mapping exists under /workspace.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "JetBrains-plugin-UI only; belongs in the separate JetBrains plugin repo.",
  "cluster": ""
 },
 {
  "number": 10278,
  "title": "Cline CLI fails with \"cannot access the file claude\" error when creating new tasks on Windows 10",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The Claude Code provider was rebuilt on main around `ai-sdk-provider-claude-code` in `sdk/packages/llms/src/providers/vendors/community.ts`, which added `resolveClaudeExecutable()`/`findExecutableOnPath()` with explicit Windows handling (`claude.exe` plus `.exe/.cmd/.bat` PATH probing) in commit 2ef81be70 (2026-07-18, \"make Claude Code and Codex provider packages optional peers\"), three months after this report. The exact string \"cannot access the file\" does not exist anywhere in /workspace.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Reported on CLI 2.14.0 (now 3.0.46). Very likely the pre-2ef81be70 Windows executable-resolution gap; ask the two reporters to retest before investigating.",
  "cluster": "claude-code-exit-code-1"
 },
 {
  "number": 10280,
  "title": "Add AgentBase to Cline MCP Marketplace",
  "area": "unknown",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "The MCP marketplace catalog is served remotely, not from this repo: `sdk/packages/shared/src/runtime/cline-environment.ts` sets `mcpBaseUrl: \"https://api.cline.bot/v1/mcp\"`, and `apps/vscode/src/services/mcp/McpHub.ts` only consumes it. There is no in-repo marketplace manifest to add an entry to.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Third-party listing request; only a maintainer can accept/reject it server-side. Nothing an agent can change in this repo.",
  "cluster": ""
 },
 {
  "number": 10294,
  "title": "Share: PaperClaw custom-instruction preset (research-paper generator)",
  "area": "docs",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "Self-described as \"Not a code change request\" \u2014 it links an external prompt file in the author's own repo. Nothing in /workspace is affected; custom instructions/skills are user-supplied files loaded by `sdk/packages/core/src/extensions/config/user-instruction-config-loader.ts`, requiring no repo change to use.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Promotional share by the same author as #10301; author explicitly invited closing it.",
  "cluster": "paperclaw-promo"
 },
 {
  "number": 10301,
  "title": "Integration: PaperClaw (autonomous peer-reviewed paper generator for Cline)",
  "area": "docs",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "Same author and same external project as #10294, one day later; it asks whether a PaperClaw mention would be accepted in docs/examples. No defect and no code on main is implicated \u2014 MCP/plugin integrations are user-installed via `sdk/packages/core/src/services/marketplace.ts` and MCP config, needing no upstream change.",
  "verdict": "duplicate",
  "duplicate_of": 10294,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Author offered option 3 (\"Not a fit \u2014 close this issue\"); safe to close alongside #10294.",
  "cluster": "paperclaw-promo"
 },
 {
  "number": 10303,
  "title": "Extension causes high cpu load",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "This is VS Code's auto-generated unresponsive-extension template for `claude-dev` 3.79.0 and the referenced `saoudrizwan.claude-dev-unresponsive.cpuprofile.txt` was never attached, so there is nothing to point at in code. The extension on main is a different build (`apps/vscode`, version 4.0.0, SDK-backed) than the 3.79.0 profile would describe.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Unactionable without the CPU profile; auto-closeable with a request for the profile against the 4.x build.",
  "cluster": ""
 },
 {
  "number": 10306,
  "title": "[Bug] OpenAI-compatible provider (NVIDIA SDK): context token count inflates to very High Number, <think> tags render as plain text, and tool calls appear unparsed in UI",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Sub-issues 1 and 2 are gone: `sdk/packages/llms/src/providers/ai-sdk.ts` (lines ~1049-1075) emits exactly one `type: \"usage\"` event per stream, after the stream drains, from `await stream.usage`, so the `+=` accumulation in `sdk/packages/agents/src/agent-runtime.ts` `updateUsage()` cannot double-count. Sub-issue 3 is still live: `sdk/packages/llms/src/providers/vendors/openai-compatible.ts` wraps models only with `splitToolImagesMiddleware` and there is no `extractReasoningMiddleware`/`<think>` handling anywhere in /workspace, so inline `<think>\u2026</think>` text still flows through as a plain `text-delta`.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. Build and run the new extension: `cd apps/vscode && bun run build:webview && bun esbuild.mjs`, then `DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <folder>` (or use `bun run cli` for the CLI surface).\n2. Configure the `openai-compatible` provider with base URL `https://integrate.api.nvidia.com/v1`, an NVIDIA API key, and a reasoning model that emits inline `<think>` blocks (e.g. `deepseek-ai/deepseek-r1`).\n3. Send any prompt, e.g. \"explain how cline works\".\n4. Expected: reasoning is surfaced as a collapsible reasoning trace (as `reasoning-delta` events, the way `ai-sdk.ts` handles `reasoning`/`reasoning-delta` parts).\n5. Actual (predicted): the literal `<think>\u2026</think>` text renders in the assistant message body because `openai-compatible.ts` applies no reasoning-tag extraction middleware.\n6. While there, confirm the context-usage bar no longer inflates \u2014 that half should now be correct.",
  "human_attention": "yes",
  "notes": "Only the `<think>` passthrough part is still owned by `@cline/llms`; the fix is a `extractReasoningMiddleware({ tagName: 'think' })` in the openai-compatible vendor. The `[TOOL_CALL]` plain-text half is model misbehaviour under native tool calling.",
  "cluster": "inflated-token-count"
 },
 {
  "number": 10307,
  "title": "403 Kimi For Coding is currently only available for Coding Agents such as Kimi CLI, Claude Code, Roo Code, Kilo Code, etc.",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The 403 body is Moonshot's own `access_terminated_error` and is keyed off client identity; Cline identifies itself honestly via `\"User-Agent\": \\`Cline/${clientVersion}\\`` in `sdk/packages/llms/src/providers/request-headers.ts` line 82. The thread itself shows the same 403 hitting Kimi CLI, opencode and Kimi Code CLI, followed by \"It is fine now\" / \"Working again!\" once Moonshot recovered.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Provider-side allowlist/outage. Any change here would mean spoofing another agent's identity \u2014 a product/legal decision, not a bug fix.",
  "cluster": ""
 },
 {
  "number": 10314,
  "title": "Cline reports incorrect pricing for Gemini 3 flash preview",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "All four complaints are already correct in `sdk/packages/llms/src/catalog/catalog.generated.ts`: the `gemini` provider entry (line 23009) and the `vertex` entry (~line 79539) both carry `input: 0.5, output: 3, cacheRead: 0.05, cacheWrite: 0` with no context tiers. The file the reporter blames, root `src/shared/api.ts`, does not exist on main, and their PR #10315 (still OPEN) touches only that dead legacy path.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Pricing moved from a hand-maintained `shared/api.ts` map into the generated SDK catalog; worth telling the PR author their patch targets `legacy-extension` only.",
  "cluster": ""
 },
 {
  "number": 10324,
  "title": "Exporting a history item from a different WSL distro removes it from History without saving the export",
  "area": "unknown",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The reporter is running the \"Sixth\" extension (`sixth.sixth-ai` global storage path, version 0.0.84); the string \"sixth\" appears nowhere in /workspace. Separately, the equivalent Cline path on main cannot delete history: `SdkController.exportTaskWithId` in `apps/vscode/src/sdk/SdkController.ts` (line 1748) throws `Task not found in history` / fails `fs.access(taskDirPath)` and never calls `deleteTaskFromState`.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed against a different (rebranded) extension. Even mapped onto Cline, main's export path is read-only with respect to history.",
  "cluster": ""
 },
 {
  "number": 10325,
  "title": "Cline SDK: agent.shutdown() may block indefinitely in CLI usage",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "There is no `shutdown()` on any public agent object on main: `sdk/packages/sdk/src/index.ts` just re-exports `@cline/core`, `@cline/agents` contains no `shutdown` symbol at all, and the documented teardown is now `cline.dispose()` (`sdk/packages/core/src/ClineCore.ts` line 401). The internal `SessionRuntime.shutdown` (`sdk/packages/core/src/runtime/orchestration/session-runtime-orchestrator.ts` line 629) throws when a run is in progress rather than blocking, and only awaits `activeRunPromise` after an abort. The docs page the user followed (`docs/sdk/overview.mdx`) no longer calls `agent.shutdown()`.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "API surface changed under the reporter (CLI/SDK 2.15.0). A hanging Node process after teardown is usually open handles, but there are no repro steps to confirm.",
  "cluster": ""
 },
 {
  "number": 10328,
  "title": "Unable to switch AI model in Sixth; provider settings are missing in VS Code",
  "area": "unknown",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "This describes the \"Sixth\" extension (version 0.0.85, only Telegram settings visible); \"sixth\" does not appear anywhere in /workspace. Cline's own provider/model configuration is present and rich on main \u2014 see `apps/vscode/webview-ui/src/components/settings/ApiOptions.tsx` and the per-provider components under `apps/vscode/webview-ui/src/components/settings/providers/`.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Wrong repo \u2014 same reporter cohort as #10324.",
  "cluster": ""
 },
 {
  "number": 10330,
  "title": "Model stream repeatedly ends without producing output when using AWS Bedrock \u2014 hits consecutive mistake limit",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The commenter's root cause (dropped `start-step`/`finish-step` events emptying `recordedSteps`) does not map onto main: `emitAiSdkEvents` in `sdk/packages/llms/src/providers/ai-sdk.ts` (lines 908-1040) reads `stream.fullStream` parts directly and never depends on recorded steps. The generic wrapper text the user saw is now unmasked \u2014 `extractErrorMessage` in `sdk/packages/llms/src/providers/format.ts` treats \"no output generated. check the stream for errors.\" as a `GENERIC_WRAPPER_MESSAGES` entry and surfaces the provider cause instead (commit dbf077538, 2026-06-28). Bedrock itself is wired through `@ai-sdk/amazon-bedrock` in `sdk/packages/llms/src/providers/vendors/bedrock.ts`.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Only the error *masking* is provably fixed; whether Bedrock+Claude Opus still yields empty streams is unverified. A retest on CLI 3.x would now print the real provider error, which is what a maintainer should ask for.",
  "cluster": "invalid-api-response-empty"
 },
 {
  "number": 10336,
  "title": "Claude Code provider: model uses Cline tool vocabulary (execute_command) which Claude Code cannot dispatch",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The blamed file `src/integrations/claude-code/run.ts` and the `--disallowedTools` flag do not exist on main (ripgrep for `disallowedTools` over the whole tree returns nothing). Claude Code is now an AI SDK provider: `createClaudeCodeProviderModule` in `sdk/packages/llms/src/providers/vendors/community.ts` lazily loads `ai-sdk-provider-claude-code` and returns a normal `LanguageModel`, so tool calls travel as native AI SDK tool-call parts handled in `sdk/packages/llms/src/providers/ai-sdk.ts`, not as Cline XML prompt markup forwarded to `claude -p`.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Good report with a working patch, but the patch applies to the retired 3.x integration; the SDK extension/CLI use a different Claude Code transport entirely.",
  "cluster": ""
 },
 {
  "number": 10339,
  "title": "ECONNRESET error when using Amazon Bedrock with Zscaler CA",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`createBedrockProviderModule` in `sdk/packages/llms/src/providers/vendors/bedrock.ts` passes `config.fetch` into `createAmazonBedrock`, so requests go over the platform fetch/undici stack (`apps/vscode/src/shared/net.ts`, `EnvHttpProxyAgent`) and trust Node's TLS store \u2014 the AWS profile's `ca_bundle` key, which is what makes the reporter's AWS CLI work, is read only by `fromNodeProviderChain` for credentials and is never applied to TLS. The CLI side does harvest the OS trust store into `NODE_EXTRA_CA_CERTS` (`apps/cli/bin/ca-certs.cjs`, CHANGELOG 3.0.43), but nothing honours a profile-scoped `ca_bundle`.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Two things to decide: (a) whether to honour `ca_bundle` from the resolved AWS profile for the Bedrock fetch, and (b) whether ECONNRESET here is TLS at all (cert distrust usually surfaces as UNABLE_TO_VERIFY_LEAF_SIGNATURE). Documented workaround is NODE_EXTRA_CA_CERTS per docs/troubleshooting/networking-and-proxies.mdx.",
  "cluster": ""
 },
 {
  "number": 10341,
  "title": "Installing an empty local skill fails with unclear error",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Still reproducible by inspection on main: `loadSkillMetadata` in `apps/vscode/src/core/context/instructions/user-instructions/skills.ts` (lines 171-208) parses an empty SKILL.md into empty frontmatter and logs exactly the misleading `Skill at <dir> missing required 'name' field` before returning null, and `scanSkillsDirectory` swallows everything but EACCES. The SDK-side loader already does the right thing (`parseSkillConfigFromMarkdown` in `sdk/packages/core/src/extensions/config/user-instruction-config-loader.ts` throws \"Missing instructions body in skill file.\"), so only the extension path is wrong. The community PR #10455 is still OPEN and patches the dead legacy `src/core/context/...` paths.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Owned by apps/vscode. Small fix: distinguish empty file / no frontmatter / bad YAML / missing field / name mismatch, mirroring the @cline/core loader. A maintainer should tell the #10455 author to retarget from `src/` to `apps/vscode/src/`.",
  "cluster": ""
 },
 {
  "number": 10342,
  "title": "Feature Request: Option to configure Enter key behavior in chat (send vs newline)",
  "area": "vscode-new",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "`apps/vscode/webview-ui/src/components/chat/ChatTextArea.tsx:585` still hardcodes `if (event.key === \"Enter\" && !event.shiftKey && !isComposing)` to send. Grepping the whole tree for `sendMessageOnEnter|enterBehavior|enterToSend|newlineOnEnter` returns nothing, so no such setting exists on main either.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Agent-fixable: add a state key + settings toggle and branch the keydown in ChatTextArea.tsx. Owned by apps/vscode/webview-ui.",
  "cluster": ""
 },
 {
  "number": 10345,
  "title": "Cline disappears from VSCode sidebar",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The view contribution (`claude-dev-ActivityBar` / `claude-dev.SidebarProvider`) in `apps/vscode/package.json` is unchanged on main, so nothing in the manifest explains a vanishing view. The reporter says the Cline *commands* also disappeared from the command palette, which only happens when the extension host deactivates/crashes \u2014 and no logs, repro steps, or system info were supplied.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Unactionable without the extension-host log ('Cline' output channel + 'Extension Host' log). Safe to ask-and-close.",
  "cluster": "extension-not-activating"
 },
 {
  "number": 10348,
  "title": "Claude Code Provider: Enterprise plan doesn't track costs",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The blamed heuristic is gone: `rg apiKeySource` matches nothing anywhere in the repo, and the cited file `src/core/api/providers/claude-code.ts` does not exist on main. Claude Code now goes through `createClaudeCodeProviderModule` in `sdk/packages/llms/src/providers/vendors/community.ts` (the `ai-sdk-provider-claude-code` wrapper), and its builtin spec in `sdk/packages/llms/src/providers/builtins.ts` (id `claude-code`) carries no `metadata.usageCostDisplay: \"subscription\"` \u2014 unlike `openai-codex`/`clinePass` \u2014 so cost is derived from reported token usage rather than gated on the plan's key source.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Owned by @cline/llms. Worth a one-line close referencing the rewrite.",
  "cluster": ""
 },
 {
  "number": 10349,
  "title": "vertexAI multi region",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Main no longer hand-builds `https://${region}-aiplatform.googleapis.com`. `sdk/packages/llms/src/providers/vendors/vertex.ts` just passes `{ project, location }` to `createVertexAnthropic`/`createVertex`, and the vertex builtin in `sdk/packages/llms/src/providers/builtins.ts` sets no default `baseUrl`. I unpacked the pinned `@ai-sdk/google-vertex@4.0.159` (bun.lock:814): its `getHost()` returns `aiplatform.googleapis.com` for `global` and `aiplatform.${region}.rep.googleapis.com` for `eu`/`us` \u2014 exactly the URL shape the commenter asked for \u2014 in both `dist/index.mjs` and `dist/anthropic/index.mjs`. `apps/vscode/src/sdk/model-catalog/vertex-global-endpoint.ts` additionally allowlists `claude-opus-4-7` for `location: \"global\"`.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "One residual gap, agent-fixable: `apps/vscode/src/shared/providers/vertex.json` still lists only us-east5/us-central1/europe-west1/europe-west4/asia-southeast1/global \u2014 add \"eu\" and \"us\" to expose the multi-region endpoints the AI SDK already supports.",
  "cluster": ""
 },
 {
  "number": 10352,
  "title": "Duplicate references cause cline to freeze",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "`parseMentions` in `apps/vscode/src/core/mentions/index.ts` collects mentions into a `Set` and then explicitly does `const uniqueMentions = Array.from(new Set(mentions))` under the comment 'Filter out duplicate mentions while preserving order', so the same `@/path` twice is fetched once \u2014 there is no duplicate-driven loop in the mention resolver that `apps/vscode/src/sdk/SdkController.ts:876` calls. The only other per-keystroke consumer is the highlight `replace(mentionRegexGlobal, ...)` at `ChatTextArea.tsx:967`.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter never said which model/provider and confirmed the request never reached the model, so the freeze is client-side. Needs the exact prompt text to reproduce; the dedupe path argues against the stated cause.",
  "cluster": ""
 },
 {
  "number": 10358,
  "title": "consecutiveMistakeCount should reset on user message boundary (YOLO trips on \"continue\" / task resumption)",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "In `sdk/packages/core/src/runtime/orchestration/session-runtime-orchestrator.ts`, `run()` calls `resetConversationBoundaryTrackers()` (which calls `this.mistakeTracker.reset()`), but `continue()` goes straight to `executeRun({ isContinue: true })` with no reset. `sdk/packages/core/src/runtime/host/local-runtime-host.ts:1639` routes every follow-up user message to `session.agent.continue(prompt, ...)`, so the counter genuinely survives the user-message boundary; the only other reset is the `turn-finished` branch when `succeeded > 0`.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Impact is smaller than in 3.x: the default limit is now 6 (`session-runtime-orchestrator.ts:405`, `config.execution?.maxConsecutiveMistakes ?? 6`) and HEAD commit e4091c368 (#12561) makes the limit stop the task cleanly instead of blocking on an ask. A one-line `this.resetConversationBoundaryTrackers()` in `continue()` (or a reset in `executeRun` when `input.userMessage` is present) is the natural fix; a contributor already volunteered.",
  "cluster": ""
 },
 {
  "number": 10368,
  "title": "Bug: Ollama vision models fail to process images due to incorrect Base64 format",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The blamed file no longer exists: `git log --diff-filter=D -- apps/vscode/src/core/api/transform/ollama-format.ts` shows it was deleted in `94f5a47a5` (\"sdk migration: squashed pre-2026-06-02 work\"), and `find -name 'ollama-format*'` returns nothing. Main's Ollama path is `sdk/packages/llms/src/providers/vendors/ollama.ts`, which delegates message conversion to `ai-sdk-ollama` and only layers `splitToolImagesMiddleware` on top. The referenced fix PR #11755 is CLOSED unmerged and touched only the deleted path (`gh pr view 11755`).",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "The reported root cause is dead code. I did not independently verify that `ai-sdk-ollama` populates `images` correctly for vision models, so if the symptom recurs on the SDK extension it should be filed fresh against `sdk/packages/llms/src/providers/vendors/ollama.ts`.",
  "cluster": ""
 },
 {
  "number": 10370,
  "title": "Cline removes \"data:\" on lines that contain \"metadata:\" on YAML files",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "I could not find any `data:`-stripping logic in an edit or streaming path on main. Main's edit tool is `sdk/packages/core/src/extensions/tools/executors/editor.ts`, which only normalizes line endings (`normalizeLineEndings`) and then does `content.replace(normalizedOldStr, () => normalizedNewStr)` with a *function* replacer (so `$`-patterns can't corrupt output); `apply-patch.ts` is the other path. The legacy streaming-diff pipeline is gone \u2014 `rg DiffViewProvider` matches only `CHANGELOG.md`, and previews now go through the read-only `apps/vscode/src/hosts/vscode/VscodeEditPreview.ts`.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Silent file corruption is high-impact if real, so worth a human eye, but nothing on main reproduces the described transform. Reporter used an OpenAI-compatible proxy (`global/anthropic.claude-sonnet-4-5-...`); an SSE/proxy-side artifact is at least as likely as a Cline bug. Ask for the raw API request/response log.",
  "cluster": ""
 },
 {
  "number": 10371,
  "title": "{\"message\":\"Codex API error: Invalid 'input[67].id': 'v_3oaa-GI4TUz7IP--TI0A8'. Expected an ID that begins with 'rs'.\",\"modelId\":\"gpt-5.3-codex\",\"providerId\":\"openai-codex\"}",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The error text itself is gone: `rg \"Codex API error\"` matches nothing in the repo, so the bespoke Codex client that produced it no longer exists. On main the `openai-codex` (ChatGPT Subscription) builtin has `family: \"openai\"` (`sdk/packages/llms/src/providers/builtins.ts`) and `resolveRuntimeFamily` in `sdk/packages/llms/src/providers/builtins-runtime.ts` routes it to the AI SDK OpenAI Responses provider (`createOpenAICodexProvider`, `sdk/packages/llms/src/providers/ai-sdk.ts:1315`). Nothing on main persists or replays provider reasoning item ids (`rg 'itemId|reasoningId|rs_'` finds no such handling).",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Legacy failure mode: resuming a persisted conversation replayed stored non-`rs_` item ids into the Codex `input[]` array.",
  "cluster": "codex-responses-item-pairing"
 },
 {
  "number": 10373,
  "title": "Plugin no carga en PhpStorm (error Java) + Solicitud de soporte CLI",
  "area": "jetbrains",
  "kind": "question-support",
  "relevant_to_main": "no",
  "evidence": "The JetBrains plugin lives in a separate repo; this repo only embeds cline-core for it via `apps/vscode/standalone`, and the report is a manual `.jar` sideload failure in the IDE (pure JetBrains packaging/JVM territory, no SDK involvement). The report is also internally inconsistent \u2014 it names a plugin 'Axet' and its System Information block lists VS Code 1.116.0/Node v22.22.1 rather than a JetBrains IDE \u2014 and no Java stack trace was attached.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "The secondary ask (a `claude`-style CLI) is already satisfied on main by `apps/cli` (`@cline/cli`).",
  "cluster": ""
 },
 {
  "number": 10375,
  "title": "Context window percentage display incorrectly caps at 32.8k",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "On main the LM Studio context window has a single source of truth: `apps/vscode/webview-ui/src/components/settings/providers/LMStudioProvider.tsx` builds `ModelInfo.contextWindow` from `model?.loaded_context_length ?? model?.max_context_length` and renders the 'Context Window' field `disabled` with title 'Not editable - the value is returned by the connected endpoint'; that same `selectedModelInfo?.contextWindow` is what `apps/vscode/webview-ui/src/components/chat/task-header/TaskHeader.tsx:207` feeds to `ContextWindow.tsx` (`percentage: (lastApiReqTotalTokens / contextWindow) * 100`). So the editable-setting-vs-display mismatch is structurally gone, but 32768 is exactly what LM Studio reports as `loaded_context_length` when a model is loaded at 32k.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "low",
  "repro": "1. Install LM Studio, load `qwen/qwen3.6-35b-a3b` (or any model) with a 262144 context length and run `lms server start`. 2. Run the SDK extension: `DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <folder>`, open the Cline sidebar. 3. Settings > API Provider > LM Studio, pick the loaded model, and read the disabled 'Context Window' field. 4. Start a task and watch the context bar in the task header. Expected: both the settings field and the task-header max read 262144. Actual (per report): the header max reads 32.8k and the percentage exceeds 100%. If the settings field also reads 32768, the bug is upstream in LM Studio's `/v1/models` `loaded_context_length`, not Cline.",
  "human_attention": "yes",
  "notes": "Display-only, but it also drives the auto-compaction threshold. Quick to disambiguate for anyone with LM Studio installed.",
  "cluster": "openai-compat-context-window-ignored"
 },
 {
  "number": 10378,
  "title": "Windows cmd Chinese output is garbled in Cline terminal/chat logs even with `chcp 65001`",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`createRollingCollector` in `sdk/packages/core/src/extensions/tools/executors/bash.ts` unconditionally does `const decoder = new StringDecoder(\"utf8\")` and feeds every stdout/stderr chunk through it, and `getDefaultShell` in `sdk/packages/shared/src/parse/shell.ts` returns `\"powershell\"` on win32 with no code-page or encoding negotiation at spawn. GBK (cp936) bytes therefore get decoded as UTF-8 \u2014 the exact mojibake fingerprint the commenter demonstrated.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. On a zh-CN Windows 11 box (active code page 936), run the CLI from source: `bun run cli -i`. 2. Ask it to run a command whose child process emits GBK bytes, e.g. a `.bat` saved in GBK that echoes Chinese text, or `cmd /c chcp 936 && type gbk.txt`. 3. Inspect the tool result Cline shows. Expected: the Chinese text renders correctly. Actual: replacement characters, because `bash.ts`'s `StringDecoder(\"utf8\")` decodes cp936 bytes. Fix direction: honor the active console code page (or force UTF-8 at spawn, e.g. prefix `chcp 65001` / set `PYTHONIOENCODING`) instead of assuming UTF-8.",
  "human_attention": "yes",
  "notes": "The reporter's own `chcp 65001 && echo \u4e2d\u6587` one-liner is partly user error (cmd.exe parses the literal under the pre-switch code page), but the underlying hard-coded UTF-8 decode in @cline/core is real. Note the VS Code surface goes through VS Code shell integration (`apps/vscode/src/hosts/vscode/terminal/VscodeTerminalManager.ts`), so the CLI/SDK path is the one I can confirm from code.",
  "cluster": ""
 },
 {
  "number": 10388,
  "title": "Raw XML `<response>` / `</response>` tags displayed in chat around terminal output",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Main has no XML assistant-message parser \u2014 tool calls are native and the tool set is fixed in `sdk/packages/core/src/extensions/tools/model-tool-routing.ts` (`read_files`, `search_codebase`, `run_commands`, `fetch_web_content`, `apply_patch`, `editor`, `skills`, `ask_question`, `submit_and_exit`); there is no `execute_command` and no `<response>` wrapper. The single `<response>` occurrence in the tree is a plan-mode prompt string at `apps/vscode/src/core/prompts/responses.ts:251`.",
  "verdict": "duplicate",
  "duplicate_of": 9848,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter themselves pointed at #9848 ('Cline prints raw tool invocation XML...', still OPEN). Both describe the legacy XML tool protocol leaking into rendered text.",
  "cluster": ""
 },
 {
  "number": 10391,
  "title": "mimo \u5927\u6a21\u578b \u914d\u7f6e\u5f02\u5e38",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Main ships a first-class Xiaomi/MiMo provider: `sdk/packages/llms/src/providers/providers.generated.ts:2839` defines id `xiaomi` (baseUrl `https://api.xiaomimimo.com/v1`, `defaultModelId: \"mimo-v2.5-pro-ultraspeed\"`, `modelsProviderId: \"xiaomi\"`), added by `ae033761a` (\"feat: auto generate built-in provider list\", #12204), with a generated model catalog (`catalog.generated.ts` has `xiaomi-mimo-v2.5` etc.) and a settings entry at `apps/vscode/webview-ui/src/components/settings/providers/providerSettingsRegistry.ts:99/162`. The reporter's error string ('Invalid API Response: The provider returned an empty or unparsable response') no longer exists anywhere in the repo.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed against the JetBrains plugin, but the root cause (missing provider + missing model list) lived in the shared provider catalog, so it is in scope \u2014 and resolved. The empty-response symptom belonged to the retired XML tool-call parser.",
  "cluster": "mimo-provider-config"
 },
 {
  "number": 10392,
  "title": "Abnormal configuration of MIMO large model",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Same reporter, same content as #10391 translated to English (both filed 2026-04-24 by xjxlx, both name mimo / v2.5-pro on JetBrains 3.79.0). Same code check applies: the `xiaomi` builtin at `sdk/packages/llms/src/providers/providers.generated.ts:2839` plus its generated model list already covers the request.",
  "verdict": "duplicate",
  "duplicate_of": 10391,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "A maintainer (@arafatkatze) promised a 'more proper solution' in the thread; #12204 delivered it.",
  "cluster": "mimo-provider-config"
 },
 {
  "number": 10405,
  "title": "ChatGPT Subscription gpt-5.5 doesn't work but gpt-5.4 works",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The thread converges on the same failure as #10410: the original reporter posted 'It started to work now ... Seems to be some issue on the ChatGPT side', and the remaining reporters narrowed it to large contexts, explicitly linking #10410. Main already caps the model \u2014 `sdk/packages/llms/src/providers/openai-codex-models.ts` `toOpenAICodexModel` hardcodes gpt-5.5 to `contextWindow: 400_000` / `maxInputTokens: 272_000 * 0.95`.",
  "verdict": "duplicate",
  "duplicate_of": 10410,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "The bare 400 on a fresh conversation was transient provider-side; the durable part is the context-cap issue tracked in #10410.",
  "cluster": "gpt55-context-overflow"
 },
 {
  "number": 10407,
  "title": "Cline cannot open terminal and execute commands by itself, shows completed within cline but command don't run in terminal",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "No repro steps, no provider/model, no system info, and the 'Cline Version' field contains a VS Code version ('Arm64 1.117.0'). The command surface on main is a full rewrite anyway \u2014 `apps/vscode/src/sdk/vscode-run-commands-tool.ts` backed by `apps/vscode/src/hosts/vscode/terminal/VscodeTerminalManager.ts` (OSC 633 shell integration), not the 3.x `TerminalManager` \u2014 so nothing here maps to a specific defect.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Most likely missing VS Code shell integration in the user's shell profile. Safe to ask-and-close.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 10410,
  "title": "gpt-5.5 fails when context reaches 500k, even though Cline believes the max context is 1M",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`sdk/packages/llms/src/providers/openai-codex-models.ts` now special-cases the model: `if (id.includes(\"gpt-5.5\")) return { ...model, contextWindow: 400_000, maxInputTokens: 272_000 * CODEX_EFFECTIVE_CONTEXT_WINDOW_PERCENT, maxTokens: 128_000 }`, with a comment that 'the ChatGPT/Codex backend enforces a 272K input / 128K output cap that is lower than what the generated OpenAI API catalog reports', and every other Codex model is scaled by `CODEX_EFFECTIVE_CONTEXT_WINDOW_PERCENT = 0.95` because 'the backend starts rejecting requests around 95% of a model's advertised input cap'. `git log` on that file shows `dd719dce8` (\"fix(llms): OpenAI Codex model metadata for GPT Subscription provider\", #12129). Auto-compaction is also now on by default (`26037b17a`, #12317).",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Fixed on main, but this is the batch's highest-visibility thread (12 comments, users publicly announcing they are switching tools, and a contributor mid-flight on a patch). It needs a maintainer to post the fix reference and close it, not just a bot.",
  "cluster": "gpt55-context-overflow"
 },
 {
  "number": 10412,
  "title": "Inifite loop in thinking state (#9846) follow up",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Main's loop safety only covers repeated identical *tool calls*: `sdk/packages/core/src/runtime/safety/loop-detection.ts` tracks `lastToolName`/`lastToolSignature`/`consecutiveIdenticalCount` via `toolCallSignature`, so a reasoning/text-only loop that emits no tool call is not caught by it. The backstop is the mistake tracker (`sdk/packages/core/src/runtime/safety/mistake-tracker.ts`, reason `completion_without_submit`) plus the iteration cap.",
  "verdict": "duplicate",
  "duplicate_of": 9846,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter filed this explicitly as a follow-up to #9846, which is still OPEN. Track there. Provider was `openai-compat:kimi-k2.6`, i.e. a model without reliable native tool calling.",
  "cluster": "ollama-json-tool-loop"
 },
 {
  "number": 10413,
  "title": "Invalid API Response: The provider returned an empty or unparsable response. This is a provider-side issue where the model failed to generate valid output or returned tool calls that Cline cannot process. Retrying the request may help resolve this issue.",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The stated root cause is gone: `rg '<think>|</think>|<thinking>'` across `*.ts` matches nothing on main, and neither does the error string itself (`rg 'empty or unparsable'`, `rg 'Invalid API Response'` \u2014 both empty). Tool calls on main are native, driven by the fixed tool set in `sdk/packages/core/src/extensions/tools/model-tool-routing.ts`. What remains plausible is the underlying situation: a local Qwen served by llama.cpp that emits XML pseudo tool calls in text instead of native tool calls still produces a turn with no usable tool call.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Still getting fresh reports as of 2026-07-24 (Qwen 3.5/3.6 35B A3B via llama.cpp). Worth a maintainer asking one commenter to re-test on the SDK extension, since the diagnosis they are all citing no longer describes main's code.",
  "cluster": "invalid-api-response-empty"
 },
 {
  "number": 10417,
  "title": "Model crashes while reviewing file agent is updating",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The legacy mechanism \u2014 streaming edits into a live editor and auto-scrolling it \u2014 does not exist on main: `rg DiffViewProvider` matches only `CHANGELOG.md`. Edits are applied by `sdk/packages/core/src/extensions/tools/executors/editor.ts` and merely *previewed* read-only through `vscode.diff` in `apps/vscode/src/hosts/vscode/VscodeEditPreview.ts`. HEAD commit `bcf8c1d03` (#12558) further bounds the preview open with a 5s race so 'a rejected or stalled preview open never blocks the approval ask or fails the edit'.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "The 3.x behavior of user scrolling fighting the streaming diff editor has no analogue in the SDK extension.",
  "cluster": ""
 },
 {
  "number": 10419,
  "title": "Romanian number \"6000\" incorrectly converted to \"\\u0219ase zero\" instead of \"\\u0219ase mii\"",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "`rg -i 'num2words|expand_numbers|romanian'` across the whole workspace (excluding node_modules) returns zero matches. Cline has no number-to-words or TTS text-normalization code at all; the report describes the reporter's own Python `expand_numbers` method calling the third-party `num2words` library.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Wrong repo \u2014 belongs upstream at savoirfairelinux/num2words. Almost certainly an auto-filed bug report from the reporter's own Cline session.",
  "cluster": ""
 },
 {
  "number": 10421,
  "title": "Kat Coder Pro model marked as free but consumes account credits",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The 'Free' badge is purely price-driven \u2014 `formatCompactPrice` in `apps/vscode/webview-ui/src/components/settings/common/ModelInfoView.tsx` returns 'Free' only `if (price === 0)`. The `cline` provider's model list comes from `buildClineModels()` in `sdk/packages/llms/src/providers/builtins.ts`, which is `generatedModels(\"openrouter\")`; in that section of `sdk/packages/llms/src/catalog/catalog.generated.ts` the Kat Coder entries are all priced non-zero (`kwaipilot/kat-coder-pro-v2.5`: input 0.74 / output 2.96; `kat-coder-air-v2.5`: 0.15 / 0.6), and the free-promo id `kwaipilot/kat-coder-pro` is no longer in the OpenRouter section.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Was a time-boxed promo whose backend pricing metadata said $0 while credits were still metered. Current catalog data no longer produces the 'Free' label for this family.",
  "cluster": ""
 },
 {
  "number": 10422,
  "title": "Cline UI is broken when using Firefox",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Nothing on main targets Firefox. `apps/vscode/webview-ui/src/main.css` styles the scrollbars entirely with Chromium-only `::-webkit-scrollbar*` pseudo-elements (including the `border-right-width: calc(100vw + 100vh)` thumb trick) and guards the workaround with `@supports selector(::-webkit-scrollbar)`, whose fallback branch is what Firefox takes; the webview builds on Tailwind v4 (`apps/vscode/webview-ui/package.json`: `tailwindcss ^4.1.13`, `@tailwindcss/vite`). Two independent reporters see it only in Firefox under `code-server` (Chrome and Safari render fine), which is consistent with a CSS-support gap rather than a code-server bug.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. Build the SDK extension webview: `cd apps/vscode && bun run build:webview && bun esbuild.mjs`. 2. Install/serve it under `code-server` (v4.117+ / VS Code 1.117) on any host. 3. Open the code-server URL in Firefox 150+ and in Chrome side by side, then open the Cline sidebar, the Settings > API Configuration page, and an active task view. Expected: identical layout in both browsers. Actual (per report): in Firefox the sidebar, model-config page, and task view are visibly broken while functionality still works; Chrome and Safari are fine. Capture both screenshots to isolate which rule collapses.",
  "human_attention": "yes",
  "notes": "Two reporters, still OPEN, and code-server + Firefox is a real deployment shape. Start by diffing computed styles on the `.scrollable` containers and Tailwind v4's `@property`-based custom properties.",
  "cluster": ""
 },
 {
  "number": 10424,
  "title": "MCP server suggestion: clirank-mcp-server (API discovery for agents)",
  "area": "docs",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "This is a third-party MCP server submission with a self-disclosed commercial interest, not a defect in this repo. Marketplace listings are not sourced from `cline/cline` \u2014 the only references here are documentation redirects (`docs/docs.json:368` and `docs/docs.json:916`, both pointing at `mcp-marketplace` doc pages), and submissions go through the separate `cline/mcp-marketplace` repo.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Redirect the author to the MCP marketplace submission process and close.",
  "cluster": ""
 },
 {
  "number": 10426,
  "title": "MCP tool calls incorrectly pass `task_progress` parameter to remote MCP server",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`grep -rn task_progress sdk/packages --include=*.ts` returns nothing on main; the only survivors in `apps/vscode/src/shared/ExtensionMessage.ts` are the `ClineSay.TASK_PROGRESS` message type, not a tool parameter. MCP arguments are now passed straight through from the model in `sdk/packages/core/src/extensions/mcp/tools.ts` (`arguments: input as Record<string, unknown>`), and `apps/vscode/src/sdk/vscode-runtime-builder.ts` forwards `request.arguments ?? {}` to `McpHub.callTool` with no injected fields.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "The 3.x `use_mcp_tool` XML tool appended `task_progress` to every tool schema; the SDK rewrite dropped that mechanism entirely.",
  "cluster": "mcp-task-progress-leak"
 },
 {
  "number": 10427,
  "title": "About DS Model",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "The `deepseek` provider block in `sdk/packages/llms/src/catalog/catalog.generated.ts` now contains `deepseek-v4-flash` (contextWindow 1000000) and `deepseek-v4-pro` alongside `deepseek-chat`/`deepseek-reasoner`, and `sdk/packages/llms/src/providers/providers.generated.ts` sets `defaultModelId: \"deepseek-v4-flash\"`. The catalog is regenerated from models.dev (see `sdk/packages/llms/src/catalog/README.md` and `catalog-live.ts`), last refreshed in d1837366c.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Also resolves the 1M-context complaint in the comments \u2014 the catalog entry carries contextWindow/maxInputTokens 1000000.",
  "cluster": "stale-model-catalog"
 },
 {
  "number": 10434,
  "title": "[MCP Marketplace] Add LocalPro \u2014 verified local service-provider data",
  "area": "unknown",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "The MCP marketplace catalog is fetched from `https://cline.github.io/marketplace/catalog.json` (`MARKETPLACE_CATALOG_URL` in `apps/vscode/src/core/controller/marketplace/marketplace-helpers.ts`); nothing in this repo defines catalog entries, so a server listing cannot be added here.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Belongs in the cline/mcp-marketplace repo; the reporter says they are submitting there separately.",
  "cluster": ""
 },
 {
  "number": 10438,
  "title": "Bug: Kanban sidebar agent fails with Anthropic-backed LiteLLM proxy \u2014 tool schema anyOf incompatibility",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Fixed by 770e4f063 `fix(sdk): Windows shell tool schema should take a top-level input object (#364)` (May 5 2026) plus follow-up 7512a4ca4 for `allOf`. On main `createShellTool` advertises `inputSchema: zodToJsonSchema(RunCommandsInputSchema)` (a plain object) at `sdk/packages/core/src/extensions/tools/definitions.ts:478`, `RunCommandsInputUnionSchema` is only used for input validation in `extensions/tools/helpers.ts:140`, and `sdk/packages/shared/src/tools/create.ts` now rewrites or loudly rejects any top-level `anyOf`/`oneOf`/`allOf` schema.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Unusually well-researched report; the maintainer fix landed a week after it was filed.",
  "cluster": ""
 },
 {
  "number": 10441,
  "title": "Add Hicap Responses API and reasoning settings",
  "area": "sdk-llms",
  "kind": "meta-task",
  "relevant_to_main": "yes",
  "evidence": "HiCap exists on main as an `openai-compatible`-family builtin (`sdk/packages/llms/src/providers/builtins.ts:748`, `defaults: { baseUrl: \"https://api.hicap.ai/v1\" }`), but model refresh still hits `https://api.hicap.ai/v2/openai/models` in `sdk/packages/core/src/services/llms/provider-defaults.ts:426`, there is no Responses-API routing rule for `hicap` in `sdk/packages/llms/src/providers/routing/provider-option-rules.ts`, and `apps/vscode/webview-ui/src/components/settings/providers/HicapProvider.tsx` exposes no reasoning-effort or thinking-budget controls.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Internal tracking issue for draft PR #10440; a maintainer should decide whether the remaining Responses/reasoning work is still wanted.",
  "cluster": ""
 },
 {
  "number": 10444,
  "title": "Configurable Shell Path for Background Exec on Windows (to support Git Bash / MSYS Bash for UTF-8)",
  "area": "vscode-new",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "`apps/vscode/src/sdk/vscode-run-commands-tool.ts` now snapshots the shell via `takeShellSnapshot()` \u2192 `getShellForProfile(defaultTerminalProfile)` and passes it to `createShellExecutor({ shell, env: { SHELL: shell } })` on the `backgroundExec` path; `getShellForProfile` in `apps/vscode/src/utils/shell.ts:362` resolves VS Code terminal profile paths (including Git Bash). The `StandaloneTerminalProcess.getDefaultShell()` the reporter cites no longer exists anywhere under `apps/vscode/src`.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Landed with 557d72569 `fix(vscode): shell mismatch between prompt, execution, and user configuration on Windows (#12331)`; `buildRunCommandsDescription` even adjusts prompt guidance per shell kind.",
  "cluster": "shell-override-ignored"
 },
 {
  "number": 10450,
  "title": "Chat auto-scrolls to top when processing larger responses",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The scroll machinery still exists on main at `apps/vscode/webview-ui/src/components/chat/chat-view/hooks/useScrollBehavior.ts` (`scrollToBottomSmooth`/`scrollToBottomAuto` call `virtuosoRef.scrollTo({ top: Number.MAX_SAFE_INTEGER })`, with a 500ms `keepPinnedToBottomAfterLayout` settle timer) and `components/layout/MessagesArea.tsx` (`initialTopMostItemIndex`, `atBottomThreshold={10}`). Two later fixes touched this file (1c4a7885e and d6db723c9, `fix(vscode): keep command output pinned to bottom`) but neither mentions jumping to the top of the conversation.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. Build and run the new extension: `cd apps/vscode && bun run build:webview && bun esbuild.mjs`, then `DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <some-repo>`.\n2. Configure any provider that streams long answers (e.g. anthropic/claude-sonnet, or bedrock as in the report).\n3. Open the Cline sidebar and run a task with several prior turns so the transcript is longer than the viewport.\n4. Ask for a very long single response (\"write a detailed design doc for X, at least 2000 words\").\n5. Expected: the view stays pinned near the bottom / at the start of the streaming response. Actual (per report): the virtualized list jumps to the top of the conversation while the response streams.\n6. If it does not reproduce with plain text, retry with a response containing a long code block or a large `run_commands` output row, since row-height changes drive `handleRowHeightChange`/`keepPinnedToBottomAfterLayout`.",
  "human_attention": "yes",
  "notes": "Suspect Virtuoso mis-measurement when a single very tall row is appended \u2014 `scrollTo({ top: MAX_SAFE_INTEGER })` on a partially-measured list can land at index 0.",
  "cluster": ""
 },
 {
  "number": 10453,
  "title": "Cline 'Tasks History' : Swapped!?",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "On main the history list no longer comes from a single globalState array: `apps/vscode/src/sdk/sdk-task-history.ts:listHistory()` reads per-session records from the SDK host (`host.listHistory`) and only merges leftover legacy items, `apps/vscode/src/core/storage/StateManager.ts:754` deliberately excludes `taskHistory` from the batched globalState write, and `apps/vscode/webview-ui/src/components/history/HistoryView.tsx:80` passes a `currentWorkspaceOnly` filter. The whole-array last-writer-wins overwrite that would produce a 'swap' between two windows is therefore gone, but nothing proves the reporter's symptom is fixed.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Second reporter's JetBrains variant is expected behaviour to a degree \u2014 all IDEs share `~/.cline/data`; the real question is whether the workspace filter is defaulting off. Needs a retest on the SDK extension.",
  "cluster": "task-history-loss"
 },
 {
  "number": 10459,
  "title": "Newer models for fireworks.ai are not listed:  GLM5.1, kimi k2.6, deepseek4pro despite being available at fireworks",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "The `fireworks` block in `sdk/packages/llms/src/catalog/catalog.generated.ts` now lists `accounts/fireworks/models/glm-5p1`, `accounts/fireworks/models/kimi-k2p6`, and `accounts/fireworks/models/deepseek-v4-pro` (all three ids the reporter asked for), plus newer glm-5p2/kimi-k2p7 entries. The catalog is regenerated from models.dev by `@cline/llms/scripts/generate-models.ts`.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": null,
  "cluster": "stale-model-catalog"
 },
 {
  "number": 10464,
  "title": "New Kimi-K2.6 LLM model from moonshot.ai is not listed despite beeing available through https://api.moonshot.ai/v1",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "The `moonshot` provider block in `sdk/packages/llms/src/catalog/catalog.generated.ts` lists `kimi-k2.6` (plus `kimi-k3`, `kimi-k2.7-code`), and `sdk/packages/llms/src/providers/routing/provider-options.test.ts` has explicit thinking-routing cases for `moonshotai/kimi-k2.6`.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "The linked follow-up #10544 (K2.6 broken via OpenAI Compatible) is a separate runtime problem and is not covered by the catalog entry.",
  "cluster": "stale-model-catalog"
 },
 {
  "number": 10466,
  "title": "Hive Civilization \u2014 51-server MCP fleet for Cline (notification)",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "Self-described 'notification post' with no defect and no code request; MCP listings are served from `https://cline.github.io/marketplace/catalog.json` (`MARKETPLACE_CATALOG_URL`, `apps/vscode/src/core/controller/marketplace/marketplace-helpers.ts:49`), so nothing in this repo would change.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Crypto/x402 promotional post; safely closeable.",
  "cluster": ""
 },
 {
  "number": 10469,
  "title": "Newer models for deepseek are not listed.",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "Same request as #10427 and already satisfied: the `deepseek` block in `sdk/packages/llms/src/catalog/catalog.generated.ts` contains `deepseek-v4-flash` and `deepseek-v4-pro` with `contextWindow: 1000000`, and `providers.generated.ts` defaults deepseek to `deepseek-v4-flash`.",
  "verdict": "duplicate",
  "duplicate_of": 10427,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "The sub-thread about OpenAI-Compatible ignoring a 1M context override is a distinct bug and would need its own issue if still reproducible.",
  "cluster": "stale-model-catalog"
 },
 {
  "number": 10470,
  "title": "Add Agent Broker to recommended MCP servers list",
  "area": "unknown",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "Recommended-server listings come from the external catalog at `https://cline.github.io/marketplace/catalog.json` (`MARKETPLACE_CATALOG_URL` in `apps/vscode/src/core/controller/marketplace/marketplace-helpers.ts:49`); this repo only consumes it via `fetchMarketplaceCatalog`.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Redirect the submitter to cline/mcp-marketplace.",
  "cluster": ""
 },
 {
  "number": 10471,
  "title": "Cline VSCode Extension missing Gemini 3.1 flash",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "The `gemini` block in `sdk/packages/llms/src/catalog/catalog.generated.ts` carries `gemini-3.1-flash-lite`, `gemini-3.1-pro-preview`, `gemini-3.5-flash`, `gemini-3.6-flash` and `gemini-flash-latest`. A live fetch of `https://models.dev/api.json` (the generator's source per `sdk/packages/llms/src/catalog/catalog-live.ts`) shows Google has no plain `gemini-3.1-flash` id either \u2014 only `-lite`, `-image`, `-tts`, `-live` variants \u2014 so the requested id does not exist upstream.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter most likely wanted `gemini-3.1-flash-lite`, which is present; newer flash generations are also listed.",
  "cluster": "stale-model-catalog"
 },
 {
  "number": 10472,
  "title": "Duplicate Language Model entries shown when using GitHub Copilot as API Provider",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/vscode/src/core/controller/models/getVsCodeLmModels.ts` returns `vscode.lm.selectChatModels({})` verbatim, `apps/vscode/src/shared/proto-conversions/models/vscode-lm-models-conversion.ts:16` maps 1:1 with no dedup, and `apps/vscode/webview-ui/src/components/settings/providers/VSCodeLmProvider.tsx:87-93` renders each entry labelled only `{vendor} - {family}` while keying on the full `vendor/family/version/id`. Any vendor exposing several ids under one family therefore renders as visually identical repeated rows.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Agent-fixable: dedupe by stringified selector and/or include version/id in the option label.",
  "cluster": ""
 },
 {
  "number": 10479,
  "title": "When using gpt-5.1-codex or gpt-5.2-codex fails to chain responses with \"Reasoning Effort\" set to anything above \"None\"",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "No `previous_response_id` / `previousResponseId` is set anywhere in `sdk/packages/llms/src` outside VCR cassettes and the redaction key list in `tests/provider-vcr.test.ts`. `sdk/packages/llms/src/providers/vendors/openai.ts` builds models with `provider.responses(modelId)` and sends the full conversation each request, and the codex rule in `providers/routing/provider-option-rules.ts:221` forces `store: false`, so there is no server-side response chain to go missing.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "The 3.x `OpenAiNativeHandler` chained Responses calls by id; the SDK provider does not.",
  "cluster": "codex-responses-item-pairing"
 },
 {
  "number": 10488,
  "title": "Plan Mode / Act Mode does not allow distinct headers",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/vscode/src/shared/storage/state-keys.ts:106` still declares `openAiHeaders` under the `// Global configuration (not mode-specific)` section of `API_HANDLER_SETTINGS_FIELDS`, while every model field has `planMode*`/`actMode*` twins (e.g. `planModeOpenAiModelId`, `planModeOpenAiModelInfo` at lines 163-164). The Custom Headers editor is nonetheless rendered inside the per-mode provider pane in `apps/vscode/webview-ui/src/components/settings/providers/OpenAICompatible.tsx:377-455`, so editing it in Act mode overwrites the Plan-mode value.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs a product call: either make headers mode-specific (new plan/actModeOpenAiHeaders keys) or move the editor out of the per-mode pane so the sharing is obvious.",
  "cluster": ""
 },
 {
  "number": 10492,
  "title": "Can\u2019t log in via VSCode extension Cline (Nightly)",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The reported failure mode is a browser\u2192`vscode://` callback that never returns. On main the extension no longer depends on that for Cline sign-in: `apps/vscode/src/sdk/auth-service.ts:553` comments \"Use WorkOS device auth so the browser confirmation code can be surfaced in the extension\", backed by `sdk/packages/core/src/auth/cline.ts` (`/user_management/authorize/device`, `requestWorkOSDeviceAuthorization`, device_code polling). The URI handler in `apps/vscode/src/extension.ts:154` remains only for task deeplinks and MCP OAuth.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Device-code flow arrived with the SDK migration squash 94f5a47a5; d5b966d0a later polished the in-chat verification-code box.",
  "cluster": ""
 },
 {
  "number": 10493,
  "title": "CLI freezes / input not working",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Filed against CLI 0.0.7; `apps/cli/package.json` is now at 3.0.46 and the TUI input layer has been rewritten around OpenTUI 0.4.x (`apps/cli/src/tui/components/input-bar.tsx`, `hooks/use-autocomplete.ts`, `hooks/use-root-keyboard.ts`, `hooks/root-keyboard-routing.ts`), with later fixes such as a4d093703 (fuzzy file-mention ranking) and 0aebfcdc1 (Ctrl+C/Escape alignment). I could not find the 0.0.7 input handler to confirm the reported freeze.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Ask the Windows reporter to retest on the current CLI; the surface they hit no longer exists.",
  "cluster": ""
 },
 {
  "number": 10495,
  "title": "OpenAI compatible API Not found",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Matches the root cause fixed by adb8ee0c4 `fix(vscode): model ID under chat field stale / resets to gpt-4o for OpenAI Compatible (#12552)` \u2014 the settings dropdown stored the SDK spelling `openai-compatible` while every provider-keyed path expected the legacy `openai`, so the configured model id was dropped and the request fell back to the catalog default `gpt-4o` (`sdk/packages/llms/src/providers/builtins.ts:630-639`), which 404s on featherless/chutes base URLs. 2c64c4ce5 additionally fixed model-list failures for base URLs with a trailing slash.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Several distinct reporters (featherless, chutes, z.ai); worth confirming with them once #12552 ships rather than closing blind.",
  "cluster": "custom-base-url-404"
 },
 {
  "number": 10497,
  "title": "Models can switch from plan to act mode autonomously or if prompted to do so",
  "area": "vscode-new",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Model-initiated switching is now deliberate: `apps/vscode/src/sdk/sdk-session-config-builder.ts:40-77` injects a `switch_to_act_mode` extra tool in plan mode only, and `sdk/packages/shared/src/prompt/cline.ts:45` (`PLAN_MODE_INSTRUCTIONS`) tells the model to call it once the user approves, with a prompt-level guard to \"never call it in the same turn you present a plan\". So the only remaining defect is prompt adherence, not a missing gate.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Product decision: whether to offer an opt-out that removes `switch_to_act_mode` and keeps mode switching user-only.",
  "cluster": ""
 },
 {
  "number": 10499,
  "title": "MCP tool calls execute without user approval when Auto-approve is OFF",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/vscode/src/sdk/sdk-session-lifecycle.ts:146` builds `toolPolicies` exactly once at session start from the servers `McpHub` knows about then, and `buildToolPolicies` (`apps/vscode/src/sdk/sdk-tool-policies.ts:30-37`) enumerates only those `server__tool` names \u2014 there is no `\"*\"` fallback policy and no rebuild on MCP change. In `sdk/packages/agents/src/agent-runtime.ts:1397-1409`, approval is requested only when `policy.autoApprove === false`, so any tool absent from the map (server connected or tool list refreshed after the session started) executes with no prompt.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Fail-open default; fix is either a `\"*\": { autoApprove: false }` baseline policy or recomputing toolPolicies when McpHub's tool list changes. Security/consent impact for write-capable MCP servers.",
  "cluster": "approval-bypass"
 },
 {
  "number": 10500,
  "title": "Cline asking to use Claude 4.5 Sonnet",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The nag text is gone: grepping `advanced agentic coding capabilities` / `less capable models` across the tree returns nothing (it was last present before 94f5a47a5, the SDK-migration squash). Only static provider notes remain (`Cline uses complex prompts, so behavior can vary across models` in `apps/vscode/webview-ui/src/components/settings/providers/{Xai,OpenAICompatible,Ollama,LMStudio}*.tsx`). The mistake-limit path that triggered it was reworked in HEAD commit e4091c368, which also deletes the `maxConsecutiveMistakes` setting that commenters were hacking around.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "High-frustration thread (13 comments, users leaving); worth a closing note pointing at e4091c368.",
  "cluster": "weak-model-mistake-limit"
 },
 {
  "number": 10502,
  "title": "Claude Code provider fails with \"Command failed with exit code 1\" when using claude-opus-4-7[1m] (PhpStorm plugin, subscription login)",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Claude Code no longer spawns `claude` from hand-rolled handler code: `sdk/packages/llms/src/providers/vendors/community.ts:88-118` lazily imports `ai-sdk-provider-claude-code` and delegates to `createClaudeCode()`, resolving the executable via `resolveClaudeExecutable()`. The model ids are also different \u2014 `buildClaudeCodeModels()` (`sdk/packages/llms/src/providers/builtins.ts:363-383`) exposes only `opus`/`sonnet`/`haiku`, so the bracketed `claude-opus-4-7[1m]` id that was passed to the CLI cannot be produced.",
  "verdict": "duplicate",
  "duplicate_of": 10135,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Same family as the linked #10336; both target the retired 3.x Claude Code handler.",
  "cluster": "claude-code-exit-code-1"
 },
 {
  "number": 10504,
  "title": "Terminal freezes if you list MCP servers after cancelling an add server operation",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Filed against CLI 0.0.7 (now 3.0.46). The current wizard at `apps/cli/src/wizards/mcp/index.ts` guards every prompt with `p.isCancel(...)` and returns cleanly (lines 126, 136, 176, 197, 210, 246, 269, 332, 373, 405, 492), and the menu loop at line 459/464 re-renders 'List servers'/'Add server' after a cancel. I could not find a code path that would leave the TTY in raw mode.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Another user failed to reproduce on Windows 11 and saoudrizwan asked for a retest on the latest build with no reply for two months \u2014 safely closeable.",
  "cluster": ""
 },
 {
  "number": 10506,
  "title": "clite resume fails with \"Hub connection closed\" and \"Invalid input\" \u2014 total workspace state loss",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "There is no `resume` subcommand on main: `apps/cli/src/commands/program.ts` registers only a `[prompt]` positional plus `--id <session-id>` for resuming, and the only `resume` command is `schedule resume` (`apps/cli/src/commands/schedule/handlers.ts:291`). Because `createProgram()` uses `.allowExcessArguments()` with a bare `[prompt]` argument and `commanderToParsedArgs` sets `defaultToolAutoApprove: true`, `clite resume` starts a one-shot auto-approved agent run with the literal prompt \"resume\" \u2014 which matches the maintainer's reply on the thread.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "User error on the reported symptom, but the underlying footgun is real and worth a maintainer decision: an unrecognized subcommand silently becomes an auto-approved agent prompt (program.ts `defaultToolAutoApprove: true`), which is a plausible mechanism for the reporter's untracked-file loss.",
  "cluster": "hub-session-loss"
 },
 {
  "number": 10509,
  "title": "Right click paste doesn't work in 'clite' cli",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/cli/src/tui/index.tsx:15` creates the OpenTUI renderer with `enableMouseMovement: true` (same in `apps/cli/src/tui/history-standalone.tsx:16` and `apps/cli/src/commands/auth.ts:338`). With terminal mouse reporting enabled the emulator hands right-clicks to the app instead of showing its own context menu. `apps/cli/src/tui/opentui-env.ts` explicitly documents that mouse movement must stay on for the home-screen robot animation, and there is no flag or config to turn it off.",
  "verdict": "duplicate",
  "duplicate_of": 11531,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Agent-fixable: add a `--no-mouse` / config toggle, or drop `enableMouseMovement` outside the onboarding/home robot view. Owner: `apps/cli` (OpenTUI layer).",
  "cluster": "tui-mouse-capture"
 },
 {
  "number": 10510,
  "title": "The input box bounce issue in the settings.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/vscode/webview-ui/src/components/settings/utils/useDebouncedInput.ts` still calls `setLocalValue(initialValue)` whenever the prop changes, so a 100 ms debounced save that round-trips through extension state overwrites keystrokes typed in the meantime \u2014 that is the bounce, and `DebouncedTextField.tsx` and `BaseUrlField.tsx` both use it. The reporter's exact repro (API key field) was fixed separately in `b193a81ac` (\"prevent API key field clearing on settings load\", #11925), which gave `ApiKeyField.tsx` its own focus/`hasPendingUserEditRef` guard instead of the shared hook.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Agent-fixable: port the `isFocusedRef`/`hasPendingUserEditRef` guard from `ApiKeyField.tsx` into `useDebouncedInput.ts` so every settings text field benefits.",
  "cluster": "settings-input-eats-characters"
 },
 {
  "number": 10513,
  "title": "Agent can't reliably use the search_codebase tool",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The error text comes from `parseToolInput`/`parseToolArguments` in `sdk/packages/agents/src/agent-runtime.ts:1726,1771`, which falls back to `input: {}` \u2014 matching the `search_codebase({})` in the screenshot. Since then `a1d69fee6` (2026-07-03) wired `experimental_repairToolCall: repairMalformedToolCall` into the shared `streamText` call (`sdk/packages/llms/src/providers/ai-sdk.ts:1218`, repair impl at :423) backed by the jsonrepair strategies in `sdk/packages/shared/src/parse/json.ts`, and `96b29d787` added `normalizeJsonLikeStringsForSchema` (`agent-runtime.ts:1366`).",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Repair happens before the agent runtime sees the call, because `ai-sdk.ts` only emits `tool-call-delta` on the final `tool-call` part (lines 951-976), not per streaming delta.",
  "cluster": ""
 },
 {
  "number": 10514,
  "title": "Clite not run on WSL on Intel Sandy Bridge Processor (AVX 2 lack)",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/cli/script/build.ts` builds only `bun-{linux,darwin,windows}-{x64,arm64}` targets (`allTargets` + `getBunTarget()`); there is no `bun-linux-x64-baseline` variant, and `apps/cli/script/publish-npm.ts:39` publishes only `@cline/cli-linux-x64` and friends. Bun's default x64 compile target requires AVX2, so pre-Haswell CPUs get \"Illegal instruction (core dumped)\" exactly as reported.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs a release-matrix decision: add `-baseline` targets to `allTargets`/`getBunTarget()` and publish `@cline/cli-linux-x64-baseline` (plus win32) with an install-time CPU check, or document the limitation. Two independent reporters (Sandy Bridge i5-2500, Ivy Bridge i7-3770).",
  "cluster": "cli-bun-binary-crash"
 },
 {
  "number": 10515,
  "title": "Tool Proposal: CAJAL Scientific Paper Generator for Cline",
  "area": "unknown",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "No CAJAL reference exists anywhere in the tree (`rg CAJAL` over /workspace returns nothing). The proposal is for a third-party Ollama-backed paper generator that the author says would ship as an MCP server, and Cline already loads arbitrary third-party MCP servers via `sdk/packages/core/src/extensions/mcp/{config-loader,manager,client}.ts` \u2014 no change to this repo is needed for it to work.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Effectively a promo for an external project; safely closeable with a pointer to the MCP marketplace/server docs.",
  "cluster": ""
 },
 {
  "number": 10516,
  "title": "explain changes is now triggering by itself",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The feature was deleted from main: `9c030a93b Remove Explain Changes feature (#11278)` (2026-06-04), recorded in `CHANGELOG.md:30` as \"Remove the legacy Explain Changes feature as part of the SDK migration cleanup\". `rg -i 'explainChanges|Explain Changes'` on main matches only CHANGELOG history; the only surviving explain surface is the manual `cline.explainCode` code action (`apps/vscode/src/core/controller/commands/explainWithCline.ts`, `apps/vscode/src/registry.ts:23`).",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Auto-closeable: the token-wasting auto-explain the reporter wanted disabled no longer exists in 4.0.",
  "cluster": ""
 },
 {
  "number": 10519,
  "title": "Cline compact and context problem. Help me please.",
  "area": "vscode-new",
  "kind": "question-support",
  "relevant_to_main": "unsure",
  "evidence": "Reported against the legacy 3.x marketplace build; the 3.x `ContextManager` the numbers came from does not exist on main. Context handling now lives in `sdk/packages/core/src/extensions/context/{compaction.ts,basic-compaction.ts,agentic-compaction.ts,compaction-shared.ts}` with `DEFAULT_MAX_INPUT_TOKENS = 128_000` (`compaction-shared.ts:13`), and the report gives no logs, provider config, or model context window to map onto that code.",
  "verdict": "duplicate",
  "duplicate_of": 10637,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Reads as a support question about how the context-usage bar counts the system prompt and how compaction interacts with an OpenAI-compatible DeepSeek endpoint; ask the reporter to retest on 4.0.",
  "cluster": "compaction-runs-while-disabled"
 },
 {
  "number": 10520,
  "title": "Cline Kanban MiniMax provider config not synced across state/secrets; stale provider causes invalid API key errors",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The three-way split the reporter documented is gone on main: `providers.json` is now the single source of truth (`apps/vscode/src/extension.ts:521`, `sdk/packages/core/src/services/storage/provider-settings-manager.ts:54`), and `globalState.json`/`secrets.json` are read only once by `resolveLegacyStorage()` in `sdk/packages/core/src/services/storage/provider-settings-legacy-migration.ts:268-269`. Nothing in `apps/cli` or `sdk/packages/core` writes `planModeApiProvider` anymore (`rg planModeApiProvider` hits only the VS Code webview shape). Kanban itself is an external npm package that `apps/cli/src/commands/kanban.ts:37` installs and spawns.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Four users reported the same thing. The state-split half is architecturally fixed on main; the \"long-running Kanban agent keeps a stale key until restarted\" half lives in the separate `kanban` package, so a maintainer should confirm and route/close rather than an agent guessing.",
  "cluster": ""
 },
 {
  "number": 10522,
  "title": "Disabling native tool use still triggers shell commands in native terminal",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The setting the report depends on no longer exists: `native_tool_call_enabled` is listed in the `reserved` clause of `apps/vscode/proto/cline/state.proto:398`, and `rg nativeToolCall` finds no runtime code on main. Shell execution now goes through the SDK path in `apps/vscode/src/sdk/vscode-run-commands-tool.ts` gated by `vscodeTerminalExecutionMode`, not by a native-tool-calling toggle.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Report is also internally garbled \u2014 it describes \"Codex settings\" and `config.toml`, which are OpenAI Codex CLI concepts, not Cline settings.",
  "cluster": ""
 },
 {
  "number": 10523,
  "title": "\"invalid_arguments\" / unterminated string",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The pasted log is unmistakably the new SDK shape: `metadata.invalidToolCalls[].input.rawInputText` is produced by `buildInvalidToolInput()` and `reason: \"invalid_arguments\"` by `parseToolInput()` in `sdk/packages/agents/src/agent-runtime.ts:1704-1732`, with `input: {}` substituted on parse failure. Since the report, `a1d69fee6` added jsonrepair-backed `repairMalformedToolCall` (`sdk/packages/llms/src/providers/ai-sdk.ts:423,1218`) and `ef27f4508` (\"fix: max output token handling\") addressed the truncation source; the raw text cutting off at position 12518 mid-string is consistent with hitting the output cap.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Latent related defect worth noting to whoever touches this next: `mergeToolInputText()` (`agent-runtime.ts:1775`) discards the accumulated buffer whenever an incoming delta starts with `{` or `[`, which can corrupt streamed args for providers that chunk mid-object.",
  "cluster": ""
 },
 {
  "number": 10532,
  "title": "OpenSpec workflows do not appear",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/vscode/src/core/controller/slash/getAvailableSlashCommands.ts:25-26` builds the custom slash-command list purely from the persisted `workflowToggles`/`globalWorkflowToggles` state keys. The only thing that scans `.clinerules/workflows/` and populates those keys is `refreshRules` (`apps/vscode/src/core/controller/file/refreshRules.ts:24` \u2192 `refreshWorkflowToggles`), and it is invoked only from `ClineRulesToggleModal.tsx` inside a `useEffect` gated on `isVisible`, plus `createRuleFile.ts:55`. Nothing calls it during activation or controller init, so workflows stay invisible until the panel is opened once \u2014 exactly the reported sequence.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Agent-fixable: call `refreshWorkflowToggles`/`refreshClineRulesToggles` on webview init (or have `getAvailableSlashCommands` refresh on demand) instead of relying on the modal.",
  "cluster": ""
 },
 {
  "number": 10533,
  "title": "Claude incorrectly recommended removing Prisma multiSchema   @@schema directives, causing \"public.tenants does not exist\" error and unnecessary token consumption",
  "area": "unknown",
  "kind": "question-support",
  "relevant_to_main": "no",
  "evidence": "Nothing in Cline's code produced the bad advice. The system prompt in `sdk/packages/shared/src/prompt/system.ts` contains no Prisma or ORM-specific guidance, and no tool in `sdk/packages/core/src/extensions/tools/executors/` touches `schema.prisma`. This is a report about the model's domain knowledge (anthropic/claude-sonnet-4.6), not about a defect in this repo.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Safely auto-closeable as a model-quality complaint.",
  "cluster": ""
 },
 {
  "number": 10534,
  "title": "3.83.0 and 3.82.0 tool calls fail with Deepseek v4-flash (Thinking)",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The reported string \"Cline tried to use search_files without value for required parameter\" survives on main only as an evals fixture (`evals/analysis/src/__tests__/classifier.test.ts:22`); the legacy XML tool-parsing loop that emitted it lived at repo-root `src/core/task/` and no longer exists. Main routes tool calls through AI SDK native tool calling with `repairMalformedToolCall` (`sdk/packages/llms/src/providers/ai-sdk.ts:423`) and schema-aware normalization (`sdk/packages/agents/src/agent-runtime.ts:1366`).",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Same class as #10513/#10523; the specific 3.81\u21923.82 regression is in retired code.",
  "cluster": ""
 },
 {
  "number": 10536,
  "title": "Unable to fetch models from SAP AI Core service instance",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The message still exists on main at `apps/vscode/webview-ui/src/components/settings/providers/SapAiCoreProvider.tsx:235`, but the provider behind it was rebuilt after the report: `33e521e55` (2026-05-27, SAP AI Core moved to the AI SDK community provider), `8da5ffa87`/`b56ce72fc`/`9a1e6121c` (2026-06-23 wiring + config alignment), `352a23a6d` (2026-06-25, \"stabilize SAP AI Core provider setup\"), `5a0780a6f` (2026-07-23, metering header + fetch adapter). The report has empty repro steps, no service-key details and no logs, so it cannot be mapped onto the current handler.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Ask the reporter to retest on the 4.0 SDK extension and attach the deployment/resource-group config and output channel logs.",
  "cluster": "sap-ai-core"
 },
 {
  "number": 10537,
  "title": "Cline v3.82.0 hangs indefinitely at \"Thinking...\" after executing terminal commands",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The workaround the thread converged on is now the default: `apps/vscode/src/shared/storage/state-keys.ts:78-83` sets `vscodeTerminalExecutionMode` default to `\"backgroundExec\"` (\"Default to background execution to match the CLI's behavior\"), so commands no longer depend on VS Code shell integration completion events. The shell-integration path itself was rewritten in `12703bf40` (2026-07-15) with a chunk-safe OSC 633 parser, idle read timeouts, and a fast-fail when `terminal.exitStatus` is already set (`apps/vscode/src/hosts/vscode/terminal/VscodeTerminalProcess.ts:69-76`).",
  "verdict": "duplicate",
  "duplicate_of": 12079,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "A commenter flagged this as a duplicate of the still-open umbrella issue #9143 (\"The command's output could not be captured\"). Root cause is addressed on main, but a maintainer should decide how to consolidate/close that whole cluster.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 10544,
  "title": "Moonshot Kimi K2.6 fails via OpenAI Compatible because Cline sends unsupported temperature",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "On main nothing hardcodes a sampling temperature into a request: `rg 'temperature: [0-9]'` across sdk/ and apps/ matches only `apps/vscode/src/shared/api.ts:184` (`openAiModelInfoSafeDefaults`, a webview display fallback consumed by `useProviderModelSelection`/`OpenAICompatible.tsx`, not the request path). The request path is `sdk/packages/llms/src/providers/ai-sdk.ts:64` (`temperature: request.temperature`) fed from `gateway.ts:103` and `sdk/packages/core/src/services/llms/handler-factory.ts:199,255`, all of which stay `undefined` unless configured. If a value is needed, the OpenAI-Compatible settings now expose a persisted per-model temperature override (`apps/vscode/webview-ui/src/components/settings/providers/OpenAICompatible.tsx:585`, `apps/vscode/src/shared/proto-conversions/models/modelOverrides.ts:35`).",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "The reporter's alternate ask (per-model capability metadata like `supportsCustomTemperature`) is partly there \u2014 `catalog-live.ts:197` records a `temperature` capability \u2014 but `compat.ts:75` maps it to plain `text`, so it is not currently used to gate anything.",
  "cluster": ""
 },
 {
  "number": 10548,
  "title": "Mixed languages",
  "area": "unknown",
  "kind": "question-support",
  "relevant_to_main": "no",
  "evidence": "No provider or model is given, and the pasted transcript ends with the retired mistake-limit blurb (\"...recommended to use Claude 4.5 Sonnet for its advanced agentic coding capabilities\"), which no longer exists on main \u2014 the message is now provider-neutral at `apps/vscode/src/sdk/sdk-interaction-coordinator.ts:76` (\"Cline ran into N errors in a row and stopped the task\") after `e4091c3686`. The garbled Chinese/Korean/pseudo-XML output is a local-model quality symptom, not a Cline code path.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter answered \"don't know\" for repro steps and left provider/model blank.",
  "cluster": ""
 },
 {
  "number": 10549,
  "title": "'run_commands' tool silently times out at 30s with misleading error \u2014 causes agent confusion and wasted tokens on long-running tasks",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Still exactly as described on main. `sdk/packages/core/src/extensions/tools/definitions.ts:460` uses `config.bashTimeoutMs ?? 30000`, and `executeShellCommands` (same file, ~lines 190-225) converts a `TimeoutError` into `{ result: \"\", error: \"Command failed: Command timed out after 30000ms\", success: false }` \u2014 no partial stdout/stderr, no signal that the wait (not the process) ended, and the process tree is SIGKILLed by `killProcessTree()` in `sdk/packages/core/src/extensions/tools/executors/bash.ts:159-186`. `bashTimeoutMs` is only ever set by the VS Code host (`apps/vscode/src/sdk/vscode-runtime-builder.ts:163`); the CLI exposes no way to raise it.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs a product decision on the tool's error contract: distinguish \"tool stopped waiting\" from \"command failed\", return partial output on timeout, and expose a per-call or per-config timeout. Telemetry for this was already added (`captureRunCommandsTimeout`, definitions.ts:88) but the user-visible behavior was not changed.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 10551,
  "title": "OpenAI-compatible DeepSeek V4 Pro: configured 1M context capped at 128K, tool parsing/mistake-limit issues",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Complaint 1 is visible in code: `resolveLegacyCustomProviderRegistration` in `sdk/packages/core/src/services/storage/provider-settings-legacy-migration.ts:657-665` writes `contextWindow`/`maxInputTokens = LEGACY_OPENAI_COMPATIBLE_CONTEXT_WINDOW (128_000)` for every migrated OpenAI-Compatible model, ignoring the window the user had configured in 3.x, and `sdk/packages/llms/src/providers/builtins.ts:416` applies the same 128k fallback to openai-compatible models with no catalog entry. Complaints 2 and 3 are addressed: jsonrepair tool-call repair landed in `a1d69fee6` (`ai-sdk.ts:423,1218`) and the mistake-limit text is now provider-neutral (`apps/vscode/src/sdk/sdk-interaction-coordinator.ts:76`, `e4091c3686`).",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Both PRs referenced in the thread (#10616, #11317) are still open and unmerged. The migration should read the legacy `openAiModelInfo.contextWindow` instead of hardcoding 128k; worth also confirming a freshly configured 1M window in the 4.0 OpenAI-Compatible settings survives to the request.",
  "cluster": "openai-compat-context-window-ignored"
 },
 {
  "number": 10566,
  "title": "Invalid API reponse in plan mode with qwen/qwen3.5-flash model",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The exact message (\"Invalid API Response: The provider returned an empty or unparsable response\u2026\") does not exist anywhere on main; the nearest equivalents are `\"Model returned empty response\"` thrown when `message.content.length === 0` in `sdk/packages/agents/src/agent-runtime.ts:646-652`, and `EMPTY_CONTENT_TEXT` in `sdk/packages/shared/src/llms/ai-sdk-format.ts:75`. So the wording is legacy 3.x, but the underlying failure mode (Cline-gateway model returning no parsable content) is still representable on main.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "The reporter supplied a gateway Request ID (LqtjaPhnHWNVabXjHIluSFhelMZGxSsF) and confirmed it was still happening in late June \u2014 a maintainer with gateway log access can settle in minutes whether qwen3.5-flash is returning empty completions provider-side.",
  "cluster": "invalid-api-response-empty"
 },
 {
  "number": 10569,
  "title": "cline auth does not work on headless server",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`sdk/packages/core/src/auth/cline.ts:522` now defaults to `useWorkOSDeviceAuth ?? true`, so `cline auth` runs the WorkOS device-authorization flow: it emits a verification URL plus `Enter this code in your browser: <userCode>` (line 552) and polls for tokens, with no local redirect server started at all. `createOAuthClientCallbacks` in `sdk/packages/core/src/auth/client.ts:33-45` always writes the URL to output and fires `openUrl` non-blockingly, and `apps/cli/src/commands/auth.ts:231-236` prints \"Could not open browser automatically; open the URL above manually\" on opener failure. Landed via `0c027d273` (2026-06-09).",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "The old behavior (block on xdg-open, never print the URL) is gone; safe to close asking the reporter to retest on the current CLI.",
  "cluster": ""
 },
 {
  "number": 10573,
  "title": "Error using Claude Opus 4.7 on Amazon Bedrock: `temperature` is deprecated",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The legacy handler is the culprit: `origin/legacy-extension:apps/vscode/src/core/api/providers/bedrock.ts` hardcodes `temperature: 0` at lines 407, 898, 1221 and 1359. On main `sdk/packages/llms/src/providers/vendors/bedrock.ts` contains no temperature handling at all, and the shared `buildAiSdkStreamConfig` (`sdk/packages/llms/src/providers/ai-sdk.ts:64`) forwards `request.temperature`, which is `undefined` unless the user explicitly configures one (`rg 'temperature: [0-9]'` finds no hardcoded value on the request path).",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Second reporter hit the same thing on Opus 4.8. Only residual risk on main is a user manually setting a temperature override for an adaptive-thinking Anthropic model.",
  "cluster": "bedrock-temperature-deprecated"
 },
 {
  "number": 10576,
  "title": "Cline SDK migration: Previous task restarts automatically when clicking \u201cNew Task\u201d after completion",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Filed against an early SDK-migration beta build (3.82.1777984622, 2026-05-06) of what is now `apps/vscode`. The relevant path is `SdkController.clearTask()` (`apps/vscode/src/sdk/SdkController.ts:1247-1253`), which now sets `turnStateTracker.set(\"idle\")` before `taskControl.clearTask()` and re-posts state; `showTaskWithId` carries an explicit comment about not calling `clearTask()` to avoid a session-teardown race (lines 1521-1535). `git log --since=2026-05-06 -- apps/vscode/src/sdk/SdkController.ts` shows ~30 SDK-migration fixes since the report, none named for this symptom.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "QA-labeled regression against the extension that is about to ship as 4.0, with a video attached. Cannot be resolved from code alone \u2014 a maintainer should re-run the QA steps against current main before release.",
  "cluster": ""
 },
 {
  "number": 10582,
  "title": "Bob generates incorrect comment format in go.mod files.",
  "area": "unknown",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "\"Bob\" is not a Cline product: `rg -w Bob` over /workspace matches only Discord connector test fixtures (`apps/cli/src/connectors/adapters/discord.test.ts`, `connector-host.test.ts`), and the literal `Made with Bob` appears nowhere. No Cline tool or prompt appends attribution comments to generated files.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed against the wrong repo; safely auto-closeable.",
  "cluster": ""
 },
 {
  "number": 10585,
  "title": "MCP Marketplace: Add MisarMail MCP Server (email marketing, 32 tools)",
  "area": "unknown",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "The marketplace catalog this submission targets is not in this repo: `apps/vscode/src/core/controller/marketplace/marketplace-helpers.ts` fetches it from the hardcoded `MARKETPLACE_CATALOG_URL = \"https://cline.github.io/marketplace/catalog.json\"`, and `sdk/packages/core/src/services/marketplace.ts` only installs entries handed to it. No catalog data file exists anywhere under /workspace.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Belongs in the cline/marketplace repo (catalog.json). Nothing to change in cline/cline. Vendor self-submission; unverified third-party server.",
  "cluster": ""
 },
 {
  "number": 10587,
  "title": "Cline tool call error in plan mode",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Reported against CLI beta 0.0.11 (`apps/cli/package.json` is now 3.0.46). The described symptom \u2014 every tool call from a weak model failing in a fresh session \u2014 matches commit a1d69fee6 \"fix(llms): stop AI SDK from rejecting malformed tool calls before flexible tool executors can handle them (#12061)\", which removed the strict `validate` callback and added `experimental_repairToolCall` in `sdk/packages/llms/src/providers/ai-sdk.ts` (see the comment at `toAiSdkTools` and `experimental_repairToolCall: repairMalformedToolCall` in the `streamText` call).",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Fixed by a1d69fee6 (#12061, 2026-07-03). Reporter's evidence is only a screenshot; ask them to retest on CLI 3.0.x if it recurs.",
  "cluster": ""
 },
 {
  "number": 10588,
  "title": "Terminal interaction freezes on second interaction \u2014 Cline doesn't read terminal output after command completes",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The legacy orchestrator the report describes was deleted: commit 12703bf40 \"Improve VS Code terminal reliability: OSC 633 parser, exit codes, timeout handling (#11972)\" removed `apps/vscode/src/integrations/terminal/CommandOrchestrator.ts` (685 lines) and rewrote `apps/vscode/src/hosts/vscode/terminal/VscodeTerminalProcess.ts`, which now races each `iterator.next()` against an idle timeout, `onDidCloseTerminal`, and `onDidEndTerminalShellExecution` (with `EXIT_CODE_EVENT_TIMEOUT_MS`) so the read loop can no longer park forever. Follow-ups 8b4d1973b (#12352, reclaim unobserved fallback terminals) and a41129a5d (#12320) harden the same path.",
  "verdict": "duplicate",
  "duplicate_of": 12079,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Owned by apps/vscode terminal host. Worth a confirmation retest on the 4.x extension before closing, but the exact hang mechanism no longer exists in the code.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 10590,
  "title": "replace_in_file \u304c\u53cd\u6620\u3055\u308c\u305a\u3001\u66f8\u304d\u8fbc\u307e\u308c\u306a\u304b\u3063\u305f\u3067\u3059\u3002",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`replace_in_file` is no longer a real tool on `main`: `sdk/packages/core/src/runtime/orchestration/runtime-builder.ts` maps it as a legacy alias (`replace_in_file: \"editor\"`) and `apps/vscode/src/sdk/message-translator.ts` only uses the name to render historical tasks. The success string the reporter saw, `fileEditWithoutUserChanges` (\"The content was successfully saved to ...\") in `apps/vscode/src/core/prompts/responses.ts`, has no remaining call sites \u2014 the SEARCH/REPLACE diff applier that could silently no-op was removed with the SDK migration.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed against 3.82.0. The new `editor` tool applies structured edits through `apps/vscode/src/sdk/sdk-diff-edit-coordinator.ts`; the old SEARCH/REPLACE-block applier is gone.",
  "cluster": "legacy-replace-in-file"
 },
 {
  "number": 10591,
  "title": "Cline tool call error when using GPT5.5 - Cline stops in middle and advising me to use Sonnet 4.5",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The specific message the title describes (the 3.x mistake-limit copy suggesting Claude Sonnet) does not exist on `main` \u2014 grepping the tree finds no such string. The limit now produces `buildMistakeLimitStopMessage` in `sdk/packages/core/src/runtime/safety/mistake-tracker.ts` (\"Stopped after N/M consecutive mistakes...\"), and commit e4091c368 (#12561) just changed the extension to stop cleanly at the limit instead of blocking on an ask.",
  "verdict": "duplicate",
  "duplicate_of": 10500,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Report has an empty 'Steps to reproduce' and only a screenshot. Underlying weak-tool-call cause is the same one fixed by a1d69fee6 (#12061).",
  "cluster": "weak-model-mistake-limit"
 },
 {
  "number": 10595,
  "title": "Cline SDK migration: Task size value not displays in delete task tooltip",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Commit ca128cfd9 \"Fix SDK task size in delete tooltip (#11277)\" landed on `main`. `apps/vscode/src/sdk/sdk-task-history.ts` now backfills the size by measuring the session artifact directory (`getCachedTaskSize` / `getFolderSize.loose` around lines 546/640) and caches it in `metadata.size`, with coverage in `sdk-task-history.test.ts` (\"backfills SDK task size from the session artifact directory\"). The `--` placeholder in `formatDeleteTaskSizeLabel.ts` is now only the genuinely-unknown case.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "QA-filed cosmetic issue; fixed by ca128cfd9 (#11277).",
  "cluster": ""
 },
 {
  "number": 10596,
  "title": "Cline's OpenRouter integration lacks provider pinning, inflating costs by 3-5x",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Partially addressed: `sdk/packages/llms/src/providers/builtins.ts` now attaches `OPENROUTER_STICKY_SESSION_METADATA` (`transport: \"json-body\", field: \"session_id\"`), injected per request by `resolveStickySession`/`injectJsonBodyStickySession` in `sdk/packages/llms/src/providers/ai-sdk.ts` (commit a3b329546, \"fix(llms): includes session id for OpenRouter caching (#11744)\"). The reporter's explicit asks are still unimplemented \u2014 grepping the whole tree finds no `provider.order`, no `allow_fallbacks`, and no extra-request-body field; `openRouterProviderSorting` in `sdk/packages/llms/src/providers/config.ts` remains the only routing control.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Owned by @cline/llms. Sticky sessions (a3b329546) cover the cache-locality cost argument; the allowlist / preset-passthrough / extra-body asks are open. Note one comment on the issue is affiliate spam for 'tokenbus.org'.",
  "cluster": ""
 },
 {
  "number": 10600,
  "title": "Issue with Claude Code Provider",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Every log line in the report (`Unrecognized Claude Code chunk type: result`, `Error during Claude Code execution`, `Claude Code rate limit event`) is absent from the tree, and `apps/vscode/src/integrations/claude-code/` \u2014 the file PR #11169 patches \u2014 was deleted by 94f5a47a5/1f31738b3 (2026-06-24). Claude Code now goes through the upstream `ai-sdk-provider-claude-code` package via `createClaudeCodeProviderModule` in `sdk/packages/llms/src/providers/vendors/community.ts`; the hand-rolled CLI spawner and chunk parser that produced 'Command failed with exit code 1' no longer exist.",
  "verdict": "duplicate",
  "duplicate_of": 10135,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "PR #11169 is still OPEN but edits a deleted path, so it can't merge as-is \u2014 a maintainer should close/redirect it. Also contains an obvious spam comment ('TokenMK') worth hiding.",
  "cluster": "claude-code-exit-code-1"
 },
 {
  "number": 10605,
  "title": "Cline CLI \u2013 Unintentional ACT MODE (Tab) Activation Despite Explicit PLAN MODE Instructions",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The auto-switch half is directly guarded on `main`: `createInteractiveModeSwitchTool` in `apps/cli/src/runtime/interactive/mode.ts` tells the model to 'never call this proactively, and never treat the original task request as approval', and `sendTurnWithActModeContinuation` only auto-continues when `source === \"tool\"`, so a UI toggle can never start executing a plan. The 'plan mode runs commands' half is still true and self-contradictory in code: `ToolPresets.plan` in `sdk/packages/core/src/extensions/tools/presets.ts` is documented 'read-only, no shell access' yet sets `enableBash: true` (only `enableEditor` is false).",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. Run the current CLI from source: `bun run cli -i` in a scratch repo, with any working provider credential.\n2. Add a workspace `.clinerules` containing 'NEVER modify files in Plan mode / ONLY use read operations in Plan mode / ALWAYS ask before any write operation'.\n3. Press Tab until the status bar shows PLAN.\n4. Prompt something that invites a shell command, e.g. 'figure out why the test suite fails'.\n5. Expected per the plan-mode contract and the `.clinerules` file: no `run_commands` execution without an explicit approval prompt. Actual (predicted from `ToolPresets.plan.enableBash: true`): the model calls `run_commands` in plan mode.\n6. Separately, interrupt a plan-mode turn (Ctrl-C) and check whether the mode indicator is still PLAN after the session restarts.",
  "human_attention": "yes",
  "notes": "Two separate questions for a maintainer: (a) is `enableBash: true` in the plan preset intentional (the docstring says otherwise), and (b) does mode survive a task interrupt/session rebuild. `.clinerules` is loaded on main via `sdk/packages/core/src/extensions/config/user-instruction-config-loader.ts`, so it is prompt guidance only \u2014 it cannot hard-gate tools.",
  "cluster": ""
 },
 {
  "number": 10606,
  "title": "[Bug] \"API keys are not supported by this API\" when using Vertex AI in Kanban Agent Mode (Antigravity IDE / GCP VM)",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Both halves are fixed on `main`. `createVertexProviderModule` in `sdk/packages/llms/src/providers/vendors/vertex.ts` passes `googleAuthOptions: { projectId }` (ADC/OAuth) whenever a project is configured and only falls back to `apiKey` when it is not, so an API key is no longer forced. The settings-persistence half was fixed by 84fe95de4 \"fix(vscode): persist Vertex provider settings (#11565)\" (VertexProvider.tsx + useProviderConfig.ts), with 81a69e9ad (#12143) following up. The error string 'Cline SDK send failed' from the report does not exist anywhere in the tree.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed against 3.79.0. Vertex ADC now depends on the project id persisting, which 84fe95de4 fixed.",
  "cluster": ""
 },
 {
  "number": 10607,
  "title": "Inconsistent web search-related error",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Unresolvable from the report: the only error text is inside a screenshot. There is no `web_search` tool on `main` at all \u2014 `sdk/packages/core/src/extensions/tools/definitions.ts` exposes only `fetch_web_content` (`createWebFetchTool`, `enableWebFetch`), backed by `executors/web-fetch.ts`. `gh issue view 10607 --comments` adds nothing but the Linear linkback and the stale bot.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "CLI beta 0.0.13 vs 3.0.46 today. Reporter says it did not recur in a fresh session; may be the same weak-model tool-call rejection fixed by a1d69fee6 (#12061). Ask for the error text, or close as stale.",
  "cluster": ""
 },
 {
  "number": 10608,
  "title": "Cline SDK migration: Unauthorized error shown after login when executing tasks in Cline in staging environment",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The exact message is still a live code path \u2014 `sdk/packages/core/src/runtime/orchestration/session-runtime-orchestrator.test.ts` uses it as the auth-failure fixture \u2014 but the handling changed: `executeRunWithAuthRetry` (session-runtime-orchestrator.ts ~line 713) now detects it via `isLikelyAuthError` (`sdk/packages/shared/src/types/auth.ts`), calls `config.onAuthError()` and retries once. That landed as d859a86a6 \"fix(sdk): retry runs once after refreshing expired OAuth credentials (#12369)\", alongside a3989acc3 \"don't log users out when token refresh fails due to network/server errors (#12255)\".",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "QA ticket against a staging build from 2026-05-08; the freshly-signed-up-token race is partly server-side. Needs a retest on current main rather than new investigation.",
  "cluster": ""
 },
 {
  "number": 10614,
  "title": "Inconsistent cline behavior related to auth",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "\"Randomly losing auth\" is exactly what d859a86a6 \"fix(sdk): retry runs once after refreshing expired OAuth credentials (#12369)\" and a3989acc3 \"fix(sdk): don't log users out when token refresh fails due to network/server errors (#12255)\" address. On `main`, `executeRunWithAuthRetry` in `sdk/packages/core/src/runtime/orchestration/session-runtime-orchestrator.ts` refreshes credentials and retries transparently, and `sdk/packages/core/src/auth/cline.ts` only treats 400/401/403 as fatal (line 136) rather than any refresh failure.",
  "verdict": "duplicate",
  "duplicate_of": 12094,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "CLI beta 0.0.13. Report is a screenshot plus 'maybe the auth server is driven by a LLM' \u2014 no actionable detail beyond what the two commits already fixed.",
  "cluster": "mid-session-unauthorized"
 },
 {
  "number": 10618,
  "title": "Feature request: macOS MCP integration \u2014 71 native app tools via mcp-x-mac-seed",
  "area": "unknown",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "Nothing to build in this repo: Cline already consumes arbitrary MCP servers (`sdk/packages/core/src/extensions/mcp`, `sdk/packages/core/src/services/mcp-install.ts`, `apps/vscode/src/services/mcp/McpHub.ts`), and listing a server in the marketplace means editing the external catalog at `https://cline.github.io/marketplace/catalog.json` referenced by `apps/vscode/src/core/controller/marketplace/marketplace-helpers.ts`.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Author self-promotion of their own MCP server; users can already add it via MCP settings today.",
  "cluster": ""
 },
 {
  "number": 10622,
  "title": "TUI rendering issues",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The renderer underneath the reported tearing has been replaced: commit 22a1fa2c8 \"feat(cli): upgrade opentui 0.1.102 -> 0.4.3 (#12453)\" plus 98024c424 (#12516, dialog remove() contract) and fb15324ad (#12229, use-after-free on terminal title teardown) all changed `apps/cli/src/tui`. 69 commits touch `apps/cli/src/tui` since the issue was filed against CLI 0.0.13 (now 3.0.46), so the 0.1.x OpenTUI diffing that produced overlapping lines is no longer the code in play.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter's repro (plan mode + option-select dialog under tmux/kitty) is concrete enough to retest on 3.0.x; reopen with fresh screenshots if it still tears.",
  "cluster": "tui-render-tearing"
 },
 {
  "number": 10623,
  "title": "cline stop in the middle of editing a file...and save button grey out",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The editable streaming diff view with a Save button is gone. On `main`, `apps/vscode/src/hosts/vscode/VscodeEditPreview.ts` serves a *read-only* virtual document (`EDIT_PREVIEW_URI_SCHEME = \"cline-edit-preview\"`) in a `vscode.diff` tab, so there is no user-facing save step to grey out. The related stall was also fixed by bcf8c1d03 \"fix(vscode): relative read_files paths and diff-preview stalls no longer fail read/edit tools (#12558)\", which bounds the preview open with a 5s race so a hung `vscode.diff` can no longer burn the editor tool's 30s timeout or block the approval ask.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed against 3.82 with a screenshot and no repro steps. Legacy DiffViewProvider path; the equivalent stall on the new extension is fixed by bcf8c1d03.",
  "cluster": "diff-approve-buttons-stuck"
 },
 {
  "number": 10626,
  "title": "The VS Code Cline extension occasionally encounters a gray screen in the chat interface.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The failure mode is still structurally possible on `main`: `apps/vscode/webview-ui/src/main.tsx` renders `<App />` with no error boundary, and `apps/vscode/webview-ui/src/App.tsx` has none either \u2014 the only boundary in the tree is `ChatErrorBoundary.tsx`, used solely by three MCP chat-display components (`ImagePreview`, `LinkPreview`, `McpResponseDisplay`). Any render throw from persisted message data therefore unmounts the whole React tree, which matches the commenter's key observation that reopening that one conversation reproduces the blank panel every time while other conversations render fine.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Build and run the SDK extension: from `apps/vscode`, `bun run build:webview` then `bun esbuild.mjs`, and launch `DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <folder>`.\n2. Open the Cline sidebar and run a long task until the panel goes blank (per the reporters, this happens on task completion after a long conversation).\n3. Reload the window and reopen a *different* conversation \u2014 it renders normally.\n4. Reopen the conversation that went blank. Expected: it renders. Actual: the panel is immediately blank again.\n5. With the webview devtools open (Developer: Open Webview Developer Tools), capture the React error and the offending `ClineMessage` from the persisted session, then add a root-level error boundary in `apps/vscode/webview-ui/src/main.tsx` so the panel degrades to an error card instead of a blank webview.",
  "human_attention": "yes",
  "notes": "Two independent confirmations, one on v3.89.2 and one saying it is now frequent. Two things to do: find the message shape that throws during render, and add a root error boundary so a single bad row can never blank the whole webview.",
  "cluster": "grey-blank-webview"
 },
 {
  "number": 10631,
  "title": "Streaming response hangs indefinitely on TCP half-open connections",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The named symbol is legacy (no `StreamChunkCoordinator` anywhere on `main`), but the defect carried over verbatim. `sdk/packages/llms/src/providers/ai-sdk.ts` calls `streamText({ ..., abortSignal: request.signal })` with no inter-chunk deadline and then consumes it with a bare `for await (const part of stream.fullStream)` (line ~924) / `for await (const text of stream.textStream)` (line ~1038). The only timeout in the package is `fetchJson`'s 30s cap in `sdk/packages/llms/src/providers/http.ts`, which is used for non-streaming JSON calls only. A half-open socket therefore parks the loop forever, with no stall watchdog and no path into the retry logic.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Owned by @cline/llms. Fix shape: race each `fullStream` pull against an idle timer (the VS Code terminal host already does exactly this in `apps/vscode/src/hosts/vscode/terminal/VscodeTerminalProcess.ts` after #11972) and surface a retryable error on stall. High-quality report \u2014 the reporter read the code.",
  "cluster": "stream-never-starts"
 },
 {
  "number": 10636,
  "title": "Settings panel does not work without an account",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`apps/vscode/webview-ui/src/App.tsx` still short-circuits to `<OnboardingView />` while `showWelcome` is true, so the gear is inert during onboarding \u2014 but onboarding no longer requires an account. `apps/vscode/webview-ui/src/components/onboarding/data-steps.ts` offers a 'Bring my own API key' option (`NEW_USER_TYPE.BYOK`), whose step renders the `byok_provider_config` page in `OnboardingView.tsx` using the full `ApiConfigurationSection` and finishes with a plain 'Continue' (`action: \"done\"`) \u2014 no signup. An Ollama-only user can configure `OllamaProvider.tsx` right there.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed against 3.82.0. The new onboarding covers the reporter's exact case (local Ollama, no account).",
  "cluster": ""
 },
 {
  "number": 10637,
  "title": "Cline auto compacts context silently even with autocompacting setting disabled",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Both halves are gone. The file the commenters pinpointed, `src/core/context/context-management/context-window-utils.ts` with its `deepseek -> 128_000` override, does not exist on `main` and no deepseek-keyed context-window constant survives anywhere (grep for 128000/128_000 finds only a test fixture). Compaction now reads the model's own budget via `resolveEffectiveMaxInputTokens` in `sdk/packages/core/src/extensions/context/compaction-shared.ts`, and `createContextCompactionPrepareTurn` in `compaction.ts` returns `undefined` outright when `compaction.enabled !== true`; `apps/vscode/src/sdk/cline-session-factory.ts` only passes a `compaction` block at all when `useAutoCondense` is set.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Five users hit this and one shipped a proxy workaround, so it is worth a confirming retest and a reply on the thread. Residual risk: `DEFAULT_MAX_INPUT_TOKENS = 128_000` is still the fallback when a model reports neither `maxInputTokens` nor `contextWindow`, which an OpenAI-compatible user who leaves the context window blank could still hit.",
  "cluster": "compaction-runs-while-disabled"
 },
 {
  "number": 10644,
  "title": "Cline fails to connect to existing remote Chrome debug session and ignores custom browser arguments",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`splitArgs` in `apps/vscode/src/services/browser/BrowserSession.ts` (line 30) splits on `/\"[^\"]+\"|\\S+/g` and then only strips quotes anchored at both ends (`/^\"(.*)\"$/`), so `--user-data-dir=\"C:\\path\"` keeps its embedded quotes and a path containing a space is split in two \u2014 Chrome then creates a different profile directory, exactly the reported symptom. Separately, `relaunchChromeDebugMode` (line 104) hardcodes `--user-data-dir=${os.tmpdir()}/chrome-debug-profile` before appending `userArgs`, and `launchBrowser` (line 181) silently falls back to `launchLocalBrowser()` (bundled headless Chromium) whenever the remote attach in `launchRemoteBrowser` fails.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Owned by apps/vscode browser service. The quote/space handling in `splitArgs` is a small self-contained fix; the 'silently launches a fresh Chrome when remote attach fails' behavior in `launchBrowser` is a separate UX decision. One comment on the thread is a plug for an unrelated third-party product.",
  "cluster": ""
 },
 {
  "number": 10656,
  "title": "Latest Cline still hits OpenAI Native gpt-5.3-codex, gpt-5.4 and gpt-5.5 reasoning/function_call pairing error during MCP/tool use",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The hand-rolled Responses API payload builder that round-tripped `fc_`/`rs_` item ids is gone: `sdk/packages/llms/src/providers/vendors/openai.ts` is now a thin `createOpenAI(...).responses(modelId)` wrapper (SDK migration, 8d4597dc9), and `toAiSdkMessages` in `sdk/packages/llms/src/providers/ai-sdk.ts` (lines 272-383) preserves provider metadata only for `anthropic` (signature/redactedData) and `google` (thoughtSignature) \u2014 it never sets `providerOptions.openai.itemId`. In the installed `@ai-sdk/openai` 3.0.84 responses converter, a regular function tool without an itemId is emitted as `{type:\"function_call\", call_id, name, arguments}` with no `id`, and reasoning parts without an itemId are dropped, so OpenAI has no `fc_` id to demand a paired `rs_` item for.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Six reporters and still active as of 2026-07-06, where one claims 'cline version 4.0.1'. The code says the 400 cannot be produced by the new provider path, so a maintainer should confirm which build that user is actually on before closing.",
  "cluster": "codex-responses-item-pairing"
 },
 {
  "number": 10687,
  "title": "Getting an error with any Gemini model",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The manual Gemini payload builder that could emit an empty `function_response.name` no longer exists \u2014 `sdk/packages/llms/src/providers/vendors/google.ts` is a 23-line `createGoogleGenerativeAI` wrapper. Tool results now carry the name structurally: `agent-runtime.ts` line 1543 builds every `tool-result` part with `toolName: prepared.toolCall.toolName`, and `formatMessagesForAiSdk` in `sdk/packages/shared/src/llms/ai-sdk-format.ts` (line ~618) forwards that straight through, so the name cannot be blank unless the model emitted a nameless tool call, which the AI SDK rejects first.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Report claims 'Cline Version 3.0', which is not a real release; no maintainer ever responded. Ask for a retest on 4.x if it recurs.",
  "cluster": ""
 },
 {
  "number": 10697,
  "title": "base url was difficult to enter",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Still reproducible from the code. `BaseUrlField` in `apps/vscode/webview-ui/src/components/settings/common/BaseUrlField.tsx` drives `useDebouncedInput` (`apps/vscode/webview-ui/src/components/settings/utils/useDebouncedInput.ts`) with the default 100ms debounce; that hook also force-syncs `setLocalValue(initialValue)` whenever the incoming prop changes. So a save fired mid-typing echoes the partial URL back through `apiConfiguration`/`useProviderConfig` and clobbers the characters typed in the meantime. `OllamaProvider.tsx` makes it worse by re-polling models off `ollamaBaseUrl` plus `useInterval(requestOllamaModels, 2000)`.",
  "verdict": "duplicate",
  "duplicate_of": 10510,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Owned by apps/vscode webview settings. Fix shape: raise the debounce for URL fields and skip the external-sync effect while the field is focused. The reporter's secondary ask (base-URL history) is a separate feature request.",
  "cluster": "settings-input-eats-characters"
 },
 {
  "number": 10709,
  "title": "OpenAI-compatible DeepSeek V4 Pro/Flash stuck after command status becomes \"Skipped\"",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Same root cause as #10588 \u2014 the reporter's own diagnosis in the thread is 'the terminal finished the command, but cline fails to get the status and [is] waiting [for] the output of terminal'. That read loop was rewritten by 12703bf40 \"Improve VS Code terminal reliability: OSC 633 parser, exit codes, timeout handling (#11972)\", which deleted `apps/vscode/src/integrations/terminal/CommandOrchestrator.ts` and made `apps/vscode/src/hosts/vscode/terminal/VscodeTerminalProcess.ts` race every read against an idle timeout, terminal closure, and `onDidEndTerminalShellExecution`.",
  "verdict": "duplicate",
  "duplicate_of": 10588,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "The 'Skipped' label itself still exists (`apps/vscode/webview-ui/src/components/chat/CommandOutputRow.tsx` line 265); the stuck state behind it is what #11972 fixed.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 10715,
  "title": "MCP Marketplace: Add 14 MEOK AI Labs regulatory compliance & AI safety servers",
  "area": "unknown",
  "kind": "meta-task",
  "relevant_to_main": "no",
  "evidence": "The marketplace catalog is not data in this repo: `apps/vscode/src/core/controller/marketplace/marketplace-helpers.ts` and `apps/cline-hub/src/server/marketplace.ts` both fetch `https://cline.github.io/marketplace/catalog.json`, i.e. the separate `cline/marketplace` repo. `sdk/packages/core/src/services/marketplace.ts` only handles install/uninstall of entries, and `apps/vscode/webview-ui/src/components/mcp/configuration/McpConfigurationView.test.tsx` asserts the MCP Marketplace tab is never rendered in the new extension.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Bulk 14-server promotional submission; belongs in the cline/marketplace catalog repo, no code change in cline/cline. Stale since 2026-07-14.",
  "cluster": ""
 },
 {
  "number": 10737,
  "title": "task_progress parameter incorrectly passed to Jira MCP tool calls, causing Pydantic ValidationError",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The handler the reporter and the 2026-05-27 commenter blamed, `apps/vscode/src/core/task/tools/handlers/UseMcpToolHandler.ts`, no longer exists on `main` (deleted in the SDK migration, `git log --diff-filter=D` -> 94f5a47a5). MCP tools are now built in `sdk/packages/core/src/extensions/mcp/tools.ts`, which uses the MCP server's own `descriptor.inputSchema` and forwards only the validated tool input to `provider.callTool`. `task_progress` no longer exists as a tool parameter anywhere: the only hits in the repo are the `task_progress` ClineSay message type (`apps/vscode/src/shared/ExtensionMessage.ts`, `apps/vscode/src/shared/proto-conversions/cline-message.ts`).",
  "verdict": "duplicate",
  "duplicate_of": 10426,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Also resolves the PR-stack question in the comments (#9951/#9542/#8589 all target deleted `src/core/...` paths). Same class as #8256/#9684/#9919/#10426 \u2014 those can be closed together.",
  "cluster": "mcp-task-progress-leak"
 },
 {
  "number": 10741,
  "title": "How to disable auto-update in Cline CLI? Multiple issues with v2.18.0",
  "area": "cli",
  "kind": "question-support",
  "relevant_to_main": "no",
  "evidence": "All three sub-questions are answered by code on `main`: `apps/cli/src/commands/update.ts` `autoUpdateOnStartup()` honors `CLINE_NO_AUTO_UPDATE=1` and `isAutoUpdateEnabledGlobally()`; that flag is a persisted global setting (`autoUpdateEnabled` in `sdk/packages/core/src/services/global-settings.ts`) exposed as the 'Auto update' toggle in `apps/cli/src/tui/views/config-view.tsx` (calls `setAutoUpdateEnabledGlobally`); and the `openai-compatible` 'OpenAI Compatible' provider now exists in `sdk/packages/llms/src/providers/builtins.ts`.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter confirmed the env-var workaround in-thread; the persisted toggle is the durable answer. The `@cline/cli-windows-x64` install failure they hit later is a published platform package on main (`apps/cli/script/publish-npm.ts`).",
  "cluster": ""
 },
 {
  "number": 10743,
  "title": "Cline CLI 3.0.2 with GPT-5.5 emits invalid run_commands tool calls containing unsupported timeout field",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`RunCommandsInputUnionSchema` in `sdk/packages/core/src/extensions/tools/schemas.ts` is built from plain `z.object` on zod ^4.3.6 (`sdk/packages/core/package.json`), which strips unknown keys instead of erroring, and `validateWithZod` (`sdk/packages/shared/src/parse/zod.ts`) only throws on `safeParse` failure. I ran the exact reported payload `{commands:[...], timeout:120000}` through the same schema shape with the workspace zod: it parses successfully to `{commands:[...]}`, so the 'Unrecognized key: timeout' rejection and retry loop cannot occur.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Maintainer @dominiccooney already reached the same conclusion in-thread. Only the residual feature ask remains (honor/expose a configurable run_commands timeout); the telemetry piece landed as db9971890 (#11149).",
  "cluster": ""
 },
 {
  "number": 10755,
  "title": "MCP Marketplace: Add Misar.Blog MCP Server (AI blogging, 20 tools)",
  "area": "unknown",
  "kind": "meta-task",
  "relevant_to_main": "no",
  "evidence": "Same as #10715: the catalog is served from `https://cline.github.io/marketplace/catalog.json` (see `apps/vscode/src/core/controller/marketplace/marketplace-helpers.ts` and `apps/cline-hub/src/server/marketplace.ts`), not stored in cline/cline. No catalog file exists anywhere in this repo.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Route to the cline/marketplace catalog repo; nothing in this repo changes.",
  "cluster": ""
 },
 {
  "number": 10767,
  "title": "Cerebras Provider: \"effort: property 'effort' is unsupported reasoningSummary: property 'reasoningSummary' is unsupported...\" error is shown when trying to run prompt using zai-glm-4.7 and  gpt-oss-120b LLM",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`buildCompatibleEffortOptions` in `sdk/packages/llms/src/providers/routing/generic-compatible.ts` emits `{effort, reasoningEffort, reasoningSummary: 'auto'}` for every openai-compatible provider whose reasoning is enabled, and no rule in `sdk/packages/llms/src/providers/routing/provider-option-rules.ts` suppresses `genericEffort` for Cerebras (the only Cerebras special-case on main is `isCerebrasProvider` in `model-facts.ts`, used solely to drop reasoning *history* in `ai-sdk.ts`). I called `composeAiSdkProviderOptions` for `cerebras`/`zai-glm-4.7` and `cerebras`/`gpt-oss-120b` with `reasoning:{enabled:true,effort:'medium'}`: both return a `cerebras` bucket containing exactly `effort` and `reasoningSummary`, the two keys Cerebras rejects.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Fix belongs in @cline/llms: add a Cerebras provider-option rule that suppresses genericEffort (or drops reasoningSummary for non-Responses openai-compatible providers), per sdk/packages/llms/AGENTS.md routing guidance.",
  "cluster": ""
 },
 {
  "number": 10781,
  "title": "Cline CLI v3.0.2 litellm provider uses /responses endpoint instead of /chat/completions \u2014 incompatible with many LiteLLM-compatible proxies",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The `litellm` builtin manifest in `sdk/packages/llms/src/providers/builtins.ts` still declares `protocol: \"openai-responses\"`, and `inferClient` maps that to the `openai` client, so requests go to `/v1/responses`. `sdk/packages/llms/src/providers/ids.test.ts` locks this in ('routes Responses API built-ins through the OpenAI provider factory' expects `protocol: \"openai-responses\", client: \"openai\"` for litellm). There is no per-user protocol override and no 404 fallback to `/chat/completions` anywhere in `sdk/packages/llms/src/providers`.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs a product call: flip litellm's default protocol to openai-chat, or expose a per-provider protocol override. Affects every LiteLLM proxy that doesn't implement the Responses API.",
  "cluster": ""
 },
 {
  "number": 10783,
  "title": "Cline disregards required approval",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "On `main` approval is enforced at the tool layer: `AgentRuntime` in `sdk/packages/agents/src/agent-runtime.ts` calls `requestToolApproval` for every call whose policy has `autoApprove === false`, and `apps/vscode/src/sdk/sdk-tool-policies.ts` forces `autoApprove: false` for `run_commands`/`execute_command` unless `settings.actions.executeSafeCommands` is on \u2014 so a re-proposed command should re-prompt rather than run silently. However, neither file records a rejection: there is no rejection cache anywhere (`grep` for approvalCache/approvedCommands/rememberApproval returns nothing), so the model can re-plan the same action indefinitely, which is the deeper complaint in the thread.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Run the new extension from `apps/vscode` (`bun run build:webview && bun esbuild.mjs`, then `code --extensionDevelopmentPath=apps/vscode`).\n2. In the Auto-approve bar, disable 'Execute safe commands' so `run_commands` always prompts.\n3. Ask Cline to do something that needs a shell command (e.g. 'make a backup copy of this conversation to a file').\n4. When the run_commands approval appears, press Reject.\n5. Expected: the same command is not executed, and any retry re-prompts. Actual (reported on 3.83.0): the second attempt runs without a prompt. Record whether the second attempt prompts at all, and whether the rejection is visible in the transcript sent to the model.",
  "human_attention": "yes",
  "notes": "Two separable items: (a) verify no silent-execution path survives on the SDK extension, (b) design decision on persisting a rejection record (tool + canonical args digest) as suggested in the thread. Reporter references an existing PR with conflicts/failing tests.",
  "cluster": "approval-bypass"
 },
 {
  "number": 10797,
  "title": "Cline: LLM dropdown is not clearly visible for DeepSeek provider.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Filed by QA against legacy nightly 3.82. On `main` there is no DeepSeek-specific settings component at all \u2014 `apps/vscode/webview-ui/src/components/settings/providers/` has no DeepSeekProvider, and `providerSettingsRegistry.ts` does not list `deepseek` in `CUSTOM_PROVIDER_SETTINGS_IDS`, so DeepSeek renders through the rewritten `GenericProviderSettings.tsx`/`ModelPickerWithManualEntry.tsx`. The specific styling defect cannot be located in code and the linked evidence is two private Google Drive videos.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Cosmetic; QA should re-verify against the SDK extension's generic provider settings before this is kept open.",
  "cluster": ""
 },
 {
  "number": 10814,
  "title": "cline chat window turns gray in VS Code",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "No reproduction steps and no error text to anchor on; the report itself blames a concurrent VS Code update. I could not find any code path on `main` that greys the chat surface \u2014 nothing in `apps/vscode/webview-ui/src/components/chat/chat-view/` sets a disabled/grey overlay for an in-progress task, and the webview is a full rewrite under `apps/vscode/webview-ui`.",
  "verdict": "duplicate",
  "duplicate_of": 10626,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Needs webview devtools console output / VS Code version from the reporter; likely a webview renderer crash rather than Cline logic. Stale since 2026-07.",
  "cluster": "grey-blank-webview"
 },
 {
  "number": 10825,
  "title": "ty",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "Body is keyboard mash ('jhvguvhjkhjggjhgjghghjjkhgjkhjk') plus an auto-generated VS Code issue-reporter system dump. No symptom, no code reference to check.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Safe to auto-close.",
  "cluster": ""
 },
 {
  "number": 10843,
  "title": "[Bug]: Local Ollama models (Qwen 2.5 Coder) trapped in infinite loop emitting raw JSON instead of triggering tools due to strict XML parser constraint",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The XML tool parser the report is about does not exist on `main`: `parse-assistant-message` appears only as prose in `.clinerules/cline-overview.md`, and there is no XML tool extraction anywhere under `sdk/`. Tool calls now go through the provider's native tool API in `sdk/packages/llms/src/providers/ai-sdk.ts`, with `repairMalformedToolCall` covering weak-model output (`ai-sdk.tool-calls.test.ts`). Ollama specifically uses its native API again after 7f9d2e96d (#12286). The 'Cline uses complex prompts and iterative task execution...' coercion string the reporter saw exists nowhere on `main`.",
  "verdict": "duplicate",
  "duplicate_of": 11263,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Worth a human reply only because an external contributor announced a PR implementing a JSON fallback in `parse-assistant-message.ts` / `parse-json-tool-use.ts` \u2014 files that were deleted in the SDK migration. Redirect them before they open it.",
  "cluster": "ollama-json-tool-loop"
 },
 {
  "number": 10847,
  "title": "Claude Code - Asking API Key",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "On `main` the Claude Code provider settings render a 'Claude Code CLI Path' text field and no API key input: `apps/vscode/webview-ui/src/components/settings/providers/ClaudeCodeProvider.tsx` binds `apiConfiguration.claudeCodePath` with placeholder 'Default: claude', and `providerSettingsRegistry.ts` routes `claude-code` to that custom component. The manifest in `sdk/packages/llms/src/providers/builtins.ts` has `configFields: []` and no `apiKeyEnv`. The follow-up 'Native CLI binary for darwin-arm64 not found' error is also handled: `resolveClaudeExecutable()` in `sdk/packages/llms/src/providers/vendors/community.ts` resolves the bundled per-platform binary and falls back to `claude` on PATH (2ef81be70, #12379).",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Also see e4ab360c3 'fix claude-code setting loading/persistence'. The commenters' speculation that Claude Code support was removed for legal reasons is wrong.",
  "cluster": "claude-code-exit-code-1"
 },
 {
  "number": 10853,
  "title": "Cline hangs indefinitely",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The reported 'Pending' hang on a shell command is the VS Code terminal shell-integration hang tracked in #9143, whose workaround was enabling Background Exec Mode. On `main` that is now the default: `vscodeTerminalExecutionMode` defaults to `\"backgroundExec\"` in `apps/vscode/src/shared/storage/state-keys.ts`, set by 12703bf40 (#11972, 'Improve VS Code terminal reliability: OSC 633 parser, exit codes, timeout handling').",
  "verdict": "duplicate",
  "duplicate_of": 9143,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Close as duplicate of #9143 and point at the new backgroundExec default.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 10855,
  "title": "Unable to select latest Grok Model. Stuck in infinite loop",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Every artifact of the loop is legacy 3.x prompt machinery that no longer exists on `main`: the model's 'I need to include a todo list using the task_progress parameter' comes from the retired XML tool prompt (`task_progress` survives only as a ClineSay message type in `apps/vscode/src/shared/ExtensionMessage.ts`), and neither the 'Cline uses complex prompts and iterative task execution...' coercion notice nor the 'Invalid API Response: The provider returned an empty or unparsable response' string exists anywhere in the repo. Grok models are also present in the catalog on `main` (`grok-4-1-fast-reasoning` at `sdk/packages/llms/src/catalog/catalog.generated.ts:752`).",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Native tool calling via `sdk/packages/llms/src/providers/ai-sdk.ts` removes the XML-format dependency that caused the loop.",
  "cluster": "xml-tool-parser-rejects-valid-call"
 },
 {
  "number": 10856,
  "title": "Cline CLI 3.0.7 --json mode can't resume an existing session ID",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Root cause is still in `apps/cli/src/main.ts`: when `args.id` is set it unconditionally rewrites `args` to `{...args, interactive: true, prompt: undefined}` (~lines 778-791), and the very next guard rejects `args.outputMode === \"json\" && (args.interactive || !args.prompt)` (~line 856). I reproduced it on this checkout: `bun run cli --json --id 1779126186341_2vo9c \"Repeat the last answer\"` prints `{\"type\":\"error\",\"message\":\"JSON output mode requires a prompt argument or piped stdin (interactive mode is unsupported)\"}` and exits 1.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "`--id` forcing interactive mode blocks all non-interactive resume (--json and --acp scripting). Needs a decision on headless resume semantics before the guard is relaxed.",
  "cluster": ""
 },
 {
  "number": 10857,
  "title": "Cline CLI 3.0.7 leaks running processes after handling a session task",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "I could not reproduce accumulation on `main` (Linux, from source): two consecutive one-shot runs (`bun run cli --json \"How much is 2 + 2\"` then `bun run cli \"How much is 3 + 3\"`) left exactly one process, the same shared hub daemon PID both times (`sdk/packages/core/src/hub/daemon/entry.ts --cwd /workspace --host 127.0.0.1 --port 25466`). One persistent daemon is by design (the CLI auto-spawns `@cline/cline-hub` and `apps/cli/src/commands/hub.ts` exposes `stopHubServer`), so the bug as filed is the Windows-specific failure to reuse it.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. On Windows 11, install the published CLI (`npm i -g cline`) so `cline.exe` is the platform binary rather than a source run.\n2. `tasklist | findstr cline` to confirm no cline processes.\n3. From one directory run `cline --json \"How much is 2 + 2\"`, wait for it to exit, then `tasklist | findstr cline` (expect exactly one hub daemon).\n4. From the same directory run `cline \"How much is 2 + 2\"`, wait for exit, then `tasklist | findstr cline`.\n5. Expected: still one daemon (the first is reused, as observed on Linux). Actual (reported): a second `cline.exe ... entry.js --cwd <cwd>` appears and both persist. If it reproduces, inspect hub discovery/lockfile handling under `sdk/packages/core/src/hub/discovery` for Windows path or port-file mismatches.",
  "human_attention": "yes",
  "notes": "Likely a Windows-only hub discovery/reuse bug rather than a generic leak; needs a Windows runner to confirm.",
  "cluster": ""
 },
 {
  "number": 10858,
  "title": "Cline CLI 3.0.7 --help reports wrong default directories",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Still wrong on `main`. `apps/cli/src/commands/program.ts` documents `--config <path>` as '(default: ~/.cline/data/settings)' and `--data-dir <path>` as '(default: ~/.cline)', but `--config` feeds `setClineDir` (`sdk/packages/shared/src/storage/paths.ts`), whose default is `~/.cline`, while the data dir defaults to `~/.cline/data` (`resolveClineDataDir`, and `resolveGlobalSettingsPath` -> `~/.cline/data/settings/global-settings.json`). I confirmed the strings are still emitted: `bun run cli --help` prints both lines verbatim.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Two-line help-text swap in `apps/cli/src/commands/program.ts`; safely agent-fixable.",
  "cluster": ""
 },
 {
  "number": 10861,
  "title": "Cline CLI 3.0.7 config does not persist configuration changes",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Confirmed on `main`. In `apps/cli/src/tui/views/config-view.tsx` only the 'Auto update' row persists (`setAutoUpdateEnabledGlobally`); 'Mode' calls `props.onToggleMode()`, 'Auto-approve all' calls `props.onToggleAutoApprove()`, 'Verbose' mutates `config.verbose` in place, and 'Compaction' calls `onSetCompactionMode`, which in `apps/cli/src/runtime/run-interactive.ts` only does `applyCliCompactionMode(config, mode)` + session restart. `GlobalSettingsSchema` in `sdk/packages/core/src/services/global-settings.ts` has no mode/verbose/auto-approve fields at all.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Partially improved: `~/.cline/data/settings/global-settings.json` does now get created (it exists in this environment with autoUpdateEnabled/telemetryOptOut), so only the 'no file created' half of the report is stale. Needs a decision on which TUI settings should be global-persisted vs session-scoped, and the docs at docs/getting-started/config.mdx should match.",
  "cluster": "cli-settings-not-persisted"
 },
 {
  "number": 10877,
  "title": "When pressing compact btn the message box content gets deleted",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Fixed by a1709d37e (#11764, 'fix(vscode): make compact button run real SDK compaction'). That commit replaced `onSendMessage?.(\"/compact\", [], [])` \u2014 the send path that cleared the composer \u2014 with a direct `SlashServiceClient.condense(...)` call in `apps/vscode/webview-ui/src/components/chat/task-header/ContextWindow.tsx` `handleConfirm`, which no longer touches `setInputValue`.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Exactly the fix the reporter asked for ('should bypass the prompt box message send').",
  "cluster": "prompt-lost-on-mode-switch"
 },
 {
  "number": 10881,
  "title": "Multi-root workspace - Cline choose the wrong project",
  "area": "vscode-new",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "The code the reporter linked still carries the same unimplemented TODO on `main`: `apps/vscode/src/core/workspace/WorkspaceResolver.ts` around line 185 reads '// Multiple matches - need disambiguation / Phase 2: This will trigger UI picker / For now, use primary root if it's a candidate, otherwise first match', then silently resolves to `candidateRoots[0]`.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "The reporter is asking for a roadmap answer on the Phase-2 disambiguation picker; only a maintainer can give it. Silent wrong-root selection can cause edits in the wrong project.",
  "cluster": ""
 },
 {
  "number": 10882,
  "title": "Available models for AWS Bedrock in GovCloud are incorrect",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Partly addressed, core gap remains. The Bedrock catalog on `main` now contains the GovCloud models the reporter listed \u2014 loading `GENERATED_PROVIDER_MODELS.providers.bedrock` from `sdk/packages/llms/src/catalog/catalog.generated.ts` yields 109 models including `nvidia.nemotron-super-3-120b`, `nvidia.nemotron-nano-3-30b`, `nvidia.nemotron-nano-12b-v2`, `openai.gpt-oss-120b-1:0` \u2014 and `apps/vscode/webview-ui/src/components/settings/providers/BedrockProvider.tsx` does expose a base-inference-model dropdown (`bedrock-base-model-dropdown` writing `customModelBaseId`) alongside the custom ARN field. But nothing filters the list by region: the only `us-gov` reference in the repo is the region list in `apps/vscode/src/shared/providers/bedrock.json`, and `sdk/packages/llms/src/providers/vendors/bedrock.ts` uses `region` solely for the client/credentials.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "The real ask is region-aware Bedrock model listing (ideally via ListFoundationModels for the configured region) instead of one global catalog. Worth telling the reporter the named models and the base-model override now exist.",
  "cluster": "stale-model-catalog"
 },
 {
  "number": 10894,
  "title": "Bug: MCP Server Configuration Not Applied in Portable VS Code",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The premise is wrong: `cline.mcpServers` is not a VS Code setting Cline has ever contributed \u2014 it appears nowhere in the repo, including `apps/vscode/package.json` contributions. And the portable-mode path problem is moot on `main`: MCP settings moved out of VS Code globalStorage to `resolveMcpSettingsPath()` = `~/.cline/data/settings/cline_mcp_settings.json` (`sdk/packages/shared/src/storage/paths.ts`), with `apps/vscode/src/hosts/vscode/mcp-settings-legacy-migration.ts` migrating old globalStorage files into it.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "The reporter's own 'workaround' (edit cline_mcp_settings.json) is the supported path. Only residual value is a docs note that the JSON file is authoritative.",
  "cluster": ""
 },
 {
  "number": 10919,
  "title": "Documentation references missing features in the TUI",
  "area": "cli",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Both parity gaps are real on `main`. `planActSeparateModelsSetting` exists only under `apps/vscode` (state-keys, ApiConfigurationSection, getStateToPostToWebview, ...) with zero hits in `apps/cli` or `sdk/packages`, so the TUI cannot set different Plan/Act models. `deep-planning` likewise appears only in `apps/vscode/src/shared/slashCommands.ts` and `docs/`, never in `apps/cli`. The docs page the reporter cites (`docs/core-workflows/plan-and-act.mdx`) describes the VS Code-only behavior without qualification.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Maintainer @reneehuang1 is already weighing deprecating /deep-planning in-thread, so this needs a product call: either add TUI parity (plus a config-file knob as the reporter suggests) or scope the docs to the VS Code extension.",
  "cluster": ""
 },
 {
  "number": 10922,
  "title": "GCP Vertex AI, Missing Models",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "No longer true on `main`. Loading `GENERATED_PROVIDER_MODELS.providers.vertex` from `sdk/packages/llms/src/catalog/catalog.generated.ts` returns 28 models that include the non-Gemini/non-Claude MaaS entries the reporter wanted: `zai-org/glm-5-maas`, `zai-org/glm-4.7-maas`, `deepseek-ai/deepseek-v3.2-maas`, `moonshotai/kimi-k2-thinking-maas`, `qwen/qwen3-235b-a22b-instruct-2507-maas`, `openai/gpt-oss-120b-maas`, `openai/gpt-oss-20b-maas`, `meta/llama-3.3-70b-instruct-maas`, `meta/llama-4-maverick-17b-128e-instruct-maas`. The catalog is now auto-generated from models.dev (ae033761a, #12204).",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Matches the 2026-07-11 comment that a recent release added a workaround. Close with the current Vertex model list.",
  "cluster": "stale-model-catalog"
 },
 {
  "number": 10925,
  "title": "Building cline with vsce generate errors",
  "area": "infra",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The repo-root `webview-ui/` the reporter built no longer exists; the webview is `apps/vscode/webview-ui`, whose `package.json` devDependencies already contain `vitest`, `@vitejs/plugin-react-swc` and `@testing-library/jest-dom` (the exact types `tsconfig.app.json` lists), so the TS2688/TS2307 errors are just un-installed webview deps. `CONTRIBUTING.md` instructs `cd apps/vscode && bun run install:all` on a bun workspace, not root `npm install` + `npx @vscode/vsce package`.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "User error: wrong install command. The monorepo is bun-workspace based; `npm install` at root never populates apps/vscode/webview-ui/node_modules.",
  "cluster": ""
 },
 {
  "number": 10926,
  "title": "Bedrock client error: image bytes serialized as dict instead of bytes when replaying conversation history",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The quoted error is Python botocore (`valid types: <class 'bytes'>`), which cannot come from this TypeScript codebase; `acs-copilot` (the reporter's provider) matches nothing under `sdk/packages/llms/src/providers`. On main, images travel as base64 strings (`sdk/packages/llms/src/providers/compat.ts` builds `data:${mediaType};base64,${part.data}`, `middleware/split-tool-images.ts` likewise), and `browser_action` is not in `DefaultToolNames` (`sdk/packages/core/src/extensions/tools/constants.ts`).",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Defect belongs to the reporter's internal Python Bedrock proxy (`acs-copilot`), not Cline. `browser_action` also no longer exists on main.",
  "cluster": ""
 },
 {
  "number": 10927,
  "title": "Repeated \"Response interrupted by API Error\" / \"Command failed with exit code 1\" leaves files in partially-edited, broken state after replace_in_file",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Both facets target code that is gone. The `Command failed with exit code` string comes from the execa runner `apps/vscode/src/integrations/claude-code/run.ts`, which exists only on `origin/legacy-extension`; main has zero matches for that string and routes Claude Code through `ai-sdk-provider-claude-code` (`sdk/packages/llms/src/providers/vendors/community.ts`). `replace_in_file`/`write_to_file` are not in `DefaultToolNames` (`sdk/packages/core/src/extensions/tools/constants.ts`), the 'file was reverted back to its original state' promise survives only in the orphaned `apps/vscode/src/core/prompts/responses.ts` (no importers; the live text is in `apps/vscode/src/sdk/sdk-followup-coordinator.ts`), and edits now use a virtual-document diff preview that 'never opens or modifies the real file' with a single `fs.writeFile` (`apps/vscode/src/sdk/sdk-diff-edit-coordinator.ts`).",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Facet 1 is the #10135 claude-code cluster (see #10929). Facet 2 (partial edit on interruption) is structurally impossible on main because the preview is virtual and the write is atomic.",
  "cluster": ""
 },
 {
  "number": 10929,
  "title": "Command failed with exit code 1",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`grep -rn \"Command failed with exit code\"` returns nothing under `sdk/` or `apps/` on main; the message is execa's, emitted by `apps/vscode/src/integrations/claude-code/run.ts` which only exists on `origin/legacy-extension`. Main delegates the provider to the third-party `ai-sdk-provider-claude-code` package (`createClaudeCodeProviderModule` in `sdk/packages/llms/src/providers/vendors/community.ts`), so the legacy spawn/exit-code path is gone.",
  "verdict": "duplicate",
  "duplicate_of": 10135,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Community PR #11169 (still OPEN) proposes a fix, but its only changed file `apps/vscode/src/integrations/claude-code/run.ts` does not exist on main \u2014 a maintainer needs to decide whether to land it on `legacy-extension` or close it. Multiple users affected on the shipping marketplace build; workaround in-thread is pinning Claude Code CLI 2.1.85.",
  "cluster": "claude-code-exit-code-1"
 },
 {
  "number": 10930,
  "title": "CLI Bedrock profile auth fails with AWS credential_process",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`resolveCredentialProvider` in `sdk/packages/llms/src/providers/vendors/bedrock.ts` only forwards a profile when `config.options.profile` is set (`fromNodeProviderChain({ ignoreCache: true, ...(profile ? { profile } : {}), ... })`), otherwise the SDK chain falls back to `default`/env. `sdk/packages/core/src/services/providers/provider-config-fields.ts` declares `awsProfile` as `optional: true` for `bedrock`, so `authentication: \"profile\"` can be persisted with no profile name \u2014 exactly the providers.json the reporter pasted. Nothing has touched `bedrock.ts` since the report.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Add `[profile bedrock]` to `~/.aws/config` with `credential_process = /path/to/tool credential-process`, `region = us-east-1`, and no static keys.\n2. Unset all `AWS_*` env vars.\n3. Run the CLI from source against a clean config dir: `bun run cli --config /tmp/cline-bedrock`.\n4. Choose `Bring your own provider` -> `AWS Bedrock`, enter region `us-east-1`, leave the API key blank, and leave `AWS Profile Name (optional)` blank (the field is optional in `provider-config-fields.ts`).\n5. Select `global.anthropic.claude-opus-4-6-v1` and send any message.\n6. Expected: the AWS SDK chain resolves the `bedrock` profile through `credential_process`. Actual: `AWS credential provider failed: Could not load credentials from any providers`.\n7. Re-run step 4 filling in `bedrock` as the profile name to confirm the profile name is the discriminator, then repeat with `AWS_PROFILE=bedrock` set to confirm the env path works.",
  "human_attention": "yes",
  "notes": "Suspected root cause: the CLI persists `aws.authentication: \"profile\"` without `aws.profile`, so `fromNodeProviderChain()` never sees the profile that carries `credential_process`. Later comment (#10930, Sonic-Ninja) reports the same class of failure with SSO profiles plus stale `globalState.json`/`providers.json` disagreement.",
  "cluster": ""
 },
 {
  "number": 10931,
  "title": "Cline hangs indefinitely when interactive CLI commands (like default \"git diff\")",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`apps/vscode/src/shared/storage/state-keys.ts` now defaults `vscodeTerminalExecutionMode` to `backgroundExec` ('Default to background execution to match the CLI's behavior'), and that path runs the SDK executor `sdk/packages/core/src/extensions/tools/executors/bash.ts`, which spawns with `stdio: [\"pipe\",\"pipe\",\"pipe\"]` (no TTY, so git and friends auto-disable their pager) and arms a `setTimeout` that SIGKILLs the whole process group after `bashTimeoutMs` (30s default per `createShellTool` in `sdk/packages/core/src/extensions/tools/definitions.ts`). The VS Code terminal pane is not involved, so closing it cannot wedge the session.",
  "verdict": "already-fixed",
  "duplicate_of": 9143,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "The in-thread workaround ('enable Background Exec Mode', #9143) is now the shipped default. A residual hang is still theoretically possible for users who opt back into `vscodeTerminal` mode, which uses `executeForeground` with `VSCODE_FOREGROUND_RUN_COMMANDS_TIMEOUT_MS`.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 10938,
  "title": "docs: add Tuning Engines OpenAI-compatible setup",
  "area": "docs",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "`docs/provider-config/openai-compatible.mdx` exists on main and documents the generic Base URL / API key / Model ID flow that already covers this endpoint; a repo-wide case-insensitive search for 'tuning engines' returns zero matches, so the requested vendor example genuinely is not there.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Vendor-authored docs request (author `cerebrixos`). The existing generic page already unblocks the user; adding per-vendor pages is a docs-policy call, not a defect.",
  "cluster": ""
 },
 {
  "number": 10951,
  "title": "Cline hangs during memory bank / file update task \u2014 Cancel button and \"New Task\" have no effect, requires VS Code restart",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The legacy 3.84 abort logic the report targets was replaced: cancellation now goes through `SdkTaskControlCoordinator.cancelTask()` in `apps/vscode/src/sdk/sdk-task-control-coordinator.ts`, which synchronously raises a cancel fence (epoch bump) and then awaits `sdkHost.abort(sessionId)` before emitting a `resume_task` ask. I could not reproduce or disprove the hang from code alone because the report has no deterministic trigger.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Blocked on the reporter retesting against the SDK-based 4.x extension. One thread comment is unrelated product promotion.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 10952,
  "title": "Unable to use Cline's free models, \"Insufficient balance\"",
  "area": "unknown",
  "kind": "question-support",
  "relevant_to_main": "no",
  "evidence": "Nothing in this repo computes balances; the client only classifies the Cline API's 402 payload \u2014 `apps/cli/src/tui/cline-account.ts` matches `insufficient_credits` / 'insufficient balance' strings returned by the server (see the recorded payload `{\"code\":\"insufficient_credits\",\"current_balance\":-0.14,...}` in `apps/cli/src/tui/cline-account.test.ts`). The negative balance and the free-tier-to-credit fallback both live in the Cline backend.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Maintainer reneehuang1 already acknowledged in-thread that the free-daily-limit-to-credits fallback is a backend limitation being worked on.",
  "cluster": "insufficient-balance-free-models"
 },
 {
  "number": 10953,
  "title": "cline cli auth error with Header setup (and need to confirm extension)",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`getProviderConfigFields()` in `sdk/packages/core/src/services/providers/provider-config-fields.ts` returns only `apiKey` + `baseUrl` (plus `azureApiVersion`) for `openai-compatible`, and `ProviderConfigFieldKey` has no headers member at all, so the CLI configure dialog physically cannot collect custom headers. The runtime does support them (`headers: z.record(z.string(), z.string()).optional()` in `sdk/packages/core/src/services/llms/provider-settings.ts`, forwarded to `config.headers` and consumed in `sdk/packages/llms/src/providers/request-headers.ts`). The fix exists only on the unmerged branch `origin/saoudrizwan/cli-openai-compatible-options` (commit f5ef62f5e, 2026-06-09, `git merge-base --is-ancestor f5ef62f5e main` fails).",
  "verdict": "duplicate",
  "duplicate_of": 11018,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Owned by @cline/cli + @cline/core provider-config fields. saoudrizwan told the reporter on 2026-06-12 that a header input had shipped, but the reporter replied he still saw no header field, and the branch adding it is still not on main. Worth confirming whether that branch was abandoned.",
  "cluster": "openai-compat-custom-headers"
 },
 {
  "number": 10957,
  "title": "Cline errors out with EPERM: operation not permitted, open 'c:\\Users\\XXX\"",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Report is unusable as filed: empty repro steps, no provider/model, no full path. The blamed tool is gone \u2014 `write_to_file` is not in `DefaultToolNames` (`sdk/packages/core/src/extensions/tools/constants.ts`), edits go through `editor`, whose executor `sdk/packages/core/src/extensions/tools/executors/editor.ts` calls `fs.writeFile` with no EPERM/EISDIR guard, so a raw fs error would still bubble up but never with the quoted `Error executing write_to_file` prefix.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "The truncated path `c:\\Users\\XXX` with no filename suggests the model passed a directory as the target. If confirmed, the fix is a friendly directory/permission guard in the `editor` executor.",
  "cluster": ""
 },
 {
  "number": 10964,
  "title": "Opus 4.7 not working in middle of session",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "All three failure strings still exist verbatim on main: `Tool call ... emitted invalid JSON arguments` and `Tool call arguments could not be parsed as JSON...` in `sdk/packages/agents/src/agent-runtime.ts`, and `Tool call ... was rejected before execution` in `sdk/packages/llms/src/providers/ai-sdk.ts`. The follow-on `Hub connection closed (code=1006, ...)` message is built in `sdk/packages/core/src/hub/client/index.ts`, whose reconnect backoff is still in place. The mistake-limit behavior it trips changed only very recently (HEAD e4091c368).",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Build the SDK (`bun run build:sdk`) and start the CLI interactively: `bun run cli -i`.\n2. Configure Bedrock with `global.anthropic.claude-opus-4-7-v1` (reporter was on Bedrock/macOS).\n3. Run a long ACT-mode session that makes repeated `editor` tool calls on the same large file until the context is substantially full.\n4. Expected: malformed `editor` arguments are repaired or the turn recovers. Actual: the model emits `editor({})`, the SDK rejects it three times with the `Type validation failed` / `could not be parsed as JSON` errors, the run stops, and the CLI then reports `Hub connection closed (code=1006)` / `WebSocket connection to ws://127.0.0.1:<port>/hub failed`.\n5. Verify the second half separately: whether the hub socket drops as a consequence of the aborted session, by watching `bun run cli doctor` and the hub daemon log while forcing an abort.",
  "human_attention": "yes",
  "notes": "Two distinct defects filed as one: (a) @cline/agents does not degrade gracefully when a model repeatedly emits empty/invalid tool arguments; (b) @cline/core hub client drops with a 1006 abnormal close and does not recover the interactive session. Partially touched by 107bce75b (fix(cli): recover stale interactive sessions, #11259) and HEAD e4091c368 (mistake-limit stop), so retest before deep-diving.",
  "cluster": ""
 },
 {
  "number": 10965,
  "title": "Multi option modal doesn't let you read the plan",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Fixed the day after it was filed by 899ee0bfe (2026-05-22) 'fix(cli): add inline ask question tool prompt (#10989)', which added `apps/cli/src/tui/components/inline-tool-response.tsx`. `AskQuestionResponse` there renders the option list inline in the transcript (in a scrollable `ScrollBoxRenderable`, later improved by bc184f346) instead of as an overlay, so the plan text stays visible above it. The old overlay `apps/cli/src/tui/components/dialogs/ask-question.tsx` now has no importers.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "`dialogs/ask-question.tsx` is dead code that could be deleted.",
  "cluster": ""
 },
 {
  "number": 10966,
  "title": "Cline: Multiple issues with task history.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Both code paths were restructured. On main `deleteTaskWithId` (`apps/vscode/src/core/controller/task/deleteTasksWithIds.ts`) removes the entry from state first and then always calls `postStateToWebview()`; the `origin/legacy-extension` version instead called `controller.getTaskWithId(id)` first inside a `try/catch`, so a missing task directory silently aborted the whole delete \u2014 the most likely cause of issue 1. For issue 2, `HistoryView.tsx` now does an optimistic favorite toggle via `setPendingFavoriteToggles` before awaiting `TaskServiceClient.toggleTaskFavorite`, then reconciles `setTasks`. Neither file has changed since the SDK migration squash, and I could not verify runtime behavior.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Blocked on the QA reporter: maxpaulus43 asked for a retest on the latest nightly on 2026-07-01 and got no reply. Last confirmed reproduction (2026-06-09) was against legacy VSC 3.87 / JB 1.1.59.",
  "cluster": ""
 },
 {
  "number": 10978,
  "title": "I added Global Skills, but I can't use it. It prompts that there is no use_skill tool.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The tool the model complained about is renamed on main: `DefaultToolNames.SKILLS = \"skills\"` (`sdk/packages/core/src/extensions/tools/constants.ts`), with `use_skill` surviving only as a config alias in `CONFIGURED_AGENT_TOOL_NAME_ALIASES` (`sdk/packages/core/src/runtime/orchestration/runtime-builder.ts`). The tool is registered whenever a skills executor is present (`enableSkills: !!skillsExecutor`, same file; `enableSkills: true` in the plan/act presets in `extensions/tools/presets.ts`), and the extension wires skill discovery with `includePluginSkills: true` in `apps/vscode/src/sdk/SdkController.ts`. The report is a bare screenshot with no repro steps.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed against legacy 3.84 with glm-5.1. reneehuang1 already asked the reporter to move to the nightly (SDK harness) build, where the tool is `skills`; no reply. Could equally have been the model hallucinating a missing tool.",
  "cluster": ""
 },
 {
  "number": 10980,
  "title": "Deepseek V4 context window is limited in 128K",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`resolveLegacyCustomProviderRegistration()` in `sdk/packages/core/src/services/storage/provider-settings-legacy-migration.ts` writes a migrated OpenAI-compatible model with `contextWindow: LEGACY_OPENAI_COMPATIBLE_CONTEXT_WINDOW` and `maxInputTokens: LEGACY_OPENAI_COMPATIBLE_CONTEXT_WINDOW` (both `128_000`, defined at line 165) and never reads the user's configured context window, and it does not set `settings.contextWindow` either \u2014 so `resolveKnownModelsFromConfig()` in `sdk/packages/core/src/services/llms/handler-factory.ts`, which is the code that would honor a user override via `pc.maxInputTokens`, has nothing to override with. The catalog itself is not at fault: `deepseek-v4-pro` is 1,000,000 in `sdk/packages/llms/src/catalog/catalog.generated.ts`.",
  "verdict": "duplicate",
  "duplicate_of": 10551,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Owned by @cline/core storage. Fix is to carry the legacy `openAiModelInfo.contextWindow` (or `settings.contextWindow`) through the migration instead of hardcoding 128k. Reporter mmarras independently confirmed the same 128k cap in the CLI.",
  "cluster": "openai-compat-context-window-ignored"
 },
 {
  "number": 10981,
  "title": "The \"Docs\" link from the extention leads to a 404 page",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The dead URL is `https://docs.cline.bot/features/slash-commands/workflows` (curl-verified 404). It is present in the workflows tab of `apps/vscode/webview-ui/src/components/cline-rules/ClineRulesToggleModal.tsx` on `origin/legacy-extension`, but on main that tab is gone and a repo-wide search for `slash-commands/workflows` returns zero matches. I curl-checked every `docs.cline.bot` URL under `apps/vscode/webview-ui/src` on main (14 distinct URLs) and all return HTTP 200, including the surviving `features/cline-rules` and `features/hooks` Docs links in the same modal.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Auto-closeable. The docs site could still add a redirect for `features/slash-commands/workflows` for users on the marketplace build.",
  "cluster": ""
 },
 {
  "number": 10984,
  "title": "fix: empty catch blocks silently swallow errors in 3 locations",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "None of the three cited files exist in this repository, on either branch: `git cat-file -e` fails for `src/integrations/terminal/ExecaTerminalProcess.ts`, `src/services/checkpoints/excludes.ts` and `src/utils/tts.ts` on `main`, and for the same paths under `apps/vscode/src/` on `origin/legacy-extension`. `ExecaTerminalProcess` matches zero files repo-wide; the legacy terminal directory contains `CommandExecutor.ts`/`CommandOrchestrator.ts` instead. These are Roo-Code paths.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Machine-generated issue filed against the wrong repository (Roo Code). Same author as #10985. Safely auto-closeable.",
  "cluster": ""
 },
 {
  "number": 10985,
  "title": "refactor: replace hardcoded model ID checks with supportsTemperature field",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "`src/api/transform/model-params.ts` does not exist on `main` or on `origin/legacy-extension` (`git cat-file -e` fails for both, and `apps/vscode/src/api/transform` has no tree on the legacy branch). `supportsTemperature` matches zero files repo-wide, and there are no `modelId.startsWith(\"o1\")` / `\"o3-mini\"` / `\"openai/o1-pro\"` capability checks anywhere \u2014 the only `openai/o1-pro` hits are catalog data rows in `sdk/packages/llms/src/catalog/catalog.generated.ts`.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Machine-generated issue filed against the wrong repository (Roo Code). Same author as #10984. Note that `sdk/packages/llms/AGENTS.md` explicitly rejects a Cline-maintained model capability registry, so even the underlying proposal conflicts with current architecture.",
  "cluster": ""
 },
 {
  "number": 10996,
  "title": "Together Provider: Unable to scroll towards the end of LLM list",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The widget that was filmed no longer exists: `apps/vscode/webview-ui/src/components/settings/providers/TogetherProvider.tsx` is present on `origin/legacy-extension` but deleted on main, where Together is served by `GenericProviderSettings.tsx` -> `ModelPickerWithManualEntry` -> `ModelAutocomplete.tsx`, whose option list is a plain `max-height: 200px; overflow-y: auto` container. I could not verify the scroll/placement behavior of the new picker without running the webview.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "QA issue. The JetBrains half is out of scope (separate repo UI); the VS Code half (follow-up comment: dropdown opens in the wrong direction) needs a retest against the rebuilt 4.x settings UI before it means anything.",
  "cluster": ""
 },
 {
  "number": 11010,
  "title": "Disable Robot Moving Head Animation?",
  "area": "cli",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "The animation is still unconditional on main: `apps/cli/src/tui/components/tracked-robot.tsx` and `robot-animation.tsx`/`robot-frames.ts` are rendered by `apps/cli/src/tui/views/home-view.tsx` and the onboarding screens, and there is no gate for it \u2014 searching `apps/cli/src` for `reduceMotion`, `disableAnimation`, `noAnimation` returns nothing, and the only `process.env.CLINE_*` switch in `apps/cli/src/tui` is `CLINE_FORCE_ONBOARDING` in `root.tsx`.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Cline team member gbrennon confirmed in-thread that no flag exists and suggested non-interactive `cline \"...\"` as a workaround. Small, well-scoped and agent-fixable: add a `--no-animation` flag / env var (or reduced-motion detection) around `TrackedRobot`.",
  "cluster": ""
 },
 {
  "number": 11015,
  "title": "Zed ACP integration returns only {\"stopReason\":\"end_turn\"} with no assistant text, while normal CLI works",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The ACP surface is essentially untouched since the report \u2014 only 3 unrelated commits (220a21bdc, 0c027d273, 1e1b6af51) have landed on `apps/cli/src/acp` since 2026-05-22. `AcpAgent.prompt()` in `apps/cli/src/acp/acpAgent.ts` returns `{ stopReason: \"end_turn\" }` immediately when `extractTextFromContentBlocks(params.prompt)` yields an empty string, and that helper keeps only blocks with `b.type === \"text\"`. Assistant text never comes back through the `prompt()` return value at all \u2014 it is streamed separately as `sessionUpdate: \"agent_message_chunk\"` from `apps/cli/src/acp/session-updates.ts`, so a broken subscription produces exactly the reported bare `end_turn`.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. Build the CLI from source and note its entry path (`bun run cli version`).\n2. Configure Zed's `agent_servers` with `{\"cline\": {\"type\": \"custom\", \"command\": \"<cline binary>\", \"args\": [\"--acp\"]}}` (reporter used the pnpm `cline.CMD` shim on Windows).\n3. Start a fresh ACP session in Zed and send a short prompt (the reporter used the CJK string `\u4f60\u597d`).\n4. Expected: `agent_message_chunk` session updates carrying assistant text, then `{\"stopReason\":\"end_turn\"}`. Actual: only `{\"stopReason\":\"end_turn\"}`, empty Zed UI.\n5. Isolate which of the two paths fails by logging in `apps/cli/src/acp/acpAgent.ts`: whether `extractTextFromContentBlocks` returned empty (Zed sent a non-`text` content block) or whether `sessionManager.send()` ran but `sendSessionUpdate` never fired.\n6. Confirm the control case on the same machine: `cline --json \"\u4f60\u597d\"` returns normal assistant text.",
  "human_attention": "yes",
  "notes": "Labeled `CLI`/`beta`; maintainer (renee) said 'we will investigate' and nothing landed. Two plausible root causes: Zed sending a content-block shape the `type === \"text\"` filter drops, or the session-update subscription not being attached on the Windows/ACP path.",
  "cluster": ""
 },
 {
  "number": 11017,
  "title": "Cline uses the system default shell even if it is overriden in Cline settings",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The Background Exec path on main explicitly honors the setting: `takeShellSnapshot()` in `apps/vscode/src/sdk/vscode-run-commands-tool.ts` reads `getGlobalSettingsKey(\"defaultTerminalProfile\")` and resolves it through `getShellForProfile()` (`apps/vscode/src/utils/shell.ts`, which looks the profile up in `getAvailableTerminalProfiles()` and returns `profile.path`), then passes that shell to `createShellExecutor({ shell, env: { SHELL: shell } })`. The legacy background path instead took `(terminal as any)._shellPath || this.getDefaultShell()` in `apps/vscode/src/integrations/terminal/standalone/StandaloneTerminalProcess.ts` on `origin/legacy-extension`, which is where the fall-through to the OS default came from.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Main also sets `SHELL` in the child env so subprocesses see the overridden shell rather than the inherited `fish`.",
  "cluster": "shell-override-ignored"
 },
 {
  "number": 11018,
  "title": "Unable to set custom headers anymore in OpenAi compatible (Kimi for coding setup)",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Same defect as #10953 and still present: `getProviderConfigFields()` in `sdk/packages/core/src/services/providers/provider-config-fields.ts` exposes only `apiKey`, `baseUrl` and `azureApiVersion` for `openai-compatible`, and `ProviderConfigFieldKey` has no headers member, which is exactly the reporter's 'only url, api key and model name are requested'. Persistence is not the blocker \u2014 `ProviderSettingsSchema` accepts `headers: z.record(z.string(), z.string())` (`sdk/packages/core/src/services/llms/provider-settings.ts`) and forwards it to `config.headers`, which `apps/cli/src/tui/hooks/use-model-selector.tsx` already reads. The CLI-side fix lives only on the unmerged branch `origin/saoudrizwan/cli-openai-compatible-options`.",
  "verdict": "duplicate",
  "duplicate_of": 10953,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Richer thread than #10953: reneehuang1 committed to prioritizing it on 2026-06-09 and the reporter has pinged twice since. Note the reporter's separate claim that hand-edited headers in providers.json get stripped is NOT explained by the schema (it permits them) \u2014 worth checking whether the configure flow rewrites the entry without them. One thread comment is competitor promotion.",
  "cluster": "openai-compat-custom-headers"
 },
 {
  "number": 11019,
  "title": "rite_to_file tool reports success but files are not created",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The tool being reported does not exist on main: `DefaultToolNames` in `sdk/packages/core/src/extensions/tools/constants.ts` has no `write_to_file` (writes go through `editor`/`apply_patch`), and the success string 'The file was successfully created' matches nothing in the repo. On main the write is performed by the SDK's disk executor `createEditorExecutor()` \u2014 `await fs.writeFile(...); return \"File created successfully at: ...\"` in `sdk/packages/core/src/extensions/tools/executors/editor.ts` \u2014 invoked from `apps/vscode/src/sdk/sdk-diff-edit-coordinator.ts`, whose diff preview is documented as virtual-only ('both sides are virtual, the real file is never opened or modified by the preview'), so a success message cannot be returned without the write having resolved.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Original defect was almost certainly the legacy JetBrains host diff-view save RPC silently no-op'ing on Windows; that whole path is replaced by a direct `fs.writeFile` in @cline/core. Would need a retest against a JetBrains build embedding the new standalone core before reopening.",
  "cluster": "jetbrains-command-no-output"
 },
 {
  "number": 11021,
  "title": "Cline on VS Code does not check result",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The pasted transcript shows the PowerShell error text WAS present in the terminal output, so the complaint is that the local qwen3-coder-30b model ignored it, not that Cline dropped output. On main `apps/vscode/src/hosts/vscode/terminal/VscodeTerminalProcess.ts` captures the exit code from `onDidEndTerminalShellExecution` and falls back to buffered pre-C-marker text plus a clipboard snapshot when shell integration yields nothing, so there is no output-capture defect to point at.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Model-quality complaint against a local LM Studio model; no Cline-side defect identified. Reporter never supplied a task transcript.",
  "cluster": ""
 },
 {
  "number": 11022,
  "title": "Shell Integration Unavailable error in WSL with zsh and Oh My Zsh",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The emitter for this warning lived in `apps/vscode/src/integrations/terminal/CommandOrchestrator.ts` (`say(\"shell_integration_warning\")` at lines 468-470 of the pre-fix file), which commit 12703bf40 (\"Improve VS Code terminal reliability: OSC 633 parser, exit codes, timeout handling\", #11972, 2026-07-15) deleted. On main nothing produces that say type \u2014 grep finds it only in the webview renderer `apps/vscode/webview-ui/src/components/chat/ChatRow.tsx:943`, `apps/vscode/src/shared/ExtensionMessage.ts:233` and `apps/vscode/src/shared/proto-conversions/cline-message.ts:86`.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Fixed by 12703bf40 (#11972). The new VscodeTerminalProcess emits a precise markerless-completion note instead of a blanket 'Shell Integration Unavailable' banner. Dead webview branch could be pruned by an agent.",
  "cluster": "shell-override-ignored"
 },
 {
  "number": 11026,
  "title": "Tooltip showing wrong keybinding (Alt+Shift+A) to toggle between Plan mode and Act mode",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/vscode/webview-ui/src/config/platform-configs.json` still sets `togglePlanActKeys: \"Meta+Shift+a\"`, and `apps/vscode/webview-ui/src/utils/hooks.ts` (`useShortcut`) maps the `Meta` token to `event.metaKey` \u2014 i.e. Super/Win. But `apps/vscode/webview-ui/src/components/chat/ChatTextArea.tsx:1379-1380` renders the hint by replacing `Meta` with `detectMetaKeyChar(platform)`, and `apps/vscode/webview-ui/src/utils/platformUtils.ts:28-30` returns `\"Alt\"` on Linux. So on Linux the tooltip says Alt+Shift+A while the handler actually requires Super+Shift+A.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "One-line fix, but a maintainer must decide whether to change the displayed label to 'Super' or rebind the Linux shortcut to Alt. Windows ('Win') and macOS ('CMD') labels are correct; only the Linux branch of detectMetaKeyChar lies.",
  "cluster": ""
 },
 {
  "number": 11033,
  "title": "Cline read, dead looping in act mode. Anthropic API.",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "main has real loop detection wired into the session runtime: `sdk/packages/core/src/runtime/safety/loop-detection.ts` (soft warning at 3, hard stop at 5) is instantiated and fed by `sdk/packages/core/src/runtime/orchestration/session-runtime-orchestrator.ts:444,1244`. However `checkRepeatedToolCall` only counts *consecutive identical* signatures, so an A-B-C-A-B-C read cycle like the screenshot shows would still evade it. No per-file read dedupe exists in `sdk/packages/core/src/extensions/tools/executors/file-read.ts`.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Report has no task transcript or model version; cannot reproduce. If it recurs on main, the fix would be to widen LoopDetectionTracker beyond consecutive-identical matching.",
  "cluster": ""
 },
 {
  "number": 11035,
  "title": "stuck in a screen",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Nothing concrete to check: the report is 'type in chat, it freezes' with a screenshot and no logs. The webview chat entry point on main is `apps/vscode/webview-ui/src/components/chat/ChatTextArea.tsx` and turn state is driven by `apps/vscode/webview-ui/src/components/chat/chat-view/shared/buttonConfig.ts` (`buttonsForPhase`); neither shows a defect matching 'freezes on Enter' without a repro.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Two reporters (Kimi K2.6 via OpenAI-compatible, GPT-5.5) but zero diagnostics. Needs webview devtools console output or the extension log to go anywhere.",
  "cluster": "stream-never-starts"
 },
 {
  "number": 11060,
  "title": "Cline: Existing cline history entry title becomes empty after kanban history sync to VSC",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The legacy `taskHistory` globalState array this targets no longer backs history on main; sessions are per-session manifests read through `sdk/packages/core/src/session/stores/session-manifest-store.ts` (`readSessionManifestTitle` explicitly returns undefined on malformed/missing titles 'so callers fall back to the row metadata/prompt title') with `normalizeTitle` fallbacks in `sdk/packages/core/src/services/session-data.ts:226-269`. A maintainer already replied on the issue that the extension migration should resolve it.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "QA-filed with a video. Cheapest next step is a retest of the same Kanban flow against apps/vscode on main; the storage layer it blames was replaced wholesale.",
  "cluster": ""
 },
 {
  "number": 11085,
  "title": "Integration idea: WinkTerm as self-hosted terminal backend with shared PTY AI",
  "area": "docs",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Third-party vendor proposal, not a defect. Nothing in `sdk/packages/core/src/extensions/` or `apps/vscode/src/hosts/vscode/terminal/` references WinkTerm, and a remote-PTY backend would be a new host/plugin integration rather than a change to existing code.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Author-promotion for their own project; if pursued at all it belongs in the plugin/extension surface (`sdk/packages/core/src/extensions/plugin/`), not core.",
  "cluster": ""
 },
 {
  "number": 11086,
  "title": "\"reasoning: {exclude: true}\" injected for GLM models breaks OpenAI-compatible provider endpoints that don't recognize the field",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The exact rule the reporter reverse-engineered is still on main: `sdk/packages/llms/src/providers/routing/provider-option-rules.ts:465-483` registers `family.glm.routed-reasoning`, gated only by `isGlmModel`, which in `sdk/packages/llms/src/providers/model-facts.ts:226-234` matches any model id containing 'glm'. It calls `buildRoutedGlmReasoningProviderOptionsPatch` (`sdk/packages/llms/src/providers/routing/glm-thinking.ts:78-106`), which unconditionally writes `{reasoning:{exclude:true}}` into the generic `openaiCompatible` bucket; `toProviderOptionsKey('openai-compatible')` in `routing/utils.ts:3-7` resolves to that same bucket. `provider-options.test.ts:812` even asserts the residual `openaiCompatible` bucket is expected.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Owned by @cline/llms. Fix is to scope the routed-GLM reasoning patch to gateways known to accept the `reasoning` field (openrouter, vercel-ai-gateway, cline) instead of the generic `openaiCompatible` bucket. Blocks GLM-5/5.1 on FriendliAI and self-hosted SGLang/vLLM.",
  "cluster": ""
 },
 {
  "number": 11093,
  "title": "Fix this issue: https://github.com/cline/cline/issues/9906",
  "area": "vscode-new",
  "kind": "meta-task",
  "relevant_to_main": "no",
  "evidence": "The body contains no report of its own \u2014 it only links issue 9906. `gh issue view 9906 --repo cline/cline` confirms 9906 is still OPEN ('Cannot remove attached screenshots when restoring to a checkpoint'), so this is a pure bump of an existing ticket.",
  "verdict": "duplicate",
  "duplicate_of": 9906,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Safely auto-closeable as a duplicate/bump of 9906.",
  "cluster": ""
 },
 {
  "number": 11098,
  "title": "Cline: Task automatically restarts after clicking abort button during hook execution in cline",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Hooks were rebuilt for the SDK path: `apps/vscode/src/sdk/hooks-adapter.ts` now installs `beforeRun`/`beforeTool`/`afterTool`/`afterRun` on the agent lifecycle, and `apps/vscode/src/core/hooks/hook-factory.ts:324` passes an `abortSignal` plus a 30s `HOOK_EXECUTION_TIMEOUT_MS` into `HookProcess`. The reported restart-after-abort behaviour comes from the 3.85 nightly's hook plumbing, which no longer exists in that shape, but nothing on main proves the abort/afterRun interaction is correct either.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Build and launch the new extension: from apps/vscode run `bun run build:webview` then `bun esbuild.mjs`, then `DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <workspace>`.\n2. Confirm the Hooks toggle is on (Settings -> Features -> Hooks; it defaults to true per apps/vscode/src/shared/storage/state-keys.ts:275).\n3. Add a PreToolUse hook script in the workspace hooks directory that sleeps ~20 seconds before exiting 0.\n4. Start a task that triggers a tool call so the PreToolUse hook runs and the chat shows the 'running' hook_status row.\n5. While the hook is still running, click Cancel/Abort.\n6. Expected: the run ends, afterRun fires TaskCancel, and no new turn starts. Actual (as reported on 3.85): the task stops briefly then resumes on its own.",
  "human_attention": "yes",
  "notes": "QA-filed with a video. Owner is apps/vscode `hooks-adapter.ts` plus `sdk-interaction-coordinator.ts`.",
  "cluster": ""
 },
 {
  "number": 11099,
  "title": "Kanban with cline agent fails: \"User not found\" error",
  "area": "unknown",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The failing stack is inside the `kanban` npm package's own bundled SDK copy (`node_modules/kanban/dist/cli.js`), which is not in this repo \u2014 on main the CLI only *launches* the external binary (`apps/cli/src/commands/kanban.ts`, `launchKanban`/`ensureKanbanInstalled`). The only `runWithAuthRetry` in this repo is `sdk/packages/core/src/runtime/host/local-runtime-host.ts:2105`, and it neither produces nor special-cases the string 'User not found.' (grep for that string across /workspace returns nothing), so the message originates from the Cline account API.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs routing to the kanban product/repo. Worth noting for the SDK team that `runWithAuthRetry` only retries for `isOAuthProvider` providers, so a non-OAuth 'User not found.' surfaces raw with no actionable guidance.",
  "cluster": ""
 },
 {
  "number": 11100,
  "title": "Lack of Web IDE / Codespaces Support",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "main explicitly handles VS Code Web: `apps/vscode/src/extension.ts:601-615` converts the callback URI with `vscode.env.asExternalUri` when `vscode.env.uiKind === vscode.UIKind.Web`, and `apps/vscode/src/hosts/vscode/hostbridge/env/getIdeRedirectUri.ts:5-9` returns an empty redirect URI in web so the success page does not bounce to `vscode://`. Those paths came from #9144 and #9173 (6e253dfa9, 2026-02-09), which predate this report, so either Codespaces port-forwarding is still not covered or the reporter was on an older build.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Report has empty repro steps and 'Latest (Just installed today)' as the version. A maintainer should retest sign-in in a real GitHub Codespace against apps/vscode; if it still fails the gap is in AuthHandler's local HTTP server (`apps/vscode/src/hosts/external/AuthHandler.ts`) not being reachable through Codespaces port forwarding.",
  "cluster": ""
 },
 {
  "number": 11107,
  "title": "copy-paste does not work inside cline in vs code",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "No code on main disables clipboard in the chat surface: the global shortcut handler `useShortcut` in `apps/vscode/webview-ui/src/utils/hooks.ts` only preventDefaults on an exact modifier+key match (the registered combos are `Meta+Shift+a` from `config/platform-configs.json`), and the `user-select: none` rules in `apps/vscode/webview-ui/src/components/chat/ChatTextArea.tsx:110,167` cover chrome, not message text. The follow-up comment about Ctrl+Z is a separate symptom of React-controlled textareas losing native undo history.",
  "verdict": "duplicate",
  "duplicate_of": 10198,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "No OS, no repro steps, no note of remote/WSL/Wayland. Cannot isolate from a webview/host clipboard-permission environment issue without reporter input.",
  "cluster": "webview-clipboard"
 },
 {
  "number": 11117,
  "title": "Cline: Task will not start if Hooks are enabled.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Hooks are enabled by default on main (`apps/vscode/src/shared/storage/state-keys.ts:275` -> `hooksEnabled: { default: true }`) and the adapter short-circuits when no hook script exists (`apps/vscode/src/sdk/hooks-adapter.ts:110` `if (!(await factory.hasHook(\"PreToolUse\"))) return undefined`). Hook subprocesses are now bounded: `apps/vscode/src/core/hooks/hook-factory.ts:26-27,324` enforces a 30s `HOOK_EXECUTION_TIMEOUT_MS` with an abort signal, and the SDK hook-file runner defaults to a 120s timeout (`sdk/packages/core/src/hooks/hook-file-hooks.ts:863`). An indefinite 'stuck thinking' hang therefore should not survive on main, but that is inference, not a verified fix.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "critical",
  "repro": "1. Build the new extension from apps/vscode (`bun run build:webview` then `bun esbuild.mjs`) and launch it with `DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <workspace>`.\n2. Sign in to a Cline provider and confirm Settings -> Features -> Hooks is enabled.\n3. Send a trivial task, e.g. 'what is 7+7'.\n4. Expected: the task completes normally. Actual (as reported on 3.85 nightly and JB 1.1.59 nightly): the task never progresses past the thinking state.\n5. If it reproduces, also check the standalone core path (`apps/vscode/standalone`) since the reporter saw it on JetBrains nightlies too, and inspect cline-core-service.log for a hook subprocess that never closes.",
  "human_attention": "yes",
  "notes": "Reported twice by QA (VS Code nightly and JetBrains nightly 1.1.59), and hooks default to on, so a regression here makes the product unusable. Owner: apps/vscode/src/core/hooks + sdk/packages/core/src/hooks.",
  "cluster": ""
 },
 {
  "number": 11130,
  "title": "ACP new sessions do not persist last selected provider/model in Zed on Windows",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/cli/src/acp/acpAgent.ts:129-141` hardcodes the new-session defaults: `defaultModelId = process.env.CLINE_MODEL ?? \"anthropic/claude-sonnet-4.6\"` and `providerId = process.env.CLINE_PROVIDER ?? this.authResult?.providerId ?? \"cline\"` \u2014 exactly the 'Cline + Claude Sonnet 4.6' pair the reporter sees. `unstable_setSessionModel` (line 281) and `setSessionConfigOption` (line 298) mutate only the in-memory session object. Meanwhile the SDK does persist a last-used provider: `sdk/packages/core/src/services/storage/provider-settings-manager.ts:169,221` maintains `lastUsedProvider`, which the ACP agent never reads or writes.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "The commenter's claim that this is an unfixable ACP protocol limitation is wrong \u2014 ACP's session/new response already carries `models.currentModelId` and `configOptions`, and the agent owns `lastUsedProvider` in its own settings store. Fix belongs in apps/cli/src/acp/acpAgent.ts.",
  "cluster": ""
 },
 {
  "number": 11137,
  "title": "[JetBrains] 'Accept/Reject' buttons missing after AI completes code modifications",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The stuck-on-Cancel symptom is what the old tail-walking heuristic produced. On main the shared webview derives buttons from backend-owned turn state instead: `apps/vscode/webview-ui/src/components/chat/chat-view/shared/buttonConfig.ts:370-421` (`buttonsForPhase` / `getButtonConfigFromState`) selects `BUTTON_CONFIGS.partial` (Cancel) only while `turnState.phase === \"streaming\"` and `tool_approve` on `awaiting_approval`, with the legacy `getButtonConfigForMessages` kept only as a fallback when no TurnState is present. That rework landed in 94f5a47a5 ('sdk migration: squashed pre-2026-06-02 work'), after this report.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Build the new extension from apps/vscode and launch it (`DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <workspace>`), which exercises the same webview the JetBrains plugin embeds via apps/vscode/standalone.\n2. Configure a fast, chatty provider (the reporter used deepseek-chat) and disable auto-approve for edits.\n3. Ask for a multi-file edit so several write_to_file/replace_in_file approvals are requested back to back.\n4. After each edit finishes streaming, watch the footer buttons.\n5. Expected: Save/Approve + Reject appear on every approval. Actual (as reported on JB 0.83.0): only Cancel is shown, or Accept/Reject flash and revert to Cancel.\n6. If it does not reproduce in VS Code, retest against the JetBrains plugin built on the current standalone core to confirm whether the residual defect is JetBrains-side only.",
  "human_attention": "yes",
  "notes": "Filed against JetBrains 0.83.0 (legacy core), but the button logic is the shared webview, so it is in scope until a retest says otherwise. Likely already addressed by the TurnState rework in 94f5a47a5.",
  "cluster": "diff-approve-buttons-stuck"
 },
 {
  "number": 11146,
  "title": "Failed to start core",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The stack is Kotlin/Java (`java.lang.IllegalStateException: Already terminated` from `com.google.common.base.Preconditions.checkState`), which is JetBrains-plugin code in a separate repo \u2014 grep for 'Already terminated' across /workspace returns nothing, and the only artifact this repo contributes is the packaged core under `apps/vscode/standalone/runtime-files`. A Cline contributor (@mkondratek) asked for About Info on 2026-06-09 and the reporter never replied.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Blocked on the reporter; the Guava Preconditions failure is in the JetBrains process-handle lifecycle, not in cline-core. Auto-closeable for inactivity.",
  "cluster": ""
 },
 {
  "number": 11152,
  "title": "xiaomi mimo Base URL change",
  "area": "sdk-llms",
  "kind": "question-support",
  "relevant_to_main": "no",
  "evidence": "The exact endpoint the user asked for now ships as a first-class builtin: `sdk/packages/llms/src/providers/providers.generated.ts:2897-2912` defines provider id `xiaomi-token-plan-sgp` with `defaults.baseUrl = \"https://token-plan-sgp.xiaomimimo.com/v1\"` (alongside `xiaomi`, `xiaomi-token-plan-ams` and `xiaomi-token-plan-cn`). `git log -S\"xiaomi-token-plan-sgp\"` shows it was added by ae033761a ('feat: auto generate built-in provider list', #12204, 2026-07-23), after the question was filed.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Answerable by pointing at the `xiaomi-token-plan-sgp` provider added in ae033761a; two maintainers already pinged the reporter with no response.",
  "cluster": "mimo-provider-config"
 },
 {
  "number": 11154,
  "title": "keeps engaging in endless self-reasoning without asking any questions",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "This is reasoning-text looping, not repeated tool calls, so the only safety net on main does not apply: `sdk/packages/core/src/runtime/safety/loop-detection.ts` keys purely on `toolCallSignature(call.input)` and is fed from `session-runtime-orchestrator.ts:1244` (`inspectLoopForToolCall`) \u2014 a model that keeps re-planning without emitting an identical tool call is never counted. The mistake path (`sdk/packages/core/src/runtime/safety/mistake-tracker.ts`) similarly only counts tool mistakes.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Three different models named (DeepSeek, gemini-3.1-pro-preview) with no transcript or prompt, and the last comment says the run eventually converged. Provider-side reasoning behavior more than a Cline defect; needs a captured conversation to act on.",
  "cluster": ""
 },
 {
  "number": 11155,
  "title": "Impossible to scroll in JetBrains Plugin",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The reporter's own diagnosis is a JCEF (Chromium Embedded Framework) wheel-delta limitation, and their proposed fix is to inject JS via the plugin's `executeJavaScript` host API \u2014 all of which lives in the JetBrains plugin repo. Nothing in `apps/vscode/webview-ui` or `sdk/packages/*` controls JCEF scroll speed; the only artifact this repo supplies to JetBrains is the packaged core under `apps/vscode/standalone/runtime-files`.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Legitimate usability complaint with a concrete proposed fix \u2014 needs transferring to the JetBrains plugin repo rather than closing.",
  "cluster": ""
 },
 {
  "number": 11157,
  "title": "Add HVTracker badge to README?",
  "area": "docs",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Purely a request to add two third-party badge images to `/workspace/README.md`; no code involved. A maintainer (renee, via the linear-code mirror) already replied 'we will look for a good place to put it'.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Third-party promotional badge. Editorial call on README content; no engineering work.",
  "cluster": ""
 },
 {
  "number": 11158,
  "title": "lm studio issues",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The context-size half is confirmed: `sdk/packages/llms/src/providers/builtins.ts:884-894` registers `lmstudio` as a plain `openai-compatible` provider whose only discovery source is `http://localhost:1234/v1/models`, and `sdk/packages/core/src/services/providers/model-source.ts` (`extractModelIdsFromPayload`) pulls *ids only* \u2014 the resulting entries in `local-provider-service.ts:305-315` carry no `contextWindow`, so it falls back to the 128k `DEFAULT_MAX_INPUT_TOKENS` in `sdk/packages/llms/src/catalog/catalog-live.ts:78`. Note Ollama got a native-API fix for exactly this in 7f9d2e96d (#12286); LM Studio has no equivalent even though it exposes `max_context_length` on its native `/api/v0/models`.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. Start LM Studio locally with a model loaded and its server on http://localhost:1234.\n2. Run the CLI from source: `bun run cli -i`, and select the `lmstudio` provider and the loaded model.\n3. Send any prompt and inspect the reported context window in the UI/usage row.\n4. Expected: the model's actual configured context length (LM Studio reports `max_context_length` on GET /api/v0/models). Actual: the generic 128000 default from catalog-live.ts, because only ids are read from /v1/models.\n5. Separately, for the 'Client disconnected. Stopping generation...' half, watch the LM Studio server log during the same turn to see whether Cline aborts the stream early.",
  "human_attention": "yes",
  "notes": "Two distinct problems in one issue; only the context-size half is grounded in code. Fix mirrors the Ollama native-routing work (sdk/packages/llms/src/providers/vendors/ollama.ts). The disconnect half needs its own repro.",
  "cluster": ""
 },
 {
  "number": 11159,
  "title": "Timeouts too tight and no setting to adjust them.",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "A per-provider timeout is plumbed end to end \u2014 `sdk/packages/core/src/services/providers/local-provider-service.ts:55,431,593-595,887` persists it as `settings.timeout`, `sdk/packages/llms/src/providers/config.ts:92` carries it as `timeoutMs`, and `compat.ts:527` -> `registry.ts:148` pass it into `GatewayProviderConfig.timeoutMs` (`sdk/packages/shared/src/llms/gateway.ts:125`). But the only consumer that actually enforces it on a streaming request is the Ollama vendor (`sdk/packages/llms/src/providers/vendors/ollama.ts:67-102`); the generic openai-compatible path LM Studio uses never reads it. `sdk/packages/llms/src/providers/http.ts:48` applies a 30s default only to non-streaming helper fetches.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "The plumbing already exists; the work is to honour `GatewayConfig.timeoutMs` in the openai-compatible/generic vendors and surface it in CLI provider config, the way #12286 did for Ollama. Real gap for slow local inference.",
  "cluster": ""
 },
 {
  "number": 11162,
  "title": "Google AI Studio (Gemini) 400 Error: \"property is not defined\" due to invalid MCP tool schema (e.g. serial.open)",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "MCP tool schemas are still forwarded verbatim: `sdk/packages/core/src/extensions/mcp/tools.ts:31` passes `descriptor.inputSchema` straight into `createTool`, and `normalizeToolInputSchema` in `sdk/packages/shared/src/tools/create.ts:5-79` returns the schema unchanged as soon as `typeof schema.type === \"string\"` \u2014 so `{ type: \"OBJECT\", required: [\"port\"] }` with no `properties` key reaches the provider intact. The function only injects `type: \"object\"` and validates oneOf/anyOf/allOf branches; it never backfills a missing `properties` object.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Root cause is the third-party serial-mcp-server's invalid schema, but Cline lets one bad MCP server 400 the entire request. Fix belongs in normalizeToolInputSchema (backfill `properties: {}` when `required` is present) or in createMcpTools with a per-server error surfaced instead of a generic 400.",
  "cluster": ""
 },
 {
  "number": 11163,
  "title": "DeepSeek API encoding error: Cannot convert argument to a ByteString because the character at index 299 has a value of 8212 which is greater than 255.",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The ByteString error can only come from a non-Latin-1 HTTP *header* value, not the JSON body. Every header Cline generates on main is ASCII-only and short: `sdk/packages/llms/src/providers/request-headers.ts:35-91` emits fixed values plus `X-Task-ID: <sessionId>` and `User-Agent: Cline/<version>`, and there is no path that puts message content into headers. The reporter's stated 'root cause' is Python (`bytes(my_string, 'latin-1')`), which cannot be Cline code \u2014 this repo is TypeScript \u2014 so the diagnosis is fabricated.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Most likely a user-supplied custom header or an API key pasted with a smart em dash (U+2014), or Windsurf's own host injecting a header. Needs the actual failing header set / a redacted provider config from the reporter.",
  "cluster": ""
 },
 {
  "number": 11164,
  "title": "It does not recognise DeepSeek.",
  "area": "sdk-llms",
  "kind": "question-support",
  "relevant_to_main": "no",
  "evidence": "The only identity text sent to the model is `DEFAULT_CLINE_SYSTEM_PROMPT` in `sdk/packages/shared/src/prompt/system.ts` (\"You are Cline, an AI coding agent\"), which never names the underlying model, so asking \"what model are you?\" cannot verify provider routing. The `deepseek` provider and the exact model the reporter picked (`deepseek-v4-flash`, `deepseek-v4-pro`) are present in `sdk/packages/llms/src/catalog/catalog.generated.ts`, and the model chip in `apps/vscode/webview-ui/src/components/chat/ChatTextArea.tsx` is what actually reflects the active route.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Matches the maintainer note on the issue (\"this is likely a model issue\"). Model self-report is not evidence of misrouting; no defect visible.",
  "cluster": ""
 },
 {
  "number": 11181,
  "title": "When using memorybank, Cline's sub-agents cause significant waste.",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "`ALL_DEFAULT_TOOL_NAMES` in `sdk/packages/core/src/extensions/tools/constants.ts` contains no spawn/subagent tool, and `spawn_agent` (`sdk/packages/core/src/extensions/tools/team/spawn-agent-tool.ts`) is opt-in \u2014 `subagentsEnabled` defaults to `false` in `apps/vscode/src/shared/storage/state-keys.ts`. The reporter's diagnosis still holds when it is enabled: `createSpawnAgentTool` calls `createDelegatedAgent`, which gets its own `conversationId` and is seeded only with `input.task`, so each sub-agent re-reads the workspace from scratch.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. Run the new VS Code extension from `apps/vscode` against a repo containing a `memory-bank/` directory with several files.\n2. In Cline settings, enable subagents (`subagentsEnabled`, off by default).\n3. Send \"look at this project\" so the model chooses to delegate.\n4. Watch the subagent rows: expected is one shared read pass with delegated work reusing parent findings; actual is each `spawn_agent` child starting a fresh conversation and re-reading the same files, multiplying input tokens.",
  "human_attention": "yes",
  "notes": "Cost/efficiency issue owned by `@cline/core` team tools. Default-off in the new extension reduces blast radius; the no-shared-context design is the underlying cause.",
  "cluster": ""
 },
 {
  "number": 11198,
  "title": "No changes in workspace for commit message when there a changes in workspace",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The exact error string is thrown on main by `getGitDiff` in `apps/vscode/src/utils/git.ts`, which only runs `git --no-pager diff --staged --diff-filter=d` and `git --no-pager diff HEAD --diff-filter=d`. Neither command reports untracked files, and `--diff-filter=d` drops deletions, so a workspace whose SCM change count is made up of new/deleted files yields an empty diff. `generateCommitMsg` in `apps/vscode/src/hosts/vscode/commit-message-generator.ts` then surfaces it verbatim as `[Commit Generation Failed] No changes in workspace for commit message`.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Fix is to include untracked files (e.g. `git add -N` / `--diff-filter` widening) before deciding there are no changes.",
  "cluster": ""
 },
 {
  "number": 11205,
  "title": "[Relates to #4384] replace_in_file terminal loop edge case: hallucinating tags (azure_dracula) & ignoring boundary constraints",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The SEARCH/REPLACE diff-block writer the report blames no longer exists on main: `replace_in_file` is only an alias mapped to the `editor` tool (`sdk/packages/core/src/runtime/orchestration/runtime-builder.ts` and `apps/vscode/src/sdk/sdk-tool-policies.ts`), whose `replaceInFile` in `sdk/packages/core/src/extensions/tools/executors/editor.ts` throws on zero or multiple matches instead of writing partial content. The out-of-workspace write is also blocked by default: `resolveFilePath(cwd, input.path, restrictToCwd)` with `restrictToCwd = true` in the same file (and in `apply-patch.ts`).",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "The only remaining prompt text referencing SEARCH/REPLACE lives in `apps/vscode/src/core/prompts/responses.ts`, which has no importers (dead code). The last comment on the issue is third-party tool promotion, not signal.",
  "cluster": "legacy-replace-in-file"
 },
 {
  "number": 11206,
  "title": "Could you confirm whether MiniMax M3 is currently supported, and if not, whether support can be added or compatibility improved?",
  "area": "sdk-llms",
  "kind": "question-support",
  "relevant_to_main": "no",
  "evidence": "`sdk/packages/llms/src/catalog/catalog.generated.ts` lists `MiniMax-M3` under the `minimax`, `minimax-cn`, `minimax-coding-plan` and `minimax-cn-coding-plan` providers (plus ~20 aggregators such as `fireworks`, `nvidia`, `nebius`, `ollama`), and `minimax` is a builtin provider in `sdk/packages/llms/src/providers/builtins.ts` with dedicated routing in `sdk/packages/llms/src/providers/routing/minimax-thinking.ts`.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Maintainer already replied \"This should already be fixed\"; the catalog on main confirms it. Safe to close.",
  "cluster": ""
 },
 {
  "number": 11207,
  "title": "Unable to add context or add files & images",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The failing pieces are host-side: the `No results found` label is shared webview text (`apps/vscode/webview-ui/src/components/chat/ContextMenu.tsx`), but the file search and file-open results behind it are supplied by the JetBrains host, and the reporter's error string (\"IDE can't find the following file: file:///...\") appears nowhere in this repo. Nothing in `sdk/packages/*` or `apps/vscode/standalone` produces it.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Already owned by the JetBrains team (mkondratek) in the plugin repo; reporter supplied About Info (Rider 2025.3.3, plugin 1.1.58).",
  "cluster": ""
 },
 {
  "number": 11208,
  "title": "Token Usage not accurate with Tinfoil",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Usage normalization was rewritten on main in `normalizeUsage` (`sdk/packages/llms/src/providers/ai-sdk.ts`), which reads a single per-request usage object (`stream.usage`, falling back to the finish part) and does not sum streamed chunks; the running total is accumulated once per turn in `updateUsage` in `sdk/packages/agents/src/agent-runtime.ts`. Tinfoil is now also a first-class builtin (`tinfoil` with `kimi-k2-6`, `glm-5-2` in `sdk/packages/llms/src/catalog/catalog.generated.ts`), so the reporter's OpenAI-compatible-proxy path is no longer the only route. I could not find a defect that inflates 15k to 1.4m, so this needs a live check.",
  "verdict": "duplicate",
  "duplicate_of": 11660,
  "severity": "medium",
  "repro": "1. Run the new VS Code extension from `apps/vscode` (or `bun run cli`).\n2. Configure the builtin `tinfoil` provider with a Tinfoil key and pick `kimi-k2-6`; separately configure an `openai-compatible` provider pointed at `http://127.0.0.1:3301/v1` via tinfoil-proxy with model id `kimi-k2-6`.\n3. Send a single \"hello world\" prompt on each configuration.\n4. Compare the token count in the task header against the Tinfoil dashboard for the same request. Expected: ~15k; the report claims Cline shows 212.8k for the first turn and 1.4m cumulative.\n5. If it reproduces, dump the raw provider usage payload via `recordProviderRequestCapture` / `provider-request-capture.ts` to see which field `normalizeUsage` is picking up.",
  "human_attention": "no",
  "notes": "A commenter's hypothesis (cumulative snapshots being summed as deltas) is not what `normalizeUsage` does today, but the raw-usage field precedence (`inputTokens.total` before `prompt_tokens`) is the place to look for a proxy that reports non-standard usage.",
  "cluster": "inflated-token-count"
 },
 {
  "number": 11209,
  "title": "Assistant gets stuck in infinite read-loop consuming many API calls without completing implementation tasks",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Main has a stop-on-repeated-failure guard (`sdk/packages/core/src/runtime/safety/mistake-tracker.ts`, tightened at HEAD by e4091c368 \"Stop the task at the mistake limit like the CLI\"), but that only counts mistakes \u2014 successful repeated `read_files` calls are not mistakes, and `sdk/packages/agents/src/agent-runtime.ts` has no duplicate-tool-call/loop detector. So nothing on main structurally prevents the reported re-read loop; equally there is no code defect to point at, and the report is from the legacy harness with a weak model (`openai` / `deepseek-chat`).",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Model-behavior report without a task transcript. Would need the reporter to retry on the SDK harness and share the session before it is actionable; a repeated-identical-tool-call guard in `agent-runtime.ts` is the candidate hardening.",
  "cluster": ""
 },
 {
  "number": 11216,
  "title": "Cline Ticket Number #104995434",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The body is only a tracking placeholder, but the collaborator comment pins the real defect and it still holds on main: `apps/vscode/webview-ui/src/components/settings/providers/LMStudioProvider.tsx` renders only a base-URL field and a model dropdown \u2014 it never renders `ApiKeyField` \u2014 even though the provider manifest supports a key (`apiKeyEnv: [\"LMSTUDIO_API_KEY\"]` in `sdk/packages/llms/src/providers/builtins.ts`). The CLI side is fine: `getProviderConfigFields` in `sdk/packages/core/src/services/providers/provider-config-fields.ts` returns an `apiKey` field for every non-OAuth, non-`local-auth` provider, and `lmstudio` is neither.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Fix is small and local: add `ApiKeyField` to `LMStudioProvider.tsx`. The linked Linear ticket's CLI half (CLINE-2366) appears already satisfied on main.",
  "cluster": ""
 },
 {
  "number": 11221,
  "title": "API error CODEX",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`filterOpenAICodexModels` in `sdk/packages/llms/src/providers/openai-codex-models.ts` now only exposes Codex models newer than 5.3 (`Number.parseFloat(match[1]) > 5.3`) and drops `o`/`pro`/`nano` families, so `gpt-5.3-codex`, `gpt-5.2`, `gpt-5.3-codex-spark` and `gpt-5.5-pro` \u2014 exactly the models that 400'd \u2014 are no longer selectable. That list was tightened in ecca88bb9 \"Clean-up the Codex model list (#11342)\" (2026-06-07) after this report.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Root cause was upstream: OpenAI dropped 5.3 from Codex subscription access. One loose thread in the thread is a commenter seeing gpt-5.4 edit files while in Plan mode \u2014 that is unrelated and should be filed separately if reproducible.",
  "cluster": "codex-responses-item-pairing"
 },
 {
  "number": 11223,
  "title": "Cerebras Provider: Getting model doesn't exist error when trying to use llama3.1-8b LLM",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The `cerebras` provider block in `sdk/packages/llms/src/catalog/catalog.generated.ts` now offers only `gemma-4-31b`, `zai-glm-4.7` and `gpt-oss-120b`; `llama3.1-8b` is gone (the only `llama3-1-8b` entry left in the catalog is Bedrock's `meta.llama3-1-8b-instruct-v1:0`). The catalog is generated from models.dev, so the stale entry that Cerebras rejected is no longer offered in the picker.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter confirmed it was this model only. Safe to close once the catalog version shipped in the reporter's build.",
  "cluster": ""
 },
 {
  "number": 11238,
  "title": "Can't paste API key for Anthorpic",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The shared settings input is `apps/vscode/webview-ui/src/components/settings/common/ApiKeyField.tsx`, a plain `VSCodeTextField` with no `onPaste` interception or key filtering, so nothing in this repo blocks a paste. Clipboard delivery into the embedded JCEF browser is JetBrains-plugin/host territory, which lives in a separate repo.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "JetBrains maintainer could not reproduce on 1.1.58 or nightly and is waiting on the reporter.",
  "cluster": "webview-clipboard"
 },
 {
  "number": 11241,
  "title": "Prompt gets deleted when changing from Act mode to Plan mode and hitting send",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "On main the composer is only cleared when the backend confirms the text was consumed: `togglePlanActMode` in `apps/vscode/src/sdk/sdk-mode-coordinator.ts` returns `continuationSent && hasUserContent`, and `onModeToggle` in `apps/vscode/webview-ui/src/components/chat/ChatTextArea.tsx` clears only `if (response.value)` and only when the textarea still holds the exact submitted text. That handling landed in 31cff1f75 \"fix(vscode): auto-continue the task when switching from plan to act (#11401)\" (2026-06-10), after this report.",
  "verdict": "duplicate",
  "duplicate_of": 12237,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Worth a quick sanity check on the new extension before closing, since the reporter says the loss happened at Send rather than at toggle.",
  "cluster": "prompt-lost-on-mode-switch"
 },
 {
  "number": 11263,
  "title": "bug(ollama): Infinite JSON tool loop & HTTP 400/500 errors on non-tool/streamed models",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Half of this is structurally gone: main drives Ollama through the native `/api/chat` endpoint via `ai-sdk-ollama` (`sdk/packages/llms/src/providers/vendors/ollama.ts`) and the harness consumes native tool calls, so there is no XML parser to choke on JSON tool calls. The other half still looks real: `streamText` in `sdk/packages/llms/src/providers/ai-sdk.ts` always passes `tools: toAiSdkTools(request)` (only suppressed when `providerDisablesExternalToolExecution(context)`), with no gate on the resolved model's `tools` capability, so a GGUF without tool support still receives a tools payload.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. Install a local Ollama model with no tool support (an older/custom GGUF, e.g. a plain `llama3` build) plus one with tools (`qwen2.5-coder:14b`).\n2. Run the new VS Code extension from `apps/vscode` (or `bun run cli`) and select the `ollama` provider pointed at `http://localhost:11434`.\n3. Send a simple task with the non-tool model. Expected: Cline omits the tools payload or degrades gracefully; actual (per report): Ollama returns HTTP 400 because `tools` was sent anyway.\n4. Repeat with `qwen2.5-coder:14b` and confirm native tool calls are now executed rather than looping with MODEL_NO_TOOLS_USED.",
  "human_attention": "yes",
  "notes": "Overlaps heavily with #10843 (same JSON-tool-loop complaint) and open PR #11272. Note the reporter later wrote \"I could be hallucinating this whole issue\"; the tools-capability gap in `ai-sdk.ts` is the part that is actually visible in code.",
  "cluster": "ollama-json-tool-loop"
 },
 {
  "number": 11266,
  "title": "Terminal commands emitted with HTML entities escaped (`&amp;&amp;`, `&gt;`) causing shell parse errors",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The escaping came from the legacy XML assistant-message parser, which does not exist on main \u2014 the only XML/SEARCH-REPLACE text left is unimported dead code in `apps/vscode/src/core/prompts/responses.ts`. Commands now arrive as JSON tool-call arguments and are executed by `sdk/packages/core/src/extensions/tools/executors/bash.ts`; the only HTML-entity handling anywhere in the tool path is decoding in `sdk/packages/core/src/extensions/tools/executors/web-fetch.ts`, so there is no encode step to leak `&amp;&amp;` into a shell command.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter re-confirmed on 3.89.2, which is still a legacy marketplace build. If it recurs on the SDK harness it would be pure model output, not a Cline transform.",
  "cluster": ""
 },
 {
  "number": 11268,
  "title": "Skills not being loaded",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`resolveSkillsConfigSearchPaths` in `sdk/packages/shared/src/storage/paths.ts` explicitly scans `<workspace>/.agents/skills` (via `LEGACY_AGENT_SKILLS_CONFIG_DIR`) alongside `.clinerules/skills`, `.cline/skills` and `~/.agents/skills`, and that is what the loader uses (`sdk/packages/core/src/extensions/config/user-instruction-config-loader.ts`). The commenter's folder-name workaround is also unnecessary on main: the loader falls back to the directory/file name when frontmatter `name` is absent. A real silent-load bug in this area was fixed after the report in e3c6d5107 \"fix: recognize frontmatter with a leading UTF-8 BOM (#12277)\".",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "If it recurs, the likely cause is malformed SKILL.md frontmatter rather than the search path.",
  "cluster": ""
 },
 {
  "number": 11274,
  "title": "replace_in_file appends a stray blank line at the end of every SEARCH/REPLACE block",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The SEARCH/REPLACE block writer that inserted the trailing newline is gone: on main `replace_in_file` is an alias for the `editor` tool (`sdk/packages/core/src/runtime/orchestration/runtime-builder.ts`), and `replaceInFile` in `sdk/packages/core/src/extensions/tools/executors/editor.ts` does `content.replace(normalizedOldStr, () => normalizedNewStr)` and writes the result verbatim \u2014 it only normalizes line endings to the file's own EOL and never appends a line.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "The JetBrains \"same here\" comment is also against the legacy core; it should be re-checked only after JetBrains picks up the SDK-based standalone core.",
  "cluster": "legacy-replace-in-file"
 },
 {
  "number": 11292,
  "title": "cannot see Cline icon in Activity bar,",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The contribution is intact on main: `apps/vscode/package.json` declares `viewsContainers.activitybar` with id `claude-dev-ActivityBar`, title `Cline`, icon `assets/icons/icon.svg`, and a matching webview view under `views`. The report contains no version, OS, logs or steps (all template fields are blank), so there is nothing to tie it to code.",
  "verdict": "duplicate",
  "duplicate_of": 10345,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Most likely a hidden Activity Bar item or a failed extension activation; auto-closeable with a request for logs.",
  "cluster": "extension-not-activating"
 },
 {
  "number": 11295,
  "title": "Deadlock on remote-SSH after terminal commands",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Both reported bugs target code that no longer runs. There is no system-notification call left in the extension (no `showSystemNotification` anywhere under `apps/vscode/src`, and the \"Could not show system notification\" string does not exist in the repo), and commands no longer depend on VS Code shell integration by default: `apps/vscode/src/sdk/vscode-runtime-builder.ts` defaults `vscodeTerminalExecutionMode` to `backgroundExec`. The timeout is also user-editable \u2014 `TerminalSettingsSection.tsx` posts `StateServiceClient.updateTerminalConnectionTimeout` and `apps/vscode/src/core/controller/state/updateTerminalConnectionTimeout.ts` persists it (4000 ms is only the default in `state-keys.ts`).",
  "verdict": "duplicate",
  "duplicate_of": 12079,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "The issue body was AI-generated from a legacy 3.87 session. A later commenter reports a different symptom (stuck \"thinking\" after a command completes) that a maintainer is already chasing \u2014 that thread, not this body, is the live signal.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 11296,
  "title": "Claude Code provider fails to connect with Claude Code CLI 2.1.163: command exits with code 1",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The invocation code the report blames (`apps/vscode/src/integrations/claude-code/run.ts`, which spawned `claude -p --output-format stream-json` itself) was deleted during the SDK migration \u2014 the directory does not exist on main (`apps/vscode/src/integrations/` now holds only diagnostics, editor, misc, openai-codex, terminal). Claude Code now routes through the `ai-sdk-provider-claude-code` package in `sdk/packages/llms/src/providers/vendors/community.ts`, which also auto-resolves the executable via `@anthropic-ai/claude-agent-sdk-*`.",
  "verdict": "duplicate",
  "duplicate_of": 10135,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "PR #11169, which a commenter offered as the fix, is still OPEN and only touches the deleted `apps/vscode/src/integrations/claude-code/run.ts` \u2014 it targets dead code and should not be merged as-is.",
  "cluster": "claude-code-exit-code-1"
 },
 {
  "number": 11302,
  "title": "Cline ask question but dont have any answer",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The attached screenshot shows \"Cline has a question:\" with the question text and no selectable options, on `openai-compat:MiniMax-M3` \u2014 the same provider/model as #11337. `AskQuestionInputSchema` in `sdk/packages/core/src/extensions/tools/schemas.ts` requires `options: z.array(z.string().min(1)).min(2).max(5)`, so the structured `{key,label,description}[]` MiniMax M3 emits fails validation in `createAskQuestionTool` (`sdk/packages/core/src/extensions/tools/definitions.ts`) and no options survive to the UI.",
  "verdict": "duplicate",
  "duplicate_of": 11337,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed two days before #11337 but #11337 carries the diagnosis and a proposed union-schema fix, so track it there.",
  "cluster": "ask-question-schema-too-strict"
 },
 {
  "number": 11307,
  "title": "Text input fields cursor skip",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`useDebouncedInput` in `apps/vscode/webview-ui/src/components/settings/utils/useDebouncedInput.ts` saves after 100 ms and then unconditionally re-syncs (`if (prevInitialValueRef.current !== initialValue) setLocalValue(initialValue)`) when the backend echoes state back, discarding anything typed in the interim and pushing the caret to the end \u2014 exactly the \"characters eaten, cursor jumps\" behaviour, and only when typing fast enough to cross the debounce. `DebouncedTextField.tsx` is built on it. The sibling `ApiKeyField.tsx` already guards against this with `isFocusedRef`/`hasPendingUserEditRef`; the shared hook does not.",
  "verdict": "duplicate",
  "duplicate_of": 10510,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Fix is to port the focus/pending-edit guard from `ApiKeyField.tsx` into `useDebouncedInput`.",
  "cluster": "settings-input-eats-characters"
 },
 {
  "number": 11328,
  "title": "DeepSeek V4 Pro enters reasoning loop and outputs malformed \"Checkpoint Compare Restore\" text, breaking tool execution",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The echoed words are Cline's own chat-row labels, which points at the legacy XML harness feeding rendered UI text back into the model; main sends native tool calls built in `sdk/packages/llms/src/providers/ai-sdk.ts` (`toAiSdkTools` + `streamText`) and the \"complex prompts / less capable models\" warning string does not exist anywhere under `sdk/packages/*` or `apps/vscode/src`. There is no code defect on main I can point to, and the report has no transcript.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed against 3.88.0 beta; a maintainer already asked the reporter to retry on the migrated harness and got no reply. Same family as #11209.",
  "cluster": ""
 },
 {
  "number": 11332,
  "title": "No vertical scrollbar displayed in Cline output panel",
  "area": "vscode-new",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Hiding the scrollbar is deliberate and still present: the Virtuoso message list in `apps/vscode/webview-ui/src/components/chat/chat-view/components/layout/MessagesArea.tsx` sets `scrollbarWidth: \"none\"` and `msOverflowStyle: \"none\"`, and the `.scrollable` rules in `apps/vscode/webview-ui/src/main.css` keep the webkit thumb `border-color: transparent` until `body:hover`/`body:focus-within`. So there is no persistent thumb to indicate content length or to drag.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Accessibility-flavoured styling change in the webview; agent-fixable, but it reverses an intentional design choice so it needs a product call before implementing.",
  "cluster": ""
 },
 {
  "number": 11337,
  "title": "fix(core): ask_question options schema too strict, rejects object[] from some models",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Still exactly as reported on main: `AskQuestionInputSchema` in `sdk/packages/core/src/extensions/tools/schemas.ts` declares `options: z.array(z.string().min(1)).min(2).max(5)`, and `createAskQuestionTool` in `sdk/packages/core/src/extensions/tools/definitions.ts` hard-fails through `validateWithZod` with `retryable: false, maxRetries: 0`. The analogous union-schema fix the reporter cites was merged only for read_files (dd7042fc7 \"fix(core): use union schema for read files tool input validation (#11225)\"), never for ask_question.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Owned by `@cline/core`; fix is a union schema plus object\u2192string normalization in the executor, mirroring #11225. Also explains #11302.",
  "cluster": "ask-question-schema-too-strict"
 },
 {
  "number": 11340,
  "title": "Feature: warn on likely-typosquat npm installs before execute_command runs them",
  "area": "sdk-core",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "The integration point the reporter names does not exist on main: there is no `CommandPermissionController` and no `apps/vscode/src/core/permissions/` directory (grep for `CommandPermissionController` returns zero hits repo-wide), and `CLINE_COMMAND_PERMISSIONS` survives only as a blocked-command response string in `apps/vscode/src/core/prompts/responses.ts`. Shell execution on main lives in `sdk/packages/core/src/extensions/tools/executors/bash.ts` (`spawnAndCollect`), so any typosquat inspector would have to hook the SDK bash tool, not a VS Code permission controller.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Contributor has an implementation + 39 tests ready and is asking for a go/no-go, so a maintainer decision is needed. Also needs a scope correction: the proposed hook site is legacy 3.x; on main it belongs next to the bash executor / tool-approval path in @cline/core.",
  "cluster": ""
 },
 {
  "number": 11350,
  "title": "`execute_command` does not return stdout/stderr to the agent",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "JetBrains contributor mkondratek commented that this is the same as #10948 and that the fix already shipped to the JetBrains nightly channel; `gh issue view 10948` confirms it is CLOSED (2026-06-05, labels JetBrains + Windows). On main the standalone/JetBrains build cannot use the VS Code terminal path at all \u2014 `apps/vscode/src/sdk/vscode-terminal-execution-mode.ts` clamps standalone hosts to `backgroundExec`, which returns real piped stdout/stderr via `spawnAndCollect` in `sdk/packages/core/src/extensions/tools/executors/bash.ts`.",
  "verdict": "duplicate",
  "duplicate_of": 10948,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "JetBrains plugin terminal integration (separate repo). Safely closeable as a duplicate pointing at the nightly fix.",
  "cluster": "jetbrains-command-no-output"
 },
 {
  "number": 11356,
  "title": "Technical support ignores me.",
  "area": "unknown",
  "kind": "question-support",
  "relevant_to_main": "no",
  "evidence": "No code defect is described: the report is a request to unblock a Cline account after credential sharing, referencing support tickets #102479561 / #107052694. Nothing in /workspace is implicated; account state lives in the Cline backend, not in this repo.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Already routed: collaborator BarreiroT offered to whitelist the account over email. Account-support ticket filed as a GitHub issue; closeable.",
  "cluster": ""
 },
 {
  "number": 11362,
  "title": "Cline: Displays message after starting a new task while previous task Is pending approval",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The reason string still exists on main: `apps/vscode/src/sdk/sdk-task-control-coordinator.ts:75` calls `this.options.interactions.clearPending(\"Task cleared\")`, which resolves the pending tool approval with `{ approved: false, reason }` in `apps/vscode/src/sdk/sdk-interaction-coordinator.ts:238`. However the raw-JSON render path is legacy \u2014 no producer of a `{\"error\": ...}` payload exists in `apps/vscode/src`, and `apps/vscode/src/sdk/message-translator.ts` now has dedicated denial-suppression (`recordDeniedToolApproval`, `checkDeniedToolApproval`, `isSuppressedToolApprovalDenial`) that did not exist in 3.87.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "low",
  "repro": "1. Run the new extension from source: `cd apps/vscode && bun run build:webview && bun esbuild.mjs`, then `DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <folder>`.\n2. In Cline settings, turn OFF every auto-approve action (read, edit, commands) so tool calls require explicit approval.\n3. Start a task with a prompt that forces an edit, e.g. \"add a docstring to src/index.ts\".\n4. Wait until the tool-approval ask is rendered and do NOT approve it.\n5. Click \"Start New Task\" (this calls SdkTaskControlCoordinator.clearTask -> clearPending(\"Task cleared\")).\n6. Open History and reopen the first task.\nExpected: the abandoned task renders a readable \"task was cleared\" state (or the denied tool row is suppressed entirely).\nActual (as reported on 3.87): a raw `{\"error\":\"Task cleared\"}` string is rendered in the transcript.",
  "human_attention": "yes",
  "notes": "QA-filed. Verify against apps/vscode; the message-translator denial suppression may already hide it, in which case close as fixed by the SDK cutover.",
  "cluster": ""
 },
 {
  "number": 11364,
  "title": "Subagent document reads frequently return truncated content, forcing parent task to re-read docs directly",
  "area": "sdk-core",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "There is no Cline-side truncation of subagent results on main: `sdk/packages/core/src/extensions/tools/team/spawn-agent-tool.ts` returns `result.text` verbatim (no slice/truncate; the only `truncateText` helpers in that directory are for team-run previews in `team-tools.ts`). What is bounded is the subagent's own file reading \u2014 `MAX_READ_OUTPUT_CHARS = 48_000` in `sdk/packages/core/src/extensions/tools/executors/output-limits.ts`. So the 'truncation' is the subagent model summarizing/hitting its own output budget, not a defect.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "The concrete ask (size-check documents before delegating, or skip delegation for large sets) is a prompt/orchestration change in `sdk/packages/core/src/extensions/tools/team/`. One +1 from another user.",
  "cluster": ""
 },
 {
  "number": 11369,
  "title": "[BUG] Hub WebSocket rejects connections when authToken is empty string (regression in v0.0.43)",
  "area": "hub",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The reporter's root cause (read out of minified `dist/hub/daemon/entry.js`) does not match main: the client never depends on `CLINE_HUB_AUTH_TOKEN` \u2014 it reads the token from the discovery record (`record.authToken` in `sdk/packages/core/src/hub/client/index.ts:899` and `:1000`, plus `resolveLocalHubAuthToken`) and sends it as the `cline-hub-auth.<token>` subprotocol at `:352`, matching `readWebSocketAuthToken` in `sdk/packages/core/src/hub/server/hub-websocket-server.ts:242`. The token-propagation bug behind the 1006 symptom was fixed two days after this was filed by commit 6138bdfe4 (\"feat: Enforce a production singleton Cline Hub\", #11372, 2026-06-10), whose description includes \"Preserve and propagate auth tokens when retiring incompatible hubs\" and \"made discovery auth tokens required again\".",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Fixed by 6138bdfe4 (#11372). The requested behavior (accept empty-token connections) should NOT be implemented \u2014 auth is required by design; the token is distributed via the discovery file.",
  "cluster": "hub-ws-empty-authtoken"
 },
 {
  "number": 11370,
  "title": "[BUG] Hub WebSocket code 1006 \u2014 server generates random token rejecting no-auth clients",
  "area": "hub",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Same reporter, same symptom (WebSocket close 1006 from the v0.0.43 hub daemon), filed 17 minutes after #11369 with a more detailed decompilation of the same `XB`/`Vy` auth functions. Same code check applies: `sdk/packages/core/src/hub/server/hub-websocket-server.ts:292` mints a token via `createHubAuthToken()` and the client reads it back out of the discovery record in `sdk/packages/core/src/hub/client/index.ts` rather than from an env var, and 6138bdfe4 (#11372) repaired token propagation/discovery-record handling.",
  "verdict": "duplicate",
  "duplicate_of": 11369,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Close as a duplicate of #11369; both are superseded by 6138bdfe4.",
  "cluster": "hub-ws-empty-authtoken"
 },
 {
  "number": 11374,
  "title": "Provider/docs request: Starveri API OpenAI-compatible GPT/Codex gateway",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "No `starveri` entry exists in `sdk/packages/llms/src/providers/ids.ts` or `sdk/packages/llms/src/providers/builtins.ts` (grep returns nothing). The pattern an OpenAI-compatible preset would follow is present, e.g. the `aihubmix` `BUILTIN_SPECS` entry at `sdk/packages/llms/src/providers/builtins.ts:737` with `apiKeyEnv` and `defaults.baseUrl`.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Purely a scope/policy call: does Cline want to list independent third-party OpenAI-compatible gateways as presets? The reporter explicitly says to close it if not. Same class of request as #11406.",
  "cluster": ""
 },
 {
  "number": 11375,
  "title": "/compact fails to trigger condense tool on OpenAI/GPT provider (cline:openai/gpt-5.5), but works after switching to Claude Opus 4.6",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The reported mechanism \u2014 a `condense` tool that must appear in the assistant's tool registry \u2014 no longer exists on main. `/compact` in `apps/vscode` now runs `compactSessionMessages` in `apps/vscode/src/sdk/sdk-compaction.ts`, which calls `createContextCompactionPrepareTurn` (`sdk/packages/core/src/extensions/context/compaction.ts:248`) with `{ mode: \"manual\" }`. That is an out-of-band `prepareTurn` pipeline step (`agentic-compaction.ts` / `basic-compaction.ts`), so compaction never depends on the main model choosing to call a `condense` tool and cannot be diverted into `attempt_completion`.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed against 3.88.1. The provider-dependent tool-registry failure mode is structurally impossible in the SDK compaction path; closeable with a note to retest on the SDK extension.",
  "cluster": ""
 },
 {
  "number": 11376,
  "title": "Websocket-related:",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Filed against CLI 3.0.20; `apps/cli/package.json` is now 3.0.46, and several hub-lifecycle fixes have landed since (`107bce75b fix(cli): recover stale interactive sessions and suppress shutdown races` #11259, `1bb083328 fix(cli): isolate hub daemon abort handling` #12500). I could not find a specific defect on main matching the description because the report has no logs or error text \u2014 only a screenshot and the sentence 'cline local server seems to be stopped'. The reporter re-confirmed on 2026-06-25 that it still happens.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. From /workspace run the CLI from source in interactive mode: `bun run cli -i` (this auto-spawns the `@cline/cline-hub` daemon). A provider credential is required (e.g. `ANTHROPIC_API_KEY`).\n2. Start in plan mode with auto-approve enabled.\n3. Submit a prompt that produces a long streaming turn.\n4. While the turn is still streaming, toggle to act mode.\n5. If the turn errors, run `/fork` and submit another prompt.\nExpected: the mode toggle applies (or is queued) and the session keeps its hub connection; `/fork` yields a usable session.\nActual (reported): the mode switch fails, the hub/local server appears stopped, and `/fork` + prompt fails repeatedly until a brand-new session is created.\nCollect `bun run cli doctor` output and the hub daemon log alongside the failure.",
  "human_attention": "yes",
  "notes": "Suspect a race between mid-turn mode switching and hub session state. Needs a maintainer with the hub session lifecycle in hand; the report itself is too thin to act on without a repro attempt.",
  "cluster": "hub-session-loss"
 },
 {
  "number": 11377,
  "title": "Cline unexpectedly resets to start screen due to Extension Host crash (STATUS_STACK_BUFFER_OVERRUN) \u2014 likely triggered by better-sqlite3",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The log evidence is entirely legacy: none of the strings the report is built on \u2014 `schedule assistant presentation`, `Failed to save ui messages`, `Current ask promise was ignored` \u2014 exist anywhere in /workspace, so that task loop is gone. The blamed native module IS still present though: `better-sqlite3` ^12.4.1 in `apps/vscode/package.json:505` and `apps/vscode/standalone/runtime-files/package.json`, used by `apps/vscode/src/core/locks/SqliteLockManager.ts` and `sdk/packages/shared/src/db/sqlite-db.ts`. Nothing in the report ties the -36861 crash to a specific call site.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "critical",
  "repro": null,
  "human_attention": "yes",
  "notes": "14 extension-host crashes over 4 sessions is worth attention despite the AI-generated root-cause guess. Ask for a Windows crash dump (`%LOCALAPPDATA%\\CrashDumps`) and whether it reproduces with only Cline enabled \u2014 the reporter also runs Continue.continue and buuug7.GBK2UTF8. Retest on apps/vscode since the flooding DEBUG loop it blames no longer exists.",
  "cluster": "extension-host-crash"
 },
 {
  "number": 11379,
  "title": "CLI v3.0.21 SAP AI Core provider reads credentials from wrong key namespace \u2014 \"missing required configuration: sap.clientId, sap.tokenUrl",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The error text comes from `buildSapServiceKey` in `sdk/packages/llms/src/providers/vendors/community.ts:253-255`, which reads `options.clientId/clientSecret/tokenUrl`. The CLI writes those under `settings.sap` (see `apps/cli/src/tui/utils/provider-config-values.ts:47` and `apps/cli/src/utils/provider-readiness.ts:46`), and that block was simply never forwarded into gateway options. Commit b56ce72fc (\"fix(core): forward SAP provider options to gateway\", #11756, 2026-06-23) added exactly `if (config.providerId === \"sapaicore\") { Object.assign(options, config.sap); }` at `sdk/packages/core/src/services/llms/handler-factory.ts:78`.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Fixed by b56ce72fc (#11756); the companion VS Code wiring is 8da5ffa87 (#11745, adds `apps/vscode/src/sdk/sap-config.ts`). Closeable with a request to retry on CLI >= 3.0.30.",
  "cluster": "sap-ai-core"
 },
 {
  "number": 11380,
  "title": "Even after enabling prompts, no system messages will pop up when completing tasks in plan mode",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`enableNotifications` is a dead setting on main: it is declared in `apps/vscode/src/shared/AutoApprovalSettings.ts:25` and read/written by the settings plumbing (`updateAutoApprovalSettings.ts`, `updateTaskSettings.ts`, `newTask.ts`) and the webview toggle (`webview-ui/src/hooks/useAutoApproveActions.ts`), but nothing consumes it to emit a notification. There is no `showSystemNotification` anywhere in the repo, no `node-notifier` dependency, and no `new Notification(...)` in the webview; the only `notification` concept left is the user-authored hook payload (`NotificationData` in `apps/vscode/src/core/hooks/hook-factory.ts:125`).",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Worse on main than reported: the reporter says act mode notifies and plan mode doesn't, but on the SDK extension neither does \u2014 the toggle has no consumer at all. Either wire the emitter (including a plan-mode completion trigger) or remove the dead toggle.",
  "cluster": ""
 },
 {
  "number": 11391,
  "title": "[Windows specific] Cline: Cline is getting stuck for fraction of seconds whenever user tries to delete recent history",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The deletion path on main is serial and re-lists everything per item: `apps/vscode/src/core/controller/task/deleteTasksWithIds.ts:35` loops `await deleteTaskWithId(controller, id)`, and each iteration does `await controller.deleteTaskFromState(id)` followed by a full `await controller.postStateToWebview()`. `deleteTaskFromState` in `apps/vscode/src/sdk/sdk-task-history.ts:557` deletes the session and then re-runs `listHistory()`, which hydrates every remaining record and calls `getCachedTaskSize` (per-item disk stat). That is a plausible source of the observed sub-second UI stall, and it is worse on Windows where per-file stat is more expensive.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "low",
  "repro": "1. Build and launch the SDK extension: `cd apps/vscode && bun run build:webview && bun esbuild.mjs`, then `DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <folder>` (reporter's environment is Windows 10 22H2 + VS Code 1.123).\n2. Accumulate a large history \u2014 at least ~100 tasks in `~/.cline` \u2014 so `listHistory()` has real work to do.\n3. Open the History view, search for something so recents are populated.\n4. Delete one task from History and confirm the modal.\n5. Immediately navigate back into History or scroll it.\nExpected: no perceptible freeze; the list updates incrementally.\nActual: the webview stalls for a fraction of a second (JetBrains shows a momentarily blank history section).\nInstrument by timing `deleteTaskFromState` and `postStateToWebview` in `apps/vscode/src/core/controller/task/deleteTasksWithIds.ts`.",
  "human_attention": "yes",
  "notes": "QA-filed; maxpaulus43 could not reproduce on nightly but the reporter re-confirmed on both VS Code and JetBrains on Windows. History size is the likely missing variable. Fix direction: delete all ids first, then post state once, and avoid re-hydrating sizes on every delete.",
  "cluster": ""
 },
 {
  "number": 11394,
  "title": "DS4 says \"The `execute_command` tool launches non-interactive `/bin/zsh` that doesn't source `.zshrc` ...\"",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`getShellArgs` in `sdk/packages/shared/src/parse/shell.ts:52` returns `[\"-c\", command]` for every posix shell \u2014 no `-l` and no `-i` \u2014 and `spawnAndCollect` in `sdk/packages/core/src/extensions/tools/executors/bash.ts:136` spawns with `env: { ...process.env, ...config.env }`. zsh only sources `.zshrc` for interactive shells, so pyenv shims installed by `.zshrc` are never on PATH. This is now the default path in the extension too: `vscodeTerminalExecutionMode` defaults to `backgroundExec` (`apps/vscode/src/shared/storage/state-keys.ts:78`), which routes `execute_command` through this executor rather than a VS Code terminal.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs a product decision, not just a patch: sourcing rc files makes agent commands match the user's interactive terminal (pyenv/nvm/asdf shims) but costs startup time and adds nondeterminism. Owner is @cline/shared (`getShellArgs`) plus the `shell` option in `sdk/packages/core/src/extensions/tools/types.ts:304`.",
  "cluster": ""
 },
 {
  "number": 11402,
  "title": "Cline ignores documented .clinerules CLI-safety rules on Windows PowerShell, causing terminal stalls that need a window reload",
  "area": "vscode-new",
  "kind": "feature-request",
  "relevant_to_main": "unsure",
  "evidence": "The blamed root cause \u2014 dependence on VS Code shell-integration completion markers \u2014 is no longer the default path on main. `vscodeTerminalExecutionMode` defaults to `backgroundExec` (`apps/vscode/src/shared/storage/state-keys.ts:78`, set by commit 12703bf40 / #11972), which uses `spawnAndCollect` in `sdk/packages/core/src/extensions/tools/executors/bash.ts` with piped stdio and a 30s default timeout that kills the whole process tree (`definitions.ts:460`, `bashTimeoutMs ?? 30000`). Indefinite hangs are therefore not possible in the default mode; only `vscodeTerminal` mode still uses the 1-hour `VSCODE_FOREGROUND_RUN_COMMANDS_TIMEOUT_MS` (`apps/vscode/src/sdk/vscode-run-commands-tool.ts:47`). No pre-flight command validator/rewriter exists on main.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Two things bundled: (a) the PowerShell stall, addressed by the backgroundExec default + timeout in 12703bf40 (#11972); (b) a request for a validator that rewrites known-unsafe command forms against the user's own .clinerules, which is speculative and derived from one project's private ruleset. Closeable with an explanation of (a) and a pointer to hooks for (b).",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 11404,
  "title": "Zed restart triggers ACP error -32601 \"Method not found\": session/load when reloading Cline",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`AcpAgent.initialize` advertises `agentCapabilities: { loadSession: true }` at `apps/cli/src/acp/acpAgent.ts:94`, but the class implements only `initialize`, `newSession`, `prompt`, `cancel`, `setSessionMode`, `unstable_setSessionModel`, `setSessionConfigOption`, `authenticate` and `shutdown` \u2014 there is no `loadSession` method. `loadSession` is optional on the protocol's `Agent` interface (`@agentclientprotocol/sdk@0.16.1` `dist/acp.d.ts:699`, `loadSession?(params)`), so the unimplemented method falls through to -32601 exactly as the Zed logs show.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Two valid fixes: implement `loadSession` (rehydrating from the CLI session store, which already supports resume) or stop advertising `loadSession: true` in the capability block. Not Windows-specific despite the report \u2014 the capability mismatch is unconditional.",
  "cluster": "acp-provider-gating"
 },
 {
  "number": 11406,
  "title": "Provider request: add AIML API preset",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "No `aimlapi` id exists in `sdk/packages/llms/src/providers/ids.ts` or in `BUILTIN_SPECS` in `sdk/packages/llms/src/providers/builtins.ts`. The reporter's proposed scope is accurate against main: it mirrors the existing `aihubmix` preset at `sdk/packages/llms/src/providers/builtins.ts:737` (id, `apiKeyEnv`, `modelsProviderId`, `defaults.baseUrl`) plus a `sdk/packages/llms/src/providers/provider-keys.ts` catalog mapping, with no new handler needed because the OpenAI-compatible family already covers `Authorization: Bearer`.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Contributor did the code survey and is waiting on a go/no-go before opening a small PR. Same policy question as #11374 \u2014 worth answering both at once.",
  "cluster": ""
 },
 {
  "number": 11415,
  "title": "@cline/agents usage fails during browser build",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Still broken on main. `sdk/packages/agents/src/agent-runtime.ts:1` imports `createGateway` from `@cline/llms` and calls it at `:114`, and `sdk/packages/agents/bun.mts` externalizes `@cline/llms` (`const external = [\"@cline/llms\", \"nanoid\"]`). But `createGateway` is exported only from the node entry \u2014 `sdk/packages/llms/src/index.ts:94` (`export { createGateway, DefaultGateway } from \"./providers/gateway\"`) \u2014 and `sdk/packages/llms/src/index.browser.ts` has zero gateway exports. Since `@cline/llms` resolves to `./dist/index.browser.js` under the `browser` condition (`sdk/packages/llms/package.json` exports map), a Vite browser build gets `createGateway === undefined`, exactly as reported.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Reported against 0.0.42; agents/llms are now 0.0.65 and the export gap is unchanged. `@cline/agents` self-describes as a 'Browser-safe agent runtime' with a `browser` export condition, so this is a real contract violation. Fix is either to re-export a browser-safe `createGateway` from `index.browser.ts` or to load it lazily behind the prebuilt-model path in `resolveRuntimeConfig`.",
  "cluster": ""
 },
 {
  "number": 11417,
  "title": "Cline: No endpoints found that support tool use. Try disabling \"skills\". error is shown trying to search using open router",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The message is passed through verbatim from OpenRouter \u2014 grep for \"No endpoints found\" and \"support tool use\" returns nothing in /workspace. The actionable gap is on our side: `supportsTools` is declared in `apps/vscode/src/shared/api.ts:105` and never read anywhere in the repo, and `apps/vscode/src/core/controller/models/refreshOpenRouterModels.ts` only inspects `supported_parameters` for reasoning (`include_reasoning`/`reasoning`), never for `tools`. So Cline offers tool-incapable OpenRouter models (`openrouter/bodybuilder` in the report) and lets the provider 404 reach the user.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "low",
  "repro": "1. Build and launch the SDK extension: `cd apps/vscode && bun run build:webview && bun esbuild.mjs`, then `DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <folder>`.\n2. Settings -> API Provider -> OpenRouter, enter an OpenRouter key.\n3. Pick a model whose OpenRouter `supported_parameters` does NOT include `tools` (the report used `openrouter/bodybuilder`; `openai/gpt-5-mini` also failed for them).\n4. Send any prompt, e.g. \"write a hello world program\".\nExpected: the model picker either hides/flags tool-incapable models, or Cline surfaces an actionable message naming the model and suggesting a tool-capable one.\nActual: OpenRouter's raw 'No endpoints found that support tool use...' error is shown.",
  "human_attention": "yes",
  "notes": "QA-filed against both VS Code and JetBrains. Provider-side cause, Cline-side gap: wire `supportsTools` (populate it in refreshOpenRouterModels from `supported_parameters`) and gate the model list or the error message on it.",
  "cluster": ""
 },
 {
  "number": 11439,
  "title": "replace_in_file says SEARCH block does not match when displayed text exists as a partial-line match",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The tool being described does not exist on main. There is no `replace_in_file` in the SDK tool definitions \u2014 `sdk/packages/core/src/extensions/tools/definitions.ts:664` registers `editor`, whose `replaceInFile` (`sdk/packages/core/src/extensions/tools/executors/editor.ts:170`) does plain substring matching via `countOccurrences` + `content.replace`, with no SEARCH/REPLACE blocks and no newline-terminated line spans. A partial-line match therefore succeeds on main, and the error text differs (`No replacement performed: text not found in <path>.` at `editor.ts:184`); the reported string \"does not match anything in the file\" appears nowhere in the repo. The `replace_in_file` names left in `apps/vscode/src/sdk/message-translator.ts` and `sdk-tool-policies.ts` are legacy-name mappings for rendering historical messages.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed against 3.89.0. The exact-span line-boundary semantics that produced the misleading message were replaced by substring `old_str`/`new_str` editing at the SDK cutover.",
  "cluster": "legacy-replace-in-file"
 },
 {
  "number": 11446,
  "title": "Plan/Act separate OpenAI Compatible base URLs are coupled in v3.89.0",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Structurally still coupled on main. `openAiBaseUrl` is a single non-mode-scoped state key (`apps/vscode/src/shared/storage/state-keys.ts:120`), while the OpenAI-compatible model selection IS mode-scoped (`planModeOpenAiModelId` :163 / `actModeOpenAiModelId` :209). `resolveBaseUrl` in `apps/vscode/src/sdk/cline-session-factory.ts:607` maps `openai -> \"openAiBaseUrl\"` with no mode argument, so plan and act necessarily resolve the same URL. The same holds for every other provider in that map (anthropic, ollama, gemini, litellm, ...).",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Not a save/overwrite race \u2014 there is simply no per-mode base URL key. Fixing it means adding `planModeOpenAiBaseUrl`/`actModeOpenAiBaseUrl` (and deciding whether to do the same for the other providers in `resolveBaseUrl`) plus a migration.",
  "cluster": ""
 },
 {
  "number": 11452,
  "title": "Cline stops working after certain comands",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The hang described (Cline waits forever for a terminal-completion signal, shows 'skipped', chat freezes until the extension is restarted) depended on VS Code shell integration, which is no longer the default. Commit 12703bf40 (\"Improve VS Code terminal reliability: OSC 633 parser, exit codes, timeout handling\", #11972, 2026-07-14) flipped `vscodeTerminalExecutionMode` to default `backgroundExec` (`apps/vscode/src/shared/storage/state-keys.ts:78`) and rewrote `VscodeTerminalProcess`/added `osc633Parser.ts`. In backgroundExec, commands run through `spawnAndCollect` (`sdk/packages/core/src/extensions/tools/executors/bash.ts`) with piped stdio and a 30s default timeout that kills the process tree (`definitions.ts:460`).",
  "verdict": "duplicate",
  "duplicate_of": 12079,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed against 3.89.0. Closeable with a request to retest; if it recurs, it would be a distinct backgroundExec bug (long-running servers hitting the 30s timeout is a different, expected behavior).",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 11456,
  "title": "Fresh VS Code Insiders + fresh Cline: Anthropic connection error despite direct API success; OpenAI Native GPT-5.5 fails on tool calls",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Three separate symptoms, two of which are addressed on main. (a) The commenter's custom-base-URL-ignored problem: `resolveBaseUrl` in `apps/vscode/src/sdk/cline-session-factory.ts:607` now maps `anthropic -> \"anthropicBaseUrl\"`, and the commenter reported 3.89.2 fixed it. (b) The OpenAI-native `400 Item 'fc_...' was provided without its required 'reasoning' item` came from hand-rolled Responses-API transcript assembly; on main `openai-native` routes to the official AI SDK provider (`sdk/packages/llms/src/providers/compat.ts:144` -> `createOpenAIProvider`, defined as `createAiSdkProvider(\"openai\")` at `ai-sdk.ts:1306`), which owns reasoning/function-call item pairing. (c) The OP's own bare `[anthropic] Connection error.` with no custom base URL has no code evidence at all \u2014 related work exists (141a61fd4, #11462, threads proxy/CA-aware fetch into the SDK inference path) but it notes VS Code already used global fetch.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Ask the OP to retest on the SDK extension and attach the full Cline output channel plus proxy/VPN/corporate-CA details for the Anthropic connection error \u2014 that is the only unresolved symptom. The two comment threads (custom base URL, 3.89.2) are a different bug from the OP's and should not be conflated.",
  "cluster": ""
 },
 {
  "number": 11467,
  "title": "PowerShell window to flash on every command execution",
  "area": "hub",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Exactly the reported fix is on main: `spawnDetachedHubServer` in `sdk/packages/core/src/hub/daemon/index.ts:217` now passes `windowsHide: true` with the comment 'Prevent a console window from appearing on Windows; detached processes otherwise allocate a new visible console.' `git log -S` on that option attributes it to commit 7d119351b (\"fix(cli): suppress flickering console windows on Windows\", #11408).",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Fixed by 7d119351b (#11408). Accurate report \u2014 the reporter named the exact function and the exact missing option.",
  "cluster": ""
 },
 {
  "number": 11473,
  "title": "Cline CLI error during run_commands: emitt invalid json arguments",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The exact error text is still produced on main by `parseToolInput`/`parseToolArguments` in `sdk/packages/agents/src/agent-runtime.ts` (lines 1726 and 1771), and `parseToolArguments` is a plain `JSON.parse` with no repair. Two partial mitigations landed after the report: `292133989` (#11478) caps repeated DSML tool-call markup in `sdk/packages/core/src/session/services/message-builder.ts`, and `96b29d787` (#11803) adds `normalizeJsonLikeStringsForSchema` (`sdk/packages/shared/src/parse/json.ts`) used at `agent-runtime.ts:1366`. Neither covers the reported case: `repairMalformedToolCall` in `sdk/packages/llms/src/providers/ai-sdk.ts:445` delegates to `parseJsonStream`, which returns the input unchanged when the text does not start with `{`/`[` \u2014 which is exactly the `<|DSML|tool_calls>` fragment the model emits here.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. Build/run the CLI from main: `bun run cli -i` in a mid-size Python repo (the reporter used a Django project).\n2. Configure a provider whose model emits DeepSeek-style pseudo tool-call markup instead of native tool calls: `-P openai-compatible` pointed at an Azure Foundry (or equivalent) endpoint serving DeepSeek-V4-Flash.\n3. Switch to Plan mode (reporter notes it is far more frequent in Plan than in Act).\n4. Ask a question that pushes the model toward repeated `search_codebase` calls with array arguments, e.g. \"where does this project send email? find EmailMessage / send_mail usages\".\n5. Expected: the tool call is parsed (or the malformed text is repaired) and the search runs.\n   Actual: `Error: Tool call search_codebase emitted invalid JSON arguments: Tool call arguments could not be parsed as JSON...` and the transcript fills with raw `<|DSML|tool_calls>` text; the agent retries in a loop.",
  "human_attention": "yes",
  "notes": "Owned by @cline/agents (parse boundary) with a possible assist in @cline/llms `repairMalformedToolCall`. Worth deciding whether text-mode pseudo tool-call markup should be parsed as a fallback or whether these models should be flagged as unsupported for native tool calling.",
  "cluster": ""
 },
 {
  "number": 11476,
  "title": "Cline: Improve code action starts execution in existing thread Instead of creating a new thread",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "On main all three code actions start a fresh task: `apps/vscode/src/core/controller/commands/improveWithCline.ts:40`, `explainWithCline.ts:33` and `fixWithCline.ts:13` all call `controller.initTask(...)`, and only the notebook branch (`notebookContext && controller.task`) reuses the active task. `SdkTaskStartCoordinator.initTask` (`apps/vscode/src/sdk/sdk-task-start-coordinator.ts:64`) calls `clearTask()` and then `sessions.startNewSession(...)` with a newly minted session id, so the described 'continues in the previous thread' behavior has no code path.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Reported from JetBrains Rider with plugin bot.cline 1.1.59-nightly; the JetBrains action wiring lives in the separate JetBrains plugin repo, so if it still reproduces there it belongs to that repo, not to `main`.",
  "cluster": ""
 },
 {
  "number": 11490,
  "title": "GCP Vertex AI provider exposes gemini-3.5-flash but Vertex returns 404 model not found",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`gemini-3.5-flash` is still offered under the `vertex` provider in `sdk/packages/llms/src/catalog/catalog.generated.ts` (vertex block starting at line 79206) and is in the global-endpoint allowlist at `apps/vscode/src/sdk/model-catalog/vertex-global-endpoint.ts:64`. That catalog is auto-generated from models.dev and carries no per-project/per-region availability, so the picker can still offer a publisher model GCP has not enabled for the user's project \u2014 the 404 itself comes from Vertex, not from Cline code. The secondary complaint is already handled: `sdk/packages/llms/src/providers/routing/provider-option-rules.ts:165` sends `thinkingBudget: 128` (not 0) for Gemini Pro when reasoning is disabled, added in `cfc225071`.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Actionable part is a product decision: whether to filter the Vertex model picker by what the project/region actually publishes (e.g. probing the Vertex publisher-model list) or to surface a clearer 'not enabled for this project' error. Ignore the FuturMix comment \u2014 it is vendor self-promotion and its diagnosis of the Cline side is unsupported.",
  "cluster": "stale-model-catalog"
 },
 {
  "number": 11494,
  "title": "Cline cli 3 does not show cost",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The CLI status bar renders cost purely from accumulated `totalCost` (`formatStatusBarUsageText` in `apps/cli/src/tui/components/status-bar.tsx`, gated by `shouldShowProviderUsageCost` in `sdk/packages/llms/src/providers/billing.ts`, which defaults to 'show'). `totalCost` is summed in `sdk/packages/core/src/services/usage.ts` from per-message `metrics.cost`, which `sdk/packages/llms/src/providers/ai-sdk.ts` derives from an upstream `cost` field or the catalog `n` pricing block. A proxied Azure Foundry model or a LiteLLM deployment has neither, so it accumulates 0 \u2014 and unlike the VS Code webview (`apps/vscode/webview-ui/src/components/settings/providers/OpenAICompatible.tsx`, which exposes `inputPrice`/`outputPrice`), nothing in `apps/cli/src` lets the user supply pricing.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "low",
  "repro": "1. Run the CLI from main: `bun run cli -i`.\n2. Configure an OpenAI-compatible provider that is not in the model catalog \u2014 e.g. a LiteLLM proxy or an Azure Foundry proxy serving `DeepSeek-V4-Flash` \u2014 with `-P openai-compatible -m <model> --base-url <proxy>/v1`.\n3. Run any prompt that produces a few turns (\"tell me a story about this repo\").\n4. Expected: the status bar / exit summary shows a non-zero USD cost as CLI 2.18 did.\n   Actual: token counts appear but cost stays at $0.00, because no pricing metadata exists for that model and the CLI has no way to enter it.",
  "human_attention": "yes",
  "notes": "Two possible fixes: let the CLI accept per-model `inputPrice`/`outputPrice` like the VS Code OpenAI-compatible settings do, and/or read the `usage.cost` field LiteLLM already returns. Second reporter (zfleeman) confirms on 3.0.38 with LiteLLM.",
  "cluster": ""
 },
 {
  "number": 11495,
  "title": "always waiting when execute short command",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The legacy foreground terminal executor this was filed against no longer exists. On main `apps/vscode/src/hosts/vscode/terminal/` contains a stateful `osc633Parser.ts` plus `shellPromptHeuristics.ts` and idle-timeout hang recovery introduced by `12703bf40` (#11972), whose description names exactly this failure mode ('trusted markers that sometimes never arrive ... hangs requiring an extension restart'), and `8b4d1973b` (#12352) adds explicit indeterminate reporting for commands VS Code cannot observe.",
  "verdict": "duplicate",
  "duplicate_of": 12079,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Report has no repro steps beyond a screenshot; close pointing at the terminal rewrite in #11972 / #12352 and ask for a fresh report if it recurs on the SDK extension.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 11497,
  "title": "nemotron3 ultra returning errors via openrouter api connection",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The pasted payload is OpenRouter's own upstream failure (`code: 504`, `metadata.error_type: \"timeout\"`) surfaced verbatim: `sdk/packages/llms/src/providers/ai-sdk.ts` captures the provider error in `onError` and yields `{ type: \"finish\", reason: \"error\" }` with the message untouched. Nothing in `sdk/packages/llms` special-cases `nvidia/nemotron-3-ultra-550b-a55b:free`; it is a free-tier OpenRouter route timing out upstream.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Provider-side outage on a `:free` OpenRouter route. Safe to close pointing at OpenRouter.",
  "cluster": ""
 },
 {
  "number": 11506,
  "title": "Application cant recover after an error occurs like timeout.",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The symptom (one API error poisons the session so every later message fails until you fork) matches the history-poisoning bug fixed after this report by `23c9bf1c0` (#11726): `AgentRuntime` used to persist an assistant message with `content: []` after a failed stream, which the formatter later rewrote to the `ERROR: EMPTY CONTENT` sentinel (`sdk/packages/shared/src/llms/ai-sdk-format.ts:75`). Main now rejects that turn up front \u2014 `sdk/packages/agents/src/agent-runtime.ts:645-651` throws the upstream error or `Model returned empty response` instead of persisting it. Cannot confirm it is the same failure without the reporter's session.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Ask the reporter to retest on CLI >= 3.0.30 (first release containing #11726) and, if it still recurs, to attach the session JSON from `~/.cline/data` showing the state after the timeout.",
  "cluster": ""
 },
 {
  "number": 11523,
  "title": "plan_mode_respond in Plan Mode is not recognized as a valid tool use \u2014 \"[ERROR] You did not use a tool in your previous response\"",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The enforcement no longer exists on main. `formatResponse.noToolsUsed` (`apps/vscode/src/core/prompts/responses.ts:33`) has zero callers repo-wide; `plan_mode_respond` survives only as a legacy enum value and webview label (`apps/vscode/src/shared/tools.ts:23`) and appears nowhere under `sdk/packages`; SDK plan mode uses `switch_to_act_mode` (`sdk/packages/shared/src/prompt/cline.ts`); and the consecutive-mistake tracker has no 'no tool used' reason at all (`sdk/packages/core/src/runtime/safety/mistake-tracker.ts:39-42` \u2014 only `api_error`, `invalid_tool_call`, `tool_execution_failed`). PR #11538 was closed because it patched `apps/vscode/src/core/task/*`, which on main holds only `focus-chain/` and `tools/`, not the task engine.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter asked for a status update on 2026-07-24 and deserves a reply: the validator was removed with the SDK rewrite, which is why #11538 was closed rather than merged.",
  "cluster": "xml-tool-parser-rejects-valid-call"
 },
 {
  "number": 11527,
  "title": "Webview crashes with \"React error #31\" when opening conversations from history",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The blast radius the reporter describes (whole webview blanks, sidebar buttons dead) is still possible on main: `apps/vscode/webview-ui/src/components/chat/ChatErrorBoundary.tsx` is mounted only inside the three MCP display components (`McpResponseDisplay.tsx:277`, `LinkPreview.tsx:406`, `ImagePreview.tsx:330`), so a bad React child anywhere else in the chat tree takes down the root. The new extension still reads legacy task directories that contain the Anthropic-style `tool_use`/`thinking` blocks blamed here (`apps/vscode/src/core/storage/disk.ts:19`, `apps/vscode/src/sdk/legacy-task-handling.ts`). I could not find a commit fixing a React #31 in the chat renderer.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Run the SDK extension from main: `DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <folder>`.\n2. Seed a legacy task directory under the extension's global storage `tasks/<taskId>/` with an `api_conversation_history.json` whose assistant messages contain structured `tool_use` and `thinking` (with `signature`) content blocks, plus a matching `ui_messages.json` \u2014 i.e. a task recorded by 3.x with multi-turn tool use.\n3. Open the Cline sidebar, click the history (clock) icon, and open that task.\n4. Expected: the conversation renders (with the Legacy badge added in `2b48dc411`).\n   Actual (to verify): webview goes blank, DevTools console shows 'Minified React error #31: Objects are not valid as a React child', extension host logs 'Channel has been closed'.\n5. Regardless of outcome, note whether a chat-level error boundary would have contained it \u2014 today none exists outside the MCP components.",
  "human_attention": "yes",
  "notes": "Even if the specific #31 is gone, wrapping the chat message list in `ChatErrorBoundary` is a cheap resilience fix: today any render throw blanks the entire webview.",
  "cluster": "grey-blank-webview"
 },
 {
  "number": 11530,
  "title": "AI always gets stuck when thinking and automatically operating the diff editor",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The legacy `VscodeDiffViewProvider` this was filed against is gone; main drives edits through `apps/vscode/src/sdk/sdk-diff-edit-coordinator.ts`, and `bcf8c1d03` (#12558) specifically bounds a hung `vscode.diff` open behind a 5s race so a stalled preview no longer burns the tool timeout. The report has no repro steps and mixes the stall with a 30 MB conversation file and multi-GB heap growth, so it may be two separate problems (diff stall vs. unbounded conversation size).",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Ask for a retest on the SDK extension after #12558; if the memory half persists, it should be refiled with the conversation size and a heap snapshot.",
  "cluster": "gpt55-file-create-stall"
 },
 {
  "number": 11531,
  "title": "CLI TUI: mouse right-click copy/paste and drag-select don't work in VTE terminals (Ptyxis/GNOME Terminal)",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The reporter's root-cause analysis checks out against main: `apps/cli/src/tui/index.tsx:12` calls `createCliRenderer({ exitOnCtrlC: false, autoFocus: false, enableMouseMovement: true })` unconditionally, so xterm mouse reporting is always on and VTE only returns selection/context-menu to itself while Shift is held. There is no opt-out \u2014 no `--no-mouse`/`disableMouse` flag or config key anywhere under `apps/cli/src`, and `history-standalone.tsx` and `commands/auth.ts` construct renderers the same way.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Smallest fix is a `--no-mouse` flag (or config key) threaded into `enableMouseMovement`. Note the community-team comment could not reproduce on Ptyxis, but a second reporter confirmed on GNOME Terminal 3.52 \u2014 worth checking whether VTE version or the `enableMouseMovement` (motion-tracking) mode specifically is the differentiator.",
  "cluster": "tui-mouse-capture"
 },
 {
  "number": 11537,
  "title": "Integration idea: BGPT evidence retrieval tool",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "No defect or code claim to verify. The author is promoting their own `bgpt-mcp` server; Cline already loads arbitrary MCP servers with no repo change required (`apps/cli/src/commands/mcp.ts`, MCP wiring in `sdk/packages/core`), so nothing in this repo needs to be modified for the described integration.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Vendor self-promotion; belongs in the MCP marketplace/discussions, not an issue.",
  "cluster": ""
 },
 {
  "number": 11539,
  "title": "cline v3.x crashes with SIGILL on pre-Haswell CPUs (no AVX2) due to Bun-embedded binary",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Still unfixed on main: `getBunTarget()` in `apps/cli/script/build.ts:163-167` returns plain `bun-${os}-${arch}` compile targets with no `-baseline` variant, and `buildCompiledBinary` passes that straight to `Bun.build({ compile: { target } })` (line 194-200). The Node wrapper `apps/cli/bin/cline` has no CPU-capability preflight (no AVX/cpuinfo check anywhere in it), so users get a bare SIGILL. Fix PRs #11412 (Linux, open) and #12273 (Windows, draft) are both unmerged.",
  "verdict": "duplicate",
  "duplicate_of": 10514,
  "severity": "critical",
  "repro": null,
  "human_attention": "yes",
  "notes": "Duplicate of the earlier #10514 (2026-05-02) but this is the better-written report and carries the Windows confirmation (0xC0000005 on Ivy Bridge, CLI 3.0.40). Cheapest partial mitigation is the reporter's option 2: an AVX2 preflight in `apps/cli/bin/cline` before exec'ing the Bun binary.",
  "cluster": "cli-bun-binary-crash"
 },
 {
  "number": 11542,
  "title": "Cline enters a high-volume request loop under repeated tool-use completions",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Confirmed on main, and the contributor's analysis in the thread is accurate. `sdk/packages/agents/src/agent-runtime.ts:629-631` loops `while (this.config.maxIterations === undefined || this.state.iteration < this.config.maxIterations)`, i.e. unbounded when unset. The CLI never sets it: `apps/cli/src/main.ts:1092` populates only `maxConsecutiveMistakes: args.retries ?? 3`, and the only other `maxIterations` reference in `apps/cli/src` is the schedule wizard (`apps/cli/src/wizards/schedule/index.ts:223`). Neither existing guard catches this \u2014 the mistake counter only counts errors, and loop detection only catches identical consecutive calls, while the reproducer varies the command each turn.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs a maintainer decision on the default cap and whether it should be user-overridable (`--max-iterations`); the reporter's review notes on the proposed PR ask for a CLI-level regression test in `apps/cli/src/main.test.ts` rather than an AgentRuntime test. Unbounded provider spend makes this worth prioritizing.",
  "cluster": ""
 },
 {
  "number": 11544,
  "title": "Improve developer experience",
  "area": "infra",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "I ran the exact `postprotos` command from `apps/vscode` in check mode (`biome format --config-path ./biome.jsonc src/shared/proto src/core/controller src/hosts/ webview-ui/src/services src/generated --no-errors-on-unmatched`) and it reported 'Checked 303 files in 83ms. No fixes applied' \u2014 the committed codegen and controller/host sources are already format-clean, so the script no longer rewrites hundreds of files. The second complaint (root uses bun, `apps/vscode` uses npm) was resolved by `82d1846a4` (#11632, 'Migrate apps/vscode from npm/node to bun'); `apps/vscode/package.json` scripts now run under bun.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "The `.git-blame-ignore-revs` suggestion is still worth keeping in mind for future bulk reformats, but the reported symptom no longer reproduces.",
  "cluster": ""
 },
 {
  "number": 11546,
  "title": "Guard against accepting attempt_completion when task progress is incomplete or verification fails",
  "area": "sdk-core",
  "kind": "feature-request",
  "relevant_to_main": "unsure",
  "evidence": "The mechanics this asks to guard no longer exist in the form described. `attempt_completion` survives only as an alias to `submit_and_exit` (`sdk/packages/core/src/runtime/orchestration/runtime-builder.ts:88`), and `createSubmitAndExitTool` (`sdk/packages/core/src/extensions/tools/definitions.ts:797-827`) takes only `summary` and `verified` \u2014 there is no completion command whose exit code could be re-fed. `task_progress` / focus-chain has no presence under `sdk/packages` at all, so the 'unchecked items' precondition has no source of truth on the SDK side.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Idea is still sound but needs re-scoping to the SDK model: the natural hook is the `verified` field on `SubmitInput` plus a host-side `completesRun` gate, not `task_progress`. Product/SDK-owner call.",
  "cluster": ""
 },
 {
  "number": 11547,
  "title": "Cline Generating unnecessary chat histories",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The companion report the author links, #11543 ('Failed to delete chat history'), was closed by maintainer @maxpaulus43 as fixed on nightly. The trigger described here (Cline hangs, Enter stops responding, relaunch produces several same-timestamp entries) overlaps `0220b9a50` (#11839, 'queue chat submits during active turns'), and the history writer itself was rewritten in `2b48dc411` (`apps/vscode/src/sdk/sdk-task-history.ts`, ~277 lines replaced). I found no code on main that would deliberately mint duplicate history items, but also no test asserting dedupe.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Ask the reporter to retest on the SDK extension now that #11543 is fixed; if duplicates still appear, the interesting artifact is the on-disk task directory listing with identical creation timestamps.",
  "cluster": "task-history-loss"
 },
 {
  "number": 11550,
  "title": "Terminal commands time out indefinitely when multiple terminals accumulate",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Fixed by `8b4d1973b` (#12352, 'fix(vscode): reclaim unobserved fallback terminals'), whose PR body names this issue directly ('retaining every terminal causes the accumulation reported in #11550'). It reworks `apps/vscode/src/hosts/vscode/terminal/VscodeTerminalManager.ts` / `VscodeTerminalRegistry.ts` / `VscodeTerminalProcess.ts` so `sendText` fallback terminals are reclaimed at the next acquisition and unobservable commands are reported indeterminate rather than hanging. It builds on the bounded idle-timeout hang recovery from `12703bf40` (#11972).",
  "verdict": "duplicate",
  "duplicate_of": 12079,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Merged but the issue was never closed \u2014 safe to close citing #12352.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 11551,
  "title": "Cannot perform any actions",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The failing machinery is gone from main. The message 'Cline tried to use <tool> without value for required parameter' has no producer left: `formatResponse.missingToolParameterError` (`apps/vscode/src/core/prompts/responses.ts:48`) has zero callers, and the string only survives in a fixture at `evals/analysis/src/__tests__/classifier.test.ts:22`. `write_to_file` itself exists only as an alias to `editor` (`sdk/packages/core/src/runtime/orchestration/runtime-builder.ts:97`), and tool arguments are now native JSON-schema tool calls validated in `sdk/packages/agents/src/agent-runtime.ts`, not the XML parser that was choking here.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Classic legacy XML tool-parse failure (model kept emitting `absolutePath` while the parser wanted `path`). Auto-closeable pointing at the SDK extension.",
  "cluster": ""
 },
 {
  "number": 11562,
  "title": "fails to activate",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "This is an auto-generated VS Code issue-reporter dump against extension 3.89.2 whose entire body is 'failed to activate for long' plus A/B experiment flags. There is no activation error, no extension-host log, and no repro, so there is nothing in `apps/vscode/src/extension.ts` (the `activate` entry point on main) to check it against.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Unusable as filed. Auto-closeable with a request for the 'Cline' output channel and extension-host log.",
  "cluster": "extension-not-activating"
 },
 {
  "number": 11564,
  "title": "Cline CLI Websocket drop",
  "area": "hub",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The reporter himself confirms it is resolved ('In 3.0.27 this seems to be fine') and correctly identified the cause: the shared-hub singleton work, `6138bdfe4` (#11372, 'Enforce a production singleton Cline Hub') under `sdk/packages/core/src/hub`, which is the 3.0.24 changelog line he quoted. Related follow-ups `81792d20c` (#11258, keep hub daemon alive on runtime abort) and `107bce75b` (#11259, recover stale interactive sessions) landed in the same window.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter-confirmed fixed; just needs closing.",
  "cluster": ""
 },
 {
  "number": 11568,
  "title": "# Bug: `cline kanban` server leaks ~1 GB/sec at startup \u2192 JS heap OOM crash",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`cline kanban` contains no server. `apps/cli/src/commands/kanban.ts` only resolves the `kanban` binary on PATH, installs it if missing (`npm install -g kanban@latest`, `KANBAN_INSTALL_COMMANDS`), and spawns it as a child process (`spawnKanbanProcess` -> `spawn(getKanbanCommand(), [], ...)`, line 190). There is no kanban server source anywhere in this repo, so the runaway `fs`-read/UTF-8-decode allocation in the reporter's V8 stack is inside the separately published `kanban` npm package (0.1.68).",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Excellent report \u2014 needs routing to whoever owns the `kanban` npm package rather than triage against this repo. The command is Cline-branded, so a user hitting this has no way to know it is a different codebase.",
  "cluster": ""
 },
 {
  "number": 11576,
  "title": "mac os \u5f00\u53d1\u4eba\u5458:\u91cd\u65b0\u52a0\u8f7d\u7a97\u53e3Developer: Reload Window \u4f1a\u8bdd\u4e22\u5931",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Two-sentence report against legacy 3.89.2 with no repro steps, no logs and no provider info ('Developer: Reload Window loses the session, cannot find the conversation history'). On main the restore path is entirely different \u2014 `apps/vscode/src/sdk/SdkController.ts` resolves tasks via `SdkTaskHistory` (`showTaskWithId`, line 1537) rather than the legacy in-memory task, and `b72d7f1a6` (#12553) recently reworked webview retention across sidebar moves. I could not reproduce the claim from code alone.",
  "verdict": "duplicate",
  "duplicate_of": 11663,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Ask whether the task disappears from the history list entirely or just from the open pane, and retest on the SDK extension.",
  "cluster": "task-history-loss"
 },
 {
  "number": 11578,
  "title": "Cline does not work on Android Studio Quail 1",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Root cause is the IDE runtime, not this repo: Android Studio Quail ships a JBR without JCEF, and a mismatched JCEF generation produces `NoSuchMethodError: JCefAppConfig.isRemoteEnabled()`. Maintainer @mkondratek confirmed the detection/guidance fix landed in the JetBrains plugin (separate repo). Nothing under `apps/vscode/standalone` (the embedded cline-core the plugin consumes) or `sdk/packages` is implicated \u2014 the failure happens before any core code runs, at webview creation.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "JetBrains-plugin-UI only; already owned by a JB maintainer who is keeping it open for the remaining blank-panel case. Workaround in-thread: keep the bundled JBR and install the 'Web Browser (JCEF)' plugin.",
  "cluster": ""
 },
 {
  "number": 11580,
  "title": "Cline kanban Unknown or disabled provider \"openai-compatible\".",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`openai-compatible` is a fully registered built-in provider on main: declared at `sdk/packages/llms/src/providers/ids.ts:17` (`OPENAI_COMPATIBLE = \"openai-compatible\"`), included in `BUILT_IN_PROVIDER_IDS` (line 86), and given a manifest in `sdk/packages/llms/src/providers/builtins.ts:630`. `openai` is in fact an *alias to* `openai-compatible` (`ids.ts:80-84`), the opposite of what the drive-by comment claims. It was restored by `9739e4d11` (#10761) on 2026-05-14, before this report. The error text comes from `ProviderRegistry.resolveModel` (`sdk/packages/llms/src/providers/registry.ts:211`), reached from the external `kanban` app, which `apps/cli/src/commands/kanban.ts` merely spawns and which is not in this repo.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "kanban 0.1.68 almost certainly bundles an older SDK snapshot. Ignore the FuturMix comment \u2014 the suggested `\"provider\": \"openai\"` shape is wrong on main (it is the alias, not the canonical id) and the post is vendor self-promotion.",
  "cluster": ""
 },
 {
  "number": 11583,
  "title": "Sambanova models not updated",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The sambanova builtin spec in `sdk/packages/llms/src/providers/builtins.ts` sets `modelsProviderId: \"sambanova\"` and `provider-keys.ts` maps `modelsDevKey: \"sambanova\"`, but `sdk/packages/llms/src/catalog/catalog.generated.ts` has no `sambanova` key at all (157 provider keys, none matching), and the spec has no `defaultModelId`. Running `MODEL_COLLECTIONS_BY_PROVIDER_ID.sambanova` against the built `@cline/llms` returns 0 models with `defaultModelId: \"default\"`, so on main the SambaNova picker is empty rather than merely stale.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Owned by @cline/llms. models.dev evidently no longer publishes a sambanova provider, so the generated catalog silently drops it. Fix is either a curated `modelsFactory` (like `buildClaudeCodeModels`/`buildOpenAICodexModels`) or a live fetch of https://api.sambanova.ai/v1/models. Reporter is a SambaNova employee offering the canonical list.",
  "cluster": "stale-model-catalog"
 },
 {
  "number": 11592,
  "title": "Suddenly spent lots of credits",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "For the Cline-managed provider the displayed cost comes from the gateway response, not local math: `normalizeUsage` in `sdk/packages/llms/src/providers/ai-sdk.ts` prefers `market_cost`/`cost`/`upstream_inference_cost` from the provider payload and only falls back to `calculateUsageCostFromPricing` (which does correctly subtract cache-read/cache-write tokens from billable input) when no explicit cost is returned. Nothing in /workspace can produce the reported $0.48 for 280k input tokens at $0.43/M, so the number originates server-side in Cline billing, which is not in this repo.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Two independent reporters with matching numbers on deepseek-v4-pro. Needs a maintainer with access to the Cline billing/gateway logs to check whether market_cost was inflated; not resolvable from the OSS repo.",
  "cluster": ""
 },
 {
  "number": 11613,
  "title": "cline_mcp_settings.json \u81ea\u52a8\u51fa\u73b0 JSON \u683c\u5f0f\u9519\u8bef\u5bfc\u81f4 MCP \u914d\u7f6e\u5931\u6548",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`sdk/packages/core/src/extensions/mcp/config-loader.ts` now writes the settings file through `atomicWriteSettingsFile()` (temp file + `renameSync`) guarded by a cross-process populated-directory lock (`tryAcquireSettingsLock`/`settingsLockDir`, `SETTINGS_LOCK_STALE_MS`), with a comment explicitly citing concurrent CLI/VS Code/JetBrains writers producing half-written files. `git log -S atomicWriteSettingsFile` shows both were introduced by 4829f08b3 (2026-06-18), one day after this issue was filed.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Fixed on main by 4829f08b3 \"fix(vscode): reliable MCP OAuth on the SDK extension (ENG-2108, CLINE-2304) (#11529)\", which added the atomic write + lock exactly for the multi-window corruption described here.",
  "cluster": ""
 },
 {
  "number": 11619,
  "title": "Failed to change preferred language",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The whole path exists and looks wired on main: `apps/vscode/webview-ui/src/components/settings/PreferredLanguageSetting.tsx` calls `updateSetting(\"preferredLanguage\", ...)`, `apps/vscode/src/core/controller/state/updateSettings.ts` persists it via `stateManager.setGlobalState(\"preferredLanguage\", ...)`, `getStateToPostToWebview.ts` echoes it back, and `apps/vscode/src/sdk/cline-session-factory.ts` appends `# Preferred Language\\n\\nSpeak in <lang>` to the system prompt. I could not find a defect in that chain.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Report is a single screenshot with no error text; unclear whether the dropdown reverts, the value fails to persist, or the model simply ignores the instruction. Needs the reporter to say which.",
  "cluster": "ui-localization"
 },
 {
  "number": 11622,
  "title": "Cline freezes due to excessive terminal command length",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The VS Code host on main still executes agent commands through a real VS Code terminal: `apps/vscode/src/hosts/vscode/terminal/VscodeTerminalProcess.ts` calls `terminal.shellIntegration.executeCommand(command)` and falls back to `terminal.sendText(command, true)`, and `VscodeTerminalManager.ts` does the same. Nothing in that path or in `sdk/packages/core/src/extensions/tools/definitions.ts` caps command length \u2014 `RUN_COMMAND_QUERY_PREVIEW_LIMIT` only truncates the echo sent back to the model, not what is typed into the pty \u2014 so a multi-kilobyte heredoc is still pasted verbatim into the shell.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Build and launch the SDK extension: `cd apps/vscode && bun run build:webview && bun esbuild.mjs`, then `DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <scratch-repo>`.\n2. Configure a capable model (e.g. anthropic/claude-sonnet-4-6) and switch to Act mode.\n3. Prompt: \"Create a single-page HTML slide deck about X by writing the whole file in one shell command using a heredoc\" so the agent emits one `run_commands` call whose command string is >8KB.\n4. Expected: the command runs to completion (or is chunked/rejected with a clear error) and the turn continues.\n5. Actual (to verify): the terminal receives the giant single line, `VscodeTerminalProcess` never observes the OSC 633 completion marker, and the turn hangs until the window is reloaded.",
  "human_attention": "yes",
  "notes": "Two parts: (a) the executor should bound or chunk oversized command strings instead of relying on the pty, (b) prompt guidance should steer the model to `write_to_file`/editor instead of heredocs. #12703bf40 (OSC 633 parser + timeout handling, 2026-07-15) and #8b4d1973b may have reduced the hang window \u2014 worth retesting on main before deeper work.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 11643,
  "title": "Ferramenta n\u00e3o \u00e9 executada causando loop infinito de chamadas repetidas",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The exact injected string lives only in `apps/vscode/src/core/prompts/responses.ts` (`noToolsUsed`), and a repo-wide grep shows nothing imports `formatResponse` outside `apps/vscode/src/core/prompts/__tests__/*` \u2014 it is orphaned legacy XML-tool-loop code. The live loop on main is the SDK's native tool-calling path: `sdk/packages/core/src/runtime/safety/mistake-tracker.ts` only knows `api_error | invalid_tool_call | tool_execution_failed`, and there is no 'You did not use a tool' text anywhere under `sdk/packages/`.",
  "verdict": "duplicate",
  "duplicate_of": 11523,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Same defect as #11523 (\"plan_mode_respond in Plan Mode is not recognized as a valid tool use\", filed 2026-06-13, still open). Both are legacy-3.x-only: the XML tool parser and its `noToolsUsed` retry message do not exist in the live path on main, and e4091c368 additionally makes the mistake limit stop the run instead of looping.",
  "cluster": "xml-tool-parser-rejects-valid-call"
 },
 {
  "number": 11649,
  "title": "overlapping ghost test still an issue in CLI",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The CLI TUI on main is OpenTUI-based (`apps/cli/src/tui/`), and `apps/cli/package.json` now pins `@opentui/core`/`@opentui/react` at `0.4.3`, upgraded from `0.1.102` by commit 22a1fa2c8 (`feat(cli): upgrade opentui 0.1.102 -> 0.4.3 (#12453)`) with a follow-up patch in 98024c424. The reporter was on CLI 3.0.27, i.e. the pre-upgrade renderer, so the specific inpainting artifact may or may not survive the renderer swap \u2014 I found no code on main that I can point at as the cause.",
  "verdict": "duplicate",
  "duplicate_of": 10622,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Duplicate of #10622 (\"TUI rendering issues\", still open) \u2014 the reporter and a commenter both say so. Worth one retest on main after the OpenTUI 0.4.3 upgrade before keeping it open; Windows/WSL terminal specifics matter.",
  "cluster": "tui-render-tearing"
 },
 {
  "number": 11660,
  "title": "GLM 5.2 token count unrealistically high",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "`sdk/packages/core/src/services/usage.ts` accumulates raw per-request `inputTokens` (`accumulateUsageTotals`), and the doc comment on `getCurrentContextSize` states that normalized `inputTokens` is deliberately the full prompt size including cache-read/cache-write portions. `normalizeUsage` in `sdk/packages/llms/src/providers/ai-sdk.ts` only splits out cached tokens when the provider reports `prompt_tokens_details.cached_tokens`/`cache_read_input_tokens`, which SiliconFlow's OpenAI-compatible endpoint does not, so the displayed total is provider-reported and cumulative by design.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "The later commenter's diagnosis matches the code: cache-inclusive `prompt_tokens` from a SiliconFlow/vLLM deployment inflates both the token counter and the pricing fallback in `calculateUsageCostFromPricing`. Needs the raw `usage` object from the reporter to confirm; not a Cline-side arithmetic bug.",
  "cluster": "inflated-token-count"
 },
 {
  "number": 11661,
  "title": "[BUG] CLI run.start race condition with session.create causes total context loss \u2014 forensic evidence with log timestamps",
  "area": "hub",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Two of the three claims do not survive a read of main. Bug 1: `HubRuntimeHost.startSession` in `sdk/packages/core/src/hub/runtime-host/hub-runtime-host.ts` `await`s the `session.create` reply before returning, and `runTurn` (which sends `run.start`) is a separate later call, so the CLI cannot legitimately fire `run.start` first. Bug 2: `createSessionId` in `sdk/packages/shared/src/session/index.ts` is `${Date.now()}_${nanoid(5)}` over a 36-char alphabet, not a bare ULID, so same-millisecond collisions need a 1-in-60M nanoid clash \u2014 reuse of one id across hours is session resume, not collision. Bug 3 is plausible: `session.detach` still goes through the generic 30s request/reply path (`HUB_DEFAULT_COMMAND_TIMEOUT_MS` in `sdk/packages/shared/src/hub.ts`, pending-reply timeout in `sdk/packages/core/src/hub/client/index.ts`).",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "High-effort report about context loss, so worth a maintainer read, but the stated timestamps are internally inconsistent (a CLI timeout logged 41s before the hub reply it supposedly missed) and two of three root causes are misdiagnosed. Ask for hub-daemon.log + cline.log from a current CLI (v4) before investing; the timeout message text has since changed to include requestId/clientId, which will make correlation easier.",
  "cluster": "hub-session-loss"
 },
 {
  "number": 11662,
  "title": "Cline CLI ACP requires authorization via ChatGPT or Cline when OpenRouter authorization is configured [Zed]",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/cli/src/acp/auth.ts` hardcodes `ACP_AUTH_METHODS = [{ id: \"cline\" }, { id: \"openai-codex\" }]`, and `AcpAgent.newSession` in `apps/cli/src/acp/acpAgent.ts` throws `RequestError.authRequired(undefined, \"Call authenticate before creating a session\")` unless `process.env.CLINE_API_KEY` is set or `tryRestoreAuth()` finds a key \u2014 and `tryRestoreAuth()` iterates only over `ACP_AUTH_METHODS`, so a persisted OpenRouter (or any other BYOK) credential from `cline auth` is never considered.",
  "verdict": "duplicate",
  "duplicate_of": 12043,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "ACP mode is unusable in Zed for every BYOK provider. Fix is to have `tryRestoreAuth()` consult `ProviderSettingsManager` for the configured default provider (or `CLINE_PROVIDER`) rather than only the two OAuth methods.",
  "cluster": "acp-provider-gating"
 },
 {
  "number": 11663,
  "title": "Complete tasks threads disappear form the task history, mixed with other workspaces",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The 'mixed workspaces' half is real by design on main: `SessionHistoryListOptions` in `sdk/packages/core/src/runtime/host/history.ts` has no workspace filter (only `limit`/`hydrate`/`includeSubagents`), history is stored globally under `~/.cline/data`, and `apps/vscode/webview-ui/src/components/history/HistoryView.tsx` initialises `showCurrentWorkspaceOnly` to `false`, so the 'Workspace Only' filter is opt-in. The 'tasks vanish entirely after a restart' half I could not tie to any code \u2014 `apps/vscode/src/sdk/sdk-task-history.ts` merges SDK sessions with legacy on-disk tasks via `readTaskHistory`/`legacy-state-reader`.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Two complaints in one. The workspace mixing is expected behaviour with an existing filter (maybe make it sticky/default-on); the disappearing-task claim needs the reporter's `~/.cline/data` layout and Cline output-channel log to reproduce. Reported on Intel macOS specifically.",
  "cluster": "task-history-loss"
 },
 {
  "number": 11664,
  "title": "[BUG][Rider] Cline see wrong build output",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The shared executor is not obviously wrong: `sdk/packages/core/src/extensions/tools/executors/bash.ts` spawns via `node:child_process.spawn` and raises `CommandExitError(code ?? 1, output)` on non-zero exit. The suspicious part is `getShellArgs` in `sdk/packages/shared/src/parse/shell.ts`, which runs Windows commands as `powershell -NoProfile -NonInteractive -Command \"<cmd>\"`; with the reporter's `2>&1` the native `dotnet build` stderr is folded into the PowerShell success stream, which is a known way to lose `$LASTEXITCODE` propagation. I could not confirm which terminal implementation Rider actually drives, since the JetBrains host lives in a separate repo.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "If reproducible it is an SDK-side PowerShell exit-code bug (in scope per the JetBrains rule), because a silently-zero exit code makes the agent believe broken builds succeeded. Needs confirmation of whether Rider routes through cline-core's bash executor and whether the same `dotnet build x.sln 2>&1` misreports in the VS Code SDK extension on Windows.",
  "cluster": ""
 },
 {
  "number": 11665,
  "title": "* Error: JSON Parse error: Unexpected identifier \"h_reason\"",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "`\"Unexpected identifier\"` / `\"Unterminated string\"` are JavaScriptCore (Bun) `JSON.parse` messages, and `h_reason` is the tail of a `finish_reason` key split mid-token \u2014 i.e. an SSE `data:` line parsed before the chunk was complete. I could not find the offending parser on main: no such error string exists anywhere outside node_modules, and streaming is delegated to the AI SDK through `sdk/packages/llms/src/providers/ai-sdk.ts`/`stream.ts` rather than a hand-rolled SSE splitter.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "gbrennon already failed to reproduce and asked the reporter to retest on the v4 CLI with no reply. Reported against 3.0.27 with an interrupt-then-resend sequence; close for inactivity unless the reporter confirms on v4.",
  "cluster": ""
 },
 {
  "number": 11667,
  "title": "Cline window is empty in Webstorm Build #WS-261.25134.101 (Pop OS)",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "This is a blank JCEF tool window in the JetBrains plugin (version 1.1.58), with no agent, provider, or core interaction reported. Per the scope rule the JetBrains plugin lives in a separate repository and only issues whose root cause is in `sdk/packages/*` or `apps/vscode/standalone` are in scope; nothing in /workspace renders the JetBrains tool window, and `apps/vscode/standalone` only ships the headless cline-core service.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Classic JCEF/Linux rendering failure. Should be redirected to the JetBrains plugin repo; ask for idea.log and whether the IDE is running under Wayland.",
  "cluster": ""
 },
 {
  "number": 11669,
  "title": "Cline hangs indefinetely after executing python script in powershell",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The command still goes through a real VS Code pty on main (`terminal.shellIntegration.executeCommand` / `terminal.sendText` in `apps/vscode/src/hosts/vscode/terminal/VscodeTerminalProcess.ts`), and multi-line PowerShell input of the kind in the report can leave the shell at a continuation prompt. However that file now has explicit markerless-quiet-timeout and dead-pty handling (`MARKERLESS_MAX_QUIET_TIME`, the `terminal.exitStatus !== undefined` fast-fail), added after this issue by 12703bf40 (2026-07-15) and 557d72569 (2026-07-18).",
  "verdict": "duplicate",
  "duplicate_of": 12079,
  "severity": "high",
  "repro": "1. On a Windows host with PowerShell as the default VS Code shell, run the SDK extension from `apps/vscode` (`bun run build:webview && bun esbuild.mjs`, then launch with `--extensionDevelopmentPath`).\n2. In Act mode, ask Cline to run the multi-line `python -c \"...\"` snippet from the issue (missing `import os`, undefined `free`) with a trailing `2>&1`.\n3. Approve the command.\n4. Expected: the NameError output is captured, a non-zero exit code is reported, and the turn continues.\n5. Actual (to verify): the terminal shows the traceback but the turn never completes and the webview stays busy until reload.",
  "human_attention": "no",
  "notes": "Same failure family as #11622 (terminal never signals completion). Retest on main first \u2014 the OSC 633 parser and timeout work landed a month after this report and may already cover it.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 11671,
  "title": "docs(mcp): CLI MCP config path is wrong \u2014 docs say `~/.cline/mcp.json`, code reads `~/.cline/data/settings/cline_mcp_settings.json`",
  "area": "docs",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The reporter's analysis checks out verbatim on main: `resolveMcpSettingsPath()` at `sdk/packages/shared/src/storage/paths.ts:321` returns `join(resolveClineDataDir(), \"settings\", CLINE_MCP_SETTINGS_FILE_NAME)` with `CLINE_MCP_SETTINGS_FILE_NAME = \"cline_mcp_settings.json\"`, while `docs/mcp/mcp-overview.mdx:28` still says `- **CLI:** ~/.cline/mcp.json`, and the stale path also survives at `docs/tools-reference/all-cline-tools.mdx:46` and `docs/cli/cli-reference.mdx:303`. `gh pr view 11723` shows the community fix is still OPEN and unmerged.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Docs-only, agent-fixable, and PR #11723 already exists \u2014 it just needs review/merge. Three files to touch; `docs/getting-started/config.mdx` already documents the correct path.",
  "cluster": ""
 },
 {
  "number": 11688,
  "title": "/deep-planning is gone: reflect this in documentation",
  "area": "docs",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/cli/src/tui/commands/slash-command-registry.ts` lists only settings, config, model, account, mcp, plugins, compact, skills, fork, undo, clear, history, help, quit \u2014 no `deep-planning`. The only surviving definition is `apps/vscode/src/shared/slashCommands.ts`, while `docs/core-workflows/using-commands.mdx` still documents `/deep-planning` in the command table (line 22) and gives it its own section (lines 43-52).",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Already acknowledged by @reneehuang1 (\"we are aware and working on larger doc update\"), so it is tracked; docs-only and agent-fixable.",
  "cluster": ""
 },
 {
  "number": 11693,
  "title": "StreamableHTTP transport skips initialize handshake \u2014 sends bare GET without session ID, fails with \"Missing session ID\" on local/non-TLS MCP servers",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Root cause found and reproduced on main. `apps/vscode/src/services/mcp/schemas.ts` builds a Zod union whose SSE branch precedes the streamableHttp branch and whose `transportType` legacy alias only maps the literal `\"http\"` (line 142); the SSE branch's fallback is `|| \"sse\"` (line 119). Parsing the reporter's exact config with the real schema returns `type: \"sse\"`: `bun -e 'ServerConfigSchema.parse({transportType:\"streamableHttp\", url:\"http://proxmox-mcp.home:8000/mcp\", disabled:false})'` \u2192 `{\"url\":\"\u2026\",\"type\":\"sse\"}`. `McpHub.ts` then builds an `SSEClientTransport`, which opens a bare `GET` with `Accept: text/event-stream` and no `Mcp-Session-Id` \u2014 exactly the packet capture. By contrast `sdk/packages/core/src/extensions/mcp/config-loader.ts` (`mapLegacyTransportType`) does accept `transportType: \"streamableHttp\"`, so core and the VS Code host disagree.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Two-line fix in `apps/vscode/src/services/mcp/schemas.ts`: make the SSE branch reject `transportType === \"streamableHttp\"` and let the streamableHttp branch accept it (mirroring `mapLegacyTransportType` in core). The reporter's secondary ask is also still valid \u2014 `Logger.error(\\`Transport error for \"${name}\":\\`, error)` in `McpHub.ts` and `StreamableHttpReconnectHandler.ts` logs an empty message. Likely also explains #9652.",
  "cluster": "mcp-streamablehttp-transport"
 },
 {
  "number": 11695,
  "title": "Spurious \"result missing\" / \"[ERROR] You did not use a tool in your previous response!\" despite a valid tool call (succeeds on identical retry)",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Same finding as #11643: the injected string exists only in `apps/vscode/src/core/prompts/responses.ts` (`noToolsUsed`), which nothing imports outside `apps/vscode/src/core/prompts/__tests__/`, and no equivalent text exists under `sdk/packages/`. The live loop on main uses native tool calls plus `MistakeTracker` (`sdk/packages/core/src/runtime/safety/mistake-tracker.ts`), whose reasons are `api_error | invalid_tool_call | tool_execution_failed`. The `\"result missing\"` phrase appears on main only in `apps/vscode/src/sdk/initial-message-sanitizer.ts` as a legacy-history migration placeholder.",
  "verdict": "duplicate",
  "duplicate_of": 11523,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Duplicate of #11523 and #11643 \u2014 all three are the retired 3.x XML tool parser. Safe to close against #11523 with a note that the SDK extension uses native tool calling.",
  "cluster": "xml-tool-parser-rejects-valid-call"
 },
 {
  "number": 11697,
  "title": "Export adminQuoteRequestApi doesn't exist in target module",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "The body is a Next.js 16 / Turbopack build error from the reporter's own project (`lib/sdk/services/laravel/quoteRequest/quoteRequest.api.ts`), with \"Steps to reproduce: N/A\". No symbol, path, or error string in it exists anywhere in /workspace \u2014 `adminQuoteRequestApi` and `quoteRequest.api.ts` return no hits.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Looks like the user's build error was pasted into the Cline issue tracker by mistake. Safely auto-closeable.",
  "cluster": ""
 },
 {
  "number": 11701,
  "title": "Bug: \"Plan\" mode unexpectedly writes files & causes infinite loop with strict plan",
  "area": "sdk-core",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "`sdk/packages/core/src/extensions/tools/presets.ts` already sets `enableEditor: false` for the `plan` preset, so the editor tool is not exposed in Plan mode \u2014 but the same preset keeps `enableBash: true` despite its own doc comment saying \"Plan mode (read-only, no shell access)\", so the agent can still create files via `run_commands`. `resolveToolPresetName`/`resolvePresetFlags` in `sdk/packages/core/src/extensions/tools/runtime.ts` are the only gate, and the VS Code host adds no Plan-specific policy. The infinite-retry half is addressed by `MistakeTracker` plus e4091c368 (HEAD), which stops the run at the mistake limit instead of blocking on an ask.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "The concrete code smell worth a maintainer decision: the `plan` preset comment claims no shell access but enables bash, which is the actual mechanism by which Plan mode writes files. The rest of the issue (separate plan/act permission tiers, allow-listed artifact writes) is a design request.",
  "cluster": ""
 },
 {
  "number": 11717,
  "title": "Failure in Cline's thought process or inability to use a tool properly",
  "area": "vscode-new",
  "kind": "question-support",
  "relevant_to_main": "unsure",
  "evidence": "The quoted sentence does not exist anywhere on main \u2014 it lived in `apps/vscode/src/core/task/index.ts`, a file that no longer exists on main (only `apps/vscode/src/core/task/focus-chain` and `tools` remain), and the commit that removed it (4d60a8afd, `fix(vscode): soften mistake-limit-reached copy`) is not an ancestor of HEAD. On main the equivalent path is `MistakeTracker` (`sdk/packages/core/src/runtime/safety/mistake-tracker.ts`) with `buildMistakeLimitStopMessage`, and e4091c368 (HEAD) makes hitting the limit stop the run cleanly rather than block on an ask while the loop keeps calling the provider.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "No repro steps and no logs \u2014 the report is 'my model keeps hitting the consecutive-mistake limit', which is model/provider behaviour. The exact copy the user quotes is legacy-only; a second commenter reports it on openai-compat, which usually means a provider with weak native tool-calling support.",
  "cluster": ""
 },
 {
  "number": 11747,
  "title": "OpenCode provider has no models in CLI model picker",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`sdk/packages/llms/src/catalog/catalog.generated.ts` now contains an `opencode` provider block; `git show ae033761a~1:\u2026/catalog.generated.ts | grep -c '\"opencode\"'` returns 0 while the same grep on main returns a match, so it was added by ae033761a (`feat: auto generate built-in provider list (#12204)`, 2026-07-23). Querying the built package confirms the runtime effect: `MODEL_COLLECTIONS_BY_PROVIDER_ID.opencode` now has 58 models (gpt-5.6-sol, claude-sonnet-5, glm-5.2, deepseek-v4-pro, \u2026) with `defaultModelId: \"openai/gpt-5.6-sol\"`, instead of the single synthesized fallback described in the issue.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Fixed on main by ae033761a. @minh2416294's proposed curated `modelsFactory` is no longer needed \u2014 the generated catalog covers it \u2014 and they should be told before they open the PR.",
  "cluster": "stale-model-catalog"
 },
 {
  "number": 11748,
  "title": "Access foundry Models via Cline",
  "area": "sdk-llms",
  "kind": "question-support",
  "relevant_to_main": "unsure",
  "evidence": "Both providers exist on main: Azure OpenAI is reachable through the openai-compatible vendor, which injects `api-version` (`readAzureApiVersion`/`withAzureApiVersion`/`createAzureApiVersionFetch` in `sdk/packages/llms/src/providers/vendors/openai-compatible.ts`, fed by `AzureConfig` in `providers/config.ts`), and NVIDIA NIM is a generated provider (`sdk/packages/llms/src/providers/providers.generated.ts`, baseUrl `https://integrate.api.nvidia.com/v1`, env `NVIDIA_API_KEY`). The later comment's failure \u2014 `404 Resource not found` with `modelId: gpt-5.6-sol, providerId: openai` \u2014 is an Azure deployment-name mismatch, not a missing code path.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Configuration support, not a defect: Azure Foundry requires the deployment name as the model id plus the correct `api-version`. Original reporter never supplied the details a maintainer asked for.",
  "cluster": ""
 },
 {
  "number": 11750,
  "title": "CLI silently exits when launched from a Windows drive-root working directory (e.g. D:\\) \u2014 ZodError: workspaces.<root>.hint too_small",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Root cause confirmed and reproduced. `generateWorkspaceInfoWithDiagnostics` in `sdk/packages/core/src/services/workspace/workspace-manifest.ts` sets `hint: basename(rootPath)` with no fallback (rootPath comes from `normalizeWorkspacePath` = `resolve()`), while `WorkspaceInfoSchema` in `sdk/packages/shared/src/session/workspace.ts` declares `hint: z.string().min(1).optional()`. Replaying it: `path.win32.basename(path.win32.resolve(\"D:\\\\\")) === \"\"`, and `WorkspaceInfoSchema.safeParse({rootPath:\"D:\\\\\", hint:\"\"})` fails with `{\"code\":\"too_small\",\"minimum\":1,\"path\":[\"hint\"]}` \u2014 byte-for-byte the reporter's hub log.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Two-part fix: fall back to `rootPath` (or drop the key) when `basename()` is empty in `workspace-manifest.ts`, and surface `session.create` ZodErrors in the CLI instead of exiting silently. Users are already installing a third-party marketplace monkey-patch to work around it.",
  "cluster": ""
 },
 {
  "number": 11760,
  "title": "Cline: Favorite mark older history item moves it to the \"Today\" section after deleting another history item",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "On main `apps/vscode/src/sdk/sdk-task-history.ts` derives the history item timestamp from the session record's write time (`sessionHistoryRecordToHistoryItem` sets `ts: dateStringToTimestamp(item.updatedAt ?? item.endedAt ?? item.startedAt)`), and `updateSession()` \u2014 the path `SdkController.toggleTaskFavorite` calls \u2014 deliberately stamps `updatedAt: new Date().toISOString()`. `apps/vscode/webview-ui/src/components/history/HistoryView.tsx` then buckets on `isToday(task.ts)`, so favoriting an old task moves it into the \"Today\" group as soon as the list is refreshed (e.g. after deleting another entry).",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Agent-fixable: either keep a separate creation timestamp for grouping, or have toggleTaskFavorite write metadata without bumping updatedAt. Owned by apps/vscode (sdk-task-history + HistoryView).",
  "cluster": ""
 },
 {
  "number": 11774,
  "title": "[CLI] Plan \u2194 Act mode switch creates new session, loses all conversation context",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`apps/cli/src/runtime/interactive/session-runtime.ts` `applyMode()` now calls `restartWithCurrentMessages()`, which re-reads the current messages and calls `restartWithMessages(..., { preserveSessionId: true })`; the comment there states config-only restarts must keep the session id \"otherwise each restart mints a new session history entry for the same conversation\". That guarantee landed in 78c83cdf3 \"fix(cli): preserve session id when in same session (#12188)\" (2026-07-10), after 10d1c41b7 \"fix(cli): prevent empty session from racing a mode-change restart (#12056)\" (2026-07-03) removed the race where a message sent right after a toggle booted an empty session.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter saw it once, mid-conversation, which matches the empty-session race fixed by #12056; the id-preservation fix in #12188 closes the remaining 'new session on every restart' path.",
  "cluster": ""
 },
 {
  "number": 11785,
  "title": "Global Dirs differ severely, docs dont convey information efficiently",
  "area": "docs",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Still inconsistent three ways on main: `docs/getting-started/config.mdx` claims \"Global workflows resolve from `~/.cline/data/workflows/`\", `sdk/packages/shared/src/storage/paths.ts` `resolveWorkflowsConfigSearchPaths()` actually searches `~/Documents/Cline/Workflows` and `~/.cline/workflows` (no `data/` segment), and the VS Code extension's `apps/vscode/src/core/context/instructions/user-instructions/workflows.ts` reads only `ensureWorkflowsDirectoryExists()` from `apps/vscode/src/core/storage/disk.ts`, which hardcodes `~/Documents/Cline/Workflows`. `docs/customization/cline-rules.mdx` still documents `~/Documents/Cline/Rules` as the global rules dir.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs a product decision on the canonical global config root before docs can be fixed; the extension host (disk.ts) and the SDK path resolver disagree, and skills now land in a third location (~/.agents/skills). Reporter links a newer follow-up, #12523.",
  "cluster": ""
 },
 {
  "number": 11787,
  "title": "After sending a message it often disappears and only gets sent on the second attempt.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The legacy 3.89.2 chat input no longer exists on main; the new submit path is `apps/vscode/webview-ui/src/components/chat/chat-view/components/layout/InputSection.tsx` + `hooks/useMessageHandlers.ts`, and 0220b9a50 \"fix(vscode): queue chat submits during active turns (#11839)\" (2026-06-25) changed it so a submit during streaming/awaiting-approval is queued instead of dropped. The only remaining path I found that clears the composer without sending its text is `executeButtonAction`'s `retry` case in `useMessageHandlers.ts`, which calls `clearInputState()` after sending a bare `yesButtonClicked`.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter never answered the maintainer's request to retest; no reproducible defect visible on main's input path. Auto-closeable, with a pointer to #11839.",
  "cluster": "prompt-lost-on-mode-switch"
 },
 {
  "number": 11793,
  "title": "apply_patch fuzzy matcher is O(n\u00b2) per hunk \u2014 pegs CPU, spikes RAM, times out on ~15 KB files",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Unchanged on main: `sdk/packages/core/src/extensions/tools/executors/apply-patch-parser.ts` `findContext()` still loops every start index calling `calculateSimilarity()` \u2192 `levenshteinDistance()`, which allocates a full `new Array<number>(rows * cols)` DP matrix over character length, with no size cap or early exit (the first loop even runs the similarity scan at every index before an exact match is found, purely to track `bestSimilarity`). `git log --since=2026-06-20 -- <that file>` returns no commits, and no commit references PR #11915.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "An outside contributor (PR #11915) is waiting on maintainer direction: size-guard the Levenshtein call vs. switch to line-oriented matching. Owned by @cline/core tool executors.",
  "cluster": ""
 },
 {
  "number": 11799,
  "title": "Cline w/ ChatGPT suscription + openai-codex:gpt-5.5 is using terminal commands to interact with the codebase rather than editing files directly",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The `openai-codex` provider exists on main (`sdk/packages/llms/src/providers/openai-codex-models.ts`, and the `provider.openai-codex` rule in `sdk/packages/llms/src/providers/routing/provider-option-rules.ts` which sets `systemMessageMode: \"remove\"` and moves the system prompt into `instructions`), but nothing in that routing removes or downgrades the edit tools, so I cannot see a defect that forces shell-based editing. Note bcf8c1d03 (2026-07-26) fixed a genuine cause of terminal fallback in the new extension \u2014 relative `read_files` paths resolving against `process.cwd()` (`/`) and failing with ENOENT.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Filed against the legacy 3.89.2 prompt/tool stack, which no longer exists on main. Worth asking the reporter to retest the SDK build now that bcf8c1d03 landed.",
  "cluster": ""
 },
 {
  "number": 11821,
  "title": "CLI prints hook dispatch failed: session.hook requires a valid hook event payload despite the run completing successfully -- possibly a residual case not covered by #11259",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Every element the reporter cites is unchanged on main: `handleSessionHook` in `sdk/packages/core/src/hub/server/handlers/run-handlers.ts` still returns the exact `invalid_hook_payload` / \"session.hook requires a valid hook event payload\" error whenever `parseHookEventPayload(envelope.payload?.payload)` is falsy, `HookEventPayloadSchema` in `sdk/packages/shared/src/hooks/events.ts` is still the flat passthrough object with the 10-value `HookEventNameSchema`, and `apps/cli/src/utils/hooks.ts` still guards dispatch only with `isShuttingDown()`. `git log --since=2026-06-20` on those files shows no fix.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. Build the CLI from main (`bun run build:sdk`, then `bun run cli`). 2. Run a long non-interactive turn with piped stdin and auto-approve, e.g. `cat taskfile.txt | bun run cli -P openai-compatible -m <model> --auto-approve true`, against a slow OpenAI-compatible endpoint so the run and hub shutdown overlap. 3. Repeat ~20 times (the reporter saw ~14% of sessions fail) and tail `hub-daemon.log`. 4. Expected: exit code 0 whenever the hub log shows `run.start` completing with `ok:true`. Actual: the CLI prints `error: hook dispatch failed: session.hook requires a valid hook event payload` plus `error: The operation timed out.` and exits non-zero. 5. To pin the payload, log `envelope.payload?.payload` at the top of `handleSessionHook` in `sdk/packages/core/src/hub/server/handlers/run-handlers.ts` \u2014 the reporter's audit of `createRuntimeHooks` in `apps/cli/src/utils/hooks.ts` found no malformed construction, so the likely culprit is an envelope whose `payload.payload` is dropped/empty on a socket closing mid-dispatch.",
  "human_attention": "yes",
  "notes": "Exceptionally well-researched report with hub-log frequency data (20/142 sessions). False-failure signal breaks CI/batch automation. Likely a residual of the #11259 shutdown race that the isShuttingDown() guard does not cover.",
  "cluster": ""
 },
 {
  "number": 11828,
  "title": "many times cline remove the whole folder. and nothing to recover",
  "area": "unknown",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "No model, prompt, approval settings, or steps were provided, so there is nothing to ground. On main there is no file/folder-delete tool in `sdk/packages/core/src/extensions/tools/executors/` \u2014 bulk deletion can only come from a shell command through the command tool, and I found no destructive-command guard (grep for `rm -rf`/`dangerous`/`destructive` under `sdk/packages/core/src` only hits an unrelated doc comment in `ClineCore.ts`).",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Two maintainers asked for model/prompt/auto-approve details and got no usable answer. Unactionable as filed; safe to close pending reporter input. If the team wants a follow-up, the real question is whether auto-approved shell commands should be screened for destructive patterns.",
  "cluster": ""
 },
 {
  "number": 11830,
  "title": "Unable to use JetBrains Plugin with PyCharm 2026.2 EAP 7",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The whole stack trace is JetBrains-plugin UI code (`bot.cline.intellij.toolWindow.ClineBrowserPanel.<init>` failing with `NoClassDefFoundError: com/intellij/ui/jcef/JBCefApp`); grepping /workspace for `ClineBrowserPanel` returns nothing, so none of this code lives in this repo. The secondary ACP failure is an npm/npx cache ENOENT inside PyCharm's bundled Node runtime, not cline-core.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "A second reporter confirms plugin 1.1.63 works on IntelliJ 2026.2 (comment 2026-07-24). Closeable as fixed in the JetBrains plugin repo.",
  "cluster": ""
 },
 {
  "number": 11854,
  "title": "ChatGPT Subscription gpt-5.5 could be stuck after created a new file right after a previous",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The legacy DiffViewProvider the reporter hit (3.89.2 / rolled-back 4.0.x) is gone on main; the equivalent path is `apps/vscode/src/sdk/sdk-diff-edit-coordinator.ts`, and bcf8c1d03 \"fix(vscode): relative read_files paths and diff-preview stalls no longer fail read/edit tools (#12558)\" (2026-07-26) bounds the diff-preview open with a 5s race precisely because \"a hung vscode.diff call was unbounded: on auto-approve it burned the editor tool's 30s execution timeout ... and on manual approval it delayed the approval ask indefinitely\". That is the same failure shape, but it is a same-day fix that nobody has validated against this reporter's scenario.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Run the SDK extension from main: `cd apps/vscode && bun run build:webview && bun esbuild.mjs`, then `DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <scratch-repo>`. 2. Configure the `openai-codex` provider with gpt-5.5 (or any model that emits back-to-back file creations). 3. Leave file edits on manual approval (do not enable auto-approve for edits). 4. Ask for a task that creates several new files in immediate succession, e.g. \"create src/a.ts, src/b.ts and src/c.ts, each with a one-line export\". 5. Expected: an approval prompt appears for each file and the task proceeds. Actual (as reported): after the second file is created with no delay, the diff tab hangs and the task stalls until you Ctrl-S and restart VS Code. 6. If it no longer stalls, verify bcf8c1d03's 5s preview-open race in `apps/vscode/src/sdk/sdk-diff-edit-coordinator.ts` is what is saving it by temporarily raising the timeout.",
  "human_attention": "yes",
  "notes": "Reporter re-confirmed on 4.0.1/4.0.2/4.0.6 and was left hanging when the responding maintainer left the team; likely same root cause class as #11919. Worth a direct follow-up now that #12558 landed.",
  "cluster": "gpt55-file-create-stall"
 },
 {
  "number": 11858,
  "title": "Confusing token usage with the context window.",
  "area": "vscode-new",
  "kind": "question-support",
  "relevant_to_main": "unsure",
  "evidence": "The complaint is entirely in a screenshot with no numbers in the text, and no steps. The model is listed as `stepfun-3.5-flash`; main's catalog entry is `stepfun/n.7-flash` in `sdk/packages/llms/src/catalog/catalog.generated.ts`, so I cannot tell whether the grievance is a wrong `contextWindow` in the catalog or the usual confusion between cumulative tokens in/out and current context size (`getCurrentContextSize` in the CLI/webview usage rows).",
  "verdict": "duplicate",
  "duplicate_of": 11660,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Needs the actual numbers (tokens shown vs. context window) and the exact model id before anything can be checked.",
  "cluster": "inflated-token-count"
 },
 {
  "number": 11859,
  "title": "ChatGPT Subscription gpt-5.5 suddenly can't create file",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Same reporter, provider and file-write surface as #11854 but a different symptom (permanent failure late in a long task rather than a one-off stall), with \"steps to reproduce: not known\". On main the write path is `apps/vscode/src/sdk/sdk-diff-edit-coordinator.ts` plus the editor tool override registered in `apps/vscode/src/sdk/vscode-session-host.ts`; nothing there fails only after many iterations, and the reporter's own hypothesis (context management) points at compaction, which was rewritten since (26037b17a, #12317).",
  "verdict": "duplicate",
  "duplicate_of": 11854,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Possibly the same root cause as #11854; if #11854 is reproduced on the SDK build, retest this one in the same session rather than triaging it separately.",
  "cluster": "gpt55-file-create-stall"
 },
 {
  "number": 11879,
  "title": "# CI/CD test coverage gaps in SDK packages \u2014 two untested code paths",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Both gaps are still live on main. Gap 2: `mergeToolInputText(current: string, incoming: string)` in `sdk/packages/agents/src/agent-runtime.ts` still calls `incoming.trimStart()` with no runtime type check, and its only call site (`if (event.inputText) { assembly.inputText = mergeToolInputText(...) }`) does not check typeof either, so a non-string delta throws exactly as reported. Gap 1: session metadata is still round-tripped through raw `JSON.parse(JSON.stringify(...))` in `sdk/packages/core/src/hub/runtime-host/hub-runtime-host.ts` (`toJsonRecord`, `toJsonSerializable`, `toSessionRecord`), which throws on cyclic structures; no cycle-safe serializer exists in `sdk/packages/`.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "PR #11884 (community) covers Gap 2 and is unmerged \u2014 `git log --grep=11884` finds nothing on main. Gap 1 is owned by @cline/core (hub runtime host), Gap 2 by @cline/agents. Both are cheap fixes plus the regression tests the reporter specified.",
  "cluster": ""
 },
 {
  "number": 11881,
  "title": "Cline is somehow very slow when using Bring-Your-Own-Key (BYOK) while logged out / session expired",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The reporter explicitly labels it a suspicion with no timings or logs. Cline-account traffic on main lives in `sdk/packages/core/src/account/cline-account-service.ts` and `sdk/packages/core/src/auth/cline.ts`; I found no call from those into the per-request BYOK path (provider handlers are built in `sdk/packages/llms/src/providers/`), so I cannot confirm a per-prompt auth round-trip when logged out.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter said they will retest on 4.x. Needs a timing comparison (logged-in vs. expired session, same BYOK model) before it is actionable; the JetBrains plugin also wraps its own auth UI, so the delay may not originate in this repo.",
  "cluster": ""
 },
 {
  "number": 11893,
  "title": "Cline: Customize tab icon mismatch in VSC",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "On main the Customize nav entry uses lucide's `PuzzleIcon` (`apps/vscode/webview-ui/src/components/menu/Navbar.tsx`), while a second affordance labelled \"Customize\" uses `codicon-law` (`apps/vscode/webview-ui/src/components/cline-rules/ClineRulesToggleModal.tsx`), so the product is internally inconsistent \u2014 but the issue compares VS Code against the JetBrains plugin, which ships its own webview build, and I cannot diff the two screenshots from code.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Most likely version skew between the VS Code and JetBrains webview bundles rather than a defect. If the team wants icon parity, the decision is which of PuzzleIcon vs codicon-law is canonical.",
  "cluster": ""
 },
 {
  "number": 11909,
  "title": "ACP session/fork support status",
  "area": "cli",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "`apps/cli/src/acp/acpAgent.ts` on main implements `newSession`, `prompt`, `cancel`, `setSessionMode` and `authenticate`, and advertises `loadSession: true` in its capabilities \u2014 there is no `fork`/`session/fork` handler anywhere in `apps/cli/src/acp/`. So the answer is still \"not supported\".",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs a maintainer answer at minimum (yes/no/roadmap). The underlying capability exists in the SDK \u2014 sessions can be started with pre-seeded messages \u2014 so a fork handler would be a thin ACP-layer addition.",
  "cluster": ""
 },
 {
  "number": 11910,
  "title": "Cline 4.0.0 configuration issue",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Still present on main: `apps/vscode/src/utils/fs.ts` `readDirectory()` converts any failure, including ENOENT for a directory the user simply does not have, into `throw new Error(\\`Error reading directory at ${directoryPath}\\`)`, and `readDirectoryRecursive()` in `apps/vscode/src/core/context/instructions/user-instructions/rule-helpers.ts` catches it and emits `Logger.error(\\`Error reading directory ${directoryPath}: ...\\`)`. Since the rules/workflows scan includes `~/Documents/Cline/Rules` and `~/Documents/Cline/Workflows` (see `disk.ts`), opening Customize floods the output channel with error-level logs exactly as reported.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Cosmetic/log-noise only, agent-fixable: treat a missing optional directory as an empty result instead of Logger.error. Related to the directory-layout confusion in #11785.",
  "cluster": ""
 },
 {
  "number": 11914,
  "title": "deepseek-v4-flash identifies itself as Claude instead of DeepSeek",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Cline's system prompt on main contains no Claude/Anthropic persona: `sdk/packages/shared/src/prompt/system.ts` opens with \"You are Cline, an AI coding agent...\" (and the YOLO variant likewise), and grepping `sdk/packages` for `You are Claude`/`I am Claude`/`powered by Claude` matches only the Claude Code *provider* plumbing (`providers/builtins.ts`, `providers/vendors/community.ts`). Nothing injects an Anthropic identity into the conversation.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Model self-identification artifact (widely observed with distilled/low-cost models), not a Cline defect. The commenter's explanation on the thread is correct.",
  "cluster": ""
 },
 {
  "number": 11919,
  "title": "Still hanging after allowing backround shell use.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The reporter's own follow-up narrows this to \"if I want to control saving files, it does NOT always give me a prompt for approval\" \u2014 i.e. a missing/late approval ask, not a shell issue. On main that ask is produced through `apps/vscode/src/sdk/sdk-diff-edit-coordinator.ts` / `sdk-interaction-coordinator.ts`, and bcf8c1d03 (#12558, 2026-07-26) fixed exactly a case where a hung `vscode.diff` preview \"delayed the approval ask indefinitely\" on manual approval. The original report is against legacy 3.89.2, whose approval plumbing no longer exists here.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Build and launch the SDK extension from main (`cd apps/vscode && bun run build:webview && bun esbuild.mjs`, then `DISPLAY=:1 code --no-sandbox --user-data-dir=/tmp/vscode-userdata --extensionDevelopmentPath=/workspace/apps/vscode <repo>`). 2. In settings, disable auto-approve for both file edits and commands so every write and command requires manual approval. 3. Start a long multi-file task (10+ edits) on a medium-size repo. 4. Expected: an approval prompt for every edit/command, and the agent continues immediately after each approval. Actual (as reported): the agent goes idle with no visible prompt for long stretches, and only resumes when the user types 'proceed', after which it re-reads and re-edits files it already changed. 5. Watch the extension host output channel for edit-tool timeouts while it is idle, and compare against the same run with auto-approve on (the reporter says auto-approve makes it work).",
  "human_attention": "yes",
  "notes": "Same suspected root cause family as #11854 (stalled diff preview blocking the approval ask). The 're-reads and re-does work after prodding' part is a separate context/resume concern worth checking alongside #11940.",
  "cluster": "gpt55-file-create-stall"
 },
 {
  "number": 11920,
  "title": "404 Page not found [macos]",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`sdk/packages/llms/src/providers/vendors/anthropic.ts` passes the user's value straight through as `createAnthropic({ baseURL: config.baseUrl, ... })`. The AI SDK Anthropic provider posts to `${baseURL}/messages` (its default baseURL already ends in `/v1`), whereas the legacy Anthropic SDK appended `/v1/messages`, so a proxy base URL saved as `https://<host>/raw/anthropic` now 404s and must be rewritten to `.../raw/anthropic/v1`. There is no normalization for this provider, unlike `normalizeOllamaBaseUrl()` in `providers/vendors/ollama.ts`, which explicitly strips a \"legacy OpenAI-compat /v1 suffix\" for exactly this class of migration breakage.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Silent migration regression for anyone on a self-hosted/proxied Anthropic endpoint; the failure is an opaque '404 Page not found'. Fix options are a base-URL normalizer for the anthropic vendor (mirroring ollama.ts) or a one-time settings migration in providers.json.",
  "cluster": "custom-base-url-404"
 },
 {
  "number": 11923,
  "title": "4.0.0 is a shit show",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The 4.0.0 split the reporter describes (UI writes `cline_mcp_settings.json`, runtime reads `global-settings.json`) does not exist on main: `apps/vscode/src/sdk/SdkController.ts` constructs `McpHub` with the settings directory derived from the SDK's `resolveDefaultMcpSettingsPath()`, `McpHub.getMcpSettingsFilePath()` resolves the same `cline_mcp_settings.json` for every read/write, and 50797dd82 (#11818, 2026-06-26) added `apps/vscode/src/hosts/vscode/mcp-settings-legacy-migration.ts` to fold legacy files in. Config *format* compatibility is also handled \u2014 `sdk/packages/core/src/extensions/mcp/config-loader.ts` accepts both the nested `transport` shape and legacy flat `command`/`url` entries. What I cannot verify from code is the reporter's 2026-07-22 retest on the nightly SDK build, where a correctly-loaded Serena server still exposed no tools to the agent.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Mostly venting, but the July 22 nightly retest is fresh signal against the build about to ship: MCP server loads yet its tools never reach the agent's tool list, plus 'quit doesn't quit' (the hub daemon surviving CLI/extension exit). Both deserve a maintainer look; the reporter has disengaged, so reproduce with Serena in-house rather than asking them again.",
  "cluster": ""
 },
 {
  "number": 11940,
  "title": "New Update 4.0.1 the agents keeps loosing its focus on the task .",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Filed against 4.0.1, which is the rolled-back pre-SDK build, so the resume path involved is not the one on main; here resume goes through `apps/vscode/src/sdk/SdkController.ts` + `apps/vscode/src/sdk/sdk-task-history.ts` (`getLegacyResumeInitialMessages`, transcript hydration). The reported behaviour \u2014 after cancel + resume the agent answers the *first* prompt of the task again \u2014 would show up as the resumed message list replaying an earlier user turn, and I found no such defect by inspection; the maintainer's questions (consistency, exact model, provider) were never answered.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "If #11919's 're-does work after prodding' complaint is reproduced, check this resume-replay behaviour in the same session \u2014 they may share a cause.",
  "cluster": ""
 },
 {
  "number": 11941,
  "title": "CI/CD test coverage gap \u2014 message ID concatenation on repeated persistence (codec not idempotent for tool result messages)",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Fixed on main: `sdk/packages/core/src/runtime/config/agent-message-codec.ts` now computes `const toolIdSuffix = \\`_tool_${block.tool_use_id}\\`` and emits `id: isPureToolResult || baseId.endsWith(toolIdSuffix) ? baseId : \\`${baseId}${toolIdSuffix}\\``, so re-encoding an already-encoded message cannot grow the id. The exact regression test the reporter asked for exists in `agent-message-codec.test.ts` (\"A second round-trip of the split parts must not grow the ids\"). `git log -S 'baseId.endsWith(toolIdSuffix)'` attributes it to 26037b17a \"feat(core): default to agentic compaction (#12317)\" (2026-07-21).",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Only the round-trip idempotence test was requested for the codec; the other coverage suggestions (persisted-artifact id assertions in local-runtime-host and the live messages contract test) are still not implemented, but the underlying defect is gone.",
  "cluster": ""
 },
 {
  "number": 11944,
  "title": "Plugin sandbox fails to initialize in VS Code extension 4.0.0 \u2014 plugin-sandbox-bootstrap.js not bundled",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The packaging gap is still there on main. `sdk/packages/core/bun.mts` emits `plugin-sandbox-bootstrap.ts` as a separate entrypoint into `dist/extensions/`, and both CLI build scripts copy it (`apps/cli/bun.mts` copyFileSync into `./dist/extensions/plugin-sandbox-bootstrap.js`, `apps/cli/script/build.ts` likewise) \u2014 but `apps/vscode/esbuild.mjs` has no equivalent step, so none of the five candidates in `resolveBootstrap()` (`sdk/packages/core/src/extensions/plugin/plugin-sandbox.ts`) can resolve inside the extension's `dist/`, and the jiti fallback is unavailable because esbuild inlines it.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Also a roadmap question the reporter asked and nobody answered: is the plugin subsystem supported in the VS Code extension at all? The extension does install plugins (`apps/vscode/src/core/controller/marketplace/marketplace-helpers.ts` writes to `~/.cline/plugins/_installed`) while never emitting the sandbox bootstrap, so plugins can appear 'Installed' with setup() silently never running. Related: #11065.",
  "cluster": ""
 },
 {
  "number": 11945,
  "title": "cline keeps crashing without any error inside a container",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The crash is a Bun runtime segfault (`panic(main thread): Segmentation fault`, Bun v1.3.13, Linux arm64) inside the shipped standalone binary (`/usr/lib/node_modules/cline/bin/.cline`, produced by `apps/cli/script/build.ts` + `apps/cli/script/postinstall.mjs`). Main still pins the same runtime \u2014 `\"bun\": \"1.3.13\"` and `\"packageManager\": \"bun@1.3.13\"` in the root `package.json` \u2014 so nothing has changed that would clear it, and a second reporter hit it under WSL.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. On an arm64 host (Apple Silicon), start a Linux container, e.g. `lima`/`nerdctl` with Ubuntu, or plain `docker run --rm -it --platform linux/arm64 ubuntu:24.04 bash`. 2. Install Node 22+ and `npm i -g cline` so the `@cline/cli-linux-arm64` standalone binary is fetched and hard-linked by `apps/cli/script/postinstall.mjs`. 3. Run `cline` (no arguments). 4. Expected: the interactive TUI or a provider sign-in screen. Actual: Bun's crash banner and `panic(main thread): Segmentation fault`, then `Trace/breakpoint trap (core dumped)`. 5. Bisect the runtime vs. our bundle: run the same container with `bun` 1.3.13 installed standalone and execute the CLI from source (`bun run cli`); if that survives, the fault is in the compiled standalone binary rather than Bun itself. 6. Also try a newer Bun to see whether bumping the pinned version in the root `package.json` clears it.",
  "human_attention": "yes",
  "notes": "Two independent reporters (lima/nerdctl on macOS arm64, and WSL on Windows) and a maintainer already said they were investigating, with no follow-up. Container/devcontainer usage is completely blocked, and the likely remedy is an upstream Bun bump in the pinned toolchain.",
  "cluster": "cli-bun-binary-crash"
 },
 {
  "number": 11979,
  "title": "Cline buttons freeze after first save/accept; second suggested change cannot be saved",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "On main the manual-approval path opens the edit diff preview via `onToolApprovalAsk` -> `SdkDiffEditCoordinator.openForApproval` (apps/vscode/src/sdk/SdkController.ts:317), and commit bcf8c1d03 (#12558, 2026-07-26) bounded that call because \"a hung vscode.diff call ... on manual approval it delayed the approval ask indefinitely\" \u2014 the exact freeze-after-first-edit/needs-extension-host-restart symptom reported. The 5s race guard is now at apps/vscode/src/sdk/sdk-diff-edit-coordinator.ts:30 and :285.",
  "verdict": "duplicate",
  "duplicate_of": 10035,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Fix landed only on 2026-07-26 and has not shipped to the reporter's 4.0.2 build; worth asking the reporter (and daemenseth) to re-verify on a build containing bcf8c1d03 before closing.",
  "cluster": "diff-approve-buttons-stuck"
 },
 {
  "number": 11993,
  "title": "Segmentation fault",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Report body is a single screenshot with no stack trace, no command, and no repro steps; the \"Cline Version 1.3.13.0\" field is a Bun version, not a CLI version, so I could not map it to any code in apps/cli or sdk/packages/*. Nothing in the report names a symbol, file, or error string to check on main.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Maintainer (renee) already said they are investigating (CLINE-2596); a native segfault in the Bun-compiled CLI binary warrants follow-up, but the reporter must supply the command, OS/arch and crash log first.",
  "cluster": "cli-bun-binary-crash"
 },
 {
  "number": 12001,
  "title": "Cline VS Code extension v4.0.4 may slowly add orphaned terminal sessions which clogs Win OS",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Terminals are deliberately never disposed on task abort (apps/vscode/src/hosts/vscode/terminal/VscodeTerminalManager.ts:456 keeps `//info.terminal.dispose()` commented out), so accumulation is possible in principle; however main now evicts and reclaims dead terminals via `evictTerminal` + `TerminalRegistry.queueTerminalForCleanup` / `disposeTerminalsPendingCleanup` (VscodeTerminalManager.ts:234-244, :312, VscodeTerminalRegistry.ts:92-104), all added in 12703bf40 (#11972, 2026-07-15) after this v4.0.4 report.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Report is explicitly anecdotal (\"more of a cautionary tale\") with no repro. Ask the reporter to re-check terminal-process counts on a build containing #11972 before spending time on it.",
  "cluster": ""
 },
 {
  "number": 12004,
  "title": "CLI 3.0.34 crashes with `n.content.map is not a function` after long conversations / large MCP tool_result",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Both decode paths on main still call `.map` on `message.content` with no `Array.isArray` guard: `agentMessageToMessageWithMetadata` (sdk/packages/core/src/runtime/config/agent-message-codec.ts:102) and `agentMessagesToMessages` (same file, :127). The encode direction does normalize (`normalizeContentBlocks`, :148), confirming the asymmetry the reporter describes. Last change to that file is 26037b17a (compaction default), so nothing has fixed it.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "PR #12032 (open, unmerged) adds the guard plus regression tests in agent-message-codec.test.ts. A guard is a band-aid \u2014 a maintainer should also decide whether something upstream is legitimately producing string `content` on an `AgentMessage`, since the type says `AgentMessagePart[]`.",
  "cluster": ""
 },
 {
  "number": 12013,
  "title": "Cline: While enabling MCP server, getting error like \"MCP server fails with \"MCP error -32000: Connection closed\"",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The exact failing path exists on main: `McpHub.toggleServerDisabledRPC` (apps/vscode/src/services/mcp/McpHub.ts:1294) does deleteConnection -> connectToServer on every toggle and surfaces the raw transport error as `Failed to update server state: ...` (:1338). That file has had no commits since 2026-07-01, so the re-connect-on-toggle behavior is unchanged.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. Run the new extension from apps/vscode (`bun run build:webview && bun esbuild.mjs`, then launch VS Code with `--extensionDevelopmentPath=/workspace/apps/vscode`).\n2. Install any stdio MCP server from the marketplace tab so it appears in cline_mcp_settings.json and connects.\n3. Toggle the server OFF in the MCP servers view; confirm the row goes to the disabled stub state.\n4. Toggle it back ON.\n5. Expected: the server reconnects and its tools reappear. Actual (reported): error toast `Failed to update server state: MCP error -32000: Connection closed` and the server stays disconnected.\n6. If it does not reproduce on the first toggle, cycle OFF/ON several times \u2014 the reporter's video shows it after a disable/enable round-trip. Capture the MCP server's stderr from the Cline output channel to see whether the child process died during `initialize` or the previous connection was never fully torn down.",
  "human_attention": "yes",
  "notes": "Two independent reporters plus QA; one says it appears on IntelliJ 2026.1.4 but not 2025.3, which points at the shared McpHub/standalone core rather than JetBrains UI. Suspect a teardown race in deleteConnection before connectToServer.",
  "cluster": ""
 },
 {
  "number": 12018,
  "title": "Cerebras `zai-glm-4.7` reasoning-only responses are misreported as \"Connection error\"",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The literal string \"Connection error.\" appears nowhere in apps/ or sdk/ on main; it is the `openai` npm SDK's APIConnectionError message, and `openai` is no longer used in any request path on main (its only reference is a doc comment in apps/vscode/src/shared/net.ts:25). On main a reasoning-only, budget-exhausted turn hits `finishReason === \"max-tokens\"` with no tool calls and throws MAX_TOKENS_INCOMPLETE_TURN_MESSAGE \u2014 \"Model reached the maximum output token limit before completing the turn\" (sdk/packages/agents/src/agent-runtime.ts:45-46, :673-675), which is the user-friendly message the reporter asked for.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Cerebras is still a first-class provider on main (sdk/packages/llms/src/providers/builtins.ts:683-690, defaultModelId zai-glm-4.7). Max-output-token budgeting for reasoning models was also reworked in ef27f4508 (#12031, 2026-07-16).",
  "cluster": "opaque-connection-error"
 },
 {
  "number": 12020,
  "title": "Destructive shell commands run without approval when the model sets requires_approval=false",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The blamed file `apps/vscode/src/core/task/tools/handlers/ExecuteCommandToolHandler.ts` does not exist on main (apps/vscode/src/core/task only contains focus-chain/ and tools/subagent/), and `requires_approval` appears nowhere in sdk/packages/* \u2014 the SDK `run_commands` tool has no such field. Approval on main is decided host-side from user settings only: `SdkInteractionCoordinator` consults `request.policy.autoApprove` / `shouldAutoApproveTool` (apps/vscode/src/sdk/sdk-interaction-coordinator.ts:90) which resolves through `isToolAutoApproved` (apps/vscode/src/sdk/sdk-tool-policies.ts:49-73).",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "The attached PR #11638 targets deleted code. Adjacent nuance worth noting on main: `isToolAutoApproved` maps every command tool to a single `actions.executeSafeCommands` toggle with no destructive-pattern check, so that toggle auto-approves all commands (it defaults to false in DEFAULT_AUTO_APPROVAL_SETTINGS).",
  "cluster": "approval-bypass"
 },
 {
  "number": 12026,
  "title": "[ErrorService] Logging exception after the Gemini API stopped accepting requests from unrestricted API keys on June 19, 2026",
  "area": "sdk-llms",
  "kind": "question-support",
  "relevant_to_main": "unsure",
  "evidence": "The reporter's log lines come from the retired 3.x stack: neither \"[Prompt variant] Selected\" nor the `TODO_SECTION` prompt component exists anywhere in apps/ or sdk/ on main (apps/vscode/src/core/prompts now only contains responses.ts). `gemini-3.1-pro-preview` is still a valid catalog entry (sdk/packages/llms/src/catalog/catalog.generated.ts:2484), but the reported 400 INVALID_ARGUMENT carries no field-level detail, so I could not tie it to any request builder on main.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Most likely a Google-side API-key restriction issue as the reporter himself hypothesizes; needs the full request/response body (or a repro on a 4.x SDK build) to distinguish from a Cline request-shape bug.",
  "cluster": ""
 },
 {
  "number": 12035,
  "title": "[SDK] Session metadata is not passed to tool calls",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Confirmed on main: `StartSessionInput.sessionMetadata` exists (sdk/packages/core/src/runtime/host/runtime-host.ts:115) and is only used for the session record (local-runtime-host.ts:529, :772), while the `agentConfig` object built in local-runtime-host.ts:625-694 never sets `toolContextMetadata`. Consequently `session-runtime-orchestrator.ts:847-851` builds the tool context from only `modelSupportsImages` + `this.config.toolContextMetadata` + the internal telemetry key, so custom tools see no session metadata.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs a small API decision: forward `input.sessionMetadata` into `toolContextMetadata`, or expose `toolContextMetadata` on StartSessionInput (it is currently unreachable from ClineCore.start). Owner: @cline/core.",
  "cluster": ""
 },
 {
  "number": 12040,
  "title": "PROVIDER_API_KEY_MAP has a dead `claude_code` entry (provider id is `claude-code`)",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Verified verbatim on main: `claude_code: \"apiKey\"` at apps/vscode/src/sdk/cline-session-factory.ts:337 inside PROVIDER_API_KEY_MAP (declared :300), while the canonical id in the ApiProvider union is `\"claude-code\"` (apps/vscode/src/shared/api.ts:6) and `resolveApiKey` looks the map up by that id (cline-session-factory.ts:424). The lookup therefore always returns undefined.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Cosmetically trivial but blocked on a maintainer decision: PR #12041 (open) removes the entry as a no-behavior-change cleanup, while a second contributor wants to rename it to `claude-code`, which WOULD change behavior by forwarding the legacy top-level apiKey. Two contributors are waiting on direction.",
  "cluster": ""
 },
 {
  "number": 12042,
  "title": "macOS 27 Golden Gate Developer Beta (Apple Silicon): CLI binary is killed by AMFI (CODE SIGNING: cs_invalid_page)",
  "area": "infra",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The release pipeline has no macOS signing at all: .github/workflows/cli-publish.yml builds all six platform binaries in one cross-compile step (`bun script/build.ts --install-native-variants`, line ~148) and then publishes straight to npm, and neither the workflow nor apps/cli/script/build.ts contains any `codesign`/`notarytool` step (build.ts only lists `{ os: \"darwin\", arch: \"arm64\" }` targets at :62-63). That matches the reporter's `Signature=adhoc / TeamIdentifier=not set`.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Release-engineering decision, not a code bug: darwin CLI binaries need Developer ID signing + notarization (the desktop app workflow already signs/notarizes). Currently only reproducible on the macOS 27 developer beta, but it becomes a hard install-blocker for every macOS user when 27 ships. Reporter's workaround is re-running `codesign --force --deep --sign -` locally.",
  "cluster": "cli-bun-binary-crash"
 },
 {
  "number": 12043,
  "title": "Zed - ACP model selector in only shows 2 hardcoded options, ignores authenticated providers",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Confirmed on main: `ACP_AUTH_METHODS` holds only `cline` and `openai-codex` (apps/cli/src/acp/auth.ts:9-12); `buildProviderConfigOption` maps over exactly that constant (apps/cli/src/acp/acpAgent.ts:563-574); `setSessionConfigOption` rejects anything else with `Unknown provider` via `isAcpAuthMethodId` (acpAgent.ts:316-321); and `newSession` defaults to `cline` / `anthropic/claude-sonnet-4.6` (acpAgent.ts:130-133) without consulting ProviderSettingsManager or the client's default_config_options.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs a product call on whether ACP is intentionally scoped to OAuth sign-in providers. Real financial impact: users silently get billed on the Cline hosted tier while believing they configured a self-paid provider. A contributor (@edobusy) has offered to implement the merge of ProviderSettingsManager providers into the selector and is waiting for direction.",
  "cluster": "acp-provider-gating"
 },
 {
  "number": 12044,
  "title": "MCP server initialization timeout (1500ms) should be configurable",
  "area": "sdk-core",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Both constants the reporter reverse-engineered from the bundle are literal in source on main: `MCP_REQUEST_TIMEOUT_MS = 5_000` and `MCP_CONNECT_TIMEOUT_MS = 1_500` (sdk/packages/core/src/extensions/mcp/client.ts:41-42), and the latter is passed directly as the `initialize` request timeout (:165-175). No server config field feeds it \u2014 the only `timeoutMs` in this area is `CreateMcpToolsOptions.timeoutMs` for tool calls (extensions/mcp/types.ts:32, tools.ts:32).",
  "verdict": "duplicate",
  "duplicate_of": 12344,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Well-specified and agent-fixable: thread a per-server (and/or global) initialize timeout from cline_mcp_settings.json through McpServerRegistration into client.ts:175. Note the connect path already retries twice (newline then framed protocol mode), so the effective startup wait is ~3s.",
  "cluster": "mcp-initialize-timeout"
 },
 {
  "number": 12045,
  "title": "Operations button in Stock tab returns \"unexpected server error\"",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "The report describes a bug in the reporter's own \"Custom Stock Manager\" web application (log in, click the Stock tab, click Operations), not in Cline: no Cline surface, command, setting, provider or error string is referenced, and nothing in apps/ or sdk/ contains an \"Operations\"/\"Stock\" UI.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Looks like a bug report meant for the user's own project that was filed on the Cline template by mistake. Safely auto-closeable.",
  "cluster": ""
 },
 {
  "number": 12048,
  "title": "Commands don't get executed",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "This is exactly the failure mode fixed by 12703bf40 (#11972, 2026-07-15), which added apps/vscode/src/sdk/vscode-terminal-execution-mode.ts. Its header comment states that `vscodeTerminal` mode \"is meaningless \u2014 and silently a no-op \u2014 on the standalone (JetBrains/CLI) build, where vscode.window.createTerminal is a stub\", and that a `vscodeTerminal` value saved by the real VS Code extension leaks into standalone sessions through the shared ~/.cline state; `getEffectiveTerminalExecutionMode` (:32-37) now clamps standalone hosts to `backgroundExec`, and it is applied at apps/vscode/src/sdk/vscode-session-host.ts:146.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "critical",
  "repro": null,
  "human_attention": "yes",
  "notes": "Silent no-op with a fabricated 'exit code 0, no output' result is the worst class of bug (the model acts on fake success), and three reporters hit it. Fix is on main but the JetBrains plugin pins its own cline-core build, so a maintainer should confirm which plugin release first carries #11972 before closing.",
  "cluster": "jetbrains-command-no-output"
 },
 {
  "number": 12053,
  "title": "Cline: User gets insufficient balance error",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The error is server-originated: main only classifies it, via `code === \"insufficient_credits\"` in SdkController (apps/vscode/src/sdk/SdkController.ts:1098-1111) and ClineError.getErrorType -> ClineErrorType.Balance (apps/vscode/src/services/error/ClineError.ts:163-165). I found no account-switching code on main that recomputes or caches a balance, so I cannot locate a client-side defect for the reported org->personal switch.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Filed by QA with a video; the reporter notes the account display flips to 'Cline external' after the failure, which suggests the active-account/credential selection is not propagating to the request rather than a genuine zero balance. Likely needs backend/account-service investigation, which is outside this repo.",
  "cluster": "insufficient-balance-free-models"
 },
 {
  "number": 12060,
  "title": "Generate Git Commit Message does not work for add only",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Confirmed on main: `getGitDiff` only runs `git --no-pager diff --staged --diff-filter=d` and then falls back to `git --no-pager diff HEAD --diff-filter=d` (apps/vscode/src/utils/git.ts:204-215). Neither command reports untracked files, so a workspace whose only change is a brand-new unstaged file yields an empty diff and throws `No changes in workspace for commit message` (:216-218), which commit-message-generator.ts:93 renders as `[Commit Generation Failed] ...`. No commits have touched either file since 2026-07-01.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Agent-fixable: include untracked files in the unstaged fallback (e.g. `git add --intent-to-add`-style listing via `git status --porcelain` plus `git diff --no-index /dev/null <file>`), matching what the VS Code SCM view shows the user.",
  "cluster": ""
 },
 {
  "number": 12062,
  "title": "team_run_task sync mode reports status \"running\" after the task has completed",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Confirmed verbatim on main: the sync branch of the `team_run_task` tool resolves with `mode: \"sync\", status: \"running\"` alongside the message \"Task dispatched to ... and completed in sync mode\" (sdk/packages/core/src/extensions/tools/team/team-tools.ts:541-550), while the async branch correctly returns `status: \"queued\"` (:513-521).",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Self-contradictory tool result actively misleads the orchestrating model into polling/retrying a finished teammate. One-line source fix plus a TeamRunTaskToolResultSchema update; a contributor (@CoderSufiyan) says they have the patch and regression test ready and is waiting for a maintainer.",
  "cluster": ""
 },
 {
  "number": 12063,
  "title": "team_run_task gives misleading \"Teammate was not found\" error when passed the lead agentId",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Confirmed on main: `routeToTeammate` rejects with the generic `Teammate \"<id>\" was not found` when the member exists but its role is not \"teammate\" (sdk/packages/core/src/extensions/tools/team/multi-agent.ts:1018-1022); `shutdownTeammate` has the identical conflation at :908-911. team_run_task forwards the id straight into that path (team-tools.ts:533-540).",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Error-message precision only; PR #12084 (open) adds a lead-specific guard before routing. Same conflation exists in shutdownTeammate and could be fixed in the same pass.",
  "cluster": ""
 },
 {
  "number": 12066,
  "title": "math fomula can't be rendered",
  "area": "vscode-new",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Not JetBrains-specific: the shared webview markdown renderer on main registers only remark-gfm and three local plugins, with rehype-highlight as the sole rehype plugin (apps/vscode/webview-ui/src/components/common/MarkdownBlock.tsx:79-85). There is no remark-math / rehype-katex anywhere in apps/vscode/webview-ui, so `$$...$$` and LaTeX environments render as raw text in VS Code too.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Agent-fixable: add remark-math + rehype-katex (or a lightweight alternative) to MarkdownBlock.tsx and ship the KaTeX stylesheet with the webview build; benefits both the VS Code and JetBrains surfaces.",
  "cluster": ""
 },
 {
  "number": 12067,
  "title": "Abnormal Huge Context Detected",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "A cumulative-metrics inflation bug of exactly this shape (per-message token estimates derived from cumulative request metrics, producing wildly oversized context and spurious auto-compaction) was fixed in be97d951f (#12022, 2026-07-01) \u2014 see the regression test \"does not treat cumulative request metrics as per-message token counts\" (sdk/packages/core/src/extensions/context/compaction.test.ts:49) guarding `createTokenEstimator` in sdk/packages/core/src/extensions/context/compaction-shared.ts. Whether the reporter's 4.0.6 build predates that commit is not determinable from the issue.",
  "verdict": "duplicate",
  "duplicate_of": 11660,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Spurious auto-compaction destroys conversation context, so this is worth confirming rather than closing. Ask for the provider/model (field was left blank) and the task's reported input/cache token metrics; a wrong estimator or a model whose maxTokens ~ contextWindow both funnel through resolveMaxInputTokens in compaction.ts:99-113.",
  "cluster": "inflated-token-count"
 },
 {
  "number": 12070,
  "title": "Follow-up questions are not rendered correctly",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "On main the follow-up options reach the webview as a plain `string[]`: the ask_question tool validates its input against `AskQuestionInputSchema`, which requires `options: z.array(z.string().min(1)).min(2).max(5)` (sdk/packages/core/src/extensions/tools/schemas.ts:258-271), and the host just JSON-stringifies them into the `followup` ask (apps/vscode/src/sdk/sdk-interaction-coordinator.ts:123-134) for OptionsButtons.tsx:38 to render. A single concatenated string should therefore be rejected by zod before rendering, so I could not confirm the concatenation path the reporter (and PR #12085) assume.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Reported with a local llama-server gemma model. A maintainer should get the raw tool-call payload before merging PR #12085, which normalizes the concatenated shape in the webview \u2014 if the schema is really rejecting it upstream, the UI fix papers over a small-model tool-call parsing problem in the wrong layer.",
  "cluster": ""
 },
 {
  "number": 12073,
  "title": "Cline bot regularly stuck in thinking mode",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Same symptom, same environment class (VS Code Remote SSH, long-running command turns) and same code area as #12079 \u2014 the VS Code terminal integration under apps/vscode/src/hosts/vscode/terminal/ (osc633Parser.ts, VscodeTerminalProcess.ts). A Cline maintainer (saoudrizwan) replied to this issue with nothing but a link to a comment on #12079.",
  "verdict": "duplicate",
  "duplicate_of": 12079,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Track under #12079; the SSH shell-integration guidance dominiccooney posted there applies directly.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 12076,
  "title": "When using OpenRouter's `Preset` models in Cline, it keeps repeating the same response.",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "OpenRouter presets are simply unimplemented: grepping `preset` across sdk/packages/llms/src returns no matches, so `openrouter/auto@preset/presets-test` is passed through as an opaque model id with no catalog entry (hence the 400s on other spellings). The repetition is the expected shape of the no-tool-call path: when an assistant turn contains no tool calls the runtime appends a completion reminder and loops (sdk/packages/agents/src/agent-runtime.ts:681-695), which a preset member model without tool-call support will never escape.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Two separable asks: (1) first-class OpenRouter preset support in @cline/llms including model metadata; (2) the loop is now bounded by the consecutive-mistake tracker's `completion_without_submit` reason (sdk/packages/core/src/runtime/safety/mistake-tracker.ts:169-176), tightened for VS Code in e4091c368.",
  "cluster": ""
 },
 {
  "number": 12079,
  "title": "Command execution shows \"skipped\" in Cline terminal and hangs on \"thinking\" \u2014 requires extension restart to recover",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The owning code is on main under apps/vscode/src/hosts/vscode/terminal/ (osc633Parser.ts, VscodeTerminalProcess.ts, VscodeTerminalManager.ts). Commit 12703bf40 (#11972, 2026-07-15) rewrote OSC 633 parsing, moved to the newer shell-execution-end API and added unobserved-command eviction (VscodeTerminalManager.ts:234-244), but the SSH/no-shell-integration case still falls back to a timeout-and-scrape path, which the maintainer (dominiccooney) states in-thread \"won't get stuck, but they were never great and still are not great\".",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Build and run the new extension from apps/vscode (`bun run build:webview && bun esbuild.mjs`, then `code --extensionDevelopmentPath=/workspace/apps/vscode`).\n2. Connect the window to a remote over Remote-SSH (or code-server / a Coder workspace) and open a workspace folder there. Explicitly do NOT source shell integration on the remote side, i.e. leave out `. \"$(code --locate-shell-integration-path zsh)\"`.\n3. Confirm terminal execution mode is the default `vscodeTerminal` (not Background Exec) and Checkpoints are enabled.\n4. Start a task and have the agent run several commands in sequence, including one that exits non-zero (e.g. `python` where only `python3` exists, exit 127) and one that emits >1 screen of output.\n5. Expected: each command reports its output and exit code back and the turn advances. Actual (reported by 6+ users): a command shows as \"skipped\" in the Cline terminal row and the turn hangs on \"thinking\" indefinitely; only restarting the extension host recovers it.\n6. Repeat with `terminal.integrated.shellIntegration.enabled: true` plus the remote-side shell hook to confirm the hang is specific to the markerless fallback path.",
  "human_attention": "yes",
  "notes": "Highest-traffic issue in this batch (15 comments, 6+ affected users) and already maintainer-owned. Two distinct sub-cases are being conflated: markerless/SSH terminals (fallback scrape) and local terminals where a malformed command breaks the prompt. One reporter also found disabling Checkpoints stopped an infinite `schedule assistant presentation` log loop \u2014 worth checking independently.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 12080,
  "title": "Cline CLI Crashes on Launch (Windows, regardless of args)",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The crash is a Bun runtime panic (`panic(main thread): Segmentation fault`) inside the compiled `@cline/cli-windows-x64` binary, not a JS-level throw; `main` still pins the same runtime (`package.json` `packageManager: bun@1.3.13`, `.github/workflows/cli-publish.yml` uses `bun-version: 1.3.13`) and still ships single-file Bun executables from `apps/cli/script/build.ts` (win32 -> `windows` target). I found no Windows-startup fix in `git log --since=2026-07-01 --grep=windows`, but the report is against 3.0.7 while `apps/cli/package.json` is now 3.0.46.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "critical",
  "repro": null,
  "human_attention": "yes",
  "notes": "Reporter cannot produce diagnostics (crashes before any Cline code runs). Ask for a retest on 3.0.46 plus the bun.report link; if it persists it is a Bun 1.3.13 issue we ship, so the fix is a runtime bump in the CLI build pipeline. The only non-bot comment is the reporter warning that a now-deleted spam comment linked malware.",
  "cluster": "cli-bun-binary-crash"
 },
 {
  "number": 12090,
  "title": "Team mode doesn't display a status to let users know it's active.",
  "area": "cli",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "On `main` team activity is only rendered as plain transcript lines: `apps/cli/src/tui/hooks/use-agent-events.ts` `handleTeamEvent` appends `[team] ...` / `[team run] ...` chat entries, and `apps/cli/src/tui/components/status-bar.tsx` contains no team state at all (grep for `team` in that file returns nothing). So there is no persistent indicator or spinner while teammate runs execute.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Owned by `apps/cli` TUI. Contributor @CoderSufiyan already claimed it; the natural fix is a team segment in `status-bar.tsx` driven by the existing `TeamEvent` stream.",
  "cluster": ""
 },
 {
  "number": 12094,
  "title": "Spurious Unauthorized errors",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "`sdk/packages/core/src/runtime/orchestration/session-runtime-orchestrator.ts` now has `executeRunWithAuthRetry`, which detects `isLikelyAuthError(result.text)`, calls `config.onAuthError()` and re-runs the turn once; `sdk/packages/core/src/runtime/host/local-runtime-host.ts` supplies that callback by force-refreshing OAuth credentials for OAuth providers. This landed in d859a86a6 (#12369, 2026-07-18), after the issue was filed on 2026-07-06.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Fix commit d859a86a6 'fix(sdk): retry runs once after refreshing expired OAuth credentials'. Worth asking the reporter to confirm on a build that includes it before closing, but the exact symptom (long session dies with the Unauthorized string, resumes on any prompt) is what that commit targets.",
  "cluster": "mid-session-unauthorized"
 },
 {
  "number": 12095,
  "title": "Infinity spinning loading model and converstation.",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The report has no text, only a screenshot, and names provider `LLMGATE` with models `Gemini-3.5-flash`/`Deepseek-v4-pro`. There is no `llmgate` provider id on `main` (`sdk/packages/llms/src/providers/provider-ids.generated.ts` has `llmgateway`, not `llmgate`), so this is a user-defined custom provider and I cannot map the hang to any concrete code path.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Needs the provider config, `cline doctor` output, and `~/.cline/data/logs/cline.log`. Unreproducible as filed.",
  "cluster": "stream-never-starts"
 },
 {
  "number": 12096,
  "title": "[Cerebras] \"Connection error\" on every request while identical curl to api.cerebras.ai succeeds (Cline 4.0.6, VS Code 1.127.0, macOS arm64)",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The generic `Connection error.` string comes from the Stainless-generated `@cerebras/cerebras_cloud_sdk` client used by the retired handler at `apps/vscode/src/core/api/providers/cerebras.ts` (present on the `legacy-extension` branch, absent on `main` \u2014 `apps/vscode/src/core/api/` contains only `index.ts`). On `main` Cerebras is a plain `openai-compatible` builtin (`sdk/packages/llms/src/providers/builtins.ts` id `cerebras`, baseUrl `https://api.cerebras.ai/v1`) driven by the AI SDK, whose network failures surface as `Cannot connect to API: <cause>` (`@ai-sdk/provider-utils` `handleFetchError`) \u2014 i.e. the error transparency the reporter asks for already exists.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Both the failing code path and the poor error message belong to the legacy 4.0.x marketplace build. Ask the reporter to retest on an SDK-based build; if it still fails the new message will name the real cause (DNS/TLS/proxy).",
  "cluster": "opaque-connection-error"
 },
 {
  "number": 12097,
  "title": "attempt_completion frames all post-completion messages as feedback - no branch for new/unrelated requests",
  "area": "sdk-agents",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The hardcoded string the report blames does not exist anywhere on `main`: `rg 'Consider their input to continue the task'` and `rg 'attempt completion again'` across the repo (excluding node_modules) both return nothing. The completion tool itself was renamed \u2014 `sdk/packages/core/src/runtime/orchestration/runtime-builder.ts` aliases `attempt_completion` to `submit_and_exit`, and `sdk/packages/core/src/runtime/host/local-runtime-host.ts` treats it purely as a telemetry/lifecycle signal; post-completion input arrives as an ordinary new turn via `prepareTurnInput`, with no feedback wrapper.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "The only `attempt_completion` text left in `apps/vscode/src/core/prompts/responses.ts` is the tool-use reminder, not a post-completion wrapper.",
  "cluster": ""
 },
 {
  "number": 12099,
  "title": "search_files mangles absolute Windows paths, suppresses the error, then blames the model",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "On `main` the search tool takes no path argument at all: `SearchCodebaseInputSchema` in `sdk/packages/core/src/extensions/tools/schemas.ts` is `{ queries: string[] }`, and `createSearchTool` in `sdk/packages/core/src/extensions/tools/definitions.ts` always passes the fixed `config.cwd` to the executor, so there is nothing to join onto the workspace root. Failures are also no longer swallowed \u2014 the executor wrapper returns `{ success: false, error: 'Search failed: ...' }` instead of `Found 0 results`. The model-capability nag is gone too: `sdk/packages/core/src/runtime/safety/mistake-tracker.ts` `buildMistakeLimitStopMessage` emits a generic message, and e4091c368 (#12561) removed the max-mistakes setting. `apps/vscode` only keeps `search_files` as a display alias (`apps/vscode/src/sdk/message-translator.ts`).",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs a human only to redirect @AriaZhao-coder, who announced a PR to fix path joining and error suppression in code that no longer exists on `main`. Worth noting separately that `main` currently cannot search outside the workspace at all \u2014 that would be a new feature request, not this bug.",
  "cluster": ""
 },
 {
  "number": 12108,
  "title": "Unbounded hub-daemon.log from zombie sessions degrades Dashboard performance",
  "area": "hub",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Both halves are still present on `main`. `sdk/packages/core/src/hub/server/handlers/run-handlers.ts` sets `HUB_RUN_HEARTBEAT_MS = 30_000` and `setInterval` unconditionally emits `logHubMessage(\"warn\", \"run.heartbeat\", ...)` for as long as `runTurn` is outstanding, with no cap or stuck-run abort. `sdk/packages/core/src/hub/daemon/index.ts` `openDetachedHubLogFile()` does `openSync(logPath, \"a\")` on `~/.cline/data/logs/hub-daemon.log`, and a repo-wide grep for log rotation/size limits in `sdk/packages/core/src`, `apps/cline-hub/src` and `apps/cli/src` returns nothing.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Two separable fixes: (1) size/retention-capped rotation for hub-daemon.log, (2) a run-level timeout so `run.start` sessions cannot heartbeat for days. The reporter's `elapsedMs` values (~598,586,345 ms \u2248 7 days) match a run that never settles.",
  "cluster": ""
 },
 {
  "number": 12111,
  "title": "Cline Nightly regularly exceeds context limit with ChatGPT 5.5 instead of compacting properly",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Compaction fires only when the estimated transcript crosses `COMPACTION_TRIGGER_RATIO` (0.9) of `resolveEffectiveMaxInputTokens` in `sdk/packages/core/src/extensions/context/compaction-shared.ts`, and that budget comes from the catalog, which lists `gpt-5.5` at contextWindow 1,050,000 / maxInputTokens 922,000 for `openai-native` in `sdk/packages/llms/src/catalog/catalog.generated.ts` \u2014 far above the ~400K the reporter observes. Token counting is a crude `Math.ceil(chars / 3)` (`sdk/packages/shared/src/llms/tokens.ts`), and there is no context-window-exceeded recovery anywhere (grep for `context_length_exceeded` / `isContextWindowError` across `sdk` and `apps` returns nothing), so an overflow is fatal rather than triggering a forced compaction.",
  "verdict": "duplicate",
  "duplicate_of": 10410,
  "severity": "high",
  "repro": "1. Build/run `apps/vscode` from `main` (or the nightly) and select provider `openai-native` with model `gpt-5.5`.\n2. Confirm the resolved budget: `sdk/packages/llms/src/catalog/catalog.generated.ts` reports contextWindow 1,050,000 / maxInputTokens 922,000, so compaction will not trigger until ~830K estimated tokens.\n3. Start a long agentic task that reads many large files so the transcript grows past the account's real input limit (the reporter sees ~400K).\n4. Expected: Cline compacts before the provider rejects the request. Actual: the provider returns a context-length error and the task stops with no auto-recovery.\n5. Cross-check by switching to `gpt-5.4` (smaller catalog budget) \u2014 the reporter's workaround \u2014 and confirm compaction fires.",
  "human_attention": "no",
  "notes": "Two candidate root causes to separate: (a) the catalog's gpt-5.5 input budget overstating the real limit, (b) no fallback that force-compacts and retries when the provider reports context overflow. 10cb9bd97 (#12142, 'Add compaction budget hardening', 2026-07-09) and 4a97b46f5 (#12217) landed after this report and may have partially addressed (a).",
  "cluster": "gpt55-context-overflow"
 },
 {
  "number": 12112,
  "title": "HDPO",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "Body is `> HAV FFO` quoted from a commit-line comment on 7cdaca0; there is no surface, version, reproduction, or identifiable symbol to look up anywhere in /workspace. Same author as 12117 ('GDPO'), which is equally contentless.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Auto-closeable.",
  "cluster": "gdpo-hdpo-noise"
 },
 {
  "number": 12115,
  "title": "Telemetry is still initialized and shutdown even when disabled, causing slow VS Code quit on macOS",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/vscode/src/services/telemetry/TelemetryProviderFactory.ts` `getDefaultConfigs()` never consults the user's `telemetrySetting` or VS Code's `telemetry.telemetryLevel`; it pushes a PostHog config whenever `isPostHogConfigValid(posthogConfig)` and an OpenTelemetry config whenever `getValidOpenTelemetryConfig()` returns one, then `createProvider` eagerly constructs `PostHogClientProvider.getClient()` / `new OpenTelemetryClientProvider(otelConfig)` and awaits `.initialize()`. Teardown always pays the cost too: `apps/vscode/src/common.ts` `tearDown()` calls `PostHogClientProvider.getInstance().dispose()` and `telemetryService.dispose()` unconditionally, and `apps/vscode/src/extension.ts` `deactivate()` awaits it.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "The 'disabled means no events sent, not no machinery' conclusion is exactly what the code does. The ~6s quit delay is the reporter's measurement and still needs confirming, but the early-return-a-no-op-provider fix they suggest is straightforward and also answers their privacy question.",
  "cluster": ""
 },
 {
  "number": 12117,
  "title": "GDPO",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "Body is the issue template with `1. GDPO 2. HDPO 3. MILESONE 4. RTOP/`, version `01`, empty repro steps. Nothing maps to any file, setting, or symbol in /workspace. Same author and same day as 12112.",
  "verdict": "duplicate",
  "duplicate_of": 12112,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Auto-closeable alongside 12112.",
  "cluster": "gdpo-hdpo-noise"
 },
 {
  "number": 12118,
  "title": "CLINE chat window buffer gets too large",
  "area": "vscode-new",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "The chat list is already virtualized (`apps/vscode/webview-ui/src/components/chat/chat-view/components/layout/MessagesArea.tsx` renders through `react-virtuoso`), so the growth is the retained message array and per-row content, not DOM nodes. There is no user-facing way to prune old turns without starting a new task \u2014 the only trimming on `main` is model-side compaction (`sdk/packages/core/src/extensions/context/compaction.ts`). The PR the maintainer pointed at, #11967, merged 2026-07-08 as 'perf(sdk): stop listSessions hot loop from hanging the extension host', which addresses a different hot loop.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Maintainer @dominiccooney already engaged. The ask (prune completed/irrelevant sections of a live conversation from the UI) is a product decision on top of the existing compaction machinery.",
  "cluster": ""
 },
 {
  "number": 12119,
  "title": "`StreamableHttpReconnectHandler` loops forever when server returns `200` + immediately-closed SSE on `GET /mcp` (fast-flap pattern)",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Both mechanisms the reporter traced are verbatim present on `main`. In `apps/vscode/src/services/mcp/StreamableHttpReconnectHandler.ts` the reconnect loop does `await this.callbacks.connectToServer(); ...; this.attempts = 0` immediately on success, so the `maxAttempts: 6` cap in `DEFAULT_RECONNECT_CONFIG` is never reached under fast-flap. And `apps/vscode/src/services/mcp/McpHub.ts` constructs `new StreamableHttpReconnectHandler(name, {...})` inside the `StreamableHTTPClientTransport` branch of `connectToServer`, so every reconnect also creates a fresh handler with `attempts = 0`. The `404 -> 405` GET shim they cite is likewise still the only special case in `McpHub`'s fetch wrapper.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "@chaitanya202's analysis in the comments is correct and worth reading: the retry budget needs to be server-scoped (owned by McpHub) with a stability window before reset, not transport-scoped. A design call is needed so the Linear-MCP idle-disconnect case #9642 fixed does not regress.",
  "cluster": "mcp-streamablehttp-transport"
 },
 {
  "number": 12120,
  "title": "Cannot not use (in ACP mode) without an account",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/cli/src/acp/auth.ts` hardcodes `ACP_AUTH_METHODS = [{ id: 'cline' }, { id: 'openai-codex' }]`, and `apps/cli/src/acp/acpAgent.ts` `newSession()` throws `RequestError.authRequired(...)` unless `this.authResult` is set or `process.env.CLINE_API_KEY` is present. `tryRestoreAuth()` only probes persisted credentials for those two provider ids, so a user who configured their own OpenAI-compatible provider via `cline auth` / config files is still forced through the Cline sign-in \u2014 exactly the reported behavior. `initialize()` also advertises only those two `authMethods` to the ACP client.",
  "verdict": "duplicate",
  "duplicate_of": 12043,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Owned by `apps/cli`. Fix direction: let `newSession` accept any already-configured provider (respect `CLINE_PROVIDER`/persisted provider settings) instead of gating on the two OAuth methods; note the default model is also hardcoded to `anthropic/claude-sonnet-4.6` in the same function.",
  "cluster": "acp-provider-gating"
 },
 {
  "number": 12123,
  "title": "Unable to find/change Cline System Commands/Prompts",
  "area": "vscode-new",
  "kind": "question-support",
  "relevant_to_main": "no",
  "evidence": "Both things the reporter says are missing exist on `main`: custom instructions are surfaced through `apps/vscode/webview-ui/src/components/cline-rules/ClineRulesToggleModal.tsx` (rules/workflows toggles), and the Ollama request timeout they wanted is a real setting \u2014 `apps/vscode/webview-ui/src/components/settings/providers/OllamaProvider.tsx` binds `requestTimeoutMs` (default 30000), declared in `apps/vscode/src/shared/storage/state-keys.ts`. Their own log shows `llama3.1:8b` emitting a bare `ask_followup_question` tool call and the stream aborting, which is small-local-model behavior, not a Cline defect.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Support request. Worth a docs pointer: there is no supported way to replace the system prompt wholesale, only to add rules/instructions \u2014 that expectation gap is the real content of the issue.",
  "cluster": ""
 },
 {
  "number": 12132,
  "title": "CLI: \"text part msg_... not found\" streaming from OpenAI-compatible backend that omits `content` key on empty deltas (e.g. llama.cpp server-rocm)",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The `msg_`-prefixed part id is the tell: `@ai-sdk/openai`'s Responses transport emits `text-start` only on `response.output_item.added` with `item.type === 'message'` (id = `value.item.id`) and `text-delta` with `id: value.item_id`; `ai`'s `streamText` recorder then enqueues `error: 'text part ' + part.id + ' not found'` when a delta arrives with no matching start. Cline routes LiteLLM through that transport: `sdk/packages/llms/src/providers/builtins.ts` declares the `litellm` builtin with `protocol: \"openai-responses\"`. The plain `openai-compatible` path would have used the fixed id `txt-0`, so the reporter's chat-completions-shaped analysis is slightly off but the failure is real and provider-config-driven.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Stand up a LiteLLM proxy in front of a backend that does not emit a Responses-API `response.output_item.added` message item before its first text delta (llama.cpp `server-rocm` per the reporter).\n2. From `main`, run `bun run cli -P litellm -m <model> \"say hi in 5 words\"`. Note `sdk/packages/llms/src/providers/builtins.ts` gives `litellm` `protocol: \"openai-responses\"`, so this hits `/v1/responses`.\n3. Expected: the reply streams. Actual: `error: text part msg_XXXX not found` on the first turn, reproducible on brand-new sessions and in `--tui`.\n4. Control: point the same proxy at a backend that emits well-formed Responses events (or override the provider protocol to `openai-compatible`) and confirm the turn succeeds \u2014 that isolates the missing start event as the trigger.",
  "human_attention": "yes",
  "notes": "Two things to decide: whether `litellm` should default to `openai-responses` at all given LiteLLM commonly fronts chat-completions backends, and whether `@cline/llms` should tolerate a text delta with no start (synthesize the start) rather than letting the AI SDK error the run out. The reporter did careful A/B isolation; their bug is real even though the stated mechanism is chat-completions-flavored.",
  "cluster": ""
 },
 {
  "number": 12133,
  "title": "Git commit --amend should not be considered a \"safe command\"",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "There is no safe-command classifier anywhere on `main`. `apps/vscode/src/sdk/sdk-tool-policies.ts` `isToolAutoApproved` resolves every command tool (`isCommandTool` = `run_commands` | `execute_command`) to the single flag `settings.actions.executeSafeCommands`, and `apps/vscode/src/shared/AutoApprovalSettings.ts` still names that field `executeSafeCommands` while the webview labels it 'Execute commands' (`apps/vscode/webview-ui/src/components/chat/auto-approve-menu/constants.ts`). So enabling it auto-approves `git commit --amend` and everything else.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Maintainer @dominiccooney already flagged this as a migration regression and asked @reneehuang1/@saoudrizwan whether to rename the option or rebuild a real safe-command list, and whether to align with the CLI (which has no 'safe' concept). That is a product decision, not an agent-fixable bug. Related discussion on #12150.",
  "cluster": ""
 },
 {
  "number": 12153,
  "title": "CLI crashes after ~15s \u2014 session.create times out, hub-daemon reports kanban/dist/entry.js MODULE_NOT_FOUND",
  "area": "hub",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`sdk/packages/core/src/hub/daemon/index.ts` `resolveDaemonEntryPath()` derives the daemon entry from `import.meta.url` of whichever copy of `@cline/core` spawned it, so a hub launched from a different package (the globally installed `kanban`, which embeds core) records a `.../kanban/dist/...` entry path that breaks when that package is upgraded \u2014 matching the reported `Cannot find module \".../kanban/dist/entry.js\"` plus `EADDRINUSE` on 127.0.0.1:25463. There is no `kanban` entry path in this repo (grep across `apps/cli/src`, `apps/cline-hub/src`, `sdk/packages/core/src` finds only the CLI's `kanban` launcher in `apps/cli/src/commands/kanban.ts`). The 30s client-side ceiling is real too: `sdk/packages/core/src/hub/client/index.ts` errors with `Hub command ... timed out`, and `hub-runtime-host.ts` `recoverLocalHubStartupDeadlock` only recovers when the hub is *idle*, not when it is degraded.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. `npm i -g cline@latest` and `npm i -g kanban@latest` so both packages ship a copy of `@cline/core`.\n2. Start the hub from the kanban side (run `kanban`, or `cline --kanban`) so the discovery record points at the kanban-owned daemon entry.\n3. Upgrade or reinstall `kanban` to a version whose bundle layout differs (reporter: 0.1.69, whose entry is `dist/index.js`, not `dist/entry.js`).\n4. Run `cline`. Expected: CLI starts interactively. Actual: hub-daemon.log shows `Cannot find module .../kanban/dist/entry.js` then `EADDRINUSE`, and the CLI exits after ~15s with `HubCommandError: Hub command session.create timed out after 30000ms`.\n5. Confirm the workaround the reporter found \u2014 `cline --thinking medium \"hello\"` succeeds because it bypasses the hub.",
  "human_attention": "yes",
  "notes": "Cross-product hub-ownership problem in the shared-daemon design, not a kanban packaging bug alone. Worth deciding whether the daemon entry path should be re-resolved (and the discovery record invalidated) when the recorded path no longer exists, and whether `recoverLocalHubStartupDeadlock` should also fire for a degraded-but-listening hub.",
  "cluster": "hub-session-loss"
 },
 {
  "number": 12157,
  "title": "Rules, hooks and workflows should NOT be saved in ~/Documents",
  "area": "vscode-new",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Still hardcoded on `main` in both layers. `sdk/packages/shared/src/storage/paths.ts` defines `resolveDocumentsClineDirectoryPath()` as `join(HOME_DIR, \"Documents\", \"Cline\")` and feeds it into `resolveHooksConfigSearchPaths`, `resolveRulesConfigSearchPaths` (via `resolveDocumentsExtensionPath(\"Rules\")`), `resolveWorkflowsConfigSearchPaths` and `resolvePluginConfigSearchPaths`, with no `$XDG_*` handling. `apps/vscode/src/core/storage/disk.ts` goes further and `mkdir`s them unprompted \u2014 `ensureRulesDirectoryExists`, `ensureWorkflowsDirectoryExists`, `ensureMcpServersDirectoryExists`, `ensureHooksDirectoryExists` \u2014 and `ensureMcpServersDirectoryExists()` is invoked when McpHub is constructed in `apps/vscode/src/sdk/SdkController.ts`.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Maintainer @dominiccooney already agreed and added that the path is watched and aggressively recreated. Needs a product decision on the target location (`~/.cline` vs XDG) and a migration for existing users; touches `@cline/shared` paths plus every app that calls the `ensure*DirectoryExists` helpers.",
  "cluster": ""
 },
 {
  "number": 12158,
  "title": "CLI does not save general settings",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The `/settings` toggles only mutate the in-memory `Config`. `apps/cli/src/runtime/run-interactive.ts` wires `onAutoApproveChange` to `setInteractiveAutoApprove`, which in `apps/cli/src/runtime/interactive/approvals.ts` just sets `autoApproveAllRef.current` and calls `applyInteractiveAutoApproveOverride({ targetPolicies: config.toolPolicies, ... })`; `onCompactionModeChange` calls `applyCliCompactionMode` in `apps/cli/src/utils/compaction-mode.ts`, which assigns `config.compaction = {...}` and returns. Neither writes to disk, and `apps/cli/src/tui/contexts/session-context.tsx` re-seeds state on startup from `config.toolPolicies[\"*\"]?.autoApprove !== false` \u2014 so every restart falls back to the file/default value.",
  "verdict": "duplicate",
  "duplicate_of": 10861,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Severity is driven by the safety direction the reporter calls out: because the in-memory auto-approve override is lost, a user who turned auto-approve off gets it back on at next launch. Fix belongs in `apps/cli` (persist through the same store `/settings` reads from).",
  "cluster": "cli-settings-not-persisted"
 },
 {
  "number": 12161,
  "title": "Error: JSON error injected into SSE stream",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The literal string is not produced by Cline or its shipped deps \u2014 grep for `JSON error injected` across /workspace (including node_modules) returns nothing \u2014 so it is an upstream payload (OpenRouter injecting an error object into an in-flight SSE stream) surfaced verbatim. What is grounded on `main` is that Cline has no mid-stream recovery: `openrouter` is a plain `openai-compatible` builtin in `sdk/packages/llms/src/providers/builtins.ts`, and there is no request/stream retry configuration in `sdk/packages/llms/src` (grep for `maxRetries` there returns nothing), so a mid-stream error aborts the turn.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "As filed there are no repro steps, model, or logs. Still worth a human because the underlying gap \u2014 mid-stream provider errors kill a long session with no retry or resume \u2014 is real and matches other reports in this batch (12094-style interruptions). Ask for the model id and whether it correlates with a specific OpenRouter upstream.",
  "cluster": ""
 },
 {
  "number": 12172,
  "title": "Prompt caching setting persists across Plan/Act mode models",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The reporter's inspection of `globalState.json` matches the schema on `main`: `apps/vscode/src/shared/storage/state-keys.ts` declares `awsBedrockUsePromptCache` under 'Global configuration (not mode-specific)', while the Bedrock model selection is per-mode (`planModeAwsBedrockCustomModelBaseId`, `planModeAwsBedrockCustomSelected`, and the act-mode counterparts). There are no `planMode*/actMode*` prompt-cache keys. The SDK carries the same single-value shape \u2014 `sdk/packages/llms/src/providers/builtins.ts` maps the setting to the path `aws.usePromptCache`, read once in `sdk/packages/llms/src/providers/compat.ts`.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs a design choice between the reporter's two options: make prompt caching mode-scoped like the model selection, or ignore the flag for models whose ModelInfo lacks the `prompt-cache` capability (the catalog already tracks that capability, which makes option 2 cheap and fixes the plain non-split case too).",
  "cluster": ""
 },
 {
  "number": 12174,
  "title": "Cline: Mac Specific: Unable to install certain MCP servers from the Marketplace",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The install path exists on `main` and is short: `apps/vscode/src/core/controller/marketplace/installMarketplaceEntry.ts` -> `installMarketplaceEntryFromCatalog` -> `installMcpMarketplaceEntry` (`marketplace-helpers.ts`) -> `installMcpServer` in `sdk/packages/core/src/services/mcp-install.ts`, then `controller.mcpHub.reconcileMcpServersFromSettingsRPC()`. I verified the settings file is not split-brained \u2014 `installMcpServer` falls back to `resolveDefaultMcpSettingsPath()` (`~/.cline/data/settings/cline_mcp_settings.json`) and `SdkController.ts` constructs `McpHub` with `path.dirname(resolveDefaultMcpSettingsPath())`, so both sides read the same file. Errors are surfaced (`MarketplaceView.tsx` `handleInstall` sets `error` on throw), so a completely silent no-op is not explained by the code I read.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. On macOS, run `apps/vscode` from `main` and open MCP > Marketplace.\n2. Install a server that succeeds (control) and note it appears under Installed.\n3. Install one of the reported entries (Aikido, Serena, AWS Data Analytics, AWS LAC, AWS Pricing) and watch three things: the `error` banner in `MarketplaceView`, whether `~/.cline/data/settings/cline_mcp_settings.json` gained the server, and the extension host log.\n4. If the settings file did gain the server but Installed stays empty, the bug is on the listing side (`listInstalledMarketplaceEntries` -> `isMcpInstalled`, which matches on `getEntryArgs(entry)[0]` against `mcpHub.getServers()` names); if the file is unchanged, the bug is in `getEntryArgs`/`parseMcpInstallArgs` for those catalog entries.\n5. Expected: server installs and appears under Installed. Actual (per reporter's video): nothing happens.",
  "human_attention": "yes",
  "notes": "QA-filed with a video, so the repro is cheap. Two leads worth checking first: (a) `isMcpInstalled` matches by the first install arg / entry id / entry name, so a name mismatch in those specific catalog entries would make a successful install look like a no-op; (b) those servers are `uvx`/`docker`-style stdio servers, and macOS GUI VS Code does not inherit the login-shell PATH \u2014 compare with 099c6179e, which fixed exactly that class of problem for the desktop app.",
  "cluster": ""
 },
 {
  "number": 12176,
  "title": "Cline: Installed MCP server gets removed from MCP server section when user relaunch IDE after turning off installed MCP server",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`apps/vscode/src/services/mcp/McpHub.ts` `getServers()` deliberately returns only enabled servers (`this.connections.filter((conn) => !conn.server.disabled)`), but `listLocalMarketplaceInstalledEntries` in `apps/vscode/src/core/controller/marketplace/marketplace-helpers.ts` builds the Customise > installed list from `controller.mcpHub?.getServers()` and then sets `enabled: server.disabled !== true` \u2014 a field that can never be false, proving the intent was to include disabled servers. So a disabled MCP server is filtered out of the installed list, which is exactly the reported disappearance. The disabled state itself persists correctly (`toggleServerDisabledRPC` writes `disabled: true` to the settings file and `connectToServer` pushes a disconnected stub connection for it), and the MCP configuration view is fine because it goes through `notifyWebviewOfServerChanges` -> `getSortedMcpServers`, which does not filter.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Small, well-localized fix: give `listLocalMarketplaceInstalledEntries` (and probably `isMcpInstalled`, which has the same `getServers()` dependency and would report a disabled server as not installed) an all-connections accessor instead of the enabled-only `getServers()`.",
  "cluster": ""
 },
 {
  "number": 12184,
  "title": "Spawn agents to review changes sometimes is breaking lines per word in TUI",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The subagent/team transcript rows are still rendered on main by `apps/cli/src/tui/components/chat-entry.tsx` case \"team\", which puts a long `<text selectable content={entry.text}>` inside a `<box flexDirection=\"row\">` with no width/flexGrow/flexShrink and no `wrapMode`; in that layout the text node collapses toward its min-content width (longest word), which is exactly the one-word-per-line screenshot. No commit touching that renderer exists since the 2026-07-09 report.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "low",
  "repro": "1. Run the CLI from source: `bun run cli -i` in a git repo with uncommitted changes.\n2. Configure a remote provider/model (e.g. `cline:deepseek/deepseek-v4-pro`) via `cline auth` or env var.\n3. Make the terminal window wide (the reporter used a 3832px-wide terminal under tmux) so the transcript area is much wider than the longest word.\n4. Prompt: `read changes and spawn an agent to review the changes you made; report the review back`.\n5. Watch the grey `*`-prefixed team/subagent status rows as the review text streams in.\nExpected: review text wraps at the transcript width.\nActual: text intermittently wraps after every word, one word per line.",
  "human_attention": "no",
  "notes": "Owned by @cline/cli TUI rendering. Suspect the `flexDirection=\"row\"` bullet+text pattern in chat-entry.tsx needs `flexGrow={1}`/explicit `wrapMode=\"word\"` on the text child; the same pattern is used by the error/status rows so a shared fix is possible.",
  "cluster": "tui-render-tearing"
 },
 {
  "number": 12198,
  "title": "Cline hangs completely after skipping a command execution, requires VS Code restart",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The reporter himself flags #12079 (\"Command execution shows 'skipped' in Cline terminal and hangs on 'thinking'\", open, filed 2026-07-05) as the same bug, and the \"Skipped\" label is just the fall-through status in `apps/vscode/webview-ui/src/components/chat/CommandOutputRow.tsx` (`CommandStatusMap.skipped`) for a command row that never reached completed \u2014 i.e. a stuck command lifecycle, not a user action. On main the whole terminal stack was replaced by 12703bf40 (#11972, 2026-07-14, OSC 633 parser/exit codes/timeouts) after this report, and contributor @dominiccooney could not reproduce on 4.0.7 or nightly.",
  "verdict": "duplicate",
  "duplicate_of": 12079,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Fold into #12079 and carry the extra data points there (two reporters state no Skip was ever clicked, so the trigger is runtime-side, and only an extension-host restart recovers). The underlying #12079 is still open and does need attention.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 12200,
  "title": "GLM 5.2 produces malformed file paths / search blocks when routed through Cline Pass, but works correctly via OpenRouter",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "All three quoted errors come from the legacy edit stack: `\"Document not initialized\"` exists only at `origin/legacy-extension:apps/vscode/src/integrations/editor/FileEditProvider.ts:42` and has no match anywhere on main, and `replace_in_file`/`write_to_file` survive on main only as legacy aliases mapped to the SDK `editor` tool (`sdk/packages/core/src/runtime/orchestration/runtime-builder.ts:94`). `apps/vscode/src/integrations/editor/` on main contains only `EditPreview.ts` and `CommentReviewController.ts` \u2014 FileEditProvider is gone.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "The Cline-Pass-specific framing is also contradicted in-thread: @shinodark reports the identical errors with GLM 5.2 via OpenRouter and @breathingcyborg with Ollama, so this is a weak-model malformed-path problem, not a gateway routing problem. Worth a retest on the SDK build against the `editor` tool before closing.",
  "cluster": ""
 },
 {
  "number": 12208,
  "title": "Cline 4.0.7 isn't loading on VS Codium",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The reporter's own log shows `[Cline] extension activated in 151ms` and `ExtensionService#_doActivateExtension saoudrizwan.claude-dev` succeeding, so activation is fine and the failure is view resolution. On main the sidebar view is registered unconditionally at `apps/vscode/src/extension.ts:119` (`vscode.window.registerWebviewViewProvider(VscodeWebviewProvider.SIDEBAR_ID, ...)`, id `claude-dev.SidebarProvider`), and `apps/vscode/package.json` declares `engines.vscode: ^1.101.0` with `activationEvents: [onLanguage, onUri, onStartupFinished]`, all satisfied by the reporter's VS Codium 1.112. Nothing in the log or the code identifies the defect.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Reporter states it is workspace-dependent (\"loads in some projects, not most others\") and that it eventually self-recovered after `Cline: Focus on view`. Needs a VS Codium repro with a specific workspace plus the webview/renderer log, not just the extension-host log. Also a churn-risk report worth a human reply.",
  "cluster": "extension-not-activating"
 },
 {
  "number": 12210,
  "title": "Using Meta MuseSpark v1.1,  Cline returns error \"Cline uses complex prompts...\"",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The quoted string is the old 3.x `mistake_limit_reached` copy and does not exist anywhere on main (`rg \"iterative task execution\"` \u2192 no matches; the only surviving \"Cline uses complex prompts\" text is the static provider-settings warning in `apps/vscode/webview-ui/src/components/settings/providers/OpenAICompatible.tsx:603`). On main the same condition renders \"Cline ran into N errors in a row and stopped the task\" from `apps/vscode/src/sdk/sdk-interaction-coordinator.ts` `handleConsecutiveMistakeLimitReached`. The trigger is unchanged though: a model that can't emit valid tool calls trips `MistakeTracker` in `sdk/packages/core/src/runtime/safety/mistake-tracker.ts`.",
  "verdict": "duplicate",
  "duplicate_of": 5915,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "#5915 is the canonical thread for this and is already labeled \"Model Quality\". The bug template itself requires reproducibility on Claude Sonnet 4.5 (.github/ISSUE_TEMPLATE/bug_report.yml:8); a custom OpenAI-compatible endpoint that cannot do native tool calls is out of that bar.",
  "cluster": "weak-model-mistake-limit"
 },
 {
  "number": 12220,
  "title": "CLI ask_question() just spins",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Every link in the chain still exists on main and looks wired: `ask_question` is registered by `sdk/packages/core/src/extensions/tools/definitions.ts:927` and is `enableAskQuestion: true` in both the act and plan presets (`sdk/packages/core/src/extensions/tools/presets.ts:33,51`); the executor is passed from `apps/cli/src/runtime/interactive/session-runtime.ts:158` through `askQuestionRef`, set by `apps/cli/src/tui/hooks/use-runtime-dialog-bridge.tsx` `setAskQuestion`, and rendered by `apps/cli/src/tui/views/chat-view.tsx` via `InlineToolResponse`. `askQuestion` is also a valid hub-routed executor (`sdk/packages/shared/src/hub.ts:609`). No fix commit since 3.0.39 (main is 3.0.46).",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Build the SDK (`bun run build:sdk`) and start the CLI TUI: `bun run cli -i`. The hub daemon auto-spawns (backendMode \"auto\", so the session may live in the detached hub process \u2014 that is the interesting case).\n2. Configure a provider/model that reliably calls tools (reporter used `openai-codex:gpt-5.6-terra`).\n3. Send: `Ask me which package manager I prefer using ask_question.`\nExpected: the `InlineToolResponse` ask_question dialog renders with the question and selectable options, and typing/selecting resolves the tool call.\nActual: an indefinite spinner; no question or options render; the composer stays in normal mode and keystrokes are queued as the next task message.\n4. Repeat with `--force-local`-style local backend (yolo/sandbox mode forces `forceLocalBackend`) to isolate whether the hub RPC round-trip for the `askQuestion` capability is the broken hop.",
  "human_attention": "yes",
  "notes": "Second reporter (@hajilok) confirms. Prime suspect is the hub tool-capability round trip for `askQuestion` (`sdk/packages/core/src/hub/server/hub-client-contributions.ts` toolExecutor contributions) rather than the TUI bridge, since the local-runtime path resolves the ref synchronously.",
  "cluster": "ask-question-schema-too-strict"
 },
 {
  "number": 12226,
  "title": "Cline: Queued prompt Is lost after using \"Start New Task\" and reopening the original task.",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The queue is pure in-memory session state: `pendingPrompts: PendingPrompt[]` is a field on `ActiveSession` (`sdk/packages/core/src/types/session.ts:35`) managed by `sdk/packages/core/src/runtime/turn-queue/pending-prompt-service.ts`, and there is no persistence of it anywhere in the storage layer. The VS Code side only ever reads it live off the active session (`apps/vscode/src/sdk/SdkController.ts:1958` calls `activeSession.sdkHost.pendingPrompts(\"list\", ...)`), so \"Start New Task\" disposing the session drops the queue and reopening from history rehydrates messages but not queued prompts.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Owned by @cline/core (turn-queue). Needs a product decision: persist `pendingPrompts` alongside the session record, or warn/flush the queue when a task is switched away from. QA-filed with a video, and the same loss will hit any surface that disposes a session.",
  "cluster": ""
 },
 {
  "number": 12235,
  "title": "Cline gets stuck at step 8 (verification) during Phase 4 implementation and does not proceed",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "\"Step 8\" is the reporter's own blueprint numbering, not a Cline concept; the only concrete detail is that Cline hung while running syntax checks and \"starting the dev server\". On main a foreground command that never exits is expected to block the turn until the user detaches \u2014 `apps/vscode/src/sdk/vscode-run-commands-tool.ts` awaits `process` and exposes `applyDetach`, surfaced as the \"Proceed While Running\" button (`apps/vscode/webview-ui/src/components/chat/chat-view/shared/buttonConfig.ts:82,197`). No logs, no session id, no error text to distinguish that from the genuine hang in #12079.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Ask whether the \"Proceed While Running\" button was offered (expected behavior for a dev server) or whether the UI was frozen with no affordance (then it is #12079). Low-signal report against a weak model (deepseek-v4-flash) with no diagnostics.",
  "cluster": ""
 },
 {
  "number": 12236,
  "title": "Skill does not show up using /",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Real parity gap on main. `apps/vscode/src/core/controller/slash/getAvailableSlashCommands.ts` builds the autocomplete list from `BASE_SLASH_COMMANDS` (`apps/vscode/src/shared/slashCommands.ts` \u2014 newtask/deep-planning/smol/newrule/reportbug) plus local/global/remote workflow toggles only; skills are never enumerated. The CLI does enumerate them: `sdk/packages/core/src/extensions/config/runtime-commands.ts:62` emits `listCommandsForKind(watcher, \"skill\")`, surfaced via `listInteractiveSlashCommands` \u2192 `apps/cli/src/tui/hooks/use-slash-commands.ts` `skillCommands` \u2192 `useAutocomplete`.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Reproduced by inspection, and @nejtr0n confirms the same in JetBrains (same webview + same controller). Fix is to have `getAvailableSlashCommands` read skill runtime commands from the user-instruction service the way the CLI does, respecting the SKILL.md `disabled` frontmatter.",
  "cluster": ""
 },
 {
  "number": 12237,
  "title": "Typed prompt intermittently lost when switching from Act mode to Plan mode before sending",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "On main this loss should not be possible: `apps/vscode/webview-ui/src/components/chat/ChatTextArea.tsx` `onModeToggle` clears the composer only when `response.value` is true AND the textarea still holds the exact submitted text, and `apps/vscode/src/sdk/sdk-mode-coordinator.ts` `togglePlanActMode` returns `continuationSent && hasUserContent`, where `continuationSent` requires `autoContinue`, which is only ever set for `modeToSwitchTo === \"act\"`. So an act\u2192plan toggle can never report the composer as consumed. The guard predates the report (31cff1f75, 2026-06-24), and the reported 4.0.8 build is the legacy engine, whose `Controller.togglePlanActMode` instead calls `cancelTask()` on an act\u2192plan switch.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs a retest on the SDK-based extension. If it still reproduces there, the likely culprit is the send racing the session rebuild (`SdkModeCoordinator.waitForPendingRebuild`), not the composer-clearing path.",
  "cluster": "prompt-lost-on-mode-switch"
 },
 {
  "number": 12238,
  "title": "Cannot access my account through VS Code in my laptop browser",
  "area": "unknown",
  "kind": "question-support",
  "relevant_to_main": "no",
  "evidence": "The failing URL `/api/v1/auth/authorize` is only a constant in this repo (`sdk/packages/core/src/auth/cline.ts:26`, `apps/vscode/src/shared/cline/api.ts:2`); the endpoint itself lives on Cline's backend. \"Forbidden \u2014 Your client does not have permission to get URL ... from this server\" is a Google-frontend/WAF 403 served before any Cline code runs, and the reporter says the same account signs in fine from mobile, so it is network/IP-scoped, not a client defect.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Route to backend/support: someone with access to the auth service's edge rules needs to check whether that user's ISP/IP range or a corporate proxy is being blocked. Nothing to fix in this repo.",
  "cluster": ""
 },
 {
  "number": 12239,
  "title": "after restart to resolve hanging sessions - complete session gets lost occationally",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Task history on main no longer comes from the legacy `state/taskHistory.json` + `tasks/<id>/` store the 4.0.8 build used; it is served from `@cline/core` session history records via `apps/vscode/src/sdk/sdk-task-history.ts` (with `legacy-state-reader.ts` only as a migration path), so the persistence path that lost the session is not the one running on main. I could not find a flush/debounce hole to blame \u2014 the `saveClineMessagesTimer` field in `apps/vscode/src/sdk/sdk-message-coordinator.ts:22` is never actually set, so it is dead code, not a delayed write.",
  "verdict": "duplicate",
  "duplicate_of": 11663,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Data loss, so worth a human look despite the thin report (no steps, no version detail beyond 4.0.8, filed as a follow-on to the #12079 hang). Needs: which store the missing task lived in (`~/.cline/data` vs legacy global storage), and whether the task ever appeared in History before the restart.",
  "cluster": "task-history-loss"
 },
 {
  "number": 12240,
  "title": "Cline session not found issue is back",
  "area": "hub",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The `session_not_found` code is still live on main across the hub boundary \u2014 `sdk/packages/core/src/runtime/host/runtime-host.ts:25` (`SESSION_NOT_FOUND_ERROR_CODE`), `sdk/packages/core/src/hub/server/handlers/session-handlers.ts:661,690,772,793,817,927`, and `hub-runtime-host.ts:1447` \u2014 and the in-flight fix PR #11011 (\"fix(core): recover hub sessions after daemon replacement\") is still OPEN and unmerged (`gh pr view 11011`), with no matching commit on main.",
  "verdict": "duplicate",
  "duplicate_of": 11661,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "@minh2416294's comment correctly identifies #11661 as the forensic parent, and the local-model-never-fails observation matches the process-boundary theory (local sessions stay in one in-proc map). Merge into #11661; the real ask is unblocking PR #11011, which is stalled.",
  "cluster": "hub-session-loss"
 },
 {
  "number": 12242,
  "title": "GPT 5.5 not working -  'function_call' was provided without its required 'reasoning' item",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "`@cline/llms` cannot round-trip OpenAI Responses-API reasoning items. `sdk/packages/llms/src/providers/vendors/openai.ts` builds the model with `provider.responses(modelId)`, and `shouldIncludeReasoningHistory` (`ai-sdk.ts:254`) keeps reasoning in history for openai-native, but `toAiSdkMessages` (`ai-sdk.ts:288-312`) only re-attaches `providerOptions.anthropic.{signature,redactedData}` on reasoning parts and only `providerOptions.google.thoughtSignature` on tool-call parts \u2014 nothing captures or replays `openai.itemId` / `reasoningEncryptedContent`. In `@ai-sdk/openai@3.0.84` that path falls into the `else` branch and warns \"Non-OpenAI reasoning parts are not supported. Skipping reasoning part\", so the function_call is sent while its paired `rs_...` item is dropped, which is exactly the 400 the reporter got.",
  "verdict": "duplicate",
  "duplicate_of": 10656,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Owned by @cline/llms. Fix is to capture the openai reasoning item id / encrypted content off `part.providerMetadata.openai` in `emitAiSdkEvents` and replay it as `providerOptions.openai` in `toAiSdkMessages`. Blocks gpt-5.5 on the native OpenAI provider once a tool call is in history.",
  "cluster": "codex-responses-item-pairing"
 },
 {
  "number": 12246,
  "title": "Repeated command execution and task continuation after stop in Git review task",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The \"kept going after I stopped it\" half is visible on main: `apps/vscode/src/sdk/vscode-run-commands-tool.ts:366` sets `applyAbort = () => process.continue()`, and `apps/vscode/src/hosts/vscode/terminal/VscodeTerminalManager.ts:27` documents that `continue()` \"resolves promise and stops event emission\" \u2014 it explicitly does not terminate the command. The \"repeated identical commands\" half also has a real gap: `sdk/packages/core/src/runtime/safety/loop-detection.ts` only tracks `lastToolName`/`lastToolSignature`, so a repeating *cycle* of different git commands (fetch \u2192 diff --stat \u2192 diff -- file \u2192 branch \u2192 fetch \u2026) never trips the consecutive-identical detector.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Contributor @CoderSufiyan independently traced the same `process.continue()` gap on main. Safety-relevant: @fastfret888 reports commands (builds, deploys) firing after \"Task Completed\". Two separable fixes \u2014 kill the foreground process on abort, and extend loop detection beyond strictly-consecutive identical calls.",
  "cluster": ""
 },
 {
  "number": 12247,
  "title": "`reasoning-delta` stream parts are silently dropped in `@cline/llms` (wrong field name: `part.reasoning` instead of `part.delta`)",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The claimed defect is disproven and the reporter conceded in-thread. `ai@6.0.224`'s `streamText` fullStream emits `{ type: \"reasoning-delta\", ..., text: chunk.delta }` (node_modules/.bun/ai@6.0.224.../dist/index.mjs:7783-7785), so the second fallback `part.text` in `sdk/packages/llms/src/providers/ai-sdk.ts:939` already picks the content up and `?? part.delta` would change nothing. The secondary symptom is also stale: `sdk/packages/agents/src/agent-runtime.ts:646` only errors when `message.content.length === 0`, and reasoning is pushed as a `reasoning` content part (`agent-runtime.ts:936-955`), so a reasoning-only turn is no longer treated as empty.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "The only substantive leftover is the reporter's follow-up that he hit \"Model returned empty response\" (`agent-runtime.ts:650`) with LM Studio on main before he could exercise the original path \u2014 that is a distinct symptom and should be split into a fresh issue with a raw SSE capture. The suggested cleanup (one shared fallback helper for the text-delta/reasoning-delta branches) is a no-behavior-change, agent-fixable tidy-up.",
  "cluster": ""
 },
 {
  "number": 12249,
  "title": "Consider enabling required-review branch protection on `main`",
  "area": "infra",
  "kind": "meta-task",
  "relevant_to_main": "no",
  "evidence": "The premise is factually wrong. `gh api repos/cline/cline/rulesets/3416434` shows an active repository ruleset \"Main branch protection\" covering `refs/heads/main` and `refs/heads/legacy-extension`, with `pull_request` rules `required_approving_review_count: 1`, `require_code_owner_review: true`, `require_last_push_approval: true`, `required_review_thread_resolution: true`, squash-only merges, a required `test` status check, plus deletion and non-fast-forward protection \u2014 and `bypass_actors: null`, i.e. nobody can merge around it. The ruleset was last updated 2026-07-02, ten days before this issue was filed.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Close with a pointer to the ruleset. The one sub-request not currently enabled is `dismiss_stale_reviews_on_push` (currently false); if the team wants it, that is an admin-only settings change, not a code change.",
  "cluster": ""
 },
 {
  "number": 12252,
  "title": "[Bug] Temperature parameter deprecated error when using Claude Sonnet 5 on Amazon Bedrock with Application Inference Profile",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The legacy handler hardcoded the parameter: `origin/legacy-extension:apps/vscode/src/core/api/providers/bedrock.ts` sets `temperature: 0` at lines 407, 892, 898, 1221 and 1359. On main the Bedrock vendor module `sdk/packages/llms/src/providers/vendors/bedrock.ts` (171 lines) contains no `temperature` reference at all, and the generic path only forwards it when the caller supplied one (`sdk/packages/llms/src/providers/ai-sdk.ts:64` `temperature: request.temperature`, undefined by default; nothing in `apps/vscode/src/sdk/` sets a Bedrock temperature).",
  "verdict": "duplicate",
  "duplicate_of": 10573,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Residual risk only if a user explicitly configures a temperature for a model that rejects it \u2014 main has no capability gate that strips `temperature` for models lacking the `temperature` capability (the only such check, `sdk/packages/core/src/services/llms/provider-defaults.ts:500`, is Poolside-specific). Worth a small hardening ticket, not this bug.",
  "cluster": "bedrock-temperature-deprecated"
 },
 {
  "number": 12258,
  "title": "Can't set cloudflare router",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The actual blocker the reporter describes is \"in Linux can't paste strings into cline web view settings\" under Rider \u2014 a JCEF/JetBrains-host clipboard problem, not code in this repo. The workaround error he then hit is legacy-only: \"Anthropic API key is required\" exists solely at `origin/legacy-extension:apps/vscode/src/core/api/providers/anthropic.ts:47` and has no match anywhere on main. Hand-editing `globalState.json`/`secrets.json` is not a supported configuration path.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Two separate things: route the Linux paste failure to the JetBrains plugin repo, and note that the config he hand-wrote left `modelId: claude-sonnet-5` under `providerId: openai-compatible`, which is why an Anthropic key was demanded.",
  "cluster": ""
 },
 {
  "number": 12264,
  "title": "Repeated \"Cline uses complex prompts and iterative task execution\" prompts",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The specific complaint \u2014 being re-prompted over and over with no way to say \"keep going\" \u2014 was removed by e4091c368 (#12561, 2026-07-26): `apps/vscode/src/sdk/sdk-interaction-coordinator.ts` `handleConsecutiveMistakeLimitReached` no longer returns a blocking `ask: \"mistake_limit_reached\"` promise; it emits one `say: \"error\"` row and returns `{ action: \"stop\" }`, and the `maxConsecutiveMistakes` setting was deleted. The quoted wording also no longer exists on main at all.",
  "verdict": "duplicate",
  "duplicate_of": 10500,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Fixed by e4091c368 (current HEAD). The remaining substance \u2014 weak models tripping the limit in the first place \u2014 is the long-running #5915 (labeled Model Quality); @JanWoldstad's comment links it explicitly.",
  "cluster": "weak-model-mistake-limit"
 },
 {
  "number": 12265,
  "title": "Cline: Thinking state does not disappear in chat after switching from Act Mode to Plan Mode during task execution.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Visible on main. An act\u2192plan toggle takes the non-autoContinue branch of `apps/vscode/src/sdk/sdk-mode-coordinator.ts` `performRebuildSessionForMode`, so the only cleanup is `cancelRunningTurnForModeChange` \u2192 `SdkMessageCoordinator.finalizeMessagesForSave`, which stamps the last `api_req_started` with `info.cancelReason = \"user_cancelled\"` and never sets `info.cost` (`apps/vscode/src/sdk/sdk-message-coordinator.ts:121-134`). The webview's `RequestStartRow` derives its state purely from cost \u2014 `const hasCost = cost != null` and `apiReqState = hasError ? \"error\" : hasCost ? \"final\" : hasReasoning ? \"thinking\" : \"pre\"` (`apps/vscode/webview-ui/src/components/chat/RequestStartRow.tsx:146,152`) \u2014 with no `cancelReason` handling, so the shimmering \"Thinking...\" row (line 236) stays up.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Cosmetic but misleading (implies a live run). Fix either side: teach `RequestStartRow` to treat `cancelReason` as terminal, or have the mode-change abort finalize the api_req_started the way a normal turn end does. QA filed it against both VS Code and JetBrains.",
  "cluster": ""
 },
 {
  "number": 12272,
  "title": "Feature Request: Custom Skills Toggle / Management (Enable/Disable Individual Skills)",
  "area": "vscode-new",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "Already implemented. `apps/vscode/webview-ui/src/components/cline-rules/ClineRulesToggleModal.tsx` has a dedicated \"skills\" tab with per-skill `toggleSkill(isGlobal, skillPath, enabled)` and `ToggleSkillRequest`, and 072e237d0 (#11294, ENG-1995) made that toggle write `disabled: true` into the SKILL.md frontmatter via `apps/vscode/src/core/context/instructions/user-instructions/skills.ts` `setSkillDisabledInFrontmatter`, which is exactly what removes the skill from the model's list and the prompt.",
  "verdict": "already-fixed",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "The named skills (youtube-transcript-summarizer, Lark/Feishu, etc.) are not bundled in the extension \u2014 nothing matches them in the tree \u2014 so they arrive via the marketplace into a scanned skills directory (`apps/vscode/src/core/storage/skill-directories.ts`) and do appear in the toggle list. Reply pointing at the Skills tab.",
  "cluster": ""
 },
 {
  "number": 12278,
  "title": "Security advisory: malicious AsyncAPI package versions published (Miasma RAT)",
  "area": "infra",
  "kind": "meta-task",
  "relevant_to_main": "no",
  "evidence": "No exposure. `docs/package-lock.json` is the only lockfile in the repo containing the dependency and it pins `@asyncapi/specs` at 6.8.1 (plus `@asyncapi/parser` 3.4.0), while the malicious versions are 6.11.2 / 6.11.2-alpha.1. None of `@asyncapi/generator`, `generator-helpers`, or `generator-components` appear in any lockfile (`bun.lock`, `docs/package-lock.json`, `evals/*`, `apps/vscode/standalone/runtime-files/package-lock.json`).",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "The advisory itself is legitimate and correctly says \"you are most likely safe\"; the lockfile check confirms it. Safe to close with the pinned version as the answer. Optionally add a lower-bound constraint in `docs/package.json` to prevent a future resolve into the bad range.",
  "cluster": ""
 },
 {
  "number": 12293,
  "title": "Cline gets stuck in infinite loop of identical command executions and fails to capture stdout/stderr output",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The implementation blamed for the Windows stdout-capture failure was deleted from main by 12703bf40 (#11972, 2026-07-14) \u2014 that commit removes `apps/vscode/src/integrations/terminal/standalone/{StandaloneTerminal,StandaloneTerminalManager,StandaloneTerminalProcess,StandaloneTerminalRegistry,shellArgs}.ts` and `CommandOrchestrator.ts`, leaving only `constants.ts`/`types.ts` in that directory; commands now run through `apps/vscode/src/hosts/vscode/terminal/VscodeTerminalManager.ts` + the new `osc633Parser.ts`. Separately, the looping half would not be caught on main either: `sdk/packages/core/src/runtime/safety/loop-detection.ts` only compares against `lastToolName`/`lastToolSignature`, so alternating `dir` / `Get-ChildItem` calls never register as consecutive-identical.",
  "verdict": "duplicate",
  "duplicate_of": 12048,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Contributor @CoderSufiyan already closed the proposed fix (#12310) as superseded and asked for a retest on a build containing #11972. Open question a human should answer: what does the JetBrains standalone host use for terminals now that the standalone terminal manager is gone, since `VscodeTerminalManager` depends on `vscode.window.onDidStartTerminalShellExecution`.",
  "cluster": "jetbrains-command-no-output"
 },
 {
  "number": 12294,
  "title": "cline: high-intent procedure SEO pages",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "No defect and no request: the body is a two-line boilerplate blurb (\"Autonomous coding agent as an SDK, IDE extension, or CLI assistant\") followed by a \"Soft note\" linking to intentkiln.com, from a throwaway account (onspotrepair4u-lgtm). Nothing to check against the tree.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "SEO link spam. Close and report the account.",
  "cluster": ""
 },
 {
  "number": 12299,
  "title": "Cline Authorization(CLI)",
  "area": "cli",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The exact message the reporter saw is now the fixture for the mid-run auth-retry path added in commit d859a86a6 (`fix(sdk): retry runs once after refreshing expired OAuth credentials`, #12369, 2026-07-18), see `sdk/packages/core/src/runtime/orchestration/session-runtime-orchestrator.test.ts` (\"SessionRuntime auth retry\" suite, the `Unauthorized: Please make sure you're using the latest version of Cline and re-authenticate your Cline account.` authFailure fixture). That commit added the `onAuthError` hook + single-flight `RuntimeOAuthTokenManager` refresh and also fixed `isLikelyAuthError` to lowercase string inputs so the server's raw `Unauthorized: ...` body matches; before it, only turn-start could refresh, which is exactly the reported mid-session drop on CLI 3.0.38.",
  "verdict": "duplicate",
  "duplicate_of": 12094,
  "severity": "high",
  "repro": null,
  "human_attention": "no",
  "notes": "Ask reporter to retest on CLI >= 3.0.44 (releases c2faf38d7 / 396032cd3 postdate the fix). Owner: @cline/core session runtime.",
  "cluster": "mid-session-unauthorized"
 },
 {
  "number": 12300,
  "title": "PLAN MODE: read_file tool calls return empty results \u2014 content only visible in next user message turn",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Not a tool-availability problem on `main`: the plan preset in `sdk/packages/core/src/extensions/tools/presets.ts` sets `enableReadFiles: true` and `enableSearch: true`, and `resolveToolPresetName` maps mode `plan` to that preset, so read/list/search tools are registered in plan mode. I could not find any plan-mode-specific tool-result buffering in `sdk/packages/agents/src/agent-runtime.ts` or `apps/vscode/src/sdk/message-translator.ts`, and the only recent read-tool fix on main (`bcf8c1d03`, #12558) covers relative `read_files` paths and diff-preview stalls, not delayed result delivery \u2014 so the defect is unaccounted for and needs a live repro.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Build and run the SDK VS Code extension from `apps/vscode` (`bun run build:webview && bun esbuild.mjs`, then `code --extensionDevelopmentPath=/workspace/apps/vscode <repo>`).\n2. Configure an OpenAI-compatible provider pointed at LiteLLM with `vertex_ai/claude-sonnet-4-6` (or any non-Anthropic-native tool-calling model) to match the reporter.\n3. Switch the chat to PLAN mode and start a new task: \"Read README.md and package.json and summarize them.\"\n4. Expected: each `read_files` call returns file contents in the same turn and the model summarizes.\n5. Actual (reported): the model sees empty results, claims the files are missing/empty; the contents only surface after the user sends another message.\n6. Repeat the same prompt in ACT mode to confirm the mode-dependence.",
  "human_attention": "yes",
  "notes": "Suspect tool-result delivery/approval sequencing in `apps/vscode/src/sdk/sdk-interaction-coordinator.ts` / `message-translator.ts` rather than the agents loop. Worth checking whether the provider is a factor (litellm + vertex passthrough).",
  "cluster": ""
 },
 {
  "number": 12302,
  "title": "Bedrock Custom-ARN / application-inference-profile: native tool calling omits toolConfig while dispatching native tool blocks \u2192 Converse 400",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Confirmed on `main`: `readBaseModelInfoForProvider` in `apps/vscode/src/sdk/model-catalog/store.ts` looks the model up by raw id only (generated catalog, then `MODEL_COLLECTIONS_BY_PROVIDER_ID`), so a raw `arn:aws:bedrock:...:application-inference-profile/<id>` misses and `resolveSelection` falls back to `fallbackModelInfo(arn)`; nothing in that file consults `aws.customModelBaseId` (it is only written through to provider settings at `store.ts` and `apps/vscode/src/sdk/bedrock-config.ts`). `toSdkModelInfo` in `apps/vscode/src/sdk/cline-session-factory.ts` then seeds capabilities solely from `selection.overrides?.capabilities` plus images/prompt-cache/reasoning flags, so `tools` is never added and the AI-SDK gateway sends no tool schema while native tool blocks are still emitted.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "PR #12357 (`fix(vscode): inherit base-model capabilities for Bedrock custom-ARN native tool calls`) is OPEN and unmerged as of tip e4091c368 \u2014 needs maintainer review. Reporter has a live AIP environment and offered to validate. Note the Bedrock provider manifest in `sdk/packages/llms/src/providers/providers.generated.ts` does declare `tools`, so a provider-level fallback may be a simpler lever than base-model resolution.",
  "cluster": ""
 },
 {
  "number": 12303,
  "title": "Can't change Model Configurations for OpenAI Compatible Provider",
  "area": "jetbrains",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "Root cause is in the shared host-side model catalog, not JetBrains UI, so it is in scope. `apps/vscode/src/sdk/model-catalog/store.ts` now persists numeric edits as model-scoped overrides (`normalizeModelSelectionOverrides`, `legacyModelInfoToOverrides` handling `contextWindow` / `temperature` / `isR1FormatRequired`) and deliberately prunes provider-level `contextWindow`/`maxTokens` in `writeSelectionToProviderSettings` except for Ollama. That model-scoped path landed in `84c9b587a` (`refactor(vscode): resolve model metadata host-side`, #12130) on 2026-07-15 13:37 PDT \u2014 hours after this issue was filed against the 7/15 nightly \u2014 so the reported build predates the fix.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Contributor already asked the reporter to retest on a nightly containing #12130 and got no answer. `adb8ee0c4` (#12552) is a further OpenAI-Compatible model-id persistence fix. Close as fixed-pending-retest, or ping the reporter for the specific field + Plan/Act mode that still fails.",
  "cluster": ""
 },
 {
  "number": 12304,
  "title": "MCP Marketplace: Add GoldBean \u2014 51 Baidu AI tools (OCR, Vision, LLM, Speech, NLP)",
  "area": "unknown",
  "kind": "feature-request",
  "relevant_to_main": "no",
  "evidence": "The MCP marketplace catalog is not stored in this repo: `apps/vscode/src/core/controller/marketplace/marketplace-helpers.ts` fetches `MARKETPLACE_CATALOG_URL = \"https://cline.github.io/marketplace/catalog.json\"` and installs plugins from `OFFICIAL_PLUGINS_REPO = \"https://github.com/cline/plugins.git\"`. There is no catalog data file anywhere under `/workspace` that a submission could be added to.",
  "verdict": "out-of-scope",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Redirect to the `cline/marketplace` repo. Also reads as promotional: paid per-call API with x402/PayPal/Alipay billing and an unverified third-party domain, so it deserves a security review before listing regardless.",
  "cluster": ""
 },
 {
  "number": 12318,
  "title": "Error: Unknown session mid-conversation \u2014 hub daemon loses session state",
  "area": "hub",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The error surface is live on `main`: `sessionNotFoundReply` in `sdk/packages/core/src/hub/server/handlers/run-handlers.ts` emits `Unknown session: ${sessionId}` with `SESSION_NOT_FOUND_ERROR_CODE`, and `sdk/packages/core/src/hub/server/handlers/session-handlers.ts` raises the same message from six separate lookups. Nothing in `git log --since=2026-07-15 -- sdk/packages/core/src/hub apps/cline-hub` addresses session-state loss (`1bb083328`, #12500, only isolates CLI-side hub daemon abort handling in `apps/cli/src/index.ts`), so the reported failure is unaddressed.",
  "verdict": "duplicate",
  "duplicate_of": 11661,
  "severity": "high",
  "repro": "1. Run the CLI from source: `bun run cli -i` (auto-spawns `@cline/cline-hub`).\n2. Configure any provider credential and start a task that runs several minutes with many tool calls.\n3. While the turn is in flight, force the hub to lose its in-memory record \u2014 e.g. `bun run cli doctor` to find the daemon PID, then `kill <pid>` and let the CLI reconnect, or simply leave the session idle long enough for the daemon to be replaced.\n4. Send another message in the same TUI session.\n5. Expected: the CLI transparently rehydrates the session from disk (`sdk/packages/core/src/session/stores/session-manifest-store.ts`) and continues.\n6. Actual (reported): `* Error: Unknown session` from `run-handlers.ts` and the turn is lost.",
  "human_attention": "no",
  "notes": "Report is thin (no logs, no daemon state), but the failure class \u2014 hub-side session map losing a session the client still holds \u2014 is concrete and the repository has no reconciliation path. Owner: `@cline/core` `src/hub/`.",
  "cluster": "hub-session-loss"
 },
 {
  "number": 12323,
  "title": "VSCode/Cline/GPT5.4(high) are constantly running near 1M context size, sometimes going over.",
  "area": "sdk-core",
  "kind": "question-support",
  "relevant_to_main": "unsure",
  "evidence": "What the reporter describes is largely the documented design on `main`: `COMPACTION_TRIGGER_RATIO = 0.9` in `sdk/packages/core/src/extensions/context/compaction-shared.ts` fires compaction at 90% of `maxInputTokens` (`sdk/packages/core/src/extensions/context/compaction.ts`, `requestTriggerTokens = maxInputTokens * COMPACTION_TRIGGER_RATIO`), so a 1M-context GPT-5.4 selection legitimately climbs to ~900k before each handoff, repeatedly. The only genuinely bug-shaped claim, \"sometimes going over\", has no repro steps, no token trace, and no version beyond \"4+\".",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter themselves says \"I am not even certain if this is normal behavior\". Ask for a task export plus the context-window readout; also note `26037b17a` (#12317) changed the default to agentic compaction after this was filed.",
  "cluster": "gpt55-context-overflow"
 },
 {
  "number": 12338,
  "title": "MCP tool approval UI fires with undefined server_name before params populated",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The reported mechanism does not exist on `main`. MCP calls are no longer an XML tool with a model-supplied `server_name` parameter: `sdk/packages/core/src/extensions/mcp/tools.ts` registers one native tool per MCP tool via `defaultMcpToolNameTransform`, and `apps/vscode/src/sdk/message-translator.ts` derives `serverName` deterministically from the `serverName__toolName` tool id in `parseMcpToolName`/`buildMcpToolPayload` \u2014 the model never supplies it, so it cannot be `undefined`. The error text the reporter pasted maps to `responses.missingToolParameterError` in `apps/vscode/src/core/prompts/responses.ts`, which is dead code on `main` (grep finds no call sites).",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Reporter is on 4.0.8, which ships through `apps/vscode-rollout` and can still serve the legacy 3.x bundle; the symptoms are the legacy XML tool parser. Resolves with the SDK-extension rollout. `missingToolParameterError` in `responses.ts` is orphaned and could be deleted.",
  "cluster": ""
 },
 {
  "number": 12339,
  "title": "Enable Cline as an Agent in VS Code's Agent's window",
  "area": "vscode-new",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "`apps/vscode/package.json` (version 4.0.0) contains no `chat.agentHost` / agent-host / chat-session contribution of any kind \u2014 grep for `agentHost`, `chatParticipant`, and `chatSessions` in the manifest returns nothing \u2014 so Cline cannot appear in the VS Code 1.129 Agents window. This is a genuine unimplemented integration on `main`, not a defect.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "A contributor checked AHP docs and found no public extension-manifest registration point, so this may be blocked upstream on Microsoft. Needs a product answer on whether Cline is pursuing an AHP integration.",
  "cluster": ""
 },
 {
  "number": 12340,
  "title": "The API needs to implement the /v1/models list endpoint, and Cline needs to support automatic model selection.",
  "area": "unknown",
  "kind": "feature-request",
  "relevant_to_main": "unsure",
  "evidence": "The consumer half already exists on `main`: providers declare a `modelsSourceUrl` (e.g. `http://localhost:1234/v1/models` for LM Studio in `sdk/packages/llms/src/providers/builtins.ts`) and `sdk/packages/core/src/services/llms/provider-defaults.ts` fetches keyless live model lists from it, so Cline does discover models over `/v1/models`. The producer half \u2014 making Cline's own hosted API serve `/v1/models` \u2014 has no code in this repository; nothing under `sdk/` or `apps/` implements an inference server.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Half of the ask targets the Cline API service (different codebase); the other half is already implemented. Version field \"1.2.3\" is bogus. Reply clarifying both, then close.",
  "cluster": ""
 },
 {
  "number": 12344,
  "title": "CLI: MCP stdio servers with slow startup get silently dropped (initialize timeout is only ~1500ms)",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Exactly as reported, on `main`: `sdk/packages/core/src/extensions/mcp/client.ts` defines `MCP_REQUEST_TIMEOUT_MS = 5_000` but `MCP_CONNECT_TIMEOUT_MS = 1_500`, and `StdioMcpClient.connect()` passes the 1.5s value to the `initialize` request for both the `newline` and `framed` attempts. `McpServerRegistration` in `sdk/packages/core/src/extensions/mcp/types.ts` has no timeout field at all (only `CreateMcpToolsOptions.timeoutMs`, applied per-tool-call in `tools.ts`), so a per-server `timeout` cannot reach the handshake. The silent-drop log the reporter quoted is `sdk/packages/core/src/runtime/orchestration/runtime-builder.ts`: `[mcp] Failed to load tools from MCP server \"...\", skipping: ...`.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "PR #12363 (`fix: honor MCP stdio timeout during initialize`) is OPEN and unmerged at tip e4091c368 \u2014 needs review. Reference point: the official MCP SDK default is 60s.",
  "cluster": "mcp-initialize-timeout"
 },
 {
  "number": 12360,
  "title": "Feature Request: Allow independently collapsing the command/script section within an API Request block (separate from the output section)",
  "area": "vscode-new",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Still applicable on `main`. In `apps/vscode/webview-ui/src/components/chat/CommandOutputRow.tsx` the command text is rendered unconditionally through `<CodeBlock forceWrap source={...command...} />` with no collapse affordance, while only the output half gets one \u2014 `CommandOutputContent` receives `isOutputFullyExpanded` / `onToggle` and renders `<ExpandHandle>` when `lineCount > 5`. So the output is independently collapsible but the script is not, which is precisely the asymmetry the reporter describes.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Small, self-contained webview change: add an `ExpandHandle` around the command `CodeBlock` in `CommandOutputRow.tsx`. Filed against 3.46.0 but the gap carried into the new webview.",
  "cluster": ""
 },
 {
  "number": 12362,
  "title": "Cline VSCode Extension v4.0.9 always shows [OPENAI] Connection error. for the local LLM",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The one merged OpenAI-Compatible fix in this window, `2c64c4ce5` (#12532, 2026-07-24), only normalizes a trailing slash in `apps/vscode/src/core/controller/models/refreshOpenAiModels.ts` for the model-list fetch \u2014 it does not touch the inference path, so it cannot explain a chat-time `Connection error`. The proxy angle several reporters raise is unresolved on `main`: `apps/vscode/src/shared/net.ts` only installs undici's `EnvHttpProxyAgent` when `process.env.IS_STANDALONE === \"true\"` (JetBrains/CLI) and otherwise hands bare `globalThis.fetch` to `cline-session-factory.ts` / `sdk-api-handler.ts`, relying entirely on VS Code's own proxy patching.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "critical",
  "repro": "1. Build and launch the SDK extension from `apps/vscode`.\n2. Add an OpenAI-Compatible provider with a base URL for a local/self-hosted endpoint and a valid key; confirm `curl`/`GET /v1/models` against the same URL succeeds from the same machine.\n3. Send any chat message.\n4. Expected: a normal streamed response.\n5. Actual (reported): `{\"message\":\"Connection error.\",\"modelId\":\"...\",\"providerId\":\"openai\"}` plus `ERROR [PostHogErrorProvider]` / `ERROR [ErrorService]` in the output channel.\n6. Repeat with VS Code's `http.proxy` set (HTTP, HTTPS, and SOCKS) to reproduce @HWR010101's variant, and with a self-signed-CA endpoint to reproduce @michal-zima-cnb's.",
  "human_attention": "yes",
  "notes": "GitHub state is OPEN/REOPENED with 9 comments, labeled `extension-regression`; at least four distinct reporters, one of whom still fails on cline-nightly-4.0.1784858290. Likely two different root causes bundled together (stale config/cache \u2014 @keeratita fixed it by clearing config \u2014 and proxy/TLS). Worth splitting.",
  "cluster": "opaque-connection-error"
 },
 {
  "number": 12376,
  "title": "Proposal: Agent Trust Cards (ATC) \u2014 SSL certificates for AI agents",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "No actionable change against any path in `/workspace`; the author explicitly writes \"Not asking for adoption \u2014 just sharing\". The whole proposal is an external hosted service (`marketnow.site/api/atc`) with links to the author's own repo, release, and dev.to post, and no proposed integration point in `sdk/packages/*` or `apps/*`.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Self-promotion of a third-party CA service. Safe to close as not-actionable; consider redirecting to Discussions.",
  "cluster": ""
 },
 {
  "number": 12377,
  "title": "Task context drift: assistant resumes a completed previous task instead of prioritizing the latest user request",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The mechanism the reporter blames exists on `main` and already contains the mitigation they ask for: `apps/vscode/src/sdk/sdk-followup-coordinator.ts` injects `\"[TASK RESUMPTION] This task was interrupted. It may or may not be complete, so please reassess the task context. The conversation history has been preserved. New instructions from the user: ...\"`, and `apps/vscode/src/sdk/sdk-user-message-mapping.ts` classifies that prefix as a synthetic prompt. So the resumption prompt does instruct reassessment; whether the model honors it is non-deterministic and the report has no task export or model trace.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Reads as prompt-adherence drift on gpt-5.5 rather than a code defect. Would need a task export to evaluate; otherwise this is prompt tuning in `sdk-followup-coordinator.ts`.",
  "cluster": ""
 },
 {
  "number": 12385,
  "title": "Kimi K3 model returns repeated Invalid API Response errors after first message",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "Neither user-visible string exists anywhere on `main`: grep across `/workspace` finds no `Invalid API Response`, no `empty or unparsable response`, and no `repeated tool call failures`. The SDK harness's equivalent is the terse `\"Model returned empty response\"` in `sdk/packages/agents/src/agent-runtime.ts`. That matches the reporters' own finding that the CLI (which runs the SDK harness) works fine with K3 while only the marketplace extension fails \u2014 i.e. the defect lives in the legacy 3.x bundle still served through `apps/vscode-rollout`.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "Five distinct users hitting it in production on Cline's own built-in K3 route (not BYOK), and @reneehuang1 confirmed it is not the earlier GPU-capacity blip. Legacy-only by the scope rule, but a human should decide whether to hotfix the legacy bundle or accelerate the SDK-extension rollout for K3. The final comment (@brucemikhailif-coder) is abusive noise and warrants moderation.",
  "cluster": "invalid-api-response-empty"
 },
 {
  "number": 12388,
  "title": "Checkpoint restore fails with \"No checkpoint found at or before run N\" after internal continuations",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The reporter's root-cause analysis holds on `main`. `createCheckpointHooks` in `sdk/packages/core/src/hooks/checkpoint-hooks.ts` does `beforeRun: async ({snapshot}) => { ...; runCount += 1; }` with no distinction between a user turn and an internal continuation, and `sdk/packages/agents/src/agent-runtime.ts` routes both `run()` and `continue()` through the same private `execute()`, which calls `callBeforeRunHooks()` unconditionally. Meanwhile `apps/vscode/src/sdk/sdk-checkpoints.ts` computes its run number by counting `isVisibleCheckpointUserMessage` entries (`say === \"task\"` / `\"user_feedback\"`), so the two counters diverge permanently after the first continuation and `checkpoint-restore.ts` then throws `No checkpoint found at or before run N in session ...`.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "high",
  "repro": null,
  "human_attention": "yes",
  "notes": "PR #12387 is OPEN and unmerged at tip e4091c368 despite two commenters thanking the author for \"the fix\" \u2014 needs maintainer review. Affects both VS Code and CLI since the counter lives in `@cline/core`.",
  "cluster": ""
 },
 {
  "number": 12389,
  "title": "Cline regularly stalls on CLI commands, requiring VS Code relaunch",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "This is the terminal-integration area that `main` has been actively reworking: `8b4d1973b` (`fix(vscode): reclaim unobserved fallback terminals`, #12352, merged 2026-07-22, three days after this was filed) rewrote `apps/vscode/src/hosts/vscode/terminal/VscodeTerminalManager.ts`, `VscodeTerminalProcess.ts` and `apps/vscode/src/sdk/vscode-run-commands-tool.ts` specifically to classify unobservable terminal outcomes and reclaim managed `sendText` fallbacks \u2014 i.e. the \"command finished but Cline never returns / result missing\" class. Related merges `a41129a5d` (#12320), `a5211a3a5` (#12359) and `557d72569` (#12331) touch the same path, so it is unclear whether the reporter's 4.0.9 stall survives.",
  "verdict": "duplicate",
  "duplicate_of": 12079,
  "severity": "high",
  "repro": "1. Build and launch the SDK extension from `apps/vscode` on macOS (reporter is on Darwin arm64 25.5.0, VS Code 1.129.1).\n2. Configure the LM Studio provider with `qwen/qwen3.6-27b`.\n3. Start an ACT-mode task over a Python project that forces many shell calls, e.g. \"find every TODO in this repo with find + grep and summarize them\".\n4. Let it run 10+ `execute_command` invocations, including at least one long-output `grep -r` and one command whose output exceeds the terminal buffer.\n5. Expected: every command returns and the loop continues.\n6. Actual (reported): after some command the turn hangs for minutes showing \"result missing\", and only a full VS Code restart recovers.\n7. Re-run against tip of `main` (post #12352) to determine whether it is already fixed.",
  "human_attention": "no",
  "notes": "Three reporters, one of whom uninstalled. @sjalander's added detail (\"as soon as a long command line is used, Cline hangs\") points at output/length handling in `vscode-run-commands-tool.ts` and overlaps with #12395.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 12395,
  "title": "CLINE allow the agent to create user input command.",
  "area": "sdk-core",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Partially addressed on `main` at the prompt layer only: `sdk/packages/core/src/extensions/tools/definitions.ts` already tells the model \"Commands must be non-interactive. Commands that require follow-up input like pagers should be skipped or used with supported flags/env (e.g. git --no-pager, --non-interactive) to bypass the interaction steps.\" There is no enforcement layer \u2014 grep for `GIT_PAGER` / `--no-pager` / `TERM=dumb` finds only `apps/vscode/src/utils/git.ts`, which hardcodes `git --no-pager diff` for Cline's own internal diffs, never for agent-issued commands.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Concrete and cheap: set `GIT_PAGER=cat` / `PAGER=cat` (or inject `--no-pager`) in the command tool's spawn env in `apps/vscode/src/sdk/vscode-run-commands-tool.ts` and the SDK bash executor. Same reporter as a comment on #12389 \u2014 the two describe the same freeze from different angles.",
  "cluster": ""
 },
 {
  "number": 12399,
  "title": "Feature request: Add Kimi Code subscription endpoint (api.kimi.com/coding) to Moonshot provider",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Confirmed missing on `main`: `sdk/packages/llms/src/providers/providers.generated.ts` defines `moonshot` with `defaults.baseUrl = \"https://api.moonshot.ai/v1\"` and a separate `moonshotai-cn` entry, and grep finds no occurrence of `api.kimi.com` or the `kimi-for-coding` / `kimi-for-coding-highspeed` model ids anywhere in the repo. So Kimi Code subscribers do have to fall back to the generic OpenAI-Compatible provider as described.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Well-specified request (endpoints, model ids, context windows, reasoning-effort mapping supplied by the reporter). Note `providers.generated.ts` is auto-generated from models.dev by `ae033761a` (#12204), so a subscription-only endpoint needs a manual entry or a generator override.",
  "cluster": ""
 },
 {
  "number": 12400,
  "title": "Feature request: Add Alibaba Cloud Token Plan subscription endpoint (token-plan.cn-beijing.maas.aliyuncs.com) to Qwen provider",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Confirmed missing on `main`: `sdk/packages/llms/src/providers/providers.generated.ts` defines `alibaba` with `defaults.baseUrl = \"https://dashscope-intl.aliyuncs.com/compatible-mode/v1\"` plus an `alibaba-cn` variant, and grep finds no `token-plan.cn-beijing.maas.aliyuncs.com` or `coding.dashscope.aliyuncs.com` anywhere in the repo.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Same shape and same reporter as #12399 (subscription endpoint not covered by the models.dev-generated catalog); worth handling together. `providers.generated.ts` is generated by #12204, so this needs a manual entry or generator override.",
  "cluster": ""
 },
 {
  "number": 12403,
  "title": "Error in legacy tool calls.",
  "area": "vscode-new",
  "kind": "question-support",
  "relevant_to_main": "yes",
  "evidence": "Not an error \u2014 it is an intentional notice on `main`. `apps/vscode/src/sdk/sdk-task-control-coordinator.ts` emits `\"\u26a0\ufe0f This is a legacy task. It may not work as well because tool names may have changed.\"` when resuming a pre-SDK task, and `apps/vscode/src/sdk/legacy-task-handling.ts` injects the matching in-conversation warning `\"Warning: this is a legacy conversation, which means tool names may have changed. Please use the most up-to-date tools you are aware of.\"` (asserted in `apps/vscode/src/sdk/sdk-task-history.test.ts`). The reporter pasted that banner into the \"What happened?\" field with no accompanying failure.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Working as designed for tasks created before the SDK migration; the guidance is to start a new task. If degraded resumption quality is the real complaint, that is a separate, better-specified issue.",
  "cluster": ""
 },
 {
  "number": 12406,
  "title": "Cline tries to access LiteLLM routes blocked by default",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Confirmed on `main`, and the JetBrains label is indeed irrelevant since the code is shared. `fetchLiteLlmPrivateModels` in `sdk/packages/core/src/services/llms/provider-defaults.ts` builds its candidate list solely from `buildLiteLlmModelInfoUrls` = `[${baseUrl}/v1/model/info, ${baseUrl}/model/info]`, tries each with `x-litellm-api-key` then `Authorization`, and if all four attempts fail throws `LiteLLM model refresh failed. Attempts: ...`. There is no `/v1/models` fallback anywhere in that fetcher, which is exactly why LiteLLM virtual keys (allowed `/v1/models`, denied `/model/info`) get a 403.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Fix is a `/v1/models` fallback in `fetchLiteLlmPrivateModels` (metadata would be thinner, but discovery would work for virtual keys). Note the reporter's exact string `Failed to fetch LiteLLM model info: Forbidden` is the JetBrains/legacy wording; on `main` the same failure surfaces as `LiteLLM model refresh failed. Attempts: ...`. Also worth confirming whether a failed refresh should block chat at all.",
  "cluster": ""
 },
 {
  "number": 12408,
  "title": "CLI TUI: Add a discoverable way to rename sessions from /history or slash commands",
  "area": "cli",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Accurate on `main`. The shell command exists \u2014 `apps/cli/src/main.ts` registers `history update` with `--title <text>` (backed by `apps/cli/src/commands/history.ts`) \u2014 but `apps/cli/src/tui/commands/slash-command-registry.ts` only registers `history` among its slash commands, with no `rename` or `title` entry, and `apps/cli/src/tui/components/dialogs/help-dialog.tsx` lists only `/history`. So there is no in-TUI rename affordance.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Maintainer already queued it and @Auntara12 volunteered; just needs assignment. Implementation is a new entry in `slash-command-registry.ts` plus an edit action in the `/history` view.",
  "cluster": ""
 },
 {
  "number": 12409,
  "title": "Sejahtera Verify",
  "area": "unknown",
  "kind": "question-support",
  "relevant_to_main": "no",
  "evidence": "Not a code report \u2014 the author says outright \"Its not a bug, just question\". Nothing in the repo relates to SMS/OTP delivery: grep for `Sejahtera`, `otp`, and `phone` across `apps/vscode/src` matches only `apps/vscode/src/shared/cline/banner.ts` (unrelated). Phone verification happens in Cline's hosted signup, which has no code in `/workspace`.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Needs a one-line human answer confirming (or denying) that \"Sejahtera Verify\" is Cline's SMS sender \u2014 a user is being asked to trust an unfamiliar OTP sender, so leaving it unanswered is a trust/anti-phishing risk. No code change.",
  "cluster": ""
 },
 {
  "number": 12413,
  "title": "Cline: Task content is cleared after reopening an interrupted task",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Both reported symptoms map to live code on main: `clearTask()` in `apps/vscode/src/sdk/sdk-task-control-coordinator.ts` rejects any outstanding interaction with the literal string `\"Task cleared\"` (`this.options.interactions.clearPending(\"Task cleared\")`), and `showTaskWithId()` in the same file installs an empty task proxy and logs `No messages found for task` when `taskHistory.getClineMessages(taskId)` returns nothing, which renders a blank chat. `cancelTask()` in that file aborts the SDK session and appends a `resume_task` ask but does not flush pending message persistence first, so an interrupt/reopen race is plausible.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Run the new VS Code extension from `apps/vscode` (`DISPLAY=:1 code --extensionDevelopmentPath=/workspace/apps/vscode <folder>`) with any working provider credential.\n2. Start a task with a prompt that produces a long multi-tool run (e.g. \"create a small express app with 5 files and run the tests\").\n3. While the assistant is mid-stream and a tool ask is pending, click Cancel (this calls `sdk-task-control-coordinator.cancelTask()`), then immediately click the Cline history icon and open a different task, then reopen the interrupted one from History.\n4. Repeat 5-10 times, alternating between cancelling during streaming and cancelling while an approval ask is pending.\n5. Expected: the reopened task shows the full prior conversation plus a `resume_task` ask. Actual (per reporter): the chat is blank, or a `Task cleared` error is shown. Check the Cline output channel for `[SdkController] No messages found for task: <id>` to confirm the empty-messages branch was taken.",
  "human_attention": "yes",
  "notes": "Owned by apps/vscode (`sdk-task-control-coordinator.ts` + session persistence in @cline/core). Suspect a race between `sessions.endActiveSession()`/abort and the SDK session message flush, so `readMessages()` returns an empty history for the interrupted session.",
  "cluster": "task-history-loss"
 },
 {
  "number": 12417,
  "title": "CLI command execution hangs on background processes (bash & + wait pattern)",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Both execution paths on main can stall on a backgrounded child. In `sdk/packages/core/src/extensions/tools/executors/bash.ts`, `spawnAndCollect()` only settles on `child.on(\"close\")` (there is no `exit` handler), and `close` waits for every inherited stdio pipe to close, so a `&`-detached server holding stdout defers resolution to the timeout. In the foreground/`integrated` terminal path, `apps/vscode/src/sdk/vscode-run-commands-tool.ts` sets `VSCODE_FOREGROUND_RUN_COMMANDS_TIMEOUT_MS = 60 * 60 * 1000` (one hour), which is indistinguishable from an indefinite hang; the only escape is the \"Proceed While Running\" detach handle registered in `executeForeground()`.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Run the new VS Code extension from `apps/vscode` on Linux/macOS with a working provider.\n2. In Cline settings > Terminal, leave the execution mode at the default `backgroundExec`, and note the mode.\n3. Ask Cline (Act mode) to run `python3 -m http.server 8123 &` (or `uvicorn app:app --port 8123 &`).\n4. Expected: the tool returns promptly with the job-control output and the server keeps running. Actual: the tool call does not return; in `backgroundExec` it only unblocks after the 30s default in `bash.ts` (`createShellExecutor` is constructed without `timeoutMs` in `vscode-run-commands-tool.ts`), and it kills the server via `killProcessTree()`.\n5. Switch the terminal execution mode to `integrated` (VS Code terminal) and repeat step 3. Expected vs actual: the turn now blocks for up to one hour (`VSCODE_FOREGROUND_RUN_COMMANDS_TIMEOUT_MS`) unless the user clicks \"Proceed While Running\".\n6. Verify the suggested fixes: adding a `child.on(\"exit\")` settle path (with a short stdio-drain grace window) in `spawnAndCollect`, and/or `set +m`/`disown` handling for `&` in the foreground path.",
  "human_attention": "yes",
  "notes": "Owned jointly by @cline/core (`extensions/tools/executors/bash.ts` close-vs-exit) and apps/vscode (`vscode-run-commands-tool.ts` foreground timeout). The reporter's root-cause analysis is essentially correct.",
  "cluster": "terminal-skipped-hang"
 },
 {
  "number": 12419,
  "title": "Cline \u754c\u9762\u5361\u6b7b\uff08gRPC \u54cd\u5e94\u8fc7\u5927 + CheckpointTracker \u5931\u8d25 + Windows \u8def\u5f84\u517c\u5bb9\u6027\u95ee\u9898\uff09",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The reporter is on `hybridtalentcomputing.cline-chinese` v3.86.6, a third-party fork of the legacy 3.x extension, and every symbol they quote is legacy-only: `rg CheckpointTracker` and `rg \"TODO_SECTION|SYSTEM_INFO_SECTION\"` return nothing anywhere in /workspace, while `git grep -l CheckpointTracker origin/legacy-extension` and `git grep -l TODO_SECTION origin/legacy-extension` both hit (`apps/vscode/src/core/task/message-state.ts`, `apps/vscode/src/core/prompts/system-prompt/templates/placeholders.ts`). Main uses a different checkpoint stack under `sdk/packages/core/src/session/checkpoint-*.ts`. Only two incidental strings survive on main: `apps/vscode/src/core/storage/documents-path.ts:19` (\"Failed to retrieve Windows Documents path\", a warning with a `~/Documents` fallback) and `apps/vscode/src/services/telemetry/TelemetryService.ts:2646` (the \"Large gRPC response\" log, which is diagnostic only).",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Third-party fork (cline-chinese) of the retired 3.x extension; the checkpoint and prompt-component code they blame does not exist on main. Auto-closeable with a pointer to official 4.x.",
  "cluster": "grey-blank-webview"
 },
 {
  "number": 12422,
  "title": "I can't see any thing after the cline was tring to do thing.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "A blank, unclickable Cline view is a known main-branch failure mode that was fixed after this report: commit b72d7f1a6 (#12553, 2026-07-25) \"fix(vscode): keep webview alive when moved between primary and secondary sidebars\" describes exactly \"the view go blank\" caused by `VscodeWebviewProvider` disposing the whole `Controller` on `WebviewView.onDidDispose` (re-resolved view served by a dead controller, so `postStateToWebview` no-ops) plus a hostbridge streaming client returning a Promise instead of the cancel function. The report predates that commit and the reporter gave no Cline version (they pasted the VS Code version 1.129.1 in the Cline Version field), so it needs re-testing on 4.0.11+.",
  "verdict": "duplicate",
  "duplicate_of": 10626,
  "severity": "high",
  "repro": "1. Build and run the extension at the commit that shipped 4.0.10 (tag `v4.0.10`, before b72d7f1a6) so the pre-fix controller-dispose behavior is present.\n2. Open a workspace, start an Act-mode task, and let it finish.\n3. Drag the Cline view from the primary sidebar to the secondary sidebar (or hide/show the view container), then interact with the panel.\n4. Expected: the chat renders and settings/history are clickable. Actual (pre-fix): the panel is blank and nothing is clickable; the extension host log shows `this.unsubscribeHostTelemetrySettings is not a function`.\n5. Repeat on tip of `main` (post b72d7f1a6) to confirm the fix covers the reporter's case; if the panel still blanks without any view move, capture the extension-host console and treat it as a separate defect.",
  "human_attention": "no",
  "notes": "Likely already fixed by b72d7f1a6 (#12553). Worth asking the reporter to retest on 4.0.11+ before closing; the report has no Cline version, no logs, and only screenshots.",
  "cluster": "grey-blank-webview"
 },
 {
  "number": 12431,
  "title": "Cline hit repeated tool call failures. Try guiding it with a new prompt.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The exact message is legacy-only: `git grep \"repeated tool call failures\" origin/legacy-extension` hits `apps/vscode/src/core/task/index.ts:2862`, and the same search across /workspace returns nothing. On main the equivalent guardrails are `sdk/packages/core/src/runtime/safety/loop-detection.ts` and the `mistake_limit_reached` ask rendered by `apps/vscode/webview-ui/src/components/chat/ErrorRow.tsx`, with different copy, and commit e4091c368 (#12561, 2026-07-26) just reworked that behavior (\"Stop the task at the mistake limit like the CLI\"). The thread also self-resolved: commenter ambondarev concluded the Playwright MCP server never errored and the failures came from narration-only model turns.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Report body is empty (no repro, no provider, no system info) and self-resolved in comments. Two of the five comments are promotional posts for an unrelated product (MartinLoop) from a non-contributor; treat those as noise.",
  "cluster": ""
 },
 {
  "number": 12437,
  "title": "Cline can not powershell7",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Two concrete defects on main match the report. First, `getAvailableTerminalProfiles()` in `apps/vscode/src/utils/shell.ts` hardcodes the PowerShell 7 profile path to `SHELL_PATHS.POWERSHELL_7` = `C:\\\\Program Files\\\\PowerShell\\\\7\\\\pwsh.exe`, even though the same file already has `getWindowsPwshInstallPaths()` which probes `ProgramW6432`, Program Files 6/7 and the Store install; and `getWindowsShellFromVSCode()` falls back to `SHELL_PATHS.POWERSHELL_LEGACY` whenever the named profile is missing from `terminal.integrated.profiles.windows`. Second, the garbling is explained by `sdk/packages/core/src/extensions/tools/executors/bash.ts:91`, where `createRollingCollector` decodes all child output with `new StringDecoder(\"utf8\")` \u2014 Windows PowerShell 5.1 on a zh-CN console emits GBK, which becomes mojibake. Background Exec routes through this collector via `createShellExecutor` in `apps/vscode/src/sdk/vscode-run-commands-tool.ts`.",
  "verdict": "duplicate",
  "duplicate_of": 11017,
  "severity": "medium",
  "repro": "1. On a Chinese-locale Windows 11 machine, install PowerShell 7 somewhere other than `C:\\Program Files\\PowerShell\\7` (e.g. the Microsoft Store build under `%LOCALAPPDATA%\\Microsoft\\WindowsApps\\pwsh.exe`).\n2. Run the new extension from `apps/vscode`, open Cline settings > Terminal, set Default Terminal Profile to \"PowerShell 7\" and keep execution mode `backgroundExec`.\n3. Ask Cline to run `Get-ChildItem` in a folder with Chinese file names, and also `$PSVersionTable.PSVersion`.\n4. Expected: `PSVersion` reports 7.x and Chinese names render correctly. Actual (per reporter): 5.1 is used and Chinese output is mojibake.\n5. Repeat with PowerShell 7 installed at the hardcoded path to isolate which of the two defects fires \u2014 a wrong `PSVersion` points at `getShellForProfile`/`SHELL_PATHS.POWERSHELL_7`, correct `PSVersion` with mojibake points at the UTF-8-only `StringDecoder` in `bash.ts`.",
  "human_attention": "no",
  "notes": "Note #12331 (2026-07-18, `fix(vscode): shell mismatch between prompt, execution, and user configuration on Windows`) shipped in v4.0.10 (tagged 2026-07-20), so the reporter already has it; the hardcoded pwsh path and the UTF-8-only output decoder are the remaining gaps.",
  "cluster": "shell-override-ignored"
 },
 {
  "number": 12466,
  "title": "Add quoted replies and preserve reading position after sending a message",
  "area": "vscode-new",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Neither capability exists on main. `apps/vscode/webview-ui/src/components/chat/chat-view/hooks/useScrollBehavior.ts` unconditionally calls `scrollToBottomSmooth()`/`scrollToBottomAuto()` on new grouped messages unless `disableAutoScrollRef.current` was set by a manual wheel/scroll, and there is no persisted reading-position anchor. Searching the chat components for a quote/reply affordance turns up nothing \u2014 `apps/vscode/webview-ui/src/components/chat/UserMessage.tsx` offers only edit-and-regenerate, not quoting a specific assistant passage.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Pure UX request against `apps/vscode/webview-ui`. Needs a product call on whether quoted replies belong in the chat surface; the scroll-position half is a smaller, self-contained change in `useScrollBehavior.ts`.",
  "cluster": ""
 },
 {
  "number": 12468,
  "title": "\u4e0a\u4e0b\u6587\u538b\u7f29",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The report (\"auto context compaction does not shrink the context, it doubles it\") is filed against v3.86.6 with no steps, logs or numbers. The compaction code that would own this on main is `sdk/packages/core/src/extensions/context/compaction.ts` (trigger at `maxInputTokens * COMPACTION_TRIGGER_RATIO`, 0.9) and `compaction-shared.ts` (`buildSummaryRequest`/`findCutIndex`); I could not find any code path there that grows the transcript, and the summary replaces messages before `findCutIndex`. Without a session id, token counts, or the before/after message counts there is nothing concrete to check against.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Legacy-era version (3.86.6) plus an empty repro template. Ask for the compaction log line (`Context compaction diagnostics` from `compaction.ts`) with before/after token counts, or close as unactionable.",
  "cluster": "compaction-runs-while-disabled"
 },
 {
  "number": 12469,
  "title": "\ud83d\udd12 Security: MCP Integration Leaks Complete process.env to MCP Servers",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The finding holds on main, at a slightly different location than cited: `sdk/packages/core/src/extensions/mcp/client.ts` spawns stdio MCP servers with `env: { ...process.env, ...(transport.env ?? {}) }` (in `spawnProcess()`, around the `spawn(transport.command, ...)` call). By contrast the VS Code host does the safe thing \u2014 `apps/vscode/src/services/mcp/McpHub.ts` builds its `StdioClientTransport` with `env: { ...getDefaultEnvironment(), ...(expandedConfig.env || {}) }`. So the CLI/hub/desktop hosts that use `@cline/core`'s own MCP client hand the full parent environment to every stdio MCP server, while the VS Code path does not.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Owned by @cline/core. Real inconsistency worth closing (align `extensions/mcp/client.ts` with the MCP SDK's `getDefaultEnvironment()` allowlist), though impact is limited to servers the user explicitly configured locally. The report is padded with self-promotion (vendor site, Zenodo DOI, @-pings of unrelated users) \u2014 judge the code claim, not the packaging.",
  "cluster": ""
 },
 {
  "number": 12474,
  "title": "Add latest Gemini models: `gemini-3.6-flash` and `gemini-3.5-flash-lite` (blocked by Gemini integration update)",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "Partially done on main. `sdk/packages/llms/src/catalog/catalog.generated.ts` now carries `gemini-3.6-flash` and `gemini-3.5-flash-lite` under the `gemini` provider (both with `contextWindow: 1048576`, `releaseDate: \"2026-07-21\"`), and as `google/gemini-3.6-flash` under `openrouter` and `vercel-ai-gateway`; that file landed in ae033761a (#12204, 2026-07-23). The `vertex` block (starting at the `\"vertex\"` key in the same file) still tops out at `gemini-3.5-flash`/`gemini-3.1-pro-preview` \u2014 no 3.6 Flash or 3.5 Flash-Lite \u2014 which is exactly the provider the commenters are using.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Remaining work: add the two models to the `vertex` catalog block, and unblock the prefill incompatibility tracked in #12521 (the 400 `Requests ending with a model turn are not supported` the commenters hit). Keep open until #12521 lands.",
  "cluster": "stale-model-catalog"
 },
 {
  "number": 12492,
  "title": "Update: MarketNow 10-layer audit + prompt injection defense + submit_skill tool",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "The body is a product advertisement for marketnow.site with signup instructions, pricing-style feature tables and contact details; it reports no Cline defect and references no Cline code. Nothing in /workspace relates to it (no `marketnow` matches anywhere in the repo).",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Unsolicited marketing filed as an issue; safe to close and lock.",
  "cluster": ""
 },
 {
  "number": 12507,
  "title": "Groq provider always sends ~42k tokens on fresh workspace, causing 413 \"Request too large\"",
  "area": "sdk-llms",
  "kind": "question-support",
  "relevant_to_main": "no",
  "evidence": "The arithmetic points at Groq's TPM accounting, not a Cline bug. Groq counts `input + max_completion_tokens` against TPM, and `sdk/packages/llms/src/providers/gateway.ts:22` sets `DEFAULT_GATEWAY_MAX_OUTPUT_TOKENS = 32_000`, which `resolveGatewayRequestMaxTokens()` uses whenever the caller does not request an explicit `maxTokens`. 42,451 \u2212 32,000 \u2248 10.4k, which matches Cline's baseline prompt: `sdk/packages/shared/src/prompt/system.ts` is ~6.6 KB and `sdk/packages/core/src/extensions/tools/definitions.ts` ~30 KB of tool schemas. The catalog entry `openai/gpt-oss-20b` under `groq` in `catalog.generated.ts` declares `contextWindow: 131072`, so nothing is misconfigured \u2014 the account's 8,000 TPM tier simply cannot fit an agentic system prompt plus a 32k output reservation.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Answerable without a maintainer: Cline is not sending 42k of content. If the team wants a follow-up, the discussable knob is lowering `DEFAULT_GATEWAY_MAX_OUTPUT_TOKENS` or deriving it from the model's remaining context for low-TPM tiers.",
  "cluster": ""
 },
 {
  "number": 12509,
  "title": "Cline: Cline completes the task eventhough user logs out in middle of the ongoing task",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Confirmed on main: `SdkController.handleSignOut()` in `apps/vscode/src/sdk/SdkController.ts` only calls `authService.handleDeauth(LogoutReason.USER_INITIATED)`, `clearRemoteConfig()`, `setRemoteConfigCoreIntegration(undefined)` and `postStateToWebview()` \u2014 it never touches the active session, so it does not call the `cancelTask()`/`sdkHost.abort(sessionId)` path in `apps/vscode/src/sdk/sdk-task-control-coordinator.ts`. The only other auth-change hook, `apps/vscode/src/core/controller/account/authStateChanged.ts`, just writes `userInfo` to global state. Nothing aborts an in-flight run on logout.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Owned by apps/vscode. Needs a product decision as much as a fix: BYOK users are not signed in at all, so the abort should probably be conditional on the active provider being Cline-managed (`cline`/`cline-pass`) rather than on sign-in state generally.",
  "cluster": "logged-out-task-gating"
 },
 {
  "number": 12510,
  "title": "Cline: Invalid model format error when using cline:gemini-3.6-flash LLM.",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The error text is not produced by this repo: `rg \"invalid model format\"` and `rg \"modelType\"` across /workspace return nothing, so it comes from the Cline backend at `https://api.cline.bot` (the `cline` provider's model list is fetched at runtime and is not in `sdk/packages/llms/src/providers/providers.generated.ts`). Every Cline-managed model id that *is* checked into the catalog is vendor-prefixed \u2014 the `cline-pass` block in `sdk/packages/llms/src/catalog/catalog.generated.ts` is entirely `cline-pass/glm-5.2`, `cline-pass/kimi-k3`, `deepseek/deepseek-v4-flash`, etc. \u2014 which is consistent with the backend rejecting a bare `gemini-3.6-flash` that has no `google/` prefix.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Run the new VS Code extension from `apps/vscode`, sign in to a Cline account, and select provider `cline`.\n2. In the model picker choose `gemini-3.6-flash` (the entry the reporter shows as `cline:gemini-3.6-flash`).\n3. Capture the outgoing request: set a breakpoint (or add temporary logging) in `buildGatewayRequest` in `sdk/packages/llms/src/providers/compat.ts`, or inspect the persisted session record, and read the `modelId` actually sent.\n4. Send any prompt.\n5. Expected: the model responds. Actual: `invalid model format. Expected format: modelType/model` and no response.\n6. The decisive check is step 3: if `modelId` is `gemini-3.6-flash` with no `google/` prefix, the client (or the model list the backend serves) is dropping the vendor segment; if it already carries `google/`, the defect is entirely server-side at api.cline.bot and this issue should move there.",
  "human_attention": "yes",
  "notes": "Filed by Cline QA against a nightly (4.0.1784810522) and reproduced on both VS Code and JetBrains, which points at shared model-id handling rather than IDE UI. May be purely server-side; step 3 of the repro decides.",
  "cluster": ""
 },
 {
  "number": 12512,
  "title": "Cline: Reset chat fails with \"Could not map edited message to persisted conversation history\" Error.",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The exact error string is thrown on main by `SdkController.editMessageAndRegenerate()` in `apps/vscode/src/sdk/SdkController.ts`, when `findSdkUserMessageIndexByOrdinal(sdkMessages, userOrdinal)` returns -1. That helper (`apps/vscode/src/sdk/sdk-user-message-mapping.ts`) counts visible user turns positionally and skips messages it classifies as synthetic via `isSyntheticSdkUserMessage`/`extractSdkUserText`, while the ordinal on the other side is computed by filtering `clineMessages` for `say === \"task\" || say === \"user_feedback\"`. The mapping is acknowledged as fragile in `sdk-user-message-mapping.test.ts`, which notes that a mis-classified synthetic prompt makes \"every later edit/regenerate ordinal shift by one\". Nothing has changed in either file since the report (latest touches are #12552 and #12558, unrelated).",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. Run the new VS Code extension from `apps/vscode` with a working provider.\n2. Start a task in Plan mode, let it answer, then toggle to Act mode (this injects the `ACT_MODE_CONTINUATION_PROMPT` synthetic user turn) and let the task complete.\n3. Send a follow-up message and let it complete.\n4. Hover the follow-up user message, click the edit (pencil) affordance, then click \"Reset Chat\".\n5. Expected: the conversation rewinds to that message and regenerates. Actual (intermittent): `INTERNAL: Could not map edited message to persisted conversation history`.\n6. To confirm the ordinal drift, log `userOrdinal` and the result of `findSdkUserMessageIndexByOrdinal` in `SdkController.editMessageAndRegenerate` and compare the visible `clineMessages` task/user_feedback count against the non-synthetic user messages returned by `sessionHost.readMessages(sourceSessionId)`. Vary the mix of mode switches, task resumption, and tool approvals to find the case where the two counts diverge.",
  "human_attention": "yes",
  "notes": "Owned by apps/vscode. Positional ordinal mapping between UI `ClineMessage`s and persisted SDK messages is the weak point; a stable id stamped on both sides would remove the whole class of failure.",
  "cluster": ""
 },
 {
  "number": 12514,
  "title": "Gemini adapter turns nested MCP arrays into STRING and drops their schema",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "no",
  "evidence": "The `array: \"STRING\"` mapping the reporter identifies exists only on the retired branch: `git grep -n 'STRING' origin/legacy-extension -- apps/vscode/src` finds the type table in `apps/vscode/src/core/prompts/system-prompt/spec.ts` (`string: \"STRING\"`, `array: \"STRING\"`, line ~236), and that whole `system-prompt/` tree is gone on main (`apps/vscode/src/core/prompts/` now contains only `responses.ts` and `__tests__`). On main, MCP tool schemas are forwarded verbatim: `createMcpTools()` in `sdk/packages/core/src/extensions/mcp/tools.ts` passes `descriptor.inputSchema` straight into `createTool`, and `toAiSdkTools()` in `sdk/packages/llms/src/providers/ai-sdk.ts` wraps it with `jsonSchema(normalizeAiSdkToolInputSchema(...))`, which only ensures a top-level `type: \"object\"` and otherwise leaves `items`/nested `properties`/`required` untouched, letting the AI SDK's Google provider do the OpenAPI conversion.",
  "verdict": "legacy-only",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "no",
  "notes": "Excellent, precisely-diagnosed report \u2014 but against the 3.x extension that the A/B rollout still ships alongside 4.x under a 4.0.x version string. Worth replying with the fact that the hand-rolled type table is gone on main and asking them to retest on the SDK build; their suggested regression test (assert the Gemini wire declaration keeps `rules`/`steps` as arrays) would still be a good addition to `ai-sdk.tool-calls.test.ts`.",
  "cluster": ""
 },
 {
  "number": 12518,
  "title": "[Feature Request] \u6dfb\u52a0\u4e2d\u6587\u754c\u9762\u652f\u6301 / Add Chinese (Simplified) Language Support",
  "area": "vscode-new",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "No localization layer exists on main: searching `apps/vscode/webview-ui/src` for `i18n`, `useTranslation` or `react-intl` returns no matches, and UI strings are hardcoded English literals throughout the chat and settings components (e.g. \"Edit and regenerate from here\", \"Reset Chat\" in `apps/vscode/webview-ui/src/components/chat/UserMessage.tsx`). Adding zh-CN therefore means introducing an i18n framework plus extraction across the whole webview, not just a language pack.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "yes",
  "notes": "Large, cross-cutting change to `apps/vscode/webview-ui` (framework choice, string extraction, a language setting in the settings surface, and ongoing translation upkeep). Needs a product/maintainer decision before any code work; note the existence of the third-party `cline-chinese` fork as prior art.",
  "cluster": "ui-localization"
 },
 {
  "number": 12520,
  "title": "fix(cli): openai-compatible provider ignores models.json contextWindow \u2014 resolveModelInfo ?? short-circuits on default   messageModelInfo",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "The reporter's versions match tip of main exactly (`apps/cli/package.json` is 3.0.46, `sdk/packages/core/package.json` is 0.0.65) and both cited code sites are present verbatim: `resolveModelInfo()` in `sdk/packages/llms/src/providers/compat.ts` still reads `config.modelInfo ?? (config.modelId ? config.knownModels?.[config.modelId] : undefined) ?? {...}`, and `resolveRuntimeConfig()` in `sdk/packages/agents/src/agent-runtime.ts` still sets `const messageModelInfo = rest.messageModelInfo ?? { id: modelId, provider: providerId }`. The 115.2k figure is confirmed by `sdk/packages/core/src/extensions/context/compaction-shared.ts` (`DEFAULT_MAX_INPUT_TOKENS = 128_000`, `COMPACTION_TRIGGER_RATIO = 0.9` \u2192 115,200) applied in `compaction.ts` when `resolveEffectiveMaxInputTokens` yields nothing.",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "The `??` ordering defect in `resolveModelInfo` is real and still shadows registry data for `handler.getModel().info` (which is what the cost calculator reads \u2014 matching the $0.00 symptom). Caveat for whoever picks this up: the *compaction trigger* has partial mitigation already \u2014 `compaction.ts` reads `context.model.info` from `tryGetModelInfo()` in `sdk/packages/core/src/runtime/orchestration/session-runtime-orchestrator.ts`, which prefers `knownModels`, and `resolveProviderMaxInputTokens()` in `agentic-compaction.ts` falls back to `knownModels` (added in 10cb9bd97, #12142, 2026-07-09). So verify which of the two symptoms still reproduces before assuming one fix covers both.",
  "cluster": "openai-compat-context-window-ignored"
 },
 {
  "number": 12521,
  "title": "Could not use latset Gemini models: \"prefill model turn\" is removed",
  "area": "sdk-llms",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Real forward-compat gap. `gemini-3.6-flash` and `gemini-3.5-flash-lite` are already in the `gemini` block of `sdk/packages/llms/src/catalog/catalog.generated.ts`, so users can select them on main, but nothing in the request pipeline prevents a trailing assistant turn: `toAiSdkMessages`/`formatMessagesForAiSdk` in `sdk/packages/llms/src/providers/ai-sdk.ts` and `sdk/packages/shared/src/llms/ai-sdk-format.ts` pass the conversation through as-is (they even emit an empty-content assistant turn rather than dropping it), and grepping for `prefill` across `sdk/packages/` returns no handling at all. Google's documented change (`Prefilling model turns is no longer supported`) therefore surfaces as the 400 `Requests ending with a model turn are not supported` the commenters pasted.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "high",
  "repro": "1. Configure the `gemini` (or `vertex` with a custom model id) provider with a real key in the new VS Code extension or the CLI.\n2. Select `gemini-3.6-flash`.\n3. Start a task and let it produce at least one assistant turn, then cancel mid-stream or hit a tool error so the persisted history ends on an assistant message.\n4. Resume/continue the task so a request is built from that history.\n5. Expected: the request succeeds. Actual: HTTP 400 `{\"error\":{\"code\":400,\"message\":\"Requests ending with a model turn are not supported.\",\"status\":\"INVALID_ARGUMENT\"}}`.\n6. Instrument `toAiSdkMessages` in `sdk/packages/llms/src/providers/ai-sdk.ts` to log the final message role, and confirm the fix by dropping or converting a trailing assistant turn for models in the `gemini-flash`/`gemini-flash-lite` families (per `sdk/packages/llms/AGENTS.md`, this belongs in `ModelInfo.metadata`/`model-facts.ts` plus a `PROVIDER_OPTION_RULES` entry, not an inline provider-id check).",
  "human_attention": "yes",
  "notes": "Blocks #12474. Google states the change applies to all future Gemini releases, so this needs a general rule rather than a per-model patch.",
  "cluster": ""
 },
 {
  "number": 12523,
  "title": "Plugin stores skills in `~/.agents/skills` but reads from `~/.cline/skills/`",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "unsure",
  "evidence": "The title's claim is contradicted by main: both roots are scanned. `getSkillsDirectoriesForScan()` in `apps/vscode/src/core/storage/skill-directories.ts` returns the global pair `~/.cline/skills` and `~/.agents/skills`, and `resolveSkillsConfigSearchPaths()` in `sdk/packages/shared/src/storage/paths.ts` appends `join(resolveClineDir(), SKILLS_CONFIG_DIRECTORY_NAME)` and `join(HOME_DIR, LEGACY_AGENT_SKILLS_CONFIG_DIR /* \".agents\" */, SKILLS_CONFIG_DIRECTORY_NAME)`; `getGlobalMarketplaceSkillPaths()` in `sdk/packages/core/src/services/marketplace.ts` checks both too. Neither file has changed since well before 4.0.10 (`skill-directories.ts` last touched 2026-06-10; the `.agents` search path landed in 9f42aea85, #11103, 2026-05-28). The report has an empty repro template, so I cannot tell whether the complaint is non-discovery or a wrong path being *displayed*.",
  "verdict": "needs-info",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Answerable by an agent: ask whether the skills under `~/.agents/skills` failed to appear in the skills list at all, or merely showed a `~/.cline/skills/...` path in the transcript, and for the exact install command used.",
  "cluster": ""
 },
 {
  "number": 12526,
  "title": "Global Cline rules are not loaded in new VS Code sessions on macOS",
  "area": "sdk-core",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Two of the reporter's three locations are legitimately in scope on main and one is not. `resolveRulesConfigSearchPaths()` in `sdk/packages/shared/src/storage/paths.ts` returns the workspace `AGENTS.md`, workspace `.clinerules`, workspace `.cline/rules`, `resolveGlobalAgentsRulesPath()` (`~/.agents/AGENTS.md`), `join(resolveClineDir(), RULES_CONFIG_DIRECTORY_NAME)` (`~/.cline/rules`) and `resolveDocumentsExtensionPath(\"Rules\")` (`~/Documents/Cline/Rules`) \u2014 so `~/.cline/rules/default-rules.md` and `~/Documents/Cline/Rules/default-rules.md` should both load, while a bare `~/.clinerules` home file is not a supported location at all. On the VS Code side `ensureRulesDirectoryExists()` in `apps/vscode/src/core/storage/disk.ts` resolves the Documents root via `getDocumentsPath()`, and rules are gated by per-rule enable/disable toggles synchronized in `apps/vscode/src/core/context/instructions/user-instructions/rule-helpers.ts`.",
  "verdict": "repro-candidate",
  "duplicate_of": null,
  "severity": "medium",
  "repro": "1. On macOS, write an unmistakable rule (e.g. `Always start every reply with the token RULE-OK.`) to `~/.cline/rules/default-rules.md`.\n2. Fully quit and relaunch VS Code, then open the Cline view and start a new task asking \"what are your rules?\".\n3. Expected: the reply honors the rule. Actual (per reporter): it does not.\n4. Open the Cline Rules popover in the chat input and check whether the rule is listed and toggled on \u2014 a listed-but-off rule points at the toggle synchronization in `rule-helpers.ts`; a missing rule points at discovery in `resolveRulesConfigSearchPaths`.\n5. Repeat with the same content at `~/Documents/Cline/Rules/default-rules.md` to separate the two supported roots.\n6. Note that `~/.clinerules` as a home-directory file is expected not to work; only the workspace-level `.clinerules` is a search path.",
  "human_attention": "yes",
  "notes": "Reporter says it started after an upgrade, which fits a toggle-state migration regression more than a path regression. Worth checking whether newly discovered global rules default to enabled after a version bump.",
  "cluster": ""
 },
 {
  "number": 12530,
  "title": "Cline: Cline initiates task creation despite user being logged out",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "There is no auth pre-flight before a task starts on main: the only `getAuthToken()` call in `apps/vscode/src/sdk` is in `account-service.ts` (balance/usage fetching), and neither the task-start path in `apps/vscode/src/sdk/SdkController.ts` nor `apps/vscode/src/sdk/sdk-task-control-coordinator.ts` consults auth state before creating a session \u2014 the failure only surfaces later as a provider `Unauthorized`. The cross-IDE half of the repro is also unguarded: `apps/vscode/src/core/controller/account/authStateChanged.ts` merely writes `userInfo` into global state, so one IDE completing the browser auth does not reconcile the other's view.",
  "verdict": "duplicate",
  "duplicate_of": 12509,
  "severity": "low",
  "repro": "1. Sign out of Cline in both VS Code (new extension from `apps/vscode`) and a JetBrains IDE running the Cline plugin.\n2. Start a sign-in flow from both IDEs at the same time so two distinct device codes are pending in the browser.\n3. Confirm authentication for only the JetBrains code.\n4. In VS Code, open the Account view and confirm it still shows signed-out.\n5. Switch to the Chat view and send a prompt with the Cline-managed provider selected.\n6. Expected: Cline blocks the run and prompts to sign in. Actual: the task is created and only fails later (or succeeds, if a stale token is still cached).\n7. Distinguish the two defects: whether an unauthenticated user can start a Cline-provider task at all, and whether the VS Code auth state failed to converge after the other IDE authenticated.",
  "human_attention": "no",
  "notes": "Filed by Cline QA against nightly 4.0.1784896810. Needs a product call \u2014 BYOK users are legitimately never signed in, so any gate has to key off the selected provider being Cline-managed, not off sign-in state alone.",
  "cluster": "logged-out-task-gating"
 },
 {
  "number": 12537,
  "title": "Add REFUTE scientific critique + calibration benchmark",
  "area": "unknown",
  "kind": "spam-noise",
  "relevant_to_main": "no",
  "evidence": "The body asks Cline to link a third-party benchmark product (bgpt.pro/refute) from \"related / evaluation / research tooling docs\", conditioned on whether \"this project surfaces LLM or agent science workflows\". It describes no Cline defect; `evals/` in this repo is a coding-agent eval harness, not a scientific-claim calibration benchmark, and nothing references REFUTE anywhere in /workspace.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Drive-by promotional link request; safe to close.",
  "cluster": ""
 },
 {
  "number": 12559,
  "title": "Cline defers devlog/logging actions until end of task instead of executing them in real-time after each milestone",
  "area": "vscode-new",
  "kind": "question-support",
  "relevant_to_main": "yes",
  "evidence": "This is model-adherence behavior, not a code defect. The user's `.clinerules` file is loaded and injected as instructions (`resolveRulesConfigSearchPaths` in `sdk/packages/shared/src/storage/paths.ts` includes the workspace `.clinerules`), and nothing in the runtime reorders or batches a model's tool calls \u2014 `run_commands` simply accepts a list, and `buildRunCommandsDescription()` in `sdk/packages/core/src/extensions/tools/definitions.ts` explicitly encourages sequencing multiple commands in one call, which is the opposite of what this rule wants. There is no scheduler in `sdk/packages/agents/src/agent-runtime.ts` that could defer the echoes; the model chose to batch them.",
  "verdict": "invalid-bogus",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "If the team wants to act on the underlying want, the right shape is a hook (`@cline/shared` hook engine) or a post-tool-use plugin that appends the devlog line deterministically, rather than relying on a prompt rule the model can batch away. Answerable without a maintainer.",
  "cluster": ""
 },
 {
  "number": 12560,
  "title": "it is impossible to remove image from chat",
  "area": "vscode-new",
  "kind": "bug",
  "relevant_to_main": "yes",
  "evidence": "Visible in code on main. `apps/vscode/webview-ui/src/components/chat/UserMessage.tsx` renders only a `<textarea>` bound to `editedText` while `isEditing`, and the `<Thumbnails ... />` block is explicitly gated on `!isEditing`, so attachments are hidden (and un-removable) in edit mode. `handleSave()` then unconditionally resends the originals: `EditMessageAndRegenerateRequest.create({ messageTs, text: editedText, images: images ?? [], files: files ?? [], restoreWorkspace })`. There is no code path by which editing a message can drop an image, which matches the reporter's \"only the text can be modified\".",
  "verdict": "confirmed-by-code",
  "duplicate_of": null,
  "severity": "medium",
  "repro": null,
  "human_attention": "yes",
  "notes": "Owned by `apps/vscode/webview-ui`. Fix is contained: render removable `Thumbnails` inside the edit branch of `UserMessage.tsx` and pass the pruned arrays into `EditMessageAndRegenerateRequest`. The secondary complaint (a non-multimodal OpenRouter model 404s on image input) would be worth a capability pre-check, since `catalog.generated.ts` already carries an `images` capability flag per model.",
  "cluster": ""
 },
 {
  "number": 12565,
  "title": "API Configuration: oLMX\ub97c ollama/lm studio\ucc98\ub7fc \ub4f1\ub85d \uac00\ub2a5\ud558\ub3c4\ub85d \ucd94\uac00",
  "area": "sdk-llms",
  "kind": "feature-request",
  "relevant_to_main": "yes",
  "evidence": "The reporter means oMLX (github.com/jundot/omlx), a macOS/Apple-Silicon MLX inference server that is an OpenAI-compatible drop-in on http://localhost:8000/v1; `rg -i olmx` and a search of `sdk/packages/llms/src/providers/provider-ids.generated.ts` show no oMLX/omlx provider id on main. Local-server presets like the ones requested do exist as hand-authored entries in `sdk/packages/llms/src/providers/builtins.ts` (`id: \"ollama\"` at ~L867 and `id: \"lmstudio\"`, family `openai-compatible`, `defaults.baseUrl: http://localhost:1234/v1`), so adding oMLX is a small addition there plus the surfaced list in `apps/vscode/webview-ui/src/components/settings/providers/providerSettingsRegistry.ts`. A workaround already exists on main: the generic OpenAI Compatible provider (`\"openai\"` in `CUSTOM_PROVIDER_SETTINGS_IDS`, UI in `apps/vscode/webview-ui/src/components/settings/providers/OpenAICompatible.tsx`) accepts an arbitrary base URL.",
  "verdict": "feature-request",
  "duplicate_of": null,
  "severity": "low",
  "repro": null,
  "human_attention": "no",
  "notes": "Owned by @cline/llms (`sdk/packages/llms/src/providers/builtins.ts`). Agent-fixable: one builtin entry modeled on `lmstudio` (family `openai-compatible`, baseUrl `http://localhost:8000/v1`, modelsSourceUrl `http://localhost:8000/v1/models`; oMLX also exposes `/v1/messages`). Until then users can point the existing OpenAI Compatible provider at oMLX, so it is answerable without maintainer input.",
  "cluster": ""
 }
]