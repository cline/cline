# CLI Development

The CLI lives in `cli-ts/` and uses React Ink for terminal UI.

- Primary highlight color (selections, spinners, success states): `color="blueBright"`. This is the light purplish-blue that matches act mode.