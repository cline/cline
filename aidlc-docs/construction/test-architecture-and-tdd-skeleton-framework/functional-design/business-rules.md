# Business Rules

- test helpers must exercise public runtime seams, not private implementation details
- test fixtures must remain generic enough for Claude, Kiro, and LangGraph follow-on work
- helper extraction must reduce duplication rather than add an unused abstraction layer
