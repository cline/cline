# Domain Entities

- `FutureRuntimeDescriptor`
- `getFutureRuntimeDescriptors()`
- `getFutureRuntimeDescriptor(runtimeId)`
- `validateFutureRuntimeDescriptorCoverage()`
- Current descriptor coverage now applies to `github-cli` and `custom-langgraph-cli`
