# Business Rules

- GitHub CLI must remain a todo-grade later candidate.
- custom LangGraph CLI must remain an explicit out-of-process target.
- future runtime descriptors must not imply active provider compatibility before onboarding exists.
- Kiro CLI is no longer a future descriptor in this framework because it has been promoted to an active MVP runtime path.
