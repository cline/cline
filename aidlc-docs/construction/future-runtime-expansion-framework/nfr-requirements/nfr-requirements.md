# NFR Requirements

- descriptor coverage must remain complete for every declared future runtime id
- future runtime planning must be explicit and reviewable
- active runtime behavior must not regress when future runtimes are still todo-grade descriptors
