/** Sentinel used by the cron-shaped UI contract for a one-time run. */
export const ONE_TIME_SCHEDULE_CRON_PATTERN = "0";

/** Metadata field carrying the epoch-millisecond time for that one-time run. */
export const ONE_TIME_SCHEDULE_RUN_AT_METADATA_KEY = "__hubScheduleRunAt";
