{
	"name": "@clinebot/enterprise",
	"version": "0.0.0",
	"internal": true,
	"description": "[INTERNAL] Enterprise composition layer for the Cline SDK",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"development": "./src/index.ts",
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist",
		"src"
	],
	"scripts": {
		"build": "bun build ./src/index.ts --outdir ./dist --target node --format esm && bunx tsc -p tsconfig.build.json",
		"clean": "rm -rf dist node_modules",
		"typecheck": "bunx tsc -p tsconfig.json --noEmit",
		"test": "bunx vitest run --config vitest.config.ts",
		"test:watch": "bunx vitest --config vitest.config.ts"
	},
	"engines": {
		"node": ">=22"
	},
	"dependencies": {
		"@clinebot/agents": "workspace:*",
		"@clinebot/core": "workspace:*",
		"@clinebot/shared": "workspace:*"
	}
}
