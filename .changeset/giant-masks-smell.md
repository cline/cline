---
"cline": minor
---

Add a new `real-esrgan-image-upscale` skill that enables offline image upscaling and enhancement using the portable NCNN Vulkan build of Real-ESRGAN after initial download. The skill operates via the CLI and automatically manages OS-specific binaries using Python.
