---
"claude-dev": patch
---

Show full workspace-relative paths for @-mention file search results on Windows. Nested files no longer collapse to their bare filename in the picker, and the same file is no longer listed twice with `/` and `\` separators.
