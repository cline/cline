---
"claude-dev": patch
---

Explain when a Cline free model's promotion has ended instead of failing with a raw "model not found" error or a Retry button that can never succeed. The chat row now offers a one-click switch to the paid version of that model, plus a shortcut to the model picker, and the Cline and ClinePass model pickers stop advertising the bundled free models once Cline reports that no promotions are running.
